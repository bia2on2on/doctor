# PHPStan — F1-3 (گروه 4 از 7)
#
# سطح: بالاترین سطحی که روی وضعیت واقعی کد بدون baseline و بدون
# ignore-path پاس می‌شود («level matched to real code state» — مصوب کارفرما).
# سطح با Probe موقت (.github/workflows/phpstan-probe.yml) روی CI تعیین شد؛
# بعد از تعیین، این فایل مرجع است و Gate از آن سقف پایین نمی‌آید.
#
# scope: فقط src/ (کد تولیدی PSR-4 ClinicCore). tests/ با اجرای واقعی پوشش
# دارد؛ bin/ (اسکریپت‌های عملیاتی CLI) فعلاً خارج scope است.
parameters:
    level: 5
    paths:
        - src
    scanFiles:
        - vendor/php-stubs/wordpress-stubs/wordpress-stubs.php
