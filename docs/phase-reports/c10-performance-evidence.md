# C10 — بستهٔ شواهد عملکرد فاز ۲ (Performance Evidence Package)

| | |
|---|---|
| **وضعیت** | **بازبینیِ شواهد عملکرد کامل / واجد شرایط فنی برای بستن — بستنِ رسمی مالک در انتظار (PENDING)** — C10 رسماً بسته **نشده** |
| **ماهیت** | فقط مستندسازی/شواهد (docs only) — بدون تغییر کد محصول، تست، workflow، schema، migration |
| **مبنا** | چک‌پوینت ادغام‌شدهٔ `bd2634a` (`origin/main`؛ PR #24 MERGED — بستنِ رسمی C9) — هر ۴ گیت پس‌از‌ادغام روی همان SHA سبز (پایین) |
| **سطح** | LEVEL-2 داخلی — زیرفاز Phase 2 (Multi-Clinic Core) طبق `docs/governance/project-phase-taxonomy.md`؛ **تابعِ** Owner Roadmap؛ فاز مستقل نیست |

> این سند، بستهٔ شواهد/بازبینیِ داخلی C10 است و مطابق الگوی بسته‌شدن C8 (بستهٔ شواهد/
> مستندات بدون پیاده‌سازی) و C9 (تعیین scope/شواهدِ محدودشده پیش از هر اقدام) تدوین شد.
> **هیچ بخشی از این سند مجوز پیاده‌سازی، بهینه‌سازی، migration یا شروع فاز جدید نیست.**

## ۱. هدف و مرز (Purpose / Boundary)

C10 = بستهٔ **بازبینی شواهد عملکردِ داخلی فاز ۲**، محدود به فوندیشن Multi-Clinic.

Provenance برچسب: C10 در کامیت `b6f6c93` (2026-09-09) فقط به‌عنوان یک قلم در صف داخلی
زیربسته‌های Phase 2 در `phase2-state.md` معرفی شد؛ **scope مصوب و معیار پذیرش مستقلی
ندارد** و شواهد مخزن، تأیید صریح مالک بر ترتیب دقیق `C4…C10` را اثبات نمی‌کند
(تصحیح مستند در `3589b15`). وجود برچسب = مجوز پیاده‌سازی نیست.

C10 **نیست**:

- **Owner Roadmap Phase 17 (Performance) نیست** و آن را شروع یا جایگزین نمی‌کند —
  مهندسی عملکرد تجاری گسترده همچنان Phase 17 است (NOT STARTED)؛
- بهینه‌سازی تجاری گسترده نیست؛
- مجوز بهینه‌سازی حدسی/speculative نیست؛
- مجوز تضعیف ایزولاسیون/امنیت tenant نیست (اولویت: امنیت/صحت بر بهینه‌سازی
  میکرو — هیچ بررسی tenant نباید برای سرعت تضعیف شود)؛
- مجوز migration/تغییر schema نیست (`0021` ساخته نمی‌شود)؛
- **اثبات آمادگی عملکردی تجاری نیست.**

## ۲. شواهد اجراشدهٔ موجود (ارجاع — نه تکثیر)

- **`docs/performance/performance-baseline.md`** — اهداف مصوب عملکرد: REST تعاملی
  ‏p95 < 300ms؛ سربار افزونه روی صفحات عمومی p95 < 100ms؛ عملیات سنگین async؛
  و **متدولوژی الزامی benchmark (§3)**. منبع اهداف: Engineering Baseline §17/§29 +
  تصمیم نهایی کارفرما F2.
- **`docs/srs/SRS.md` §4.2 (NFR-PERF-1)** — P95 < 500ms در ۵۰ کاربر هم‌زمان روی
  **سرور مرجع 4vCPU/8GB** (تنها هدف دارای محیط مرجع تعریف‌شده).
- **گام قابل‌اجرای «Performance benchmark — ab (p50/p95/p99 + RPS + error rate)»**
  داخل job ‏`staging-gate` در `.github/workflows/pilot-gate.yml` — ابزار `ab`؛
  warm-up ×۲؛ `/health` و `/availability` و مرجع `/wp-json/`؛ دیتاست synthetic
  (پیش‌فرض ۴۰۰ بیمار/۳۰ روز). **این گیت هیچ آستانهٔ latency اجرا نمی‌کند** — معیار
  شکست = خطای parse یا non-2xx ≠ 0؛ ماهیتش «اندازه‌گیری» است، نه «داوری».
- **`docs/phase-reports/report-pilot-gate.md` §8** — نتایج **اجرااشدهٔ واقعی** در محیط
  Staging با اعداد p50/p95/p99/RPS/error (۷ اندازه‌گیری؛ منبع ثبت‌شدهٔ خودِ گزارش:
  steps سبزِ runهای `34025267752` و `34027029638`). جمع‌بندی مستندِ همان گزارش:
  هیچ endpointی — حتی مرجع `/wp-json/` خودِ وردپرس — در آن محیط به اهداف نرسید؛
  اشباع ~۵۰ RPS مستقل از سطح هم‌زمانی ⇒ گلوگاه محیط (mod_php + تعداد محدود
  Apache workers + CPU اشتراکی runner)؛ error rate صفر؛ و صراحتاً
  «اهداف مصوب (هیچ‌کدام پاس‌شده اعلام نمی‌شوند)».
- **گیت‌های Pilot/Staging ادغام‌شده سبزِ جاری** — روی چک‌پوینت‌های ادغام‌شدهٔ متوالی،
  job «Staging Gate (fresh install → restore drill)» شامل استپ benchmark با موفقیت
  اجرا شده است؛ جدیدترین نمونهٔ تأییدشدهٔ زنده: **`bd2634a`** — Pilot/Staging
  `34651627290` (به‌همراه CI `34651627032` · Real‑WP `34651627286` · Closure
  `34651627211`، همه success). برای runهای پس از گزارش §8 فقط «اجرای موفقِ استپ»
  تأیید می‌شود؛ اعداد دقیق در Step Summary اند و از sandbox قابل بازیابی نیستند
  (محدودیت ثبت‌شده در اصلاح واژگانی عملکرد در `phase2-state.md`).

**صریحاً ادعا نمی‌شود:** اینکه اندازه‌گیری‌های Staging آستانه‌های NFR تجاری/مرجع را
پاس کرده‌اند. داوری Quality Gate عملکرد فقط با بنچمارک سرور مرجع معتبر است —
Runbook §12.3 (`report-pilot-gate.md`)، وضعیت BLOCKED_BY_ENVIRONMENT.

## ۳. NOT MEASURED / NOT VALIDATED — ثبت صریح

متریک‌های زیر **اندازه‌گیری‌نشده/اعتبارسنجی‌نشده** می‌مانند:

1. **عملکرد محیط مرجع (4vCPU/8GB)** در برابر آستانه‌های NFR (سرور مرجع برپا نشده)؛
2. **سربار افزونه روی صفحات عمومی** (هدف p95 < 100ms) — هیچ اندازه‌گیری‌ای وجود ندارد؛
3. **شمارش کوئری runtime / N+1** — هیچ ابزار سنجشی (مثلاً SAVEQUERIES) در کد/تست/CI
   وجود ندارد؛
4. **حافظه (Memory)**؛
5. **بار معنادار چندکلینیکی** (≥۲ کلینیک با داده/بار متقاطع) — دیتاست pilot عملاً
   تک‌کلینیکی seed می‌شود؛
6. مانیتورینگ/هشدار عملیاتی p95 در بازهٔ زمانی (تخصیص‌یافته به Production Hardening).

**این اقلام بلوکر بستن C10 نیستند** — هیچ سند کانونی آن‌ها را شرط بستن این زیرفاز
داخلی تعیین نکرده است. این‌ها به‌عنوان **شواهد عملکردی/انتشارِ آینده** ثبت می‌شوند که
عمدتاً به **Owner Roadmap Phase 17** یا اعتبارسنجی انتشار مناسب تعلق دارند.

## ۴. بازبینی static محدود فاز ۲ (خلاصهٔ یافته‌ها)

بازبینی ایستای کراندارِ فوندیشن Multi-Clinic فاز ۲ (بدون اجرای بار جدید):

- **Predicateها و ایندکس‌های tenant** در مسیرهای داغ بازرسی شدند — ستون‌های
  `clinic_id` روی مسیرهای اصلی + ایندکس‌های clinic-محور (از جمله `idx_visit_day`،
  `u_membership`، `idx_membership_user/role`، `u_idem_scope(…,clinic_id)`،
  `u_location_slug`، `idx_location_active`) + ۲۲ FK معنایی `clinic_id → clinics(id)`
  (migration `0017`)؛
- **هیچ الگوی membership lookup به‌ازای-هر-ردیفِ آشکار** در مسیرهای بازرسی‌شده
  (صف/داشبورد/مالی) یافت نشد — خواندن‌های لیست، JOIN/دسته‌ای و کراندارند؛
- **هیچ N+1 آشکاری** در دامنهٔ بازرسی‌شدهٔ فاز ۲ یافت نشد؛
- **resolution مربوط به ScopeContext/Membership** در مسیرهای بازرسی‌شده کراندار
  بود (تعداد ثابت و اندک کوئری ایندکس‌دار به‌ازای درخواست؛ بدون caching
  بین‌درخواستی — انتخاب آگاهانهٔ fail-closed)؛
- **کش Settings** همچنان **کلید clinic_id** دارد (اصلاح C6‑E1)؛
- **هیچ مسئلهٔ فعلی tenant-leak مربوط به object cache وجود ندارد، چون object cache
  گسترده در حال حاضر اصلاً استفاده نمی‌شود** (`wp_cache_*` در `src/` به‌کار نرفته)؛
- **هیچ نقص عملکردی VERIFIED فاز ۲ که نیازمند remediation باشد یافت نشد.**

**دقت لازم:** بازرسی کد ≠ عملکردِ اندازه‌گیری‌شده. این یافته‌ها فقط «no issue found
in inspected scope» هستند؛ **عدم وجود سراسری N+1 در کل محصول ادعا نمی‌شود** و کمال
عملکرد محصول ادعا نمی‌شود.

## ۵. ریسک‌های شناخته‌شده / به‌تعویق‌افتاده (فقط مبتنی بر شواهد)

- **مشاهدهٔ گذرای «Deadlock» روی `_transient_cpms_migrate_lock`** در بار c=100 —
  ثبت‌شده در `report-pilot-gate.md` §8 و به Backlog فرستاده شده؛ ریسک ثبت‌شده است،
  **نه نقص VERIFIED**؛
- **کلیدهای کش آینده باید tenant-aware بمانند** (الگوی Settings: کلید = clinic_id) —
  الزام رو‌به‌جلو برای هر کشی که بعداً معرفی شود؛
- **بنچمارک سرور مرجع** همچنان شواهد آینده است (Runbook §12.3؛ عمدتاً Phase 17 /
  اعتبارسنجی Pilot-انتشار).

هیچ استنتاج کم‌اطمینانی به‌عنوان نقص ثبت نشده است.

## ۶. برچسب تاریخی «End Gate (۲۶بندی)»

- این برچسب که در کامیت `b6f6c93` (2026-09-09) در خط صف داخلی ظاهر شد،
  **تاریخی/داخلی است**؛
- تحقیق مخزن (جست‌وجوی کامل فایل‌ها + کل تاریخچهٔ Git + متن PRها) نتیجه داد:
  **هیچ فهرست پذیرش کانونیِ ۲۶بندی وجود ندارد و هرگز وجود نداشته است**؛
- **بازسازی/اختراع ۲۶ بند از شواهد نامربوط انجام نمی‌شود و نباید شود**؛
- منشأ عدد در مخزن ثبت نشده است؛ شباهت عددی آن با سرشماری تاریخی «**۲۶ جدول دارای
  clinic_id**» (`final-pre-phase2-gate-report.md` §۲ / `phase0.5-target-model.md`
  د‑۶‑۲) **فقط یک استنتاج (INFERRED) است و به‌عنوان واقعیتِ provenance ثبت نمی‌شود**.

## ۷. وضعیت C10 و مسیر بستن

- **وضعیت: «بازبینی شواهد C10 کامل / واجد شرایط فنی برای بستن — بستن رسمی مالک در
  انتظار (PENDING)».** C10 هنوز رسماً بسته نشده است؛ بستن رسمی = تصمیم صریح مالک.
- **Owner Roadmap Phase 2 همچنان IN PROGRESS است.** End Gate فاز ۲ و Phase 3
  شروع نمی‌شوند. Phase 17 هم شروع نشده است.
- **الزامات واقعی و کانونیِ بستن Phase 2** (بدون اختراع قلم جدید): تعیین تکلیف
  اقلام باز صف داخلی (اکنون فقط C10 — یعنی همان تصمیم بستن رسمی مالک)، قاعدهٔ
  Gate نقشهٔ راه (تأیید صریح Product Owner بر خروجی + معیارهای پذیرش فاز)، الگوی
  شواهدِ هر بسته‌بندی (گیت‌های سبز روی SHA ادغام‌شده)، و ماندن اقلام به‌تعویق‌افتاده
  (S2/S3، Jobs/SMS/timezone، UX چندکلینیکی wp-admin) در وضعیت به‌تعویق مگر تصویب
  جداگانه. برچسب «End Gate (۲۶بندی)» خودش هیچ فهرست تعریف‌شده‌ای افزودن نمی‌کند.

## ارجاعات

- `docs/performance/performance-baseline.md` (اهداف مصوب + متد §3)
- `docs/srs/SRS.md` §4.2 (NFR-PERF-1..6)
- `docs/phase-reports/report-pilot-gate.md` §8 (نتایج اجراشدهٔ Staging + Runbook §12.3)
- `.github/workflows/pilot-gate.yml` — job ‏`staging-gate`، استپ benchmark ‏`ab`
- `docs/phase-reports/phase2-state.md` — §C10 (خلاصهٔ این بسته) + Queue + اصلاح
  واژگانی عملکرد
- `docs/project-current-state.md` — وضعیت شواهد عملکرد + وضعیت C10
- `docs/governance/project-phase-taxonomy.md` — سطح‌بندی LEVEL-2 و قاعدهٔ تابع‌بودن
- `docs/roadmap/roadmap.md` §۰ — Owner Phase 0..20 (مرجع اجرایی؛ Phase 17 = Performance)
