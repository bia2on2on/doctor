# سیستم مدیریت مطب — Clinical Practice Management System (CPMS)

مستندات معماری و نیازمندی‌ها — نسخه 2.0 — 2026-09-08

> # 🔴 مرجع فازبندی اجرایی
>
> ## **Owner-approved Phase 0..20 Roadmap = authoritative execution roadmap.**
>
> مرجع رسمی: [`roadmap/roadmap.md`](roadmap/roadmap.md) **§۰**.
> نظام‌های **`F0..F10`**، **`Doc-Phase 1..8`** (جدول همین صفحه) و برچسب‌های **`V1`/`V1.5`/`V2`** **Legacy/Historical** هستند و **Roadmap اجرایی فعلی نیستند**.
>
> **معماری هدف الزام‌آور — [ADR-0031](adr/ADR-0031-organization-clinic-location-scoped-authorization.md):**
> `Organization → Clinic → Location → Doctor → User/Staff`
>
> **تعارض شناخته‌شدهٔ اسناد با واقعیت:** [`drift-register.md`](drift-register.md)

> **🤖 راهنمای ایجنت‌ها (الزام شروع کار):** هر ایجنت (AI یا انسان) پیش از هر کاری [`agent-guide.md`](agent-guide.md) را کامل بخواند — وضعیت فازها، قواعد الزامی کارفرما، الگوهای کد، دام‌های شناخته‌شده، فازهای باقی‌مانده و **پروتکل لاگ کار ایجنت‌ها (§9–10: هر ایجنت ورودی خود را append می‌کند)**.

> **وضعیت تاریخی (2026-09-08 — snapshot):** همهٔ فازهای تاریخی `F1..F10` + Pilot/Staging Gate + Closure Gate + Remediation انجام و merge شده‌اند (۹ PR؛ آخرین = #9 MERGED). نسخهٔ منتشرشده `1.0.0`.
> **به‌روزرسانی 2026-09-12:** **C10 = CLOSED — پذیرش/بستن رسمی با تصمیم صریح مالک** (بستهٔ شواهد در `origin/main` = `bdb135e9`، PR #25 MERGED؛ فقط دامنهٔ محدودشدهٔ بازبینی شواهد عملکرد — بدون ادعای NFR/بار/مقیاس‌پذیری/آمادگی تجاری؛ **Phase 2 همچنان IN PROGRESS**؛ Phase 3 / Phase 17 NOT STARTED *(🔴 تاریخی — اکنون **Phase 3 = COMPLETED / FROZEN** در `ebf8588`؛ رجوع به «به‌روزرسانی 2026-09-16» بالا)*؛ End Gate فاز ۲ تعریف/اجرا نشده). الزام دائمی توپولوژی‌های استقرار در [ADR-0031](adr/ADR-0031-organization-clinic-location-scoped-authorization.md) §«الزام دائمی محصول: یک هسته، سه توپولوژی استقرار» ثبت شد.
>
> **به‌روزرسانی 2026-09-16 (Phase 3 End Gate — PASS):** **Phase 2 = COMPLETED (technical)**؛ **Phase 3 — Role & Access Control = COMPLETED / FROZEN** (تکمیل فنی در `origin/main` = `ebf8588f34be1da2ff18a152dea2c8badc472056`، PR #57 MERGED 2026-09-16T16:46:59Z؛ نتیجهٔ End Gate فاز ۳ = **PHASE 3 PASS**). هستهٔ مرکزیٔ `AuthorizationService` با محدودهٔ Clinic تثبیت شد؛ فوندیشن‌‌های محدودشده (مدیریت staff/membership، پیکربندی/secrets SMS، چرخهٔ reports/export، دسترسی clinical/medical-file، Clinician Admin / Handwriting / Finance، Queue / Schedule / staff Booking / staff Patient، تفکیک patient-self/public، مدیر نصب بدون دسترسی بالینی ضمنی، پوششٔ داینامیکٔ چند‌Clinic، و Integration completion guard جلوگیری از false-green زودهنگام) برقرار شدند. پیاده‌سازی فاز ۳ شامل PR‌های #49/#50/#52/#54/#55/#56/#57 (ادغام‌شده در `origin/main`) و مدرک exact-head: ۱۹/۱۹ چک موفق در `ebf8588` است؛ فاقدِ تأیید/ادغام مالک (فقط پذیرش فنی). **این ادعای انتشار/V1/تجاری نیست و فاز ۴ نیست.** آخرین migration = `0020` (‎`0021` ساخته/تصویب نشده — رزرو یا ساخت نشده). **در checkpoint 2026-09-16، Phase 4 = Master Data همچنان NOT STARTED بود** *(historical — superseded by the 2026-09-17 closure update below; that checkpoint made no change to the roadmap).* رجوع به [`project-current-state.md`](project-current-state.md) برای جزئیات.

> **به‌روزرسانی 2026-09-17 (Phase 4 documentation closure reconciliation):** مرجعِ آن چک‌پوینت `origin/main = 70ace204d1524a8f5e83d33c67c1a09b7543e7a3` بود؛ **Phase 4 — Master Data = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** بر پایهٔ slices ادغام‌شدهٔ **PRs #59–#65**. این status فقط دامنهٔ فنیِ mergeشده را ثبت می‌کند و ادعای release/V1/commercial نیست؛ ServiceOffering، Specialty/Department/Room، Iran geography master data، M-01..M-05، یا رفع stale `clinic.phone` در Patient Portal را implemented/fixed اعلام نمی‌کند. Location timezone و مدل membership برای professional چندکلینیکی حفظ شده‌اند. **Phase 5 implementation در آن چک‌پوینت شروع نشده بود و بخشی از آن closure نبود.** *(🔴 تاریخی — superseded توسط «به‌روزرسانی 2026-09-17 (Phase 5 Slice 1 — bounded technical closure)» زیر.)* آخرین migration همچنان `0020` است و `0021` وجود ندارد.

> **به‌روزرسانی 2026-09-17 (Phase 5 Slice 1 — bounded technical closure):** مرجعِ آن چک‌پوینت `origin/main = e063b42260cb5ab740acf48dcf73c6c67b11fef6` بود (PR #67 MERGED 2026-09-17T15:36:54Z؛ head = `fa86e41e`؛ ۱۹/۱۹ چکِ exact-head موفق؛ گیت‌های پس‌از‌ادغام روی همان SHA موفق). **Phase 5 — Pricing Engine = CLOSED / TECHNICALLY COMPLETE (BOUNDED)**: pricing پایه **Clinic-scoped** و کافی برای جریان تأییدشدهٔ فعلی V1 است و Slice 1 شکافِ مشخصِ انتساب Location را با استخراج `invoice.location_id` از Visit معتبر بست (هرگز از payload). رفتار موجود V1 حفظ شد (manual `unit_price` override، fallback به `service.price`، snapshot آیتم‌های فاکتور). **ادعا نمی‌شود:** ServiceOffering، تغییر `u_service_code`، تعرفهٔ متفاوت برای *همان* Service در Locationهای مختلفِ یک Clinic، `is_active` server-side rejection به‌عنوان قراردادِ محصول، یا سیاست مالیات/VAT (REQUIRES LEGAL VERIFICATION). **هیچ فرضِ تک‌Clinic/تک‌Location وجود ندارد** — معماری مرجع `Organization → Clinic → Location` است و **پشتیبانی چند Location به‌تعویق نیفتاده**؛ تعلیقِ «تعرفهٔ per-Location برای همان Service» بر پایهٔ **نبودِ الزامِ اثبات‌شدهٔ V1** است، نه فرضِ تک‌Location. آخرین migration همچنان `0020` است و **`0021` وجود ندارد**. محدودیت‌های دائمی شواهد PR #67: historical **VALID RED = NOT AVAILABLE**؛ **direct named execution proof برای `Phase5InvoiceLocationFromVisitTest` = NOT RETRIEVED** (کامنت شواهد Integration همان PR فقط suiteهای Phase-3 Slice-6 / Phase-4 Slice-3 / Slice-4 / Location را نام می‌برد؛ شمارش‌های aggregate شاهدِ اجرای تستِ نام‌دار نیستند).

> **به‌روزرسانی 2026-09-18 (Phase 6 — bounded documentation closure):** مرجعِ آن چک‌پوینت و راستی‌آزمایی‌شدهٔ `origin/main` = **`bd5e6a1819a838648dbdcbc6914c0d8b24bba38b`** بود (**PR #76 MERGED 2026-09-18T09:59:44Z**؛ head تصویب‌شده `bd840163b33688d17af51f006aa9dccaea9e8c53`؛ والدین `fc0a598e…` + `bd840163…`؛ open PR در زمان بازبینی = ۰؛ *(🔴 تاریخی — مرجعِ جاری اکنون در «به‌روزرسانی 2026-09-18 (C7 hardening + Phase 7 Slice 1 / FR-4.6)» زیر است)*). **Phase 6 — Scheduling Engine = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** بر پایهٔ slices ادغام‌شدهٔ **PRs #69–#71 + #73–#76** (the range also contains the documentation-only **PR #72** — owner-requirement preservation, not a slice). **Phase 6 Slice 7 = CLOSED**: بلاکر همزمانی (TOCTOU) در اجرای چندشیفته با **PR #76** رفع شد؛ **VALID RED پذیرفته‌شده = `fca63d3a21a7918c950d2cf07bf43343fe0c20ef`** (run `35327220676`)؛ دو تلاش قبلیِ RED **همچنان INVALID RED هستند و به‌عنوان RED معتبر بازنویسی نمی‌شوند** (`091fae1c5c59142d7bc4d60aeb2ae8954e52ed28` — `ParseError` و صفر تست اجراشده؛ `c8b9592056307fea8213d131e135131198508674` — run `35320273122`: سوئیتِ موردنظر **اجرا شد** (`Tests: 983, Assertions: 13649, Failures: 3`)؛ خروجی‌های fatal کودکانِ fork‌شده حاوی `PHPUnit\Framework\Error\Warning: Undefined array key "id"` بود و positive controlِ نامتقاطع شکست خورد، اما **مبدأ/انتسابِ آن warning بازیابی نشد (NOT RETRIEVED)** ⇒ انتسابِ این شکست‌ها به قراردادِ همزمانیِ محصول محقق نشد — **نه نقصِ محصول ادعا می‌شود و نه نقصِ harness**). **شواهد نهایی پس‌از‌ادغام روی `bd5e6a18`: هر چهار workflow لازم terminal-success** (CI `35332404611` · Real WP `35332404609` · Pilot/Staging `35332404592` · Closure `35332404644`) و **۱۹/۱۹ check run موفق**. **استثناهای ثبت‌شدهٔ شواهد (دائمی، بدون پنهان‌سازی):** (۱) **استثنای فرآیندی Slice 7 (تاریخی، غیربلاکر)** — ایجنت write قرار بود پس از گرفتن VALID RED متوقف شود اما پیش از مجوز مدیر وارد GREEN شد؛ این نقضِ انضباطِ فرآیند/شواهد ثبت می‌شود و شواهدِ نهاییِ مستقل پذیرفته‌شده را باطل نمی‌کند. (۲) **استثنای تاریخی شواهد RED در Slice 5** — کامیت تست‌تنهای `bd9351e75407ec0449204733d3b5894fd7d33aa8` در همان SHA **هیچ check runی ندارد** (API: `total_count = 0`)؛ RED گذشته‌نگر ساخته نمی‌شود و رفتار نهایی با شواهد پذیرفته‌شدهٔ بعدی پوشش یافته است. (۳) **استثنای تاریخی پس‌از‌ادغام روی main قبلی `fc0a598e739d6e4951de3167bbc4509e0d9c5378`** — ۱۸ موفق + **۱ cancelled** (`Responsive smoke`؛ run `35310058979` = `cancelled`) که **هرگز PASS نبود** و با شواهد موفقِ بعدی روی `bd5e6a18` تضاد ندارد. **پوشش نیازمندی‌ها (حفظ‌شده):** `FR-3.1`، `FR-3.2`، `FR-3.3`، `FR-3.4`، `FR-3.6`، `FR-3.7`، `FR-3.8`، `FR-3.9`، `FR-3.10` — **`FR-3.5` متعلق به Phase 8 است و به‌عنوان پوشش Phase 6 ادعا نمی‌شود.** **hardening به‌تعویق‌افتاده (پیاده‌سازی‌نشده؛ حفظ برای اسلایس hardening برنامه‌ریزی‌شدهٔ C7):** *(🔴 تاریخی — با **PR #78** (merge `4b322d9dc8839e1efd66d712aedc06d9f22cd175`) جایگزین شد: اسلایس hardeningِ C7 ادغام شد و قلم‌های ج/د پیاده‌سازی شدند؛ جزئیات در «به‌روزرسانی 2026-09-18 (C7 hardening + Phase 7 Slice 1 / FR-4.6)» زیر. متنِ تاریخی حفظ شد و بازنویسی نشد.)* **ج** شاخهٔ legacy بدون Scope در `requireClinicianWithinTrustedClinic()` (deferrable hardening، نه بلاکر امنیتی فعلی production — مسیرهای admin تولیدی Scope صریح برقرار می‌کنند و مرز REST جداگانه scope می‌شود) و **د** override ‏`horizon_days` در payload جاب (مسیر settings بازهٔ `1..365` را clamp می‌کند ولی مسیر payload سقف بالایی ندارد). **الف** فقط بدهی کامنت/مستندات: کامنتِ نادرستِ مسیر استثنا در `ScheduleController.php` که `404` را توصیف می‌کند درحالی‌که مرز scope در حال اجرا `403 CLINIC_SCOPE_UNAVAILABLE` / `reason=location` برمی‌گرداند — رفتار محصول درست است، فایل منبع در این closure دست‌نخورده ماند و اصلاح کامنت به نخستین برش محصولیِ مجازی که آن فایل را لمس کند موکول شد. **آخرین migration همچنان `0020` است و `0021` وجود ندارد/رزرو نشده.** این closure ادعای release/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد).

> **به‌روزرسانی 2026-09-18 (C7 hardening + Phase 7 Slice 1 / FR-4.6 — bounded documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`757d7424332d87cdd3d1894ee39f9f5f01bf0a33`** است (**PR #79 MERGED 2026-09-18T18:12:54Z**؛ open PR در زمان بازبینی = ۰) *(🔴 تاریخی — مرجعِ جاری اکنون در «به‌روزرسانی 2026-09-19 (Phase 7 Slice 2)» زیر است؛ عبارتِ «Slice 2 وجود ندارد/ادعا نمی‌شود» در این بند، وضعیتِ زمانِ همان چک‌پوینت بود و با ادغامِ **PR #81** جایگزین شد — متن تاریخی حفظ شد و بازنویسی نشد)*. **C7 hardening = CLOSED:** **PR #78** (MERGED 2026-09-18T15:07:23Z؛ merge = `4b322d9dc8839e1efd66d712aedc06d9f22cd175`) دو قلم hardening به‌تعویق‌افتادهٔ Phase 6 را پیاده‌سازی کرد: **ج** خواندن‌های بدون Scope در `ScheduleService::list()`/`listExceptions()` اکنون **fail-closed**‌اند — بدون Scope صریحِ معتبرِ Clinic همان پاکت پایدار `CLINIC_NOT_FOUND`/404 و هرگز کوئریِ بدون دامانه اجرا نمی‌شود — و **د** مقدار عددی `horizon_days` در payload جاب `slots.generate` اکنون بازهٔ `1..365` مسیر settings را به‌اشتراک می‌گذارد (`<= 0` یا `> 365` = fail-closed با skip هر-پزشک طبق رفتارِ invalid-horizonِ تثبیت‌شده؛ warning `SLOTS_GEN_SKIP_INVALID_HORIZON`؛ sweep ادامه می‌یابد). شواهد پس‌از‌ادغام روی merge SHA: هر ۴ workflow لازم terminal-success (CI `35360479404` · Real WP `35360479402` · Pilot/Staging `35360479660` · Closure `35360479487`) + **۱۹/۱۹ check run موفق**. شاخه‌های سازگاریِ null-scopeٔ باقی‌مانده در نوشتن‌های موجودیت (`requireSchedule/requireExceptionForTrustedClinic`) پس از PR #78 جداگانه بازسازی/بازبینی شدند: فراخوان‌های نوشتنِ production (REST و wp-admin) Scope صریح برقرار می‌کنند، هیچ فراخوانِ فعلیِ src/bin/job بدون Scope یافت نشد، و هیچ مصرف‌کنندهٔ سازگاریِ VERIFIED به شاخهٔ no-scope وابسته نیست ⇒ طبقه‌بندی **NO CHANGE JUSTIFIED — نه patchِ C7ِ دیگری الزامی است**. واقعیتِ producerها (درایِ درخت main): `ScheduleService::regenerate()` `horizon_days` نمی‌گذارد؛ scheduler دوره‌ای (`App::scheduleRecurringJobs()` با payload تهی) نمی‌گذارد؛ **`bin/cpms slots generate --days=N` مقدار را می‌گذارد** (producer واقعیِ CLI اپراتور/سرور)؛ مسیر job-enqueue خارجیِ REST راستی‌آزمایی **نشده** است (جملهٔ محدودِ راستی‌آزمایی — **نه** ادعای سراسری دربارهٔ همهٔ سیستم‌های خارجی ممکن). **الف** (کامنتِ نادرستِ مسیر استثنا در `ScheduleController.php`) دست‌نخورده است — PR #78 آن فایل را لمس نکرد؛ بدهیِ فقط-کامنتی برای نخستین برش محصولیِ مجازی است. **Phase 7 — Appointment Engine = STARTED؛ Slice 1 / FR-4.6 = CLOSED (BOUNDED):** **PR #79** (merge `757d7424…`): سیاستِ محصولیِ **Owner-issued — متمایز از متن اصلی SRSِ FR-4.6** (SRS فقط «پیام واضح + نمایش Slotهای نزدیک خالی» می‌گوید؛ جزئیات زیر سیاستِ مالک برای رفع ابهام FR-4.6 است، **نه** عبارت SRS): روی باختِ `CLINIC_SLOT_TAKEN` (سایت‌های hold `atomicHold` و `confirm()`/`atomicClaim` نهایی) دادهٔ **الحاقی**ِ `BookingException` زیر `nearby_slots` — حداکثر **۵** جایگزینِ آزادِ **همان تاریخِ محلی**، **همان Locationِ باخته**، **همان Clinic + clinician از زمینهٔ persistedِ اسلاتِ باخته**، با ترتیب قطعیِ تثبیت‌شده، **`[]`** در نبودِ جایگزینِ واجد شرایط؛ کد/پیام/HTTP 409ِ موجود **دست‌نخورده**. شواهد: VALID RED پذیرفته‌شده `81d06619c63e23212c209bf8d30ff9ddd45dfaa6` (CI run `35370158310`؛ A/B/C)؛ تلاش تاریخی `b690e6c550f726a0329da51fbd37519e307f5c5e` (CI run `35369425482`): B در آن تلاش valid، A/C = INVALID RED کلاس D (محاسبهٔ fixture) — بازنویسی نمی‌شوند؛ کامیت محصول GREEN = `dfb37d41eba75c8543abea574f18858370aacf0d` (فقط `BookingService.php`). صداقت شواهد: fixture قطعیِ B شاخهٔ واقعیِ `confirm()`/`atomicClaim` را در runtime اجرا می‌کند اما **بازتولیدِ runtimeِ race همزمان واقعی نیست** و هرگز بیش از این ادعا نشده است. شواهد نهایی پس‌از‌ادغام روی merge SHA `757d7424…`: CI `35378860137` · Closure `35378860056` · Real WP `35378860138` · Pilot/Staging `35378860185` — همه terminal-success + **۱۹/۱۹ check run موفق**. **Phase 7 به‌طور کامل بسته نیست؛ هیچ الزامِ دیگرِ FR-4.x بسته اعلام نمی‌شود؛ Slice 2 وجود ندارد/ادعا نمی‌شود.** scopingِ فقط‌خواندنیِ Phase 7 زیرساخت booking/hold قابل‌توجهی را از پیش موجود یافت — آن scoping خودش بستنِ پیاده‌سازیِ همهٔ FR-4.x نیست. آخرین migration همچنان `0020` است و `0021` وجود ندارد/رزرو نشده. این closure ادعای release/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد).

> **به‌روزرسانی 2026-09-19 (Phase 7 Slice 2 — bounded documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`e60c62428e6109e7182d04d3266f0d15b12b0d2f`** است (**PR #81 MERGED 2026-09-19T03:08:27Z**؛ open PR در زمان بازبینی = ۰) *(🔴 تاریخی — مرجعِ جاری اکنون در بلوکِ «به‌روزرسانی 2026-09-19 (Phase 7 — bounded documentation closure)» پایین است؛ عبارت‌های «Phase 7 به‌طور کامل بسته نیست / IN PROGRESS» در این بند وضعیتِ زمانِ همان چک‌پوینت بود و با بسته‌شدنِ محدودِ Phase 7 جایگزین شد — متن تاریخی حفظ شد و بازنویسی نشد)*. **Phase 7 — Appointment Engine = STARTED / IN PROGRESS؛ Slice 2 = CLOSED (BOUNDED):** برش ۲ اینورتِ نوبت **I-3** (`docs/state-machines/appointment.md` §۴) را هنگام وجودِ **واقعاً فعالِ Visit** (ویزیتِ متصل در یکی از وضعیت‌های زندهٔ تثبیت‌شدهٔ `ACTIVE_STATUSES` با `active = 1`) برای **T5 لغو توسط بیمار**، **T6 لغو توسط کارکنان** و **T7 جابه‌جایی** اعمال می‌کند — رد با کد خطای **`HAS_ACTIVE_VISIT`** / **HTTP 409**. **چک‌این اکنون با همین معماری قفلِ موجودِ ردیف نوبت در برابر این تغییراتِ نوبت سریال می‌شود** (بررسی I-3 روی ردیف قفل‌شدهٔ نوبت، پیش از هر تغییر). اشاره‌گر کهنهٔ `active_visit_id` به‌تنهایی **واقعاً فعال تلقی نمی‌شود** و مانع‌ساز نیست. شواهد: RED نهاییِ پذیرفته‌شده `29bd8c36a263fc25b62a0f29554a7ce3224415a3` (CI run دقیق‌سر `35398347887` = `completed/failure` در همان head؛ ۱۸ موفق + ۱ شکست در check runهای همان SHA)؛ کامیت محصول GREEN = `6c16e296d73eb6caf7271d49634de58ca0e6b1c6`. صداقتِ تاریخچهٔ فرآیند (حفظ شد، بازنویسی نشد): تلاشِ نخستِ RED `14319d0fd38dae9fd55ff36e13b301a658934c9d` دارای شکست‌های جانبی ناشی از **آلودگی صف تست** بود — نقص‌های بهداشتیِ تست/هارنس که پیش از RED نهاییِ پذیرفته‌شده اصلاح شدند؛ آن تلاش به‌عنوان شاهدِ پاک ثبت **نمی‌شود**. شواهد نهایی پس‌از‌ادغام روی merge SHA `e60c6242…`: CI `35417703302` · Real WordPress Acceptance `35417703278` · Closure Gate `35417703283` · Pilot/Staging Readiness Gate `35417703280` — همه terminal-success + **۱۹/۱۹ check run موفق**. هیچ تغییر migration/schema لازم نبود؛ آخرین migration همچنان `0020` است و `0021` وجود ندارد/رزرو نشده. **بستنِ Slice 1 (PR #79) و دامنه/ادعاهایش بدون تغییر باقی است.** **Phase 7 به‌طور کامل بسته نیست؛ هیچ الزامِ دیگرِ FR-4.x بسته اعلام نمی‌شود؛ Slice 3 وجود ندارد/ادعا نمی‌شود.** این closure ادعای release/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد).

> **به‌روزرسانی 2026-09-19 (Phase 7 — bounded documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`d7484ceddf698898483122cf52bb5abb55718744`** است (**PR #85 MERGED 2026-09-19T12:04:04Z**؛ open PR در زمان بازبینی = ۰ — *(🔴 تاریخی — مرجعِ جاری اکنون در بلوکِ «به‌روزرسانی 2026-09-20 (Phase 8 Slices 1+2 — bounded post-merge documentation closure)» پایین است؛ «main جاری = d7484ce» و «آخرین migration = 0020» در این بند درستِ همان چک‌پوینت بود — متن تاریخی حفظ شد و بازنویسی نشد)*؛ پیش از آن `cf1ace137922d84b7042841e68629aedfb8334a8` = **PR #83 MERGED 2026-09-19T07:19:56Z** و `8c05c831aea6806038521bfb11fb6bcc5aa49cce` = PR #84 (فقط CI/tooling) MERGED 2026-09-19T06:54:45Z). **Phase 7 — Appointment Engine = CLOSED / TECHNICALLY COMPLETE (BOUNDED):** بسته‌شدنِ محدودِ فنی دقیقاً بر روی کارِ ادغام‌شدهٔ Phase 7 است: (۱) **زیرساخت‌های موجودِ `FR-4.x`** — hold با TTL پیش‌فرض ۱۰ دقیقه روی `cpms_slot_holds` + افزایش اتمیک `held_count`، انقضای خودکارِ هر-دقیقه‌ای idempotent (FR-4.4)، idempotency و atomic claim (از پیش موجود؛ یافتهٔ scopingِ فقط‌خواندنیِ Phase 7)؛ (۲) **Slice 1 / FR-4.6** (PR #79 — پیشنهاد جایگزین‌های `nearby_slots` روی `CLINIC_SLOT_TAKEN`)؛ (۳) **Slice 2 — I-3 + سریال‌سازیِ چک‌این** (PR #81)؛ (۴) **FR-5.3 جابه‌جاییِ منشی/کارکنان (T7)** (**PR #83**؛ merge `cf1ace13…`؛ head `54612ab8…` exact-head ۱۹/۱۹ چک موفق؛ post-merge روی merge SHA: CI `35429028253` · Real WordPress Acceptance `35429028209` · Closure Gate `35429028230` · Pilot/Staging Readiness Gate `35429028233` + ۱۹/۱۹ چک موفق) — هستهٔ مشترکِ reschedule + REST `POST /appointments/{id}/reschedule` (namespace مرسومِ `clinic/v1`) با `cpms_appt_reschedule` و Clinic scope معتبر: confirmed → rescheduled + یک جایگزین + پیوند دوطرفه؛ شمارنده‌های slot؛ fail-closed در نبودِ authorization و بین-کلینیک؛ `HAS_ACTIVE_VISIT`/409 در برابر Visit واقعاً فعال (اشاره‌گر کهنه مسدود نمی‌کند)؛ Idempotency-Key الزامی/replay؛ audit + اعلانِ داخلی + SMS جابه‌جایی. **سیاستِ محصولیِ Owner-issued (نه عبارتِ اصلی SRSِ FR-5.3 — SRS فقط «منشی می‌تواند بسازد/لغو/جابه‌جا کند در محدوده مجوز» می‌گوید):** staff از مهلتِ ۲۴ ساعتهٔ بیمار و min-leadِ مقصدِ بیمار معاف است؛ جابه‌جاییِ موفقِ staff هم اعلانِ داخلی به بیمار و هم SMS جابه‌جایی/تغییرِ موجود را می‌فرستد؛ سیاستِ بیمار (B5) دست‌نخورده می‌ماند. (۵) **FR-5.5 no-show دستی + خودکار (T8)** (**PR #85**؛ merge `d7484ce…` = main جاری؛ head `2bbb6085…` exact-head ۱۹/۱۹ چک موفق؛ post-merge روی merge SHA: CI `35441813653` · Real WordPress Acceptance `35441813668` · Closure Gate `35441813683` · Pilot/Staging Readiness Gate `35441813696` + ۱۹/۱۹ چک موفق) — **دستی:** سطح REST `POST /appointments/{id}/no-show` از طریق nonce → قابلیت `cpms_appt_no_show` → Clinic scope صریحِ معتبر (دلیل الزامی؛ نوشتنِ `no_show_at`؛ دلیل در audit `APPOINTMENT_NO_SHOW` حفظ و `appointments.reason` دست‌نخورده؛ شمارنده‌های slot دست‌نخورده؛ 403/404/403 fail-closed؛ Visit واقعاً فعال = 409 `HAS_ACTIVE_VISIT`؛ تکرار از وضعیت terminal = 409 `CLINIC_INVALID_TRANSITION`)؛ **خودکار:** sweep موجود پس از Grace Period (پیش‌فرض ۳۰ دقیقه بعد از Slot، قابل تنظیم؛ timezone موقعیت = منبعِ زمانِ عملیاتی) با **اصلاحِ اشاره‌گر کهنه** — اشاره‌گر کهنهٔ `active_visit_id` (به Visitِ غیرفعال/terminal) دیگر sweep را مسدود نمی‌کند و در تکمیلِ موفق پاک می‌شود، درحالی‌که Visit واقعاً فعال همچنان مسدود می‌کند (I-3) — و **سازگاریِ concurrency/ER-06**: چک‌این هرگز Visit واقعاً فعال را به نوبتِ terminalِ `no_show` متصل نمی‌کند (race واقعیِ test روی درخت ادغام‌شده). صداقت شواهد: عنوانِ PR #85 عبارتِ پیش‌از‌ادغام «DRAFT — TEST-ONLY RED» را حفظ کرده (تاریخِ Git به‌صورتِ عمدی بازنویسی نمی‌شود)؛ حالتِ نهاییِ ادغام‌شده شامل پیاده‌سازیِ GREEN محصول است و با ۱۹/۱۹ چک‌های merge SHA ثابت شده — عبارتِ عنوان به‌عنوان وضعیتِ merged گزارش **نمی‌شود**. **FR-5.4 — bounded non-claim (بدون اغراق ثبت می‌شود):** SRSِ FR-5.4 از `pending` با TTL استفاده می‌کند و «ساخته‌شده توسط منشی نیازمند تأیید پزشک» را **مثالِ توضیحی** است؛ معماریِ فعلی `pending` نوبت را **persistent نمی‌کند** — ظرفیتِ رزروِ تأییدنشده با **holdهای اسلات با TTL و انقضای خودکار** بیان می‌شود و createِ کارکنان به‌صورتِ عمدی create→pending→confirm را در یک operation جمع می‌کند (مستقیم `confirmed` ذخیره می‌کند — کامنتِ N-2 در `BookingService.php`)؛ بنابراین Phase 7 **هیچ workflow تأییدِ پزشکی را ادعا نمی‌کند** — چنین workflow پیاده‌سازی نشده و بدون scope محصولیِ آیندهٔ مالک‌رهبری‌شده نباید به‌عنوان الزامِ بسته‌شدن اختراع شود؛ **job انقضای نوبتِ persistent-pending وجود ندارد** (job انقضای موجود برای slot holds است، نه نوبت‌ها). آخرین migration همچنان `0020` است و `0021` وجود ندارد/رزرو نشده (روی `d7484ce…` راستی‌آزمایی شد: ۲۰ فایلِ migration `0001..0020`؛ جدیدترین = `2026_09_09_0020_idempotency_clinic_scope.php`). **هیچ فازِ دیگری کامل اعلام نمی‌شود؛ برشِ دیگرِ Phase 7 ادعا نمی‌شود.** این closure ادعای release/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد).

> **به‌روزرسانی 2026-09-20 (Phase 8 Slices 1+2 — bounded post-merge documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`440ba7e7d07579036128f288f502d390d27c98d3`** است (**PR #89 MERGED 2026-09-20T09:24:19Z** — "Merge pull request #89 from bia2on2on/arena/01a0bba9-doctor"؛ *(🔴 تاریخی — مرجعِ جاری اکنون در «به‌روزرسانی 2026-09-20 (Phase 8 Slice 3 — bounded post-merge documentation closure)» زیر است؛ «Slice 3 فقط در draft PR #90 فعال است» و «0022 روی main وجود ندارد» در این بند درستِ همان چک‌پوینتِ پیشا‌ادغام بود — متن تاریخی حفظ شد و بازنویسی نشد)* والدین `4122539885cadb28d69f62e765ee3368b50b0509` + `70d033cc3121519e882e2b18d4ada8ac1a303f81`؛ open PR در زمان بازبینی = ۱: **draft PR #90** `arena/01a0be60-doctor` @ `c17c965d32ec056f3e53be280173dc5182ef1c2e` — base `main`، **دست‌نخورده در این closure**؛ پیش از آن `4122539885cadb28d69f62e765ee3368b50b0509` = **PR #87 MERGED 2026-09-19T20:43:04Z**، `e00cec8dedd3245eb0c7dbfe772775c04e9af1f8` = **PR #88 MERGED 2026-09-19T17:52:42Z** (fixِ hardening زیرساختیِ multi-Clinic-safe — نه برشِ فاز) و `97efdb450d3e6238a8380c541d71a5122a7daada` = PR #86 (فقط docs). **Phase 8 — Patient Public Booking = IN PROGRESS؛ کامل نیست:** **Slice 1 = CLOSED (BOUNDED)** — سطحِ مشاهدهٔ عمومیِ آنونیم و read-only با shortcode صریح `clinic_id` (PR #87؛ head `0643e2b5…` exact-head ۱۹/۱۹؛ post-merge روی merge SHA `41225398…`: CI `35468244421` · Real WordPress Acceptance `35468244416` · Closure Gate `35468244423` · Pilot/Staging Readiness Gate `35468244448` — همه terminal-success)؛ **Slice 2 = CLOSED / MERGED via PR #89** — تکمیلِ جریانِ عمومیِ OTP→Holdِ احرازشده→confirm نهایی (head `70d033cc…` exact-head ۱۹/۱۹؛ post-merge روی merge SHA `440ba7e…`: CI `35502216463` · Real WP `35502216468` · Closure `35502216456` · Pilot/Staging `35502216452` + ۱۹/۱۹ check run موفق). **سیاست‌هایِ Slice 2 = Owner-issued (نه عبارتِ اصلی SRS)** — Hold-Timing (هیچ ظرفیتی پیش از احراز hold نمی‌شود) و حداقلِ هویتِ Patient جدید (gate نام در B2؛ Hold فعال/قابل-retry می‌ماند)؛ سندِ مرجع: `docs/decisions/2026-09-20-phase8-slice2-owner-decisions.md`. **Migration `2026_09_20_0021` (`cpms_otp_tokens.clinic_id`) اکنون آخرین migration روی main است** (۲۱ فایل `0001..0021`؛ روی `440ba7e…` راستی‌آزمایی شد)؛ **`0022` روی main وجود ندارد** — فقط رزروِ شاخهٔ draft PR #90 است و به‌عنوان migrationِ main توصیف **نمی‌شود**. **Slice 3 فقط در draft PR #90 فعال است — پذیرفته/merge نشده و اینجا ادعا نمی‌شود.** عباراتِ «آخرین migration = 0020» در بلوک‌های تاریخیِ بالاتر، درستِ چک‌پوینتِ خودشان هستند و بازنویسی نشدند. این closure ادعای release/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد؛ چیزی از PR #90 cherry-pick نشد).
> **به‌روزرسانی 2026-09-20 (Phase 8 Slice 3 — bounded post-merge documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`9abd3379a02a320705cadac5229726bbd1b202cb`** است (**PR #90 MERGED 2026-09-20T17:04:39Z** — "Merge pull request #90 from bia2on2on/arena/01a0be60-doctor"؛ *(🔴 تاریخی — مرجعِ جاری اکنون در بلوکِ «به‌روزرسانی 2026-09-20 (Phase 9 Slice 1 — bounded post-merge documentation closure)» زیر است؛ «Phase 9 شروع نشده» و «هیچ قلمِ deferred بازی (Patient Portal / Phase 9) به این closure کشیده نمی‌شود» در این بند درستِ همان چک‌پوینت بود — متن تاریخی حفظ شد و بازنویسی نشد)* والدین `7873d46eab1c47cc0161c8f5ebb4205938976d16` + `0f044eb518d1e9a3c91f48a49a05df4f5c62ba83` (the exact accepted PR #90 head)؛ open PR در زمان بازبینی = ۰). **Phase 8 — Patient Public Booking = CLOSED / TECHNICALLY COMPLETE (BOUNDED):** Slice 1 = CLOSED (BOUNDED — PR #87، merge `41225398…`)؛ Slice 2 = CLOSED (PR #89، merge `440ba7e…`)؛ **Slice 3 = CLOSED / MERGED via PR #90** — linked-Patient booking-subject selection (head `0f044eb5…` exact-head ۱۹/۱۹؛ post-merge روی merge SHA `9abd3379…`: CI `35524710717` · Real WordPress Acceptance `35524710714` · Closure Gate `35524710737` · Pilot/Staging Readiness Gate `35524710697` — همه terminal-success + ۱۹/۱۹ check run موفق؛ عنوانِ PR عبارتِ پیشا‌ادغامِ «test(red)…(RED)» را حفظ کرده — تاریخِ Git بازنویسی نمی‌شود؛ حالتِ نهاییِ ادغام‌شده شامل پیاده‌سازیِ GREEN محصول است). **Migration `2026_09_20_0022_slot_holds_patient_binding.php` اکنون آخرین migration روی main است** (۲۲ فایل `0001..0022`؛ روی `9abd3379…` راستی‌آزمایی شد؛ با PR #90 در دامنهٔ پذیرفته‌شدهٔ Slice-3 ادغام شد)؛ عبارتِ «0022 روی main وجود ندارد» در بلوکِ «Slices 1+2» بالاتر درستِ همان چک‌پوینتِ پیشا‌ادغام بود و به‌عنوان تاریخ حفظ شد. سیاست‌های Owner-issued قبلی (Hold-Timing؛ حداقلِ هویتِ بیمارِ جدید؛ OTPِ مقید به Clinic؛ reuseِ قطعیِ WP-user) و تفکیکِ آن‌ها از عبارتِ اصلی SRS بدون تغییر باقی است؛ PR #88 همچنان fixِ hardeningِ غیربرشی است. هیچ فازِ دیگری کامل اعلام نمی‌شود و هیچ قلمِ deferred بازی (Patient Portal / Phase 9 و غیره) به این closure کشیده نمی‌شود. این closure ادعای release/go-live/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/migration/schema/configuration تغییر نکرد).
> **به‌روزرسانی 2026-09-20 (Phase 9 Slice 1 — bounded post-merge documentation closure):** مرجع جاری و راستی‌آزمایی‌شدهٔ `origin/main` = **`a593958521386d459f76c5efa839d9962e1f67fc`** است (**PR #93 MERGED 2026-09-20T21:52:03Z** — "Merge pull request #93 from bia2on2on/arena/01a0c007-doctor"؛ والدین `b6c34c116807c99105aa603fc71ee220475a47a7` (= merge **PR #94** — fixِ collectorِ added-linesِ WPCS + governanceِ ratchet) + `9e7058cf9768ccb4dfce56eafbfb2c85f5e7c9bb` (= accepted headِ PR #93)؛ open PR در زمان بازبینی = ۰؛ هیچ closure PR موجودی #93 را پوشش نمی‌داد). **Phase 9 — Patient Portal = STARTED / IN PROGRESS؛ کامل نیست:** **Slice 1 = CLOSED (TECHNICAL; BOUNDED)** — **patient self-cancel از همان سطحِ موجودِ patient portal با استفاده از بک‌اندِ موجودِ B4** (`POST clinic/v1/appointments/{id}/cancel`)؛ **بدونِ REST route جدید**؛ **بدونِ تغییر schema** (accepted head `9e7058cf…` exact-head ۱۹/۱۹ چک موفق؛ post-merge روی merge SHA `a593958…`: CI `35539966134` · Real WordPress Acceptance `35539966131` · Closure Gate `35539966142` · Pilot/Staging Readiness Gate `35539966158` — همه `push`→`main`، bound به همان merge SHA و همه terminal-success). صداقتِ شواهد: عنوانِ PR #93 عبارتِ پیش‌از‌ادغامِ «test(red) … (RED)» را حفظ کرده — تاریخِ Git بازنویسی نمی‌شود؛ حالتِ نهاییِ ادغام‌شده شامل پیاده‌سازیِ GREEN محصول است و با ۱۹/۱۹ چکِ merge SHA ثابت شده. **آخرین migration روی main همچنان `2026_09_20_0022_slot_holds_patient_binding.php` است** (۲۲ فایل `0001`..`0022`؛ روی `a593958…` راستی‌آزمایی شد؛ `0023` وجود ندارد/رزرو نشده). **Phase 8 = CLOSED / TECHNICALLY COMPLETE (BOUNDED) بدون تغییر باقی است.** **PR #94** همچنان fixِ زیرساختِ CI + governanceِ WPCS ratchet است — **برشِ فاز نیست** و governanceِ ratchetِ آن (مبنای `2263ed67…`) بدون تغییر حفظ شد. **Phase 9 کامل نیست؛ Patient Portal به‌طور کامل ادعا نمی‌شود؛ Slice 2 در این PR scope/آغاز/ادعا نشده است.** این closure ادعای release/go-live/V1/تجاری نیست و فقط مستندات است (هیچ کد/تست/workflow/asset/migration/schema/runtime تغییر نکرد). `CHANGELOG.md` عمداً تغییر نکرد: عرفِ زندهٔ مخزن برای هر برشِ محدودِ محصول ورودیِ Unreleased **الزام نمی‌کند** — جدیدترین ورودی مربوط به 2026-09-12 است و Phase 3..8 (PRs #49–#90) هیچ ورودی ندارند.

> **وضعیت (2026-09-11 — snapshot؛ موارد C10 و چک‌پوینتِ آن با «به‌روزرسانی 2026-09-12» بالا جایگزین شد):** مرجع معتبر = [`project-current-state.md`](project-current-state.md). خلاصه: **Phase 0 / 0.5 = CLOSED** · **Phase 1A = CLOSED · Phase 1B = DEFERRED** · **Phase 2 (Multi-Clinic Core) = IN PROGRESS** (C4/C5/C6 CLOSED؛ **C7 = CLOSED** — PR #20 MERGED، merge = `a385d868`؛ **پذیرش/بستن رسمی با تصمیم صریح مالک در 2026-09-11**؛ فقط دامنهٔ تعریف‌شدهٔ C7) · **C8 = CLOSED به‌عنوان بستهٔ شواهد/مستنداتِ فوندیشن Location (بدون پیاده‌سازی)** · **C9 = CLOSED** — پیاده‌سازیِ محدودشده از طریق **PR #23 MERGED** در چک‌پوینت `7146d5b` ادغام شد و **پذیرش/بستن رسمی با تصمیم صریح مالک در 2026-09-11 (پس از ادغام)**؛ فقط دامنهٔ محدودشده: دو یافتهٔ A/Low که خودِ C7 معرفی کرده بود، در مرز REST، با حفظ `code`/status/`data` و فارسیِ پیش‌فرض؛ بدون تغییر Domain/Application، بدون migration، بدون ابزار گارْد جدید؛ **نه** رفعِ همهٔ بدهیِ i18n، **نه** پشتیبانیِ کاملِ انگلیسی، **نه** آمادگیِ globalization، **نه** بسته‌شدنِ Phase 2 (قاعدهٔ لایه‌بندیِ بومی‌سازی و بدهیِ تحمّل‌شده در `project-current-state.md` §I‑1 و `phase2-state.md` §C9) · **C10 = NOT STARTED و مجاز نشده** *(🔴 تاریخی — اکنون **C10 = CLOSED** با تصمیم صریح مالک 2026-09-12؛ رجوع به «به‌روزرسانی 2026-09-12» بالا)* (عملکرد: شواهد benchmark واقعیِ **محدودِ Staging** موجود است — `report-pilot-gate.md` §8 + گامِ `ab` در `pilot-gate.yml`؛ ولی **عملکردِ محیط مرجع/تجاری اندازه‌گیری‌نشده/اعتبارسنجی‌نشده** است و آن گزارش ادعای پاس‌شدنِ آستانه‌ها را نکرد — §12.3 ‏BLOCKED_BY_ENVIRONMENT) · **Phase 3 = NOT STARTED** *(🔴 تاریخی — اکنون **Phase 3 = COMPLETED / FROZEN** در `ebf8588`؛ رجوع به «به‌روزرسانی 2026-09-16» بالا)* · آخرین migration = `0020` (‏`0021` ساخته/تصویب نشده) · چک‌پوینت جاریِ `origin/main` = `7146d5b` *(تاریخی — چک‌پوینت جاری در «به‌روزرسانی 2026-09-12» بالا و در `project-current-state.md`)*.

> **🎯 تصمیم محصول (ADR-0027، 2026-09-06):** **یک محصول واحد چندپزشکی** — مطب تک‌پزشکی = زیرمجموعه UX درمانگاه چندپزشکی؛ یک Core/یک Schema/Features تطبیقی. **این اصل معتبر است.**
>
> ⚠️ **اما مدل دامنهٔ ADR-0027 با [ADR-0031](adr/ADR-0031-organization-clinic-location-scoped-authorization.md) جایگزین شد** (لایهٔ Organization نداشت؛ «Branch» هرگز به schema نرسید). واژهٔ «Branch» از این پس = **Location**.
>
> ⛔ **`architecture/multi-doctor-readiness-review.md` و حکم «۰ FOUNDATIONAL» آن با شواهد اجرایی Phase 0 رد شد** — ۹ قید معماری C-1..C-9 در [`phase-reports/report-phase-0-reverification.md`](phase-reports/report-phase-0-reverification.md). آن سند تاریخی است.

## اسناد مرجع فعلی (بالاترین تقدم)

| سند | نقش |
|---|---|
| [`project-current-state.md`](project-current-state.md) | **وضعیت جاری** + checkpoint ادغام‌شده + بازیابی بدون تاریخچهٔ چت |
| [`roadmap/roadmap.md`](roadmap/roadmap.md) §۰ | **Owner-approved Phase 0..20** — مرجع فازبندی اجرایی |
| [`adr/ADR-0031`](adr/ADR-0031-organization-clinic-location-scoped-authorization.md) | معماری مرجع + ۱۳ تصمیم AD-01..AD-13 |
| [`phase-reports/report-phase-0-reverification.md`](phase-reports/report-phase-0-reverification.md) | خط پایهٔ اثبات‌شده + ۹ قید معماری C-1..C-9 |
| [`architecture/phase0.5-target-model.md`](architecture/phase0.5-target-model.md) | مدل هدف، ERD، برنامهٔ Migration، Decision Register (Q1..Q13) |
| [`drift-register.md`](drift-register.md) | تعارض اسناد با معماری هدف + فاز مالک هر مورد |
| [`architecture/phase2-tenant-context-remediation-design.md`](architecture/phase2-tenant-context-remediation-design.md) | **🔴 APPROVED DESIGN DIRECTION — NOT YET IMPLEMENTED** — طراحی ترمیم Tenant Context در Jobهای پس‌زمینه / timezone عملیاتیِ Location / پیکربندی SMS per-Clinic (**شامل credentialِ sealed**) / انتساب tenant در لاگ عملیاتی / طبقه‌بندیِ **T/S/W** برای **۱۵ نوعِ واقعیِ ثبت‌شده** (§A-3) + **۶ سوالِ طراحیِ پیش از هر migration آینده** (بدونِ رزروِ شماره) + مشخصاتِ **۱۴ تستِ RED ‏(RT-1..RT-14)**. **بازبینیِ مستقلِ معماری 2026-09-12 → حکمِ مالک `B`؛ اصلاحاتِ C-1..C-9 اعمال شد** (**فقط طراحی — هیچ پیاده‌سازی/تستی انجام نشده**) |
| [`agent-guide.md`](agent-guide.md) | راهنمای عملیاتی ایجنت‌ها + قواعد الزامی |

---

## شاخه‌بندی مستندات (Doc-Phase 1..8 — نظام مستندسازی، **Legacy**)

> ⛔ **هشدار شماره‌گذاری:** ستون «فاز» در جدول زیر به **Doc-Phase** اشاره دارد — فازهای *مستندسازی* مطابق Section 56 Master Prompt. **هیچ ربطی به Phase 0..20 اجرایی ندارد.** «Doc-Phase 2» (Permission Matrix) ≠ «Phase 2» (Multi-Clinic Core).
>
> ⚠️ ستون «وضعیت» (`منتظر تأیید`) از مخزن قابل راستی‌آزمایی نیست — **OPEN DECISION (OD-4)** در Drift Register.

| مسیر | محتوا | Doc-Phase | وضعیت |
|---|---|---|---|
| [srs/SRS.md](srs/SRS.md) | نیازمندی‌های نرم‌افزار، شماره‌گذاری‌شده | 1 | منتظر تأیید |
| [srs/use-cases.md](srs/use-cases.md) | فهرست Use Case | 1 | منتظر تأیید |
| [permissions/permission-matrix.md](permissions/permission-matrix.md) | ماتریس دسترسی + مدل Capability | 2 | منتظر تأیید |
| [state-machines/appointment.md](state-machines/appointment.md) | State Machine نوبت | 3 | منتظر تأیید |
| [state-machines/visit-queue.md](state-machines/visit-queue.md) | State Machine مراجعه/صف | 3 | منتظر تأیید |
| [state-machines/payment.md](state-machines/payment.md) | State Machine مالی | 3 | منتظر تأیید |
| [erd/erd.md](erd/erd.md) | ERD کامل + تصمیمات ساختاری | 4 | منتظر تأیید |
| [erd/data-dictionary.md](erd/data-dictionary.md) | Data Dictionary + Indexes/Constraints | 4 | منتظر تأیید |
| [api/api-contract.md](api/api-contract.md) | قرارداد REST API | 5 | منتظر تأیید |
| [security/auth-authorization.md](security/auth-authorization.md) | طراحی احراز هویت/مجوز + OTP | 5/7 | منتظر تأیید |
| [wireframes/patient.md](wireframes/patient.md) | وایرفریم بیمار (Mobile-First) | 6 | منتظر تأیید |
| [wireframes/secretary.md](wireframes/secretary.md) | وایرفریم منشی | 6 | منتظر تأیید |
| [wireframes/doctor.md](wireframes/doctor.md) | وایرفریم پزشک (Tablet/Pen) | 6 | منتظر تأیید |
| [security/threat-model.md](security/threat-model.md) | Threat Model (STRIDE) | 7 | منتظر تأیید |
| [security/audit-strategy.md](security/audit-strategy.md) | استراتژی Audit Log | 7 | منتظر تأیید |
| [architecture/file-storage.md](architecture/file-storage.md) | استراتژی نگهداری فایل | 7 | منتظر تأیید |
| [architecture/handwriting-storage.md](architecture/handwriting-storage.md) | معماری ذخیره دست‌خط | 7 | منتظر تأیید |
| [architecture/handwriting-recognition.md](architecture/handwriting-recognition.md) | معماری تشخیص دست‌خط | 7 | منتظر تأیید |
| [architecture/notifications.md](architecture/notifications.md) | معماری اعلان | 7 | منتظر تأیید |
| [architecture/multi-doctor-readiness-review.md](architecture/multi-doctor-readiness-review.md) | بازبینی آمادگی چندپزشکی (ADR-0027) + Backlog هم‌ترازی | — | ⛔ **Historical** — حکم «۰ FOUNDATIONAL» با Phase 0 رد شد |
| [architecture/phase0.5-target-model.md](architecture/phase0.5-target-model.md) | مدل هدف + ERD + Migration Plan + Decision Register | — | ✅ Finalized |
| [phase-reports/report-phase-0-reverification.md](phase-reports/report-phase-0-reverification.md) | گزارش Phase 0 + Re-verification + ۹ قید معماری | — | ✅ CLOSED |
| [drift-register.md](drift-register.md) | تعارض اسناد با معماری هدف + فاز مالک | — | ✅ فعال |
| [architecture/background-jobs.md](architecture/background-jobs.md) | معماری Background Jobs | 7 | منتظر تأیید |
| [architecture/phase2-tenant-context-remediation-design.md](architecture/phase2-tenant-context-remediation-design.md) | طراحی ترمیم Tenant Context فاز ۲ (Jobها/timezone/SMS/لاگ عملیاتی) | — | 🔴 **طراحی تأییدشده — پیاده‌سازی نشده (NOT YET IMPLEMENTED)** |
| [backup/backup-recovery.md](backup/backup-recovery.md) | برنامه Backup/DR | 7 | منتظر تأیید |
| [testing/testing-plan.md](testing/testing-plan.md) | برنامه تست | 8 | منتظر تأیید |
| [scope/mvp-scope.md](scope/mvp-scope.md) | محدوده MVP/V1 | 8 | منتظر تأیید |
| [roadmap/roadmap.md](roadmap/roadmap.md) | نقشه راه توسعه | 8 | منتظر تأیید |
| [adr/](adr/) | Architecture Decision Records | 1-8 | Accepted (قابل بازبینی) |
| [phase-reports/report-phase1-8.md](phase-reports/report-phase1-8.md) | گزارش فاز + تصمیمات + ریسک‌ها + آیتم‌های تصمیم‌گیری کارفرما | — | — |

## قوانین

- هیچ کد اصلی بدون تأیید شش سند بالادستی (SRS، Permission، State Machines، ERD، API، Wireframes) شروع نمی‌شود.
  > تصحیح شماره‌گذاری: عبارت قبلی «Phase 9+» بود که به **Doc-Phase** اشاره داشت، نه به Phase 9 در Roadmap تأییدشدهٔ Owner.
- **قاعدهٔ اجرا از Phase 1 به بعد:** code + tests + documentation **همراه هم** به‌روز می‌شوند. checkpoint commit کوچک مجاز؛ **merge فقط پس از Gate نهایی فاز**.
- **اسناد تاریخی rewrite نمی‌شوند** — ADR تاریخی **Superseded** می‌شود، نه پاک.
- هر تغییر فاز قبل: Impact Analysis + Version مجدد مستند.
- کدگذاری نیازمندی‌ها: `FR-x.y` (عملکردی)، `NFR-x` (غیرعملکردی)، `ER-x` (Edge Case)، `UC-x` (Use Case).
