# تصمیم مالک — Phase 10 «Doctor Portal»: مجوز شروع + نخستین برش محدود

> **نوع سند:** ثبت تصمیم مالک (owner decision record) — فقط ثبت، بدون پیاده‌سازی.
> **تاریخ ثبت:** 2026-09-24.
> **وضعیتِ زندهٔ مرجع:** `main` = `84d664982517dd98c30678816e068e38c7d541fe` (آخرین چک‌پوینتِ زنده؛ این ادغام فقط یک اصلاحِ زیرساختِ تست/هارنس است و **هیچ تغییری در دامنهٔ محصول ایجاد نمی‌کند**).
> این سند جایگزینِ هیچ سابقهٔ تاریخی نمی‌شود و تاریخچه بازنویسی نمی‌گردد.

---

## بخش اول — مجوزِ صریحِ شروعِ Phase 10

**مالک شروعِ Phase 10 — Doctor Portal را صراحتاً مجاز دانست.**

تا پیش از این مجوز، وضعیتِ زنده و صحیحِ Phase 10 در اسناد جاری **«شروع‌نشده / NOT STARTED»** بود
(برای نمونه: `docs/roadmap/roadmap.md` ردیفِ Phase 10 = ⏳؛ `docs/drift-register.md`؛
`docs/governance/project-phase-taxonomy.md`؛ بلوکِ چک‌پوینتِ `docs/project-current-state.md`).
آن وضعیتِ «شروع‌نشده» **تاریخاً درست بود** و به‌عنوان سابقهٔ تاریخی حفظ می‌شود — بازنویسی نمی‌شود
تا وانمود شود Phase 10 همیشه در حال اجرا بوده است.

پس از مجوزِ مالک، وضعیتِ زندهٔ Phase 10 = **IN PROGRESS** است.

**Phase 10 را نباید CLOSED دانست.** فقط نخستین برشِ محدودِ آن بسته شده است (بخش دوم).

---

## بخش دوم — نخستین برشِ محدودِ مصوّب

**مالک نخستین برشِ محدود (bounded slice) را به‌عنوانِ «زیرساختِ مستقلِ Doctor Portal / Today + Live Queue» تصویب کرد.**

سپس **«نوبت‌های امروز / Today's Appointments»** نیز صراحتاً به‌عنوان یک **افزودهٔ محدودِ فقط‌خواندنی
(read-only) به همان نخستین برش** تصویب شد.

بنابراین دامنهٔ مصوّبِ نخستین برش عبارت است از:

- زیرساخت/پوستهٔ **مستقلِ** Doctor Portal — بدون chrome/نوارابزارِ wp-admin و بدون قالبِ (theme) وردپرس
- زیرساختِ **مشترکِ احراز هویت/نشستِ وردپرس** (بدون سامانهٔ احراز هویتِ جدید)
- مرزِ **فقط‌پزشک (doctor-only)** برای پورتال
- هویتِ حرفه‌ایِ **استخراج‌شده از سرور** (server-derived) — نه از ورودیِ کلاینت
- Scopeِ مطبِ (Clinic) موثق
- Scopeِ عملیاتیِ مکان (Location) موثق
- رفتارِ انتخابِ مکان بر اساسِ ۰/۱/N مکانِ واجدِ شرایط
- مکان به‌عنوان **مرجعِ منطقهٔ زمانیِ عملیاتی**
- روزِ عملیاتیِ **محلیِ مکان** (Location-local operational day)
- خلاصهٔ **امروز (Today)** — فقط‌خواندنی
- **صف زنده (Live Queue)** — فقط‌خواندنی
- **نوبت‌های امروز (Today's Appointments)** — فقط‌خواندنی، افزودهٔ محدودِ مصوّب

**۰/۱/N مکان — معنای دقیقِ مصوّب:** ۰ مکانِ واجد ⇒ fail closed (بدون داده)؛ ۱ مکانِ واجد ⇒
حلِ خودکار؛ N>1 بدون انتخابِ صریح ⇒ **انتخابِ صریحِ مکان الزامی**؛ **هیچ fallbackـِ «اول/اصلی» وجود ندارد**.

---

## بخش سوم — قصدِ پایدارِ محصول (دامنهٔ کاربرد)

**مالک قیدِ پایدارِ محصول را چنین تصریح کرده است — محصول باید برای هر دو حالت کار کند:**

1. **مطبِ کوچک (small practice):** یک پزشک + یک منشی، با یک یا چند مکان (Location) ی اندک؛
2. **بیمارستان / مرکز درمانیِ بزرگ‌تر:** چند مطب (Clinic)، چند مکان، چند پزشک و چند کارمند.

این ثبت، **فقط قصدِ پایدارِ محصول** است. **هیچ قراردادِ نقش/دسترسیِ حدسی (speculative role/permission
contract) در این برش افزوده نمی‌شود** و هیچ ساختارِ سازمانیِ تازه‌ای در این سند پیش‌فرض گرفته نمی‌شود.

---

## بخش چهارم — الزاماتِ آیندهٔ مالک که در این ثبت «پیاده‌سازی‌شده» اعلام **نمی‌شوند**

مالک الزاماتِ گسترده‌تری برای گردش‌کارهای آیندهٔ پزشک/مطب بیان کرده است.
**صرفِ وجودِ بخش‌هایی در بک‌اند به‌هیچ‌وجه به‌معنای تکمیلِ این موارد نیست** و در این سند هیچ‌کدام
تکمیل‌شده اعلام نمی‌شود. همهٔ موارد زیر **در حالِ تعلیق / پیاده‌سازی‌نشده (pending / NOT IMPLEMENTED)** می‌مانند:

- اقداماتِ صف در Doctor Portal: **call / start / recall / skip**
- عملیاتِ کامل‌ترِ نوبت‌های روزانه (broader appointment operations)
- فضای کاریِ ویزیت (visit workspace)
- تجربهٔ نگارشِ یادداشتِ خصوصی/مشترک (note authoring UX)
- گردش‌کارِ ساختاریافتهٔ نوشتنِ نسخه (prescriptions workflow)
- یکپارچه‌سازیِ دست‌خط/قلم (handwriting integration)
- گردش‌کارِ فایل‌ها در Doctor Portal (files workflow)
- جستجوی بیمار (patient search)
- مدیریتِ حضور/برنامهٔ زمانیِ پزشک (doctor schedule/presence management)
- مدیریتِ مکان‌هایِ فعالیتِ پزشک (doctor practice-location management)
- گردش‌کارِ مالی/حق‌ویزیت (finance / visit fee)
- گردش‌کارِ منشی/صندوق‌دار (secretary / cashier workflow)
- سفارشی‌سازی/ویرایشِ MRN (MRN customization/editing)

**Phase 10 = IN PROGRESS.**

---

## بخش پنجم — مرجعِ بستنِ برش

بستنِ فنّیِ نخستین برشِ محدود (Phase 10 Slice 1) با **PR #113** ثبت می‌شود:

- **merge SHA:** `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`
- **accepted head:** `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`
- گیت‌هایِ پس‌از‌ادغام روی **exact merge SHA** همگی terminal-success؛ **بدون migration**؛
  آخرین migration همچنان `2026_09_20_0022_slot_holds_patient_binding.php`.
- **PR #112 = تاریخی/منسوخ — CLOSED / NOT MERGED؛ PR #113 ادامهٔ معتبرِ ادغام‌شده است.**

**وضعیتِ زندهٔ کنونی:** `main` از آن چک‌پوینت فراتر رفته و اکنون
`84d664982517dd98c30678816e068e38c7d541fe` است؛ این تغییر فقط یک اصلاحِ زیرساختِ تست/هارنس است
(یک فایلِ غیرِ محصولی) و **هیچ تغییرِ دامنه/محصول/فاز ایجاد نکرده است** — بستنِ Slice 1 همچنان
مبتنی بر **PR #113** است.

جزئیات و شواهد در [`docs/project-current-state.md`](../project-current-state.md) و
[`docs/agent-guide.md`](../agent-guide.md) ثبت شده است.
