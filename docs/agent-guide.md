# راهنمای جامع ادامه پروژه — CPMS (کلینیک)

> **این فایل سند жив پروژه است.** هر ایجنت (AI یا انسان) که روی این repo کار می‌کند **باید** قبل از شروع این فایل را کامل بخواند و بعد از پایان کارِ خود، در بخش «لاگ کار ایجنت‌ها» (انتهای همین فایل) ورودی ثبت کند.

نسخه: 2.0 | آخرین به‌روزرسانی: 2026-09-08 | نگهدارنده: ایجنت‌های Arena (به‌صورت append-only)

---

> # 🔴 مرجع فازبندی — پیش از هر چیز این را بخوان
>
> ## **Owner-approved Phase 0..20 Roadmap = authoritative execution roadmap.**
>
> مرجع رسمی: [`docs/roadmap/roadmap.md`](roadmap/roadmap.md) **§۰**.
>
> نظام‌های **`F0..F10`**، **`Doc-Phase 1..8`** و برچسب‌های **`V1` / `V1.5` / `V2`** از 2026-09-08 **Legacy/Historical** هستند. در متن همین فایل و در گزارش‌های `docs/phase-reports/` فراوان دیده می‌شوند — **آن‌ها را به‌عنوان Roadmap اجرایی فعلی تفسیر نکن.** جدول نگاشتشان در `roadmap.md` §۲-۱ است.
>
> | Phase | عنوان | وضعیت |
> |---|---|---|
> | Phase 0 | Git Checkpoint | ✅ CLOSED — [`report-phase-0-reverification.md`](phase-reports/report-phase-0-reverification.md) |
> | Phase 0.5 | Target Architecture & Migration Plan | ✅ CLOSED — [`phase0.5-target-model.md`](architecture/phase0.5-target-model.md) |
> | Phase 1 | Security Hardening | ⏸ منتظر Gate Approval |
> | Phase 2 | Multi-Clinic Core | ✅ CLOSED (TECHNICAL) — see the current-state checkpoint |
> | Phase 3 | Role & Access Control | ✅ COMPLETED / FROZEN — merged PRs #49/#50/#52/#54/#55/#56/#57 |
> | Phase 4 | Master Data | ✅ CLOSED / TECHNICALLY COMPLETE (BOUNDED) — merged PRs #59–#65 at live main `70ace204d1524a8f5e83d33c67c1a09b7543e7a3`; remaining deferred/open Master Data items stay deferred/open |
> | Phase 5 | Pricing Engine | ✅ CLOSED / TECHNICALLY COMPLETE (BOUNDED) — Phase 5 Slice 1 merged via **PR #67** (merge `e063b42260cb5ab740acf48dcf73c6c67b11fef6`, 2026-09-17T15:36:54Z). Base pricing is **Clinic-scoped** and sufficient for the current approved V1 flow; `invoice.location_id` is derived from the validated Visit. Per-Location tariff for the *same* Service is **NOT** a proven V1 requirement (deferred on absence of a proven requirement — **not** on any single-Clinic/single-Location assumption). Latest migration remains `0020` |
> | Phase 6 | Scheduling Engine | ✅ CLOSED / TECHNICALLY COMPLETE (BOUNDED) — merged **PRs #69–#71 + #73–#76** (the range also contains the documentation-only **PR #72** — owner-requirement preservation, not a slice) at live main `bd5e6a1819a838648dbdcbc6914c0d8b24bba38b` (**PR #76** MERGED 2026-09-18T09:59:44Z; head `bd840163`). **Slice 7 = CLOSED** (concurrency-safe multi-shift conflict enforcement; accepted VALID RED `fca63d3a…`; `091fae1c…` / `c8b95920…` **remain INVALID RED**; post-merge on the merge SHA: four required workflows terminal-success + 19/19 checks success). Coverage preserved for `FR-3.1`–`FR-3.4`, `FR-3.6`–`FR-3.10` — **`FR-3.5` = Phase 8, not claimed**. The Phase-6 deferred hardening (ج / د) is **CLOSED by the C7 hardening slice (PR #78**, MERGED 2026-09-18T15:07:23Z, merge `4b322d9dc8839e1efd66d712aedc06d9f22cd175` — no-scope READ paths `list()`/`listExceptions()` fail closed without an explicit trusted Clinic scope; numeric payload `horizon_days` now shares the `1..365` bound, `> 365` fails closed/skips under the established invalid-horizon behavior; post-merge on the merge SHA: four required workflows terminal-success + 19/19 checks success); the remaining write-helper no-scope compatibility branches were reconstructed after PR #78 as **NO CHANGE JUSTIFIED** (production REST/wp-admin write callers establish explicit trusted scope; no verified compatibility consumer depends on the branch — not another required C7 patch). Latest migration remains `0020` |
> | Phase 7 | Appointment Engine | ✅ **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** — closed on the merged Phase 7 work: existing `FR-4.x` booking primitives (slot holds with TTL + automatic expiry, idempotency, atomic claim); **Slice 1 / FR-4.6** (PR #79); **Slice 2 — I-3 active-Visit protection (T5/T6/T7) + check-in serialization** (PR #81); **FR-5.3 staff/secretary reschedule (T7)** (PR #83, MERGED 2026-09-19T07:19:56Z, merge `cf1ace137922d84b7042841e68629aedfb8334a8`, head `54612ab8…` exact-head 19/19 checks success, post-merge on the merge SHA 19/19); **FR-5.5 manual + automatic no-show completion (T8) + stale-pointer correction + concurrency/ER-06 compatibility** (PR #85, MERGED 2026-09-19T12:04:04Z, merge `d7484ceddf698898483122cf52bb5abb55718744` = current main, head `2bbb6085…` exact-head 19/19 checks success, post-merge on the merge SHA 19/19). **FR-5.3 owner-issued product policy (NOT original SRS wording):** staff bypasses the patient 24h reschedule deadline and the patient destination min-lead; a successful staff reschedule produces the internal notification + reschedule SMS; patient B5 policy unchanged. **FR-5.4 bounded non-claim:** SRS FR-5.4 uses `pending` with TTL and gives doctor-approval as an illustrative example; the current architecture does not persist appointment `pending` (unconfirmed capacity = slot holds with TTL + automatic expiry; staff create collapses create→pending→confirm in one operation, persisting `confirmed`); no doctor-approval workflow is claimed (none is implemented; none may be invented without future owner-directed product scope); no persisted pending-appointment expiry job exists. — **Slice 1:** merged **PR #79** (merge `757d7424332d87cdd3d1894ee39f9f5f01bf0a33`, MERGED 2026-09-18T18:12:54Z; accepted VALID RED `81d06619c63e23212c209bf8d30ff9ddd45dfaa6` / CI run `35370158310` (A/B/C); historical attempt `b690e6c550f726a0329da51fbd37519e307f5c5e` / CI run `35369425482`: B valid at that attempt, A/C **remain INVALID RED** (class D — fixture arithmetic — not rewritten); GREEN product commit `dfb37d41eba75c8543abea574f18858370aacf0d`; post-merge on the merge SHA: four required workflows terminal-success + 19/19 checks success). Owner-issued product policy (**distinct from the original SRS wording of FR-4.6** — not an SRS claim): additive `BookingException` data `nearby_slots` on `CLINIC_SLOT_TAKEN` race loss — up to 5 free alternatives, same local date, same losing Location, same Clinic + clinician from the persisted losing-slot context, established deterministic ordering, `[]` if no eligible alternative; existing code/message/HTTP 409 unchanged. The deterministic fixture B exercises the real `confirm()`/`atomicClaim` failure branch but is **not** a runtime reproduction of a genuine concurrency race. **Slice 2 — appointment invariant I-3 (`HAS_ACTIVE_VISIT`) + check-in serialization = CLOSED (BOUNDED):** merged **PR #81** (merge `e60c62428e6109e7182d04d3266f0d15b12b0d2f`, MERGED 2026-09-19T03:08:27Z; accepted final RED `29bd8c36a263fc25b62a0f29554a7ce3224415a3` / exact-head RED CI run `35398347887` = `completed/failure` at that head; GREEN product commit `6c16e296d73eb6caf7271d49634de58ca0e6b1c6`; post-merge on the merge SHA: four required workflows terminal-success — CI `35417703302` · Real WP `35417703278` · Closure Gate `35417703283` · Pilot/Staging `35417703280` — + 19/19 checks success). Scope: enforcement of invariant **I-3** (`docs/state-machines/appointment.md` §4) while a **genuinely active Visit** exists (connected Visit in one of the established live `ACTIVE_STATUSES` with `active = 1`) for **T5 patient cancel**, **T6 staff cancel**, **T7 reschedule** — rejected with **`HAS_ACTIVE_VISIT`** / **HTTP 409**; **check-in serializes against these appointment mutations using the existing appointment-row locking architecture**; a **stale `active_visit_id` pointer by itself is NOT treated as a genuinely active Visit**; **no migration/schema change was required**. Process history preserved, not rewritten: the earlier RED attempt `14319d0fd38dae9fd55ff36e13b301a658934c9d` carried **collateral failures caused by test queue pollution** — test/harness hygiene defects, corrected before the accepted final RED; that earlier attempt is **not** recorded as clean evidence. **Phase 7 is CLOSED (BOUNDED)** — the closure claims exactly the merged work above (existing `FR-4.x` booking primitives + PR #79 / PR #81 / PR #83 / PR #85); no further Phase 7 slice is claimed and no other phase is marked complete (the read-only Phase 7 scoping found substantial booking/hold infrastructure already present — that scoping is not itself an implementation closure for every FR-4.x requirement). Latest migration remains `0020` (re-verified on live main `d7484ce…`; no `0021`) |
> | Phase 8 | Patient Public Booking | ✅ **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** — bounded post-merge documentation closure at `9abd3379a02a320705cadac5229726bbd1b202cb` = **main at the Phase-8 closure checkpoint** (since superseded as live main by the Phase 9 Slice 1 merge `a593958521386d459f76c5efa839d9962e1f67fc`) (PR #90 MERGED 2026-09-20T17:04:39Z؛ post-merge روی merge SHA: CI `35524710717` · Real WP `35524710714` · Closure Gate `35524710737` · Pilot/Staging `35524710697` + 19/19 checks success). **Slice 1 = CLOSED (BOUNDED)** — PR #87 (merge `41225398…`؛ head `0643e2b5…` 19/19)؛ **Slice 2 = CLOSED / MERGED via PR #89** (head `70d033cc…` 19/19؛ سیاست‌های Hold-Timing و حداقل‌هویتِ بیمارِ جدید **Owner-issued، نه عبارتِ اصلی SRS** — `decisions/2026-09-20-phase8-slice2-owner-decisions.md`). **Slice 3 = CLOSED / MERGED via PR #90** (head `0f044eb5…` 19/19؛ linked-Patient booking-subject selection). **آخرین migration روی main = `2026_09_20_0022_slot_holds_patient_binding.php`** (با PR #90 در دامنهٔ پذیرفته‌شدهٔ Slice-3 ادغام شد؛ `0021` `cpms_otp_tokens.clinic_id` با PR #89). Phase 8 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)؛ release/go-live/V1/commercial نیست |
> | Phase 9 | Patient Portal | ✅ **CLOSED / TECHNICALLY COMPLETE (BOUNDED) — بستن رسمی Phase 9 با تصمیم صریح مالک (2026-09-23)** (ثبت: `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` — بخش سوم) در چک‌پوینت `72bb3fdda418914d9b7b33eed207c3c1de1a951f` (merge PR #110 — تاریخِ بستن: 2026-09-23؛ post-merge روی exact merge SHA: CI `35884923760` · Real WP `35884923831` · Closure Gate `35884923751` · Pilot/Staging `35884923752` + ۱۹/۱۹ check runs موفق) — the implemented Patient Portal sequence is complete (shell + notifications + Profile + Visits + Prescriptions + Files) — **Slice 1 = CLOSED (TECHNICAL; BOUNDED)** via **PR #93** (merge `a593958…`) · **Slice 2 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #96** (merge `c18c18f…`) · **سیاستِ OWNER-ISSUED (2026-09-21)** در `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` · **Slice 3 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #99** (merge `8a324e59…`؛ پوستهٔ مستقلِ CPMS Patient Portal اکنون کدِ production روی main است؛ تأیید بصری مالک COMPLETE در accepted head) · **Slice 4 BACKEND FOUNDATION = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #101** (MERGED 2026-09-21T17:40:46Z؛ merge `28e7fbf61ec4e783f81dc3ecfd7ba92e6a2b8380` = main at the Slice-4-backend-foundation closure checkpoint (superseded as live main)؛ parents `3c1db8250f24cff509b7a544317c01043c1e1244` (merge PR #100) + `d0580bebccdeee66d492a4b1607c7c223c4b8ff2` = accepted head؛ post-merge روی exact merge SHA: CI `35633544036` · Real WP `35633543948` · Closure Gate `35633543996` · Pilot/Staging `35633544127` — همه terminal-success + ۱۹/۱۹ check runs موفق). دامنهٔ محدودِ Slice 4: انتخاب پرونده‌های فعالِ کاربر و به‌روزرسانی مقیدِ پروفایل (`GET /patient/my-records` C0 + `GET/PUT /patient/me?link_id?` C1/C2)؛ `link_id` فقط selector است نه authority؛ N>1 بدون selector با `422 CLINIC_SELECTION_REQUIRED` رد می‌شود؛ بدون fallback primary/اولین؛ بدون ادغام cross-Clinic؛ هویت موبایل/ورود read-only؛ کد ملی Clinic-scoped در ذخیره‌سازی، اعتبارسنجی‌شده، در پروفایل قابل ویرایش و الزام رزرو نیست؛ برخورد کد ملی در همان کلینیک پیش از تغییر/audit با `400 CLINIC_VALIDATION_FAILED` رد می‌شود؛ همگام‌سازی Org Identity خارج از این برش. **Slice 4 Patient Profile UI = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #103** (merge `9909ce184c973e700c838db1c74095eac5c8553c`). **My Visits = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #105** (MERGED 2026-09-23T04:37:56Z؛ merge `f09a81c132d545d1d1657029632a8b069515dd26` (main at that checkpoint, superseded as live main by the PR #107 merge `d0644706324be211e7fe68d1f9f2b8232357f704`)؛ parents `ed7db7acfa56129035d762e8388c86fded8b7c24` (merge PR #104) + `3e7d2103044ef6282953f04dea9f8c47bb18443f` = accepted head (سرِ اصلاحِ Jalali)؛ post-merge روی exact merge SHA: چهار workflow لازم terminal-success — CI `35819115348` · Real WP `35819115341` · Closure Gate `35819115342` · Pilot/Staging `35819115349` + ۱۹/۱۹ check runs موفق؛ دامنهٔ محدود: My Visits فقط‌خواندنیِ list/detail در پوستهٔ مستقلِ مصوب روی مسیرهای موجودِ C5/C6 با همان معناشناسیِ پذیرفته‌شدهٔ selector چندپرونده‌ایِ `link_id` — فقط selector نه authority؛ 0 ⇒ 404 امنِ canonical؛ 1 ⇒ auto-resolve؛ N>1 بدون selector ⇒ `422 CLINIC_SELECTION_REQUIRED`؛ selector نامعتبر/بیگانه/غیرفعال ⇒ 404 غیرِشماری؛ بدون fallback primary/اولین؛ تاریخ‌های ویزیتِ قابل‌رؤیتِ بیمار **Jalali/Shamsi** نمایش داده می‌شوند در حالی که معنای خامِ بک‌اند/ذخیره‌سازی بدون تغییر است؛ بدونِ REST route جدید، بدونِ سیستم auth جدید، بدونِ مسیر نوشتن، بدونِ UI نسخه‌ها/فایل‌ها). **My Prescriptions = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #107** (MERGED 2026-09-23T09:59:20Z؛ merge `d0644706324be211e7fe68d1f9f2b8232357f704` = main at that checkpoint (superseded as live main) [تصحیحِ نشانگرِ زنده: عبارتِ قبلی «merge `04d31fb1…` = current main» یک mislabel بود — `04d31fb…` mergeِ PR #109 است؛ PR #107 merge = `d0644706…` همان‌طور که از والدینِ ذکرشده `cfc13ab6`+`d0f390e9` برمی‌آید]؛ parents `cfc13ab64938666e2587f1cbaf132c8d6d418307` (merge PR #106) + `d0f390e997fb09e22aa00701170bc01161407153` = accepted head؛ post-merge روی exact merge SHA: چهار workflow لازم terminal-success — CI `35846142339` · Real WP `35846142325` · Closure Gate `35846142311` · Pilot/Staging `35846142296` + ۱۹/۱۹ check runs موفق؛ دامنهٔ محدود: My Prescriptions فقط‌خواندنیِ list در پوستهٔ مستقلِ مصوب روی مسیرِ موجودِ C7 `GET /clinic/v1/prescriptions` با همان معناشناسیِ پذیرفته‌شدهٔ selector چندپرونده‌ایِ `link_id` — فقط selector نه authority؛ 0 ⇒ 404 امنِ canonical؛ 1 ⇒ auto-resolve؛ N>1 بدون selector ⇒ `422 CLINIC_SELECTION_REQUIRED`؛ selector نامعتبر/بیگانه/غیرفعال/cross-clinic ⇒ 404 غیرِشماری؛ بدون fallback primary/اولین؛ محدودیت سمت سرور `is_patient_visible = 1` و حذف draft رعایت می‌شود؛ تاریخ‌های نسخهٔ قابل‌رؤیتِ بیمار بر پایهٔ تایم‌زونِ Location **Jalali/Shamsi** نمایش داده می‌شوند و معنای خامِ بک‌اند/ذخیره‌سازی بدون تغییر است؛ بدونِ REST route جدید، بدونِ endpoint موازی، بدونِ سیستم auth جدید، بدونِ اکشن refill/mutation/printing/pharmacy، بدونِ UI فایل‌ها). آخرین migration همچنان `2026_09_20_0022_slot_holds_patient_binding.php` (بدون `0023`؛ PR #107 هیچ migration ای اضافه نکرد). **Phase 9 READY FOR PHASE-CLOSURE DECISION — the implemented Patient Portal sequence is complete (shell + notifications + Profile + Visits + Prescriptions + Files) per owner-policy ordering Profile/Visits/Prescriptions/Files and wireframe §7; **My Files = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #109** (MERGED 2026-09-23T15:05:34Z; merge `04d31fb1fb1b1a8e33fbf1bbf4e3803a6d3db3eb` = main at that checkpoint (superseded as live main by the PR #110 merge `72bb3fdda418914d9b7b33eed207c3c1de1a951f`); accepted head `000a551d97b6396e6f8c9ac8428e32a074483e34`; post-merge: CI `35879016172` · Real WP `35879015943` · Closure `35879015854` · Pilot/Staging `35879016279`); no explicitly required Phase 9 capability remains pending و در زمانِ بستن هیچ قابلیتِ اضافیِ Phase 9 الزامی نبود؛ همهٔ برش‌های bounded ثبت‌شده CLOSED می‌مانند؛ آخرین migration همچنان `2026_09_20_0022_slot_holds_patient_binding.php` (بدون `0023`)؛ تاریخِ READY FOR PHASE-CLOSURE DECISION (چک‌پوینت `04d31fb…` / PR #110) به‌عنوان شواهد تاریخی حفظ می‌شود و بازنویسی نمی‌شود؛ این بستن ادعای release/go-live/V1/تجاری/تکمیل محصول/استقرار production نیست و تکمیلِ Staff Portal / mobile native / provider اعلانِ آینده / فعال‌سازیِ Org Identity / هیچ قابلیتِ آینده‌نگرانه را ادعا نمی‌کند.** |
> | Phase 10 | **Doctor Portal** — 🚧 **IN PROGRESS — NOT CLOSED.** شروع با **تصویبِ صریحِ مالک** (ثبتِ تصمیم: [`decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md`](decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md) — بخش اول)؛ **نخستین برشِ محدود = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #113** (MERGED 2026-09-24T08:27:29Z؛ merge `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`؛ accepted head `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`؛ بدون migration). دامنهٔ محدودِ برشِ نخست: پوستهٔ **مستقلِ** Doctor Portal (بدون chromeـِ wp-admin/theme) · زیرساختِ **مشترکِ** احراز هویت/نشستِ وردپرس · مرزِ **فقط‌پزشک** · هویتِ حرفه‌ایِ **سرور-محور** · Scopeِ مطبِ موثق · Scopeِ عملیاتیِ مکانِ موثق · رفتارِ **۰/۱/N مکان** (بدون fallbackـِ اول/اصلی) · مکان = مرجعِ منطقهٔ زمانی/روزِ عملیاتیِ محلی · خلاصهٔ امروز / صف زنده / **نوبت‌های امروز** (فقط‌خواندنی — افزودهٔ محدودِ مصوّبِ مالک) · ایزولاسیونِ پزشکِ خودی + مطب + مکان · Persian/RTL/responsive. **دومین برشِ محدود / اقداماتِ صف = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #117** (MERGED 2026-09-24T12:56:18Z؛ merge `87625a03fd92e966de7b13930fc3fb9651138b9f`؛ accepted head `a73c27d2dbf823754d58ded63e2df2f67af74b84`؛ post-merge روی exact merge SHA: چهار workflow لازم terminal-success — CI `36002282790` · Real WP `36002282465` · Closure Gate `36002282615` · Pilot/Staging `36002282432` — + ۱۹/۱۹ check runs موفق روی merge SHA و ۱۹/۱۹ روی accepted head؛ **بدون migration** — آخرین migration همچنان `2026_09_20_0022_slot_holds_patient_binding.php`). دامنهٔ محدودِ برشِ دوم: **Call · Start · Recall · Skip** با بازاستفاده از همان **state machineِ موجودِ صف** — **بدون حالتِ صفیِ تازه**؛ الزامِ **مکانِ موثق روی mutationها در صورت وجودِ Location Scope**؛ ردِ **غیرِشماریِ** mutationِ بین‌مکانی؛ **حفظِ رفتارِ legacy/no-Location**؛ حفظِ transaction/`FOR UPDATE`/اعتبارسنجیِ وضعیت؛ حفظِ سقفِ recall؛ الزامِ دلیلِ Skip؛ حفظِ قراردادِ اختیاریِ متنِ اتاق؛ کنترل‌هایِ پورتالِ پزشک **state-driven**؛ حفظِ nonce/capability/مالکیتِ پزشک؛ تداومِ رویت‌پذیریِ منشی/پذیرش از طریق همان history/RT/notificationِ `QUEUE_CALLED` — **بدون معماریِ realtimeِ تازه**؛ **بدون migration**. **الزامِ N>1 مکان مختصِ پورتال است** (رفتارِ صفِ مشترک/wp-admin حفظ شد). **PR #112 = تاریخی/منسوخ، CLOSED / NOT MERGED؛ PR #113 = ادامهٔ معتبر؛ PR #115 = فقط اصلاحِ زیرساختِ تست/هارنس (نه برشِ فاز)؛ PR #116 = closureِ مستنداتِ برشِ نخست (نه برشِ فاز).** **در انتظار / پیاده‌سازی‌نشده:** عملیاتِ گسترده‌ترِ نوبت · فضای کاریِ ویزیت · نگارشِ یادداشت · گردش‌کارِ نسخه · دست‌خط/قلم · گردش‌کارِ فایل‌ها · جستجوی بیمار · برنامه/حضورِ پزشک · مدیریتِ مکان‌هایِ فعالیتِ پزشک · مالی/حق‌ویزیت · منشی/صندوق‌دار · MRN. **قابلیتِ محدودِ بعدی: فضای کاریِ ویزیت (Doctor visit workspace) — باز کردنِ ویزیتِ جاریِ بیمارِ انتخاب‌شده + پروندهٔ بالینی + یادداشت‌های خصوصی/قابل‌رؤیتِ بیمار — NOT IMPLEMENTED / در انتظار** (بک‌اندِ مستقر E7/E8/E9/E14/E15 + `VisitMachine`؛ **UIِ پورتال وجود ندارد**؛ مرزِ برش پیش از شروع نیازمندِ تصمیمِ صریحِ مالک است؛ نسخه/دست‌خط/مالی/فایل افزوده نمی‌شود؛ *(تاریخی — ثبتِ «اقداماتِ صف = قابلیتِ بعدی» برای چک‌پوینتِ پیش از PR #117 درست بود و بازنویسی نمی‌شود.)*) |
> | Phase 11..20 | عنوان‌گذاری‌نشده | ⚠️ OPEN DECISION |
>
> ## معماری هدف الزام‌آور — [ADR-0031](adr/ADR-0031-organization-clinic-location-scoped-authorization.md)
>
> ```
> Organization → Clinic → Location → Doctor → User/Staff
> ```
>
> **سه قاعده‌ای که بیشترین احتمال نقض ناخواسته را دارند:**
> - **AD-02** — Organization اجباری است. هیچ مسیر موازی برای `Organization = NULL`.
> - **AD-12** — Migration = versioned forward. drop/recreate مسیر محصول نیست.
> - **AD-13** — **`clinic_id = 1` مستقیم در کد جدید ممنوع است** (و همین‌طور `organization_id = 1` و `location_id = 1`). ۵۴ مورد موجود بدهی فنی Phase 2 هستند — اضافه‌کردن مورد پنجاه‌وپنجم ممنوع است.
>   - *(راستی‌آزمایی 2026-09-12)* «۵۴» **سرشماریِ تاریخیِ Phase 0** است، **نه بدهیِ runtimeِ جاری**: Tenant Tripwire روی runtime فعال `hardcodes: 0` و allowlist `[]` گزارش می‌دهد (۵۹ self-test سبز). آشتی‌دهیِ دقیقِ «۵۴ تاریخی ↔ ۰ جاری» + شاهدِ قابلِ اجرا در [`project-current-state.md`](project-current-state.md) §D‑1. خودِ قاعدهٔ AD-13 بدون تغییر پابرجاست و **`clinic_id = 0` و Clinic مصنوعیِ «System» هم ممنوع‌اند** (جزئیات: [`architecture/phase2-tenant-context-remediation-design.md`](architecture/phase2-tenant-context-remediation-design.md) §۳-B‑2).
>
> **قاعدهٔ اجرا از Phase 1 به بعد:** code + tests + documentation باید **همراه هم** به‌روز شوند. checkpoint commit کوچک مجاز است؛ **merge فقط پس از Gate نهایی فاز**.
>
> **تعارض اسناد:** [`docs/drift-register.md`](drift-register.md) — اسنادی که هنوز مدل قدیمی را بیان می‌کنند، با فاز مالک هر مورد.

---

## 0. چک‌لیست شروع کار هر ایجنت (اجباری)

1. این فایل را کامل بخوان (مخصوصاً §5 قواعد کار و §7 دام‌ها).
2. وضعیت repo را verify کن — دستورات آماده:
   ```bash
   git log --oneline -10 && git status --short
   BR=$(git rev-parse --abbrev-ref HEAD)
   gh run list --branch "$BR" --limit 3
   gh pr list --state all --limit 5 --json number,state,title,headRefName
   ```
   > ⚠️ نسخهٔ قبلی این چک‌لیست شاخهٔ ثابت `arena/01a071c4-doctor` و `PR #1` را hardcode کرده بود. آن شاخه منسوخ است و **PR #1 از مدت‌ها پیش MERGED شده** — روی شاخهٔ جاری کار کن.
3. وضعیت فازها را **فقط** از `docs/roadmap/roadmap.md` **§۰ (Owner-approved Phase 0..20)** بگیر — نه از جدول تاریخی `F*` و نه از گزارش فاز قبلی.
4. **قاعده طلا:** Git history منبع تأیید است؛ گزارش‌های قبلی فقط handoff هستند — همه ادعاها را از کد/تست/CI verify کن.
5. هیچ فاز جدیدی بدون تأیید صریح کارفرما شروع نکن (قانون Gate — Section 56).

---

## 1. معرفی پروژه

**CPMS** = سیستم مدیریت مطب (Clinic Practice Management System) به‌صورت **افزونه WordPress** (PHP 8.1+، MySQL 8، WP 6.4+ — runtime تأییدشده روی WP 6.4/6.5/6.6/6.7.2 و PHP 8.1–8.4) — تجاری با لایسنس.

> ⛔ **تصحیح (2026-09-08):** جملهٔ قبلی «تک‌کلینیک در V1» بود. **آن جمله دیگر معتبر نیست.** طبق [ADR-0031](adr/ADR-0031-organization-clinic-location-scoped-authorization.md) (AD-01)، **Multi-Clinic / Multi-Location جزو Core است** — نه افزودنی، نه V2. مطب تک‌پزشکی هم دقیقاً از همان زنجیرهٔ `Organization → Clinic → Location` استفاده می‌کند (AD-04)؛ «حالت مطب» فقط یک تطبیق UX است، **هرگز یک مسیر کدی جدا نیست**. پیاده‌سازی در **Phase 2**.

- مسیر افزونه: `clinic-practice-management/`
- Namespace: `ClinicCore\` → `clinic-practice-management/src/`
- REST namespace: `clinic/v1`
- زبان داخلی کد/کامنت‌ها: فارسی + اصطلاحات انگلیسی

### معماری لایه‌ای (الگوی فعلی — حفظ شود)

```
src/
├── Domain/            # منطق خالص: Machineها (State Machines)، Exceptionها، Validators
├── Application/       # سرویس‌ها (Auth/Booking/Visits/Patients/Notifications/Jobs)
├── Infrastructure/    # Db (CpmsDb wrapper روی wpdb)، Repositoryها، Audit، Security، Sms، Queue، Logging
├── Rest/              # Controllerهای REST (RestBase مشترک: nonce → cap → error/success envelope)
├── Admin/             # صفحات WP-Admin (SettingsAdmin، SmsSettingsPage، SecretaryQueuePage)
├── Auth/              # RolesAndCapabilities (نقش‌ها + کدهای capability)
├── Settings/          # Settings (option-based + cache)
├── Bootstrap/         # App (DI container استاتیک + boot/activate/cron)
└── Migrations/        # MigrationRunner + فایل‌های versioned
tests/
├── Unit/              # بدون WP — domain خالص
└── Integration/       # WP_UnitTestCase — bootstrap: tests/integration-bootstrap.php
```

**قواعد لایه‌ای:**
- Repository از F3 به بعد **اجباری** (ADR-0021): فقط data-access، بدون منطق دامنه، بدون God-Repository.
- کد F1/F2 برای یکنواختی refactor نشده — فقط برای مشکل واقعی (با impact analysis).
- Controller فقط: nonce → capability → فراخوانی سرویس → envelope. منطق دامنه در سرویس.
- هر Endpoint جدید فقط با سند API Contract (قانون Section 56).

---

## 2. وضعیت فعلی (Snapshot 2026-09-05)

### فازهای کامل‌شده

| فاز | وضعیت | گزارش |
|---|---|---|
| F1 — Core Architecture (Migration/Roles/Settings/Audit/JobQueue/CI) | ✅ | `report-f1.md` |
| F2 — احراز هویت (OTP/Session/Patient Links) | ✅ | `report-f2.md` |
| F2.5 — پیامک Provider-Agnostic (ADR-0025) | ✅ | `report-f2.5-sms.md` |
| F3 — نوبت‌دهی (Schedule/Slot/Booking API/Patient CRUD) | ✅ CI سبز | `report-f3.md` |
| **F4 — مراجعه/صف (Check-in/Walk-in/Queue SM/Real-time/داشبورد منشی)** | ✅ **CI سبز** | `report-f4.md` |
| F5 — بالینی (Notes/نسخه/توصیه/پیگیری/فایل/جستجو/داشبورد پزشک + Drift بسته) | ✅ CI سبز | `report-f5.md` |
| **F6 — مالی (Invoice/Payment/Adjustment/Void/Refund/Receipt/Summary + تعرفه‌ها + داشبورد مالی منشی + ADR-0026)** | ✅ **CI سبز** | `report-f6.md` |
| F7 — دست‌خط (Canvas/Offline Sync/Conflict) | ✅ CI سبز | `report-f7.md` |
| F8 — اعلان + گزارش + Export | ✅ CI سبز | `report-f8.md` |
| F9 — Hardening (Security T-01..T-24/TP-16/DoD V1) | ✅ CI سبز | `report-f9.md` |
| **Pilot/Staging Readiness Gate** | 🔄 ۱۴ run — Release/Upgrade/Responsive سبز؛ Staging تا Backup ✅، Restore Drill آخر | `report-pilot-gate.md` |

### آخرین وضعیت فنی (verify شده)

> ⛔ **این «Snapshot 2026-09-05» تاریخی است.** اعداد به‌روزشده در جدول زیر آمده؛ متن تاریخی زیرش دست‌نخورده مانده.
>
> ### خط پایهٔ اثبات‌شده — ممیزی Phase 0 (بازبینی 2026-09-08)
>
> | سنجه | مقدار صحیح | ادعای منسوخ |
> |---|---|---|
> | جدول `cpms_*` | **۴۱** | ~~۳۹~~ |
> | فایل Migration | **۹** (`0001`..`0009`) | ~~0001–0003~~ |
> | Foreign Key | **۳۹** (۳۸ در CREATE + `fk_hwpage_bg` با ALTER) | — |
> | **FK روی `clinic_id`** | **۴ فقط** ⇒ ۲۱ جدول بدون FK (قید C-8) | — |
> | جدول دارای `clinic_id` | **۲۵** از ۴۱ | — |
> | Capability | **۴۶** (شمارش با `grep 'public const .* = ...cpms_'` بیش‌شمارش می‌کند: ۵ `ROLE_*` + ۱ `OPTION_*`) | — |
> | نقش ثبت‌شده | **۵** — `cpms_patient`, `cpms_secretary`, `cpms_doctor`, `cpms_accountant`, `cpms_manager` | ~~۳~~ |
> | route REST | **۸۰ در زمان اجرا** (۷۷ نقطهٔ ثبت در سورس — عدد را همیشه صریح بگو) | ~~۷۵~~ |
> | `permission_callback` | **۸۸** = ۶۷ `__return_true` + ۲۱ gated (اصلاح‌شده در Phase 1A؛ عدد قبلی ۸۹ یک false-positive از Docblock در `BookingController.php:312` بود). از ۶۷ مورد، **۶۲ guard مؤثر داشتند** (۵۸ داخل handler + ۴ در لایهٔ Service) و **۵ عمداً public** بودند (قید C-6). **وضعیت جاری پس از Phase 1A: هر ۸۸ ورودی gated و `__return_true` = صفر** — [`security/phase1-current-security-model.md`](security/phase1-current-security-model.md) | — |
> | `admin_post_*` / AJAX | **۲۵** / **۰** | — |
> | فایل تست | **۸۴** (۳۳ Unit + ۴۹ Integration) — تعداد *متد* تست بدون اجرای PHPUnit اثبات‌پذیر نیست | ~~۲۰۳ تست~~ |
> | سند tracked در `docs/` | **۸۴** | ~~۸۵~~ |
> | `clinic_id = 1` hardcode | **۵۴** در **۲۳ فایل** — **۲۴ داخل `Infrastructure/Repository/`**، ۳۰ بیرون؛ ۱۳ از ۲۳ فایل Repository (قید C-4) | ~~~۴۴~~ |
> | Default-Parameter مخفی | **۳** — `AuditLogger.php:45`, `Idempotency.php:37`, `Settings.php:145` | — |
> | PR | ۹ PR؛ **PR #1 MERGED**؛ آخرین = **#9 MERGED** | ~~PR #1 OPEN~~ |
> | شاخهٔ کاری | از `git rev-parse --abbrev-ref HEAD` بخوان | ~~`arena/01a071c4-doctor`~~ |
>
> **سه مکانیزمی که در ADRها ادعا شده‌اند و در کد وجود ندارند:** `AccessPolicy` (۰ فایل) · `resolvePatient()` (۰) · Dangling-Check Job (۰ از ۱۶ handler). به‌علاوه `Repository Base` وجود ندارد و **Patient Merge فقط schema است**.

- **HEAD فعلی:** Pilot/Staging Gate فعال روی `arena/01a071c4-doctor` (F9 کامل در `c5e82a7` + Gate tooling/fixes) — وضعیت لحظه‌ای در report-pilot-gate.md §15
- **PR #1:** OPEN — هر ۵ چک (Unit PHP 8.1/8.2/8.3/8.4 + Integration WP 6.7.2 + MySQL 8)
- **تست‌ها:** Unit سبز روی PHP 8.1–8.4؛ Integration بعد از F6 = ۲۰۳ تست (FinanceFlowTest ۱۷ + قبلی‌ها)، ۰ skip — شامل concurrency با fork واقعی
- **Schema:** ۳۹ جدول `cpms_*` (مهاجرت‌های 0001–0003)؛ جدول فایل = `cpms_medical_attachments`
- **Capabilities:** drift ۴۹/۴۶ در F5 بسته شد — ماتریس مبنا (۴۶)؛ مفرغ‌ها (files/search) مطابق ماتریس اضافه شدند؛ registerRole اکنون stray capهای `cpms_*` را هم پاک می‌کند (self-healing — 998ee81)

### نقشه قابلیت‌های ساخته‌شده (Backend کامل تا F6)

- **Auth/OTP:** ثبت‌نام/ورود بیمار با OTP موبایل، Patient↔WP-User link، rate limit
- **SMS:** Provider-agnostic (Log/Generic API)، Vault، Queue، Templates، SSRF guard
- **Booking:** تقویم/اسلات/ظرفیت، Hold→Confirm (اتمیک DB-level ضد double-booking)، Idempotency-Key، Reschedule/Cancel با policy، Duration snapshot
- **Patient:** CRUD + جستجو + پروفایل
- **Queue/Visits (F4):** Check-in/Walk-in، ماشین کامل V1–V15 با Row-Lock و تاریخچه append-only، صف FIFO + نوبت فوری، R1 polling با ETag/304، No-show خودکار (lazy + cron)، Checkout/Waive، داشبورد منشی WP-Admin
- **Clinical (F5):** E7 پرونده کامل، E8/E9 Notes + نسخه‌بندی append-only، E10/E11 نسخه Draft/Finalize/Void، E12/E13 توصیه/پیگیری، E14/E15 پایان/بازگشایی با Validation، C5/C6/C7 نمای بیمار (فقط patient_visible)، E16/E17/C3/C4 فایل محافظت‌شده، E18 جستجوی جامع Role-Aware، داشبورد + صفحه ویزیت پزشک (WP-Admin)
- **Finance (F6):** تعرفه‌ها (G2 — soft-delete)، D12 صدور فاکتور (V11 سیستمی)، D13 پرداخت Idempotent (M-1 روی خود جدول، 201/200+Replay)، D14 ابطال (همان‌روز)، P3 بازپرداخت، D15 اصلاح Credit/Debit (کلید تسویه مؤثر)، D17 رسید Deterministic (جلالی + چاپ UI)، D18 خلاصه مالی، V14 گارد NOT_SETTLED در Checkout؛ INV/PAY سریال با قفل کلینیک؛ محاسبات ریالِ صحیح (InvoiceCalc — TP-18)؛ داشبورد مالی منشی (WP-Admin)

---

## 3. قواعد کارفرما (Binding — هرگز نقض نشود)

این قواعد از دستورالعمل‌های صریح کارفرما آمده و **بر همه ایجنت‌ها الزامی** است:

### 3.1 تقدم اسناد (Document Precedence)

```
Client Final Decisions → Approved ADRs (scope) → Engineering Baseline → SRS
→ State Machines/ERD → API Contract → Settings Reference → Implementation
```
- تعارض مهم = **STOP** با قالب ISSUE / IMPACT / OPTIONS / RECOMMENDATION و انتظار برای پاسخ.
- تصمیم نهایی کارفرما بر همه چیز مقدم است.
- **فازبندی: منبع حقیقت = Owner-approved Phase 0..20 Roadmap** — `docs/roadmap/roadmap.md` **§۰**.
  - خطای رایج ۱: خواندن فاز بعدی از گزارش فاز قبلی — گزارش‌ها فقط خلاصه‌اند.
  - خطای رایج ۲ (**جدید و پرخطرتر**): خواندن فاز از جدول تاریخی `F0..F10` یا برچسب‌های `V1.5`/`V2` در همان فایل. آن‌ها **Legacy** هستند. `V2` منحل شده و محتوایش به Phase 2/3/4 بازتوزیع شده است.
- **تقدم اسناد به‌روزشده:** تصمیم نهایی Owner ← **ADR-0031** ← ADRهای دیگر ← Engineering Baseline ← SRS ← State Machines/ERD ← API Contract ← Settings ← Implementation.
  ADR-0031 بر ADR-0003 (کامل) و بر بخش‌هایی از ADR-0027 و ADR-0026 مقدم است.
- **تعارض شناخته‌شدهٔ سند-با-واقعیت** پیش از هر کار در `docs/drift-register.md` چک شود؛ آن موارد **از قبل ثبت شده‌اند** و نیازی به STOP مجدد ندارند — فقط در فاز مالکشان اصلاح می‌شوند.

### 3.2 شرایط STOP (فقط این موارد — بقیه با قضاوت مهندسی)

Scope change، تغییر main workflow، تغییر قاعده business کاربر-محور، تغییر permission model، schema change مهم خارج از پلن، breaking API، تغییر سیاست licensing/security، احتمال data loss، تصمیم medical/legal، dependency بزرگ/پرخطر، هزینه third-party جدید، تعارض غیرقابل‌حل منابع.

**Working mode:** سؤال مکرر درون‌فازی ممنوع؛ برای naming/test/validation/internal interface/doc-sync/امنیتِ route قضاوت خودت را به کار ببر؛ هرگز کیفیت فدای سرعت نشود.

### 3.3 Governance فاز

- هر فاز را تا کامل شدن جلو ببر (مگر STOP واقعی).
- در پایان فاز: STOP + گزارش تکمیل (قالب `report-f3.md`/`report-f4.md`: خلاصه، ماتریس AC، تغییرات، کامیت‌ها، تصمیمات درون‌فازی §4، موارد باز §5، گام بعد §6).
- ورود به فاز بعد فقط با تأیید کارفرما.

### 3.4 قواعد فنی

- **Error codes:** همه `CLINIC_*`، stable/ماشین‌خوان، registry = `docs/api/error-codes.md` (هر کد جدید آنجا ثبت شود)؛ پیام فارسی کاربر جدا از کد فنی. کلید `status` در envelope رزرو است.
- **Patient ≠ WP User، Appointment ≠ Visit، Invoice ≠ Payment** — کلاینت هرگز trusted نیست؛ UI permission ≠ backend authorization؛ بیمار A نباید داده بیمار B را ببیند؛ منشی نمی‌تواند نوت خصوصی پزشک را ببیند.
- **Authorization = Capability، نه نام نقش (ADR-0026 D-1 — همچنان معتبر):** منطق جدید هرگز `if role == X` نمی‌نویسد.
  > 🔄 **به‌روزرسانی (ADR-0031، AD-06):** «(+ Scope در V2)» منسوخ است. مدل هدف: نقش **صفتِ رابطهٔ (User, Clinic)** است نه صفتِ User؛ `User↔Clinic` رابطهٔ **M:N** است (AD-05). نقطهٔ ورود واحد `AuthorizationService::can(wpUserId, capability, ScopeContext{clinicId, ?locationId, ?resource})` می‌شود و فراخوانی مستقیم `current_user_can('cpms_…')` بیرون از آن سرویس **ممنوع** خواهد شد (با architecture test). پیاده‌سازی در **Phase 3** — امروز هنوز `user_can` الگوی جاری است. کلاس `AccessPolicy` که ADR-0002/0026 به آن ارجاع می‌دهند **وجود ندارد**. نقش‌های ستادی سفارشی (حسابدار/دستیار/…) باید بدون تغییر Business Logic کار کنند؛ نام نقش فقط برچسب Audit است. حوزه‌ها مستقل‌اند: مالی ⊥ بالینی ⊥ هویت ⊥ یادداشت خصوصی؛ Cap عمومی هرگز `cpms_private_note_*` را ضمنی نمی‌دهد. همه داشبوردهای ستادی Responsive‌اند (تبلت/قلم = بهینه‌سازی دست‌خط، نه محدودیت دستگاه).
- **2FA (V1.5):** TOTP RFC 6238 برای حساب‌های ممتاز بر اساس ACCESS نه role name.
- **SMS:** همیشه از مسیر Notification→SmsService→ProviderInterface؛ credential هرگز hardcode/log نشود.
- **Licensing:** انقضا هرگز داده پزشکی را قفل/حذف نمی‌کند؛ بعد از grace (پیش‌فرض ۷ روز) فقط عملیات جدید block؛ license server هرگز داده پزشکی نمی‌گیرد و در مسیر booking network call مستقیم ندارد.
- **Performance:** REST p95 < 300ms؛ عملیات سنگین (OCR/SMS/PDF/Export) async؛ بدون N+1/لیست بی‌سقف/polling بی‌کنترل.
- **Backup:** RPO ≤ 6h / RTO ≤ 8h (تست restore در F9/TP-16).

### 3.5 قواعد تست و CI (از تجربه‌های تأییدشده F3/F4)

- تست واقعی هرگز برای سبز شدن CI حذف/ضعیف/skip نشود؛ assertion برای پنهان‌کردن bug عوض نشود.
- Failure ناشی از production code → اصلاح production؛ ناشی از test infra → اصلاح infra.
- Integration test فقط وقتی Passed است که **واقعاً روی WordPress+MySQL اجرا و سبز شده** (CI = شاهد).
- Concurrency test باید واقعاً اجرا شود (fork واقعی — در runner لینوکسی pcntl موجود است).
- Debug instrumentation موقت بعد از تشخیص cleanup شود؛ workflow ساده بماند.
- دیباگ CI فقط **Root-Cause based** — بدون iteration تشخیصی غیرضروری؛ هر CI-fix منطقی commit+push شود.
- در Session از sleep/wait طولانی استفاده نشود؛ اگر run در حال اجراست، وضعیت را ثبت کن و کار مستقل انجام بده، بعد چک کن.
- اگر بعد از چند iteration معقول CI قرمز ماند: STOP + Root Cause Report (failing tests / exact errors / علت / راه‌حل پیشنهادی).
- لاگ‌های job از sandbox قابل دانلود نیستند (redirect به blob با EOF) — راه خواندن شکست‌ها: کامنت‌های PR (step «Post failures to PR») یا API annotations. این محدودیت به‌تنهایی دلیل تغییر production نیست.

---

## 4. نقشه اسناد کلیدی

| سند | نقش |
|---|---|
| `docs/roadmap/roadmap.md` | فازبندی + DoD هر فاز (منبع حقیقت فاز) |
| `docs/srs/SRS.md` | الزامات عملکردی (FR-x) |
| `docs/api/api-contract.md` | همه Endpointها (A/B/C/D/E/R/SM) — §0 = قواعد envelope |
| `docs/api/error-codes.md` | Registry کدهای `CLINIC_*` (منبع حقیقت) |
| `docs/state-machines/*.md` | ماشین‌های Appointment/Visit/… + invariants (J-x/T-x/V-x) |
| `docs/erd/` | مدل داده (D-x) + data dictionary |
| `docs/security/` | تهدیدها (T-01..T-24)، auth-authorization (۵ لایه) |
| `docs/adr/ADR-*.md` | تصمیمات معماری تأییدشده |
| `docs/engineering-baseline.md` | استانداردهای مهندسی |
| `docs/permissions/` | ماتریس capabilityها |
| `docs/testing/` | TP-x تست‌پلن |
| `docs/phase-reports/report-*.md` | گزارش تکمیل هر فاز |
| `docs/decisions/` | تصمیمات نهایی کارفرما |

---

## 5. الگوهای پیاده‌سازی (Patternهای جاافتاده — دنبال کنید)

### سرویس جدید (مثل `VisitService`)

```php
final class XxxService {
    public function __construct(
        private readonly CpmsDb $db,
        private readonly XxxRepository $repos, ...
    ) {}
    // عملیات state-changing:
    return $this->db->transactional(function () use (...): array {
        $row = $this->repos->findForUpdate($id);      // Row Lock (J-1 الگو)
        $to = XxxMachine::create()->machine()->assert($from, $event, $actorRole); // ماشین
        // UPDATE + تاریخچه append-only + audit
    });
}
// خطا: throw XxxException::of('CLINIC_CODE', 'پیام فارسی', 409, ['key' => val]);
```
- Exception دامنه‌ای مثل `BookingException`/`VisitException` (errorCode/httpStatus/data).
- **هرگز transactional تو در تو نکن** (nested = COMMIT ضمنی) — منطق مشترک در متد private بدون transaction.
- Envelope خطا در Controller با `wrap()`/`guard()` → `$this->error($e->errorCode, $e->httpStatus, ...)`.

### REST Controller

- ارث‌بری از `RestBase`؛ `permission_callback='__return_true'` + nonce/cap **داخل callback** (الگوی موجود).
- ثبت در `App::boot()` → `register_routes()` داخل `rest_api_init`.
- Capهای صف: `QUEUE_READ/CHECKIN/ADVANCE/CALL/CHECKOUT`؛ منشی همه را دارد جز CALL؛ پزشک CALL + CONSULT_*.

### Repository (ADR-0021)

- Constructor فقط `CpmsDb`؛ متدهای data-access خالص (`find/findForUpdate/insert/updateById/...`).
- `CpmsDb` quirks: `insert()` → bool (آیدی از `wpdb_last_insert_id()`)، `update(table, data, where)`، `fetchRowForUpdate` = SELECT … FOR UPDATE، placeholderها فقط فرمت wpdb (`%d/%s/%f` — بدون `?`).

### Admin Page

- الگوی `SecretaryQueuePage`: `add_menu_page` + cap + render با config inline (rest_url+nonce) + JS vanilla بدون وابستگی + بدون PHI در HTML اولیه.

### Job تکرارشونده

- Handler در `src/Application/Jobs/` + register در `App::dispatcher()` + افزودن به `RECURRING_JOBS` در `App` (idempotent — در هر tick دوباره زمان‌بندی می‌شود).

### تست Integration

- `WP_UnitTestCase` + `App::migrations()->migrate()` در setUp + seed با SQL مستقیم + `makeUser` با `set_role`.
- **Userهای تست باید نام یکتا** داشته باشند اگر کلاس COMMIT واقعی می‌زند (الگوی ConcurrencyTest) + cleanup دستی در tearDown.
- fixtureهای COMMIT شده با ROLLBACK پاک نمی‌شوند — cleanup دستی لازم.
- Timezone: همه UTC در DB (`nowUtc()/nowUtcSql()`)، مقایسه زمان با string 'Y-m-d H:i:s'.
- slots UNIQUE روی `(clinician_id, slot_date, slot_time)` — زمان‌های تست باید یکتا باشند.

---

## 6. CI و محیط

- Workflow: `.github/workflows/ci.yml` — ۳ job: **Unit** (matrix PHP 8.1–8.4، بدون WP) + **Integration** (WP 6.7.2 + MySQL 8، PHPUnit 9.6، `tests/bin/install-wp-tests.sh`، root/root، prefix `wptests_`) + **`phpstan`** (Static Analysis — از F1-3/گروه 4؛ `phpstan.neon.dist`: level=3 هم‌تراز وضعیت واقعی کد (با اجرای واقعی/probe)، scope `src + bin`، بدون baseline؛ ارتقا به 4+ نیازمند بازسازی تایپی — کار آینده).
- چرخه CI ≈ ۴.۵–۵ دقیقه.
- شواهد شکست را از کامنت‌های PR #1 بخوان (step «Post failures to PR» فقط در failure).
- **محیط sandbox فاقد PHP CLI است** — برای lint فایل‌های PHP از WASM:
  ```bash
  cd /tmp && npm i @php-wasm/node   # یک‌بار
  # lint.mjs: NodePHP.load() + php.writeFile(tmp) + php.cli(['php','-l',tmp])
  # نکته: argv باید با 'php' شروع شود؛ exit code را منتقل کن (stdout بعد از cli می‌میرد)
  ```
- composer/vendor در sandbox نیست — تست‌ها را از CI بخوان، نه اجرای محلی.

---

## 7. دام‌های شناخته‌شده (Pitfalls — خوانده شود!)

1. **`transactional` تو در تو = COMMIT ضمنی** → savepoint-filter فقط queryهای `/*cpms*/`-marked را به SAVEPOINT تبدیل می‌کند (فقط در تست). COMMIT خام (`$wpdb->query('COMMIT')`) تراکنش والد تست را می‌بندد → نشت fixture → Duplicate key/WP_Error در تست‌های بعدی.
2. **تداخل کلید `status` در envelope خطا** — data exception هرگز کلید `status` نداشته باشد (برعکس merge شده در `RestBase::error`).
3. **`CpmsDb::query()` bool برمی‌گرداند** — برای affected/insert از `execute()/wpdb_last_insert_id()`.
4. **wpdb prepare با null → ''** و بدون placeholder `?` — مقادیر nullable را شرطی بچین.
5. **INNODB_TRX در CI قابل استفاده نیست** (performance_schema خاموش) — برای اثبات lock از mysqli مستقل + `SET SESSION innodb_lock_wait_timeout` استفاده کن.
6. **`php.cli` در WASM**: argv باید با `'php'` شروع شود؛ بعد از اولین فراخوانی stdout مرده است — فقط exit code.
7. **GitHub token ممکن است mid-session 401 شود** — retry بی‌فایده است؛ state را commit کن و از کارفرما بخواه GitHub را در Arena reconnect کند (تجربه: برگشت).
8. **لاگ jobها از API → blob storage EOF** — شکست‌ها را از کامنت PR بخوان.
9. **فازبندی را فقط از roadmap بخوان** — گزارش‌های فاز ممکن است pointer اشتباه داشته باشند (اتفاق افتاد: report-f3 §6).
10. **Retry سایز merge**: `array_merge(['status'=>$http], $data)` خطرناک — envelope اول است.
11. **آیتم مرزی نیمه‌شب UTC** در تست‌های «امروز/فردا» — با آفست‌های زمانی مطمئن کار کن.
12. **App helpers فایل plugin-level** (مثل `cpms_request_id`) — IIFE داخل `App::boot` در پروسه تست CI دیده نمی‌شوند؛ فایل plugin `clinic-practice-management.php`.
13. **`wp eval-file` + `declare(strict_types=1)` = Fatal** — در کد eval شده، declare باید اولین statement باشد (docblock قبلش هم غیرمجاز) → در اسکریپتهای eval-file (pilot-*) declare نگذار.
14. **`wp eval-file ... | tee` خطاهای stderr را گم می‌کند** → همیشه `2>&1 | tee` + `set -o pipefail`؛ وگرنه فایل لاگ خالی می‌ماند و root cause گم می‌شود.
15. **Closure در اسکریپتهای eval-file**: متغیرهای خارجی ($wpdb/$db/...) باید صریحاً `use` شوند — خطای «Call to a member function on null» یعنی import جا افتاده.
16. **اسکریپت تستGate باید با schema واقعی نوشته شود، نه حدس**: نمونه‌ها — `cpms_patient_user_links` ستون `linked_at/mobile_at_link/clinic_id` دارد (نه created_at)؛ جدول فایل `cpms_medical_attachments` و خروجی `upload()` = `presentFile` بدون `storage_path` (مسیر از DB بخوان)؛ ستونهای SMS: `recipient/event` (نه mobile/template)؛ status پیامکها **حروف بزرگ** (`SENT`/`QUEUED`).
17. **قبل از نوشتن assert روی رفتار REST، api-contract.md/error-codes.md را بخوان**: گم‌شدن Nonce همیشه 403 `CLINIC_INVALID_NONCE` است (Guard استاندارد: Nonce→Capability)، نه 401 UNAUTHORIZED.
18. **Runnerهای tick (WP-Cron و CLI) باید یک مسیر باشند** — هر Runner جدید از `App::runTick()` عبور کند؛ وگرنه recurringها بعد از اولین اجرا می‌ایستند (باگ Gate run 34023615811).
19. **`wp rewrite structure --hard` روی برخی محیطها بی‌صدا .htaccess نمی‌نویسد** → heredoc مستقیم با قواعد استاندارد WP بنویس.
20. **Apache روی runner: PrivateTmp فعال است** → فایلهای موقت بین پروسه PHP و apache به اشتراک گذاشته نمی‌شوند؛ مسیر آپلود را explicit کن.

---

## 8. کارهای باقی‌مانده

### فازهای آینده (طبق roadmap — بدون تأیید شروع نشود)

| فاز | محتوا | DoD/تست |
|---|---|---|
| **F7** | دست‌خط: Canvas (Pressure/Tools/Zoom/Multi-page — Responsive طبق ADR-0026 D-14)، Stroke Storage، Auto-save + Offline Sync (IndexedDB) + Conflict | TP-12 |
| **F8** | اعلان + گزارش: Notification Layer + Templates (Jalali)، ۱۲ گزارش + Export (Watermark/Audit) | TP-13 |
| **F9** | Hardening: Security Review (T-01..T-24)، Performance، Backup/Restore Test، Accessibility، مستندات کاربری، Pilot | TP-16 + DoD V1 |
| **V1.5** | OCR فارسی، 2FA (TOTP)، Merge UI، ClamAV/Encryption | TP-OCR + 2FA |
| **V2** | Multi-clinic، پرداخت آنلاین، بیمه/آزمایشگاه، Push، Mobile API | — |

**مایلستون‌ها:** M1 (پایان F3) ✓ | M2 (پایان F5+F6) ✓ رسیده — چرخه کامل مطب (منشی→پزشک→مالی)، آماده Pilot داخلی | M3 (پایان F9) — Go-Live V1.

### تصمیمات باز (نیازمند کارفرما)

1. ~~Drift ماتریس Capability (۴۹/۴۶)~~ — **بسته شد (F5):** ماتریس مبنا شد؛ مفرغ‌ها (files/search) از ماتریس اضافه شدند؛ غیرماتریسی‌ها حذف؛ باگ registerRole (stray cap هرگز پاک نمی‌شد) در 998ee81 اصلاح — جزئیات report-f5.md §3.
2. **Availability UI (تقویم/OTP/Profile)** — تصمیم قبلی: فاز UI مستقل پس از فازهای Backend.
3. **PHPStan** — طبق دستور F5: Blocker نیست؛ اگر بدون اختلاف قابل اضافه‌شدن است طبق roadmap انجام شود ولی توسعه Clinical متوقف نشود.
4. **`SmsController::can()`** — envelope استاندارد `CLINIC_*` ندارد (بدهی F2.5 → F8/patch).
5. **ADR-0023 Licensing** هنوز نوشته نشده (برای F10/licensing phase).

### نکات فنی برای F7 (آماده‌شده‌ها از F6)

- **ADR-0026 (تصریح کارفرما 2026-09-06) برای همه فازهای بعد الزامی است:** Authorization فقط Capability (`cpms_*`) — هرگز `if role == X` در کد جدید؛ نقش‌های سفارشی باید بدون تغییر Business Logic کار کنند؛ همه داشبوردهای ستادی Responsive (دست‌خط = بهینه‌سازی قلم روی تبلت، نه محدودیت دستگاه — Canvas باید Resize/Orientation/DPR/Touch-Stylus را بدون از‌دست‌رفتن Stroke مدیریت کند). نقشه مهاجرت Debt نقش-محور در خود ADR.
- `VisitService::applyTransition()` اکنون public است با `$forceRole` (فقط نقش system برای V11/V12) — عمل مالی + Transition در یک Transaction (الگوی FinanceService — M-7).
- الگوی Idempotency مالی (M-1): بدون جدول عمومی — `UNIQUE(invoice_id, idempotency_key)` روی خود جدول؛ اولین 201، تکرار همان کلید 200 + `code=CLINIC_IDEMPOTENCY_REPLAY` + همان payment_id؛ برای درجِ رقابت‌زده fallback به findByIdempotencyKey.
- محاسبات پولی: ریالِ صحیح (integer) همه‌جا؛ خالص در `InvoiceCalc` (تست Unit موجود)؛ کلید تسویه مؤثر = total − credit + debit.
- عددگیری سریال: قفل ردیف کلینیک (`SELECT ... FOR UPDATE` روی cpms_clinics) قبل از MAX+1.
- Audit مبلغی با کلیدهای نقطه‌دار (`invoice.balance`)؛ اکشن‌های مرجع در audit-strategy §2 (PAYMENT_CAPTURE نه PAYMENT_CAPTURED)؛ json_encode با فاصله بعد از «:» — تست‌ها JSON را decode کنند نه substring.
- Checkout: گارد `CLINIC_NOT_SETTLED` فقط مسیر paid→check_out را می‌بندد؛ مسیر معافیت (waive با دلیل) عمداً باز است.
- بازگشت از paid (void/refund) وضعیت ویزیت را برنمی‌گرداند (V12 یک‌طرفه — Deviation مستند در report-f6 §7).
- J-5 در تست‌ها: هر ویزیت جدید در همان روز = بیمار تازه (helper makePatient) — وگرنه CLINIC_DUPLICATE_ACTIVE_VISIT.
- F5 نکات همچنان معتبر: Audit FORBIDDEN خارج از Transaction؛ Factories وابسته به Setting بدون singleton-cache؛ ستون‌های NOT NULL بدون default همیشه صریح.

---

## 9. پروتکل لاگ کار ایجنت‌ها (اجباری)

**هر ایجنت** در پایان کار خود (یا پایان turn اگر کار ادامه دارد) این موارد را در انتهای همین فایل append می‌کند:

```
### [YYYY-MM-DD HH:MM UTC] — ایجنت <شناسه> — <عنوان کار>
- فاز/محدوده: ...
- اقدامات: (خلاصه روزنامه‌وار)
- کامیت‌ها: <sha> <پیام کوتاه> (شاخه arena/01a071c4-doctor)
- CI: <run-id> → <نتیجه> روی <sha>
- تصمیمات درون‌فازی: ...
- موارد باز/تحویل به ایجنت بعد: ...
- وضعیت tree: clean/dirty
```

قواعد لاگ:
- **Append-only** — ورودی قبلی را هرگز ویرایش/حذف نکن.
- اگر کار نیمه‌تمام است، صریح بنویس «ناتمام — ادامه در …».
- هر ادعای «سبز» باید run-id داشته باشد.
- این فایل در هر commit مربوط به خودش همراه کد commit شود.

---

## 10. لاگ کار ایجنت‌ها

### [2026-09-05 ~17:30 UTC] — ایجنت Arena (ادامه F3) — بستن نهایی F3
- فاز/محدوده: F3 close-out
- اقدامات: بازیابی دسترسی GitHub؛ تأیید runهای سبز نهایی (33982059480→33982970314→**33983240011 روی 1f2b1e7 هر ۵ job**)؛ commit «honesty note» 820d4b1 + evidence 1f2b1e7؛ گزارش نهایی report-f3.md ارائه شد (۲۱/۲۱ AC؛ کاتالوگ ۱۴ باگ production در §4.1). کاربر «ادامه بده» → ورود F4.
- کامیت‌ها: 820d4b1, 1f2b1e7
- CI: 33983240011 = SUCCESS (1f2b1e7)
- وضعیت tree: clean

### [2026-09-05 18:00–19:05 UTC] — ایجنت Arena — فاز F4 کامل (مراجعه/صف)
- فاز/محدوده: F4 کامل — Backend + UI + تست‌ها + مستندات
- اقدامات (خلاصه):
  - Recon: تعارض فازبندی گزارش F3 §6 با roadmap حل شد (roadmap مقدم: F4=صف، F6=مالی؛ doc-sync در 5d46a11).
  - `VisitException` + `VisitRepository` (ADR-0021، بدون تغییر schema) + `VisitService` (V1–V15 با Row-Lock J-1، تاریخچه append-only J-3، J-4 اولویت فوری از is_walkin_express، J-5 قفل بیمار، J-6 سقف recall، ER-06 دیرهنگام→no_show+ویزیت فوری walk-in-like، FR-5.5 sweep، T9 در check_out و waive) + `QueueController` (D1/D6/D7/D8/D16 + E1–E6 + E14 + R1 با ETag/304 و rate-limit) + `VisitsNoShowHandler` + `SecretaryQueuePage` (داشبورد منشی: امروز/Drawer/Walk-in/Keyboard، polling 3s، Page Visibility).
  - تست‌ها: VisitFlowTest، RestQueueTest، VisitConcurrencyTest (TP-03b سه‌لایه: fork موازی DB + سرویس + Row-Lock)، JobQueueTest + تست چرخه جاب‌ها.
  - باگ‌های واقعی رفع‌شده: (۱) تداخل کلید status در envelope خطای RestBase (پاسخ 0)؛ (۲) جاب‌های تکرارشونده one-shot بودند (میراث F1) → زمان‌بندی idempotent در هر tick؛ (۳) E14 route ثبت شد (D16 بدون آن بی‌معنا — مستند در گزارش §4-2)؛ (۴) typo FQCN ×۲.
- کامیت‌ها: 0e77f6a, 5d46a11, b5c1eb7, a79f5c3, 39b8d7b, 9078ef0, 349d557, 73390d3, 98d18df
- CI: زنجیره 33985879588 (قرمز 6F/2E → ریشه‌یابی) → 33986638561 ✓ → 33986863341 ✓ → 33986951258/33987173206 (قرمز typo) → **33987189105 ✓ (349d557)** → 33987457187 ✓ → 33987706167 ✓ (98d18df). Integration: 133 تست/575 assertion، 0 skip.
- تصمیمات درون‌فازی (مستند در report-f4 §4): E14 در F4؛ T9 در waive؛ R1 محدود به امروز؛ اولویت صف بدون ستون جدید؛ no-show فقط confirmed (ماشین PENDING→no_show ندارد).
- موارد باز: drift capability 49/46؛ PHPStan؛ SmsController::can()؛ UI پزشک → F5.
- گزارش فاز: `docs/phase-reports/report-f4.md` (۲۱/۲۱ AC). Roadmap F4 → ✅.
- وضعیت tree: clean

### [2026-09-05 ~19:40 UTC] — ایجنت Arena — پاک‌سازی نهایی CI + تأیید
- فاز/محدوده: cleanup + verification
- اقدامات: verify شش قلم بازبینی کارفرما از repo (bootstrap/namespace، table naming، idempotency key، migration idempotent، child-process cleanup، CI) — همه حل‌شده؛ حذف باقی‌مانده‌های دیباگ workflow (DIAG grep مرده + step Diagnose schema state)؛ تأیید ۰ skip؛ unit 4×PHP سبز؛ integration سبز.
- کامیت‌ها: acb8103
- CI: **33988080619 = SUCCESS روی acb8103 (HEAD) — هر ۵ job**؛ PR #1 rollup ۵/۵ SUCCESS.
- وضعیت tree: clean

### [2026-09-05 ~20:00 UTC] — ایجنت Arena — این فایل راهنما
- اقدامات: ایجاد `docs/agent-guide.md` (همین فایل) به‌عنوان راهنمای جامع ادامه پروژه + پروتکل لاگ ایجنت‌ها.
- کامیت: 4fbcbed | CI: 33988491339 = SUCCESS (هر ۵ job).
- مورد باز: **ورود به F5 نیازمند تأیید کارفرما** (طبق Governance). تصمیم باز پیشنهادی: بستن drift capability قبل از F5.

### [2026-09-05 ~20:15 UTC] — ایجنت Arena — رفرش snapshot
- اقدامات: درخواست مجدد کارفرما برای راهنما → verify شد (فایل موجود، CI سبز)؛ فقط HEAD در snapshot به‌روز شد (4fbcbed). بدون تغییر محتوایی دیگر.
- وضعیت tree: clean

### [2026-09-05 ~20:35 UTC] — ایجنت Arena — کشف‌پذیری راهنما + doc-sync
- فاز/محدوده: مستندسازی (بدون تغییر production)
- اقدامات: (۱) ارجاع الزامی به این فایل در بالای `docs/README.md` (ایندکس اسناد) + به‌روزرسانی خط وضعیت فازها (F2.5/F4 اضافه شد)؛ (۲) ثبت رسمی **F4 در CHANGELOG افزونه** (بخش Added کامل + بخش Fixed برای دو باگ واقعی: تداخل کلید status در envelope و جاب‌های تکرارشونده one-shot) — F4 در CHANGELOG ثبت نشده بود.
- کامیت‌ها: همین ورودی | CI: docs-only — دو run مشابه قبلی سبز
- وضعیت tree: clean

### [2026-09-05 ~21:10 UTC] — ایجنت Arena — تکرار پنجم درخواست راهنما → verify
- فاز/محدوده: مستندسازی
- اقدامات: کارفرما همان درخواست را پنجمین بار فرستاد → وضعیت verify شد (فایل موجود با ۸ ورودی لاگ، کشف‌پذیر از README ریشه/docs/README/README افزونه، CI سبز روی 96a69c6 = run 33989243385، tree clean). هیچ کار جدیدی باقی نمانده بود؛ این ورودی صرفاً برای صداقت لاگ. سؤال مستقیم از کارفرما برای تعیین قدم بعد (F5 / مشکل نمایش / نیاز متفاوت).
- وضعیت tree: clean

### [2026-09-05 — منتقل‌شده] — ایجنت Arena-2 (شاخه `arena/01a07281-doctor` / PR #2) — بازبینی اولیه و ساخت دفترچه تحویل
> ورودی از `AGENTS.md` ریشهٔ شاخهٔ Agent دوم (b9467da) به اینجا منتقل شد (ادغام راهنماها — §5.3 گزارش F4). متن اصلی حفظ شده است.

- **هدف درخواست:** مطالعه وضعیت پروژه و ایجاد راهنمای پایدار برای Agentهای بعدی.
- **وضعیت قبل از شروع:** Repository شامل مستندات کامل و کد F1/F2/F2.5 و بخش‌هایی از F3 بود؛ F3 در Roadmap در حال انجام ثبت شده است.
- **اقدامات:** بررسی ساختار کامل Repository/مستندات/کد/تست‌ها؛ ایجاد `AGENTS.md`؛ ثبت وضعیت فازها، اصول توسعه، کارهای باقی‌مانده و قالب لاگ.
- **تست‌های اجراشده:** بدون اجرای تست (فقط بررسی Repository).
- **وضعیت Git:** commit 7b7131a روی شاخه `arena/01a07281-doctor` (base: main 210d437).
- **یادداشت ایجنت فعلی:** بخش‌های مفید این دفترچه (پروتکل لاگ، چک‌لیست پایان کار، ترتیب فازها) در همین راهنمای واحد جذب شد؛ `AGENTS.md` ریشه به پوینتر کوتاه تبدیل شد.

### [2026-09-05 — منتقل‌شده] — ایجنت Arena-2 (شاخه `arena/01a07281-doctor` / PR #2) — شروع F4 / Visit و Queue Slice 1
> ورودی از `AGENTS.md` ریشهٔ شاخهٔ Agent دوم (b9467da) به اینجا منتقل شد. کد این slice در Audit رسمی F4 (گزارش `report-f4.md` §5) بررسی شد: باگ‌های واقعی (permission_callback همیشه 403، نقش‌نگاشت غلط سرور، duplicate→existing به‌جای 409، race در walk-in) و نبود تست؛ merge نشد — ایده‌های خوب (Fallback Throwable، پروتکل لاگ صادقانه) اتخاذ و در شاخه اصلی پیاده شد.

- **هدف درخواست:** شروع فاز F4 پس از تکمیل نسبی F3.
- **وضعیت قبل از شروع:** State Machine و Schema مربوط به Visit/Queue از قبل وجود داشتند، اما Service، Repository و REST Controller فاز F4 وجود نداشتند. تغییرات F3 در PR شماره 1 در این شاخه merge نشده بودند.
- **اقدامات:** `VisitRepository` + `VisitService` (Check-in/Walk-in/Enqueue/Transition/Recall/Audit/History) + `VisitController` + wiring `App.php` + گزارش شروع F4.
- **تست‌های اجراشده:** PHP در محیط فعلی نصب نبود — PHPUnit و lint اجرا نشد.
- **ریسک‌های گزارش‌شده توسط خود Agent:** تست Integration، REST Argument Validation، IDOR، Today Dashboard، Real-time Polling و هماهنگی با F3 باقی مانده بود.
- **وضعیت Git:** commit b9467da روی شاخه `arena/01a07281-doctor`.

### [2026-09-05 ~21:45 UTC] — ایجنت Arena — Audit PR #2 + تکمیل §18 لایسنس صف/مراجعه → بستن F4
- فاز/محدوده: F4 close-out (دستور ۳۴-بندی کارفرما)
- اقدامات:
  - **Audit کامل PR #2** (`arena/01a07281-doctor` @ b9467da؛ base اشتباه 210d437 بدون fixهای F3): ۴ باگ واقعی (permission_callback `=== null` همیشه 403؛ نقش‌نگاشت سرور patient→secretary؛ duplicate→existing به‌جای 409 قرارداد §10؛ race در walk-in بدون قفل بیمار + recall هاردکد + بدون تست). عدم merge مستند؛ PR با کامنت Audit بسته شد (شاخه حفظ شد). جزئیات: `report-f4.md` §5.
  - **سیاست لایسنس §18 پیاده شد:** `LicenseGate` تزریقی در `VisitService` (الگوی BookingService؛ بدون Network Call)؛ Walk-in مستقل در Read-Only → `CLINIC_LICENSE_BLOCKED/503`؛ Check-in نوبت موجود و Transitionهای ویزیت در جریان مجاز. تست `VisitLicenseGateTest` (۵ تست با Gate تزریقی).
  - غنی‌سازی آمار داشبورد (`appointments_today/appointments_no_show/walk_in_today`)؛ حذف PHI غیرضروری از SELECT صف؛ Fallback `Throwable` در `QueueController::guard` (اتخاذ از PR #2).
  - انتقال لاگ‌های Agent-2 به همین فایل؛ `AGENTS.md` ریشه → پوینتر به این راهنما.
- کامیت‌ها: 4eb8eaa (کد+تست) + کامیت docs همین ورودی (شاخه arena/01a071c4-doctor)
- CI: پس از push بررسی و ثبت می‌شود (در ورودی بعدی تکمیل می‌گردد).
- وضعیت tree: پس از docs-commit → clean
- گام بعد: **F5 — Clinical با تأیید کارفرما آغاز می‌شود**؛ قدم اول: بستن Capability Drift طبق Permission Matrix و Least Privilege (Technical Alignment)، سپس استخراج Scope F5 از اسناد + cross-check با کد.

### [2026-09-06 23:00 UTC] — ایجنت F6 — مالی کامل + ADR-0026

- **F6 کامل شد** (جزئیات: `report-f6.md`): FinanceService/Controller/UI/Repos + ۱۷ تست Integration؛ CI سبز ۵/۵ روی 7121d56 (run 33996401245)؛ Integration = ۲۰۳ تست.
- **ADR-0026** ثبت شد (تصریح کارفرما: نقش‌های پویا/Capability/Scope/Responsive) — بررسی معماری بدون Blocker؛ نقشه مهاجرت Debt نقش-محور در ADR؛ مستندات SRS/Permission/Security/Roadmap هم‌راستا.
- **توقف طبق پروتکل:** F7 (دست‌خط) شروع نشده — منتظر تأیید کارفرما.

### [2026-09-06 ~23:55 UTC] — ایجنت Arena — F7 دست‌خط کامل (CI نهایی در انتظار اتصال GitHub)

- **F7 پیاده شد** (جزئیات: `report-f7.md`): Migration 0004 (background_attachment_id + ایندکس GC)، HandwritingService/Repository/Controller (F1/F1b/F1c/F2/F3 + پروتکل Revision ADR-0014 + Idempotency عمومی با context=pageId)، HandwritingGcHandler + RECURRING_JOBS، Settings hw.version_keep/max_age_days، DoctorHandwritingPage (ویرایشگر تمام‌صفحه: DPR/Pressure/Coalesced/Touch=pan-pinch/Erasor سطح-Stroke/Undo/Redo/Zoom/Full-screen/Multi-page/Template/Annotation E16/Auto-save→IndexedDB+PUT/Backoff 5..1800s/Resume online+focus/Conflict دو تب/Recovery محلی)، دکمه‌های 🖋️ در DoctorDashboardPage، HandwritingFlowTest (۱۵ تست).
- **سه ریشه شکست CI پیدا و رفع شد:** (۱) import غلط `Infrastructure\Security\Settings` به‌جای `Settings\Settings` (TypeError ساخت سرویس — آبشاری به همه REST/Jobs)؛ (۲) `pageRow` بدون document_id (INSERT نمی‌افتاد → insert_id کهنه → findPage=null) + گارد RuntimeException روی insert شکست‌خورده در Repository؛ (۳) **Flakiness نیمه‌شب UTC** در تست‌های Visit/Queue (gmdate('Y-m-d') با time()±N جفت می‌شد — اجرای 23:00–00:00 UTC مسیر lazy no-show می‌گرفت؛ اصلاح: تاریخ+ساعت Slot از یک timestamp در VisitFlowTest/VisitLicenseGateTest/RestQueueTest).
- **نکته برای CI های آینده:** CI ساعت ~23:3x UTC اجرا شد و Flakiness بالا آمد — اگر دوباره خطای 'walk_in' به‌جای 'scheduled' دیدید، اول ساعت UTC را چک کنید؛ ریشه اصلاح شد ولی بد نیست بدانید.
- **توکن GitHub وسط جلسه منقضی شد** — پس از اتصال مجدد، سندباکس بازسازی شده بود (کلون تازه): بازیابی با reset به b3875ea (نوک remote) + working tree حفظ‌شده = کامیت بازسازی‌شده 75cf798 (محتوای 190d8c5+130cb73 یکجا).
- **CI نهایی سبز ۵/۵:** run **34011934801** @ **4286aff** — Integration = ۲۱۸ تست ۰ خطا (اصلاح آخر: مقایسه Idempotent-replay با ksort — JSON objectها بدون‌ترتیب‌اند؛ ترتیب کلیدهای پاسخ ذخیره‌شده در decode با پاسخ تازه فرق داشت، مقادیر یکسان).
- **مشاهده برای F9 (Hardening):** ایندکس یونیک `cpms_idempotency_keys.u_idem_key` فقط روی `key` است ولی SELECT کتاب‌keeping چهار ستونی است (key, endpoint, wp_user_id, context_id) — کلید تکراری بین Contextها → INSERT بی‌صدا می‌افتد (بنر Duplicate در لاگ تست‌ها، بی‌ضرر فعلی). بازبینی ایندکس در F9 (جزئیات: report-f7 §9).
- **F7 بسته شد** — HEAD نهایی: **cf290be** (کد 4286aff + docs) — CI نهایی روی HEAD: run **34012034597** سبز ۵/۵. Tree پاک. توقف طبق پروتکل: F8 (اعلان+گزارش) منتظر تأیید کارفرما.

### [2026-09-07 ~00:30 UTC] — ایجنت Arena — شروع F8 (اعلان + گزارش)

- **ورود به F8 پس از تأیید کارفرما.** Scope: Notification Layer (رویداد→queue→adapter؛ N-1..N-6)، رویدادهای FR-20.2، SMS روی معماری Provider-agnostic موجود (ADR-0025)، Jalali در Templates، Queue/Retry/Dedupe، یادآوری نوبت/Follow-up، ۱۲ گزارش FR-19.2 با مدل مجوز+scope (ADR-0026/D-8)، Export (CSV محافظت فرمول‌اینجکشن، Watermark، Audit EXPORT، دانلود محافظت‌شده)، TP-13 + Report Tests.
- **تحقیق تکمیل:** notifications.md (کامل)، background-jobs.md (appt.reminder/notif.dispatch/fu.reminder/report.export)، SRS FR-19/20/21، api-contract G5/G6/R2، data-dictionary §32/33، permission-matrix §2/§3/§6، ADR-0026، performance-baseline (Export async)، file-storage (محافظت ساختاری)، wireframes (Toast منشی).
- **کد پایه موجود:** SmsService provider-agnostic کامل (templates/dedupe/queue/retry) + SmsSendJobHandler + ارسال APPTconfirmed/cancelled/rescheduled از BookingService (Jalali از قبل). cpms_notifications موجود ولی بدون NotificationService و بدون ستون read_at (نیاز Migration 0005). RolesAndCapabilities: REPORT_READ/EXPORT/FINANCE_READ موجود. FinanceService.summary موجود (D18) ولی بدون Scope پزشک.
- **تصمیم‌های طراحی F8:**
  - Internal channel = cpms_notifications (جدید NotificationService)؛ SMS = پایپ‌لاین موجود cpms_sms_messages (بدون دو-صف کردن)؛ Email/Push = V1 رها (اختیاری در کاتالوگ). PAYMENT.receipt اختیاری → V1 رها.
  - cpms_notifications باقی می‌ماند queue-native (N-2): INSERT queued؛ Job `notif.dispatch` (هر دقیقه، RECURRING) → queued→sent + Archive>90d (retention delete).
  - Dedupe: الگوی SmsService (SELECT قبل INSERT؛ UNIQUE dedupe_key)؛ کلید per-recipient (`apt:{id}:confirm:u{userId}` / `:p{patientId}`).
  - Scope گزارش‌ها: پزشکِ متصل (cpms_clinicians.wp_user_id) = OWN سرور-side (فیلتر clinician_id اجباری)؛ Aggregate مطب فقط برای دارنده cpms_report_read بدون Clinician-Link (اعطای صریح، الگوی حسابدار ماتریس §6)؛ پزشک متصل هرگز Aggregate کل مطب نمی‌گیرد (403) — D-8/D-15 + قواعد کارفرما.
  - تفکیک Aggregate⊥Detail: گزارش مالی (revenue/payment_methods/open_balances) = جمعی بدون نام بیمار + نیاز finance_read؛ گزارش عملیاتی با نام بیمار = نیاز patient_read؛ follow_ups_due = نیاز medical_read. Notes خصوصی هرگز در هیچ گزارشی نیست (کوئری نمی‌شوند).
  - Export: POST → Job `report.export` (async طبق baseline §18) → CSV (BOM + ساکس فرمول‌اینجکشن) در LocalFileStorage (خارج webroot) + اعلان Internal «آماده شد» به درخواست‌دهنده (الگوی background-jobs: «فایل + اعلان»)؛ دانلود فقط مالک + cpms_export + Audit EXPORT (request و download). PDF سرور = Backlog (پیش‌زمینه F6)؛ Print View با Watermark (کاربر+زمان) برای چاپ مرورگر.
  - یادآوری‌ها: appt.reminder/fu.reminder به‌صورت RECURRING per-tick + dedupe (J-2)؛ Quiet hours 08:00–21:00 فقط روی SMS غیرتعاملی (یادآوری‌ها)؛ OTP مستثنا (مسیر inline موجود دست‌نخورده).
- قدم بعد: Migration 0005 → NotificationService/Repository → Jobs → Wiring (Visit/Booking) → Controllers (G5/G6/R2) → ReportService/Export → UI Badge → تست‌ها → docs.

### [2026-09-06 ~06:15 UTC] — ایجنت Arena — F8 اعلان + گزارش کامل (CI سبز ۵/۵)

- **F8 پیاده و بسته شد** (جزئیات: `report-f8.md`): Migration 0005 (cpms_notifications.read_at + idx_notif_patient)، NotificationEvents/NotificationService/NotificationRepository (Dedupe الگوی SmsService + Cancel-on-appt-cancel + Quiet-hours + retention 90d داخل notif.dispatch هر ۲ دقیقه)، ۴ Job تکرارشونده (notif.dispatch/appt.reminder/fu.reminder/report.export)، Wiring رویدادها (QUEUE.called/ready_payment در VisitService؛ APPT confirmed/changed/cancelled SMS+Internal در BookingService)، NotificationsController (G6 inbox/read + R2 ETag/304/since/rate 60)، UI زنگ/پنل/Toast در SecretaryQueuePage، ReportService (۱۲ گزارش + Scope سرور-side D-8/D-15 + تفکیک Aggregate⊥Detail + بازه Bounded)، ExportService (async CSV با BOM+Formula-guard خارج webroot + اعلان آمادگی + دانلود مالک‌محور + Audit EXPORT + retention 7d/410)، Print View با Watermark؛ Settings شش کلید جدید؛ ۲۲ تست Integration جدید (NotificationFlowTest ۱۱ + ReportsAuthzTest ۱۱).
- **CI کد:** run **34015519073** @ **f11f6e0** سبز ۵/۵ — Integration = **۲۴۰ تست، ۰ خطا** (۲۱۸ قبلی + ۲۲ جدید). **CI docs نهایی:** run **34015846043** @ **c433416** سبز ۵/۵.
- **سه درس رفع‌شده در دو iteration CI:** ① `private const NS` نمی‌تواند protected والد را override کند (Fatal — بازتعریف ثابت فقط گسترده‌تر)؛ ② **`rest_do_request` رشته کوئری در route را پارس نمی‌کند** (regex مسیر `$`-انکر → 404) — پارامترهای GET در تست‌ها با `set_param` ست شوند؛ ③ ارجاع `&$this->prop` به typed property مقداردهی‌نشده در PHP 8.1+ خطاست (مقدار اولیه `= 0`).
- **F8 بسته شد** — HEAD کد: **f11f6e0** + docs نهایی؛ CI روی docs نهایی هم سبز (run 34015846043، ۵/۵). Tree پاک. توقف طبق پروتکل: F9 (Hardening) منتظر تأیید کارفرما.

### [2026-09-06 ~07:20 UTC] — ایجنت Arena — تصمیم محصول ADR-0027 (یک محصول چندپزشکی) + بازبینی آمادگی معماری

- **تصمیم نهایی کارفرما ثبت شد (ADR-0027):** One Product / One Core / One Database / Adaptive UX — مطب تک‌پزشکی = زیرمجموعه درمانگاه چندپزشکی؛ دو Plugin/Codebase/Fork ممنوع؛ Scope همیشه سرور-side؛ Specialty = دامنه (نه نقش Authorization)؛ Patient = clinic-level؛ مالی هرگز به بالینی imply نمی‌شود.
- **بازبینی آمادگی انجام شد** (`docs/architecture/multi-doctor-readiness-review.md`): ممیزی ۳۷ جدول + کد + اسناد روی ۱۰ محور درخواستی. **نتیجه: ۰ FOUNDATIONAL CHANGE REQUIRED، هیچ STOP-blocker؛ ۱۱ قلم Minor Alignment به فازها نگاشت شد**.
  - شواهد کلیدی: `clinician_id` از روز اول در Schedule/Slot/Appointment/Visit/Note/RX/FollowUp (ADR-0003)؛ Patient clinic-level + MRN کلینیک‌سوئیپ؛ Booking با clinician_id الزامی (B1/D10)؛ صف با فیلتر clinician؛ داشبورد پزشک ownClinician()؛ F8 Scope سرور-side گزارش‌ها (الگوی Enforcement اثبات‌شده)؛ LicenseGate بدون فرض تعداد پزشک؛ `u_slot(clinician_id, slot_date, slot_time)` از قبل ضد-تعارض بین-مکانی است.
  - Minor Alignmentها (phase-mapped، بدون پیاده‌سازی زودهنگام طبق قاعده کارفرما): گارد مالکیت Transition صف برای پزشک + UNIQUE Index روی clinicians.wp_user_id (تضمین 1:1) → **F9**؛ UX حالت مطب (Skip خودکار Picker با ۱ پزشک) → V1.5؛ Specialty M:N + Booking تخصص‌محور/First-available + سرویس per-clinician + Breakdown Reports + Staff Assignments/Scope + Branch + Department/Room → V2؛ Entitlement لایسنس per-doctor → F10.
  - سه مورد تصمیم‌محور با فرمت ISSUE/CURRENT/TARGET/IMPACT/MIGRATION/OPTIONS/RECOMMENDATION در §2 سند بازبینی ثبت شد (Specialty، Schedule شعبه‌ای + u_sched_day، Enforcement Scope) — توصیه همه: فاز خودشان.
- **Docs sync:** ADR-0027 جدید؛ SRS §1.1/§1.2/§2.1/§2.4 (A-1 بازنویسی، A-5 ابطال‌شده توسط F5، FR-4.11 جدید)؛ permission-matrix §6 یادداشت ADR-0027؛ roadmap v1.1 (F9/V1.5/V2 اقلام چندپزشکی)؛ wireframes/patient.md §10 (رزرو تطبیقی).
- **SRS A-5 ابطال ثبت شد:** فرض قدیمی «دسترسی پزشک به Private Notes سایر پزشکان در V1» — پیاده‌سازی F5 از ابتدا سخت‌گیرانه‌تر بود (مالکیت ویزیت خودش، تست 404) و ADR-0027 آن را قطعی کرد.
- توقف طبق پروتکل: F9 (Hardening) همچنان منتظر تأیید کارفرما؛ Backlog چندپزشکی در فازهای نگاشت‌شده اجرا می‌شود.

### [2026-09-06 ~08:30 UTC] — ایجنت Arena — F9 Hardening کامل (CI سبز ۵/۵)

- **F9 پیاده و بسته شد** (جزئیات: `report-f9.md`): ماتریس کامل T-01..T-24 با Evidence تستی — سه حفره واقعی بسته شد: ① Idempotency برای Endpointهای بدون context (Replay/In-flight خاموش به‌دلیل تبدیل NULL→0 توسط prepare + UNIQUE تک‌ستونه) — Migration 0006 با Preflight + بازنویسی کلاس؛ ② گارد مالکیت ویزیت پزشک (ADR-0027 #3) دولایه با Audit ماندگار قبل از Transaction؛ ③ UNIQUE پیوند clinician↔wp_user (Minor #12) با Preflight راهنما. Cleanup Jobs مرده ×3 فعال + دو Fatal عملیاتی (execute() به‌جای query()؛ require به‌جای require_once در MigrationRunner). Accessibility (dialog/focus/44px). user-guide.md فارسی برای Pilot. Performance NFR-PERF-1: پوشش ایندکس همه Hot-pathها تأیید؛ Benchmark ران‌تایم (k6 @50) صادقانه به محیط مرجع Pilot سپرده شد (در CI قابل اجرا نیست). TP-16: مسیر ارتقا از Restore Legacy تست‌شده + Runbook؛ Drill محیط مجزا = چک‌لیست Pilot.
- **CI نهایی کد:** run **34019234033** سبز ۵/۵ — Integration = **۲۵۴ تست** (۲۴۰ F8 + ۱۴ جدید/الحاقی: SecurityHardeningTest ۹ + MigrationTest +۵). سه راند رفع‌اشکال CI با root cause واحد در هر راند: ① `require_once` MigrationRunner (rollback→re-migrate همان process → true)؛ ② `catch (RuntimeException)` در namespace تست = کلاس ناموجود (AssertionFailedError هم RuntimeException است → گمراه‌کننده) + cleanup تست در finally (DDL وسط تراکنش تست = implicit commit → نشت state)؛ ③ چهار ریشه مستقل: NS بدون اسلش در تست (rest_no_route 404 کاذب)، UPDATE فقط یکی از دو ردیف corrupt، setUp بدون Link پزشک↔clinician، TypeError execute/query.
- **درس‌های F9 برای فازهای بعد:** ① در تستهای namespaced همیشه `\\RuntimeException` بک‌اسلش‌دار؛ ② هیچ تست State-mutatingی بدون `finally` بازیابی وقتی DDL دارد (implicit commit تراکنش WP تست را می‌شکند)؛ ③ مسیر REST در تست‌ها همیشه با اسلش ابتدای namespace (`/clinic/v1/...`)؛ ④ امضاهای `:int` هرگز مقدار `query():bool` برنگردانند — برای تعداد سطر `execute()`؛ ⑤ فایلهای Migration باید side-effect-free باشند تا `require` (نه require_once) امن باشد.
- **Docs sync:** report-f9.md، CHANGELOG (F9)، roadmap F9 ✅، api-contract §0 (دامنه Idempotency چه‌گانه)، user-guide.md (از کامیت کد).
- **F9 بسته شد.** طبق پروتокол و دستور صریح کارفرما: **گزارش Completion ارائه شد و توقف تا تأیید F10 (Go-Live/Pilot عملیاتی) — بدون تأیید وارد فاز جدید نمی‌شویم.**

### [2026-09-06 ~13:10 UTC] — ایجنت Arena — F10 شروع: بنیان لایسنس، بکاپ، بهروزرسانی امن، Health/UX، تست ۱۰۰-راهی (اجرای فاز؛ CI در انتظار)

- **F10 طبق اسپک کارفرما آغاز شد** (اجازه ویرایش/commit/push/PR؛ بدون merge به main). Git delta علیه ممیزی: HEAD `a68241bce` دستنخورده (clean). هیچ FOUNDATIONAL_CONFLICT یافت نشد.
- **ADR-0023 (پروتکل لایسنس)** + **ADR-0028 (مرز Data/Control Plane)** + **ADR-0029 (تحویل امن بهروزرسانی)** پذیرفته و ثبت شد.
- **لایسنس (مکمل Seam F3):** Domain خالص (LicenseStatus/Policy/StateMachine/EntitlementRegistry fail-closed/SignedLicenseGate/LicenseSignature Ed25519) + Infra (VendorGateway + HttpVendorGateway HTTPS/SSRF/Timeout؛ LicenseRepository + Migration `2026_09_07_0008`) + App/LicenseService (فعالسازی سرور و آفلاین-سند؛ وضعیت محلی امضاشده؛ refresh فقط در Job با Backoff). `App::licenseGate()` حالا واقعی است؛ نصب فعالنشده دیگر «بازِ ابدی» نیست — تصمیم کارفرما 2026-09-06: `ACTIVATION_PENDING` (نصب تازه؛ پنجرهٔ ۷ روزه از Migration 0008) و `ACTIVATION_GRACE` (نصب pre-F10؛ مهلت ۳۰ روزه)؛ پایان پنجره بدون سند → RESTRICTED؛ `NOT_CONFIGURED` فقط دفاعی؛ حالت توسعه فقط صریح (`CPMS_DEV_MODE`/فیلتر `cpms_license_dev_mode` → `DEVELOPMENT`) بدون تشخیص خودکار محیط؛ anti-reset + مرجع زمان سرور. وضعیتها: ACTIVE/EXPIRING/GRACE/RESTRICTED/SUSPENDED/REVOKED/INVALID/UNREACHABLE؛ قطع شبکه ≠ نامعتبر/پنجره.
- **بکاپ/بازیابی:** موتور داخل افزونه (db.sql تمام cpms_* با snapshot سازگار بدون mysqldump + mirror storage + مانیفست sha256) در ProtectedBackupStore؛ Job دوره‌ای `backup.run`؛ Retention؛ Preflight + Safety Backup + Restore با تأیید صریح (فقط cpms_*)؛ CLI `bin/cpms backup …`.
- **بهروزرسانی امن:** مانیفست انتشار Ed25519 با کلید جدا (ReleaseKeys)، بدون eval/کد از راه دور؛ entitlement گیت feature `updates`؛ UpdateService + کش transient.
- **Health/UX:** SystemHealthService (چکهای بدون PHI + Host Capability SUPPORTED/WARNINGS/UNSUPPORTED) + صفحه «CPMS (سیستم)» (مجوز/Health/بکاپ/Restore/بهروزرسانی؛ cap `cpms_config`؛ nonce؛ بدون PHI).
- **آزمون پذیرش ۱۰۰-راهی (§27/§28):** `SlotCapacityOneHundredWayTest` — ۱۰۰ فرایند همزمان با اتصال مستقل MySQL روی مسیر واقعی `SlotRepository::atomicBook`؛ ظرفیت ۱ → دقیقاً ۱ برنده؛ ظرفیت ۳ → دقیقاً ۳ (در CI اجرا میشود؛ این sandbox MySQL ندارد).
- **واحدتست محلی (WASM PHP 8.2):** ۲۸۵ تست، ۱۶٬۶۶۱ اِسert، ۰ شکست (۸ خطای محیطی شناختهشده 32-bit؛ ۸ skip نیازمند sodium). **CI سبز ۵/۵** (Unit 8.1–8.4 + Integration WP6.7/MySQL8 — run 34037362222؛ شامل 100-way پذیرش و تستهای پنجرهٔ فعالسازی). اجرای محلی WP/MySQL در این sandbox ممکن نیست (BLOCKED_BY_ENVIRONMENT).
- **گزارش F10 و توقف تا تأیید کارفرما** طبق §49–§51 در ادامه همین لاگ ثبت خواهد شد.

### [2026-09-07 ~06:40 UTC] — ایجنت Arena — ممیزی مستقل کامل + Remediation Part 1 (P2/P5/P8/P10)

- **فاز/محدوده:** پس از ممیزی مستقل فقط-خواندنی کل کد روی `6e42519` (خواسته کارفرما: «همه مشکلات را پارت‌به‌پارت رفع کن») — Part 1 = ۴ ایراد از ۱۳.
- **اقدامات:**
  - **P2 (دسترسی نقش‌ها):** ADR-0030 — Override مدیریتی `cpms_role_caps_override` + صفحه «CPMS (دسترسی‌ها)» (`cpms_config` + Nonce + Audit `ROLE_PERMISSION_CHANGED`/`RESET`)؛ Self-healing اکنون مبنای Override را محترم می‌شمارد؛ خارج از فهرست همچنان پاک می‌شود (TP-10 سالم). نقش بیمار غیرقابل‌ویرایش (P-5).
  - **P8 (Scope صف پزشک):** `VisitService::today/eventsSince/lastEventId` برای نقش doctor → فقط ویزیت‌های Clinician خودش (بدون اتصال = هیچ)؛ Repository سه متد با پارامتر اختیاری `?int $clinicianId` — بدون Schema change. مطابق Master Context §8.
  - **P5 (مقصد بیمار):** صفحه «نوبت‌های من» (Ownership-only از مسیر `listMine`) + `login_redirect` + مخفی‌کردن Admin Bar + هدایت GETهای wp-admin — فقط برای «بیمار خالص» (multi-role ستادی مستثنی).
  - **P10:** «CPMS (فنی)» → «CPMS (فنی و لاگ)».
  - Docs: ADR-0030، permission-matrix v1.5، user-guide، CHANGELOG 1.0.1، `report-remediation-part1.md`.
- **کامیت‌ها:** روی `arena/01a077e9-doctor` (لیست در PR) — کد + تست + مستندات.
- **CI:** ✅ سبز کامل ۱۴/۱۴ روی `05d50ed` — Integration run 34090300269 (۳۰۷ تست، ۱۱ تست جدید سبز؛ یک شکست اولیه در RoleCapabilitiesOverrideTest به‌دلیل مقایسه ترتیبی به‌جای مجموعه‌ای «بازگشت به پیش‌فرض» → root-cause fix در 05d50ed)؛ Unit 8.1–8.4 + Pilot/Staging Gate + Closure Gate (run 34090297534/34090297520) — همه pass. Closure Gate نشان داد WP 6.4/6.5/6.6 از قبل در Gate پوشش دارد (اصلاح P7 ممیزی).
- **تصمیمات درون‌فازی:** ① Scope پزشک در Service نه Controller (P-1)؛ ② «Override در بکاپ cpms_* فعلی نمی‌آید — fail-safe به پیش‌فرض» در گزارش ثبت شد؛ ③ Cap جدید نسخه‌های آینده برای نقش Override-دار خودکار فعال نمی‌شود (قابل‌پیش‌بینی بودن)؛ ④ P13 (Notes بدون optimistic locking) به‌عنوان رفتار عمدی + ADR آینده ثبت شد.
- **موارد باز:** Part 2 پیشنهادی = UI پزشک/برنامه هفتگی (P1) + چاپ نسخه (P12)؛ Part 3 = UI گزارش‌ها (P4)؛ Part 4 = MariaDB/WP6.4 در CI (P6/P7) + i18n (P9) + JS/CSS جداسازی (P11)؛ Part 5 = پورتال بیمار (P3 — نیازمند تصمیم محصول). **توقف تا تأیید کارفرما.**
- **CI (نهایی):** ✅ سبز ۱۴/۱۴ روی `4d5dcf7` — Integration WP6.7/MySQL8 = **۳۱۵ تست / ۰ شکست** (۸ تست جدید Part 2 سبز)؛ Unit 8.1–8.4 + Pilot/Staging + Closure همه pass (run 34093548834 و 34093539362). یک راند شکست (۳۱۵/۱E+3F) با ریشه‌یابی از کامنت خودکار PR: باگ واقعی voidPrescription + join users + assert جابه‌جای تست → کامیت 4d5dcf7.
- **وضعیت tree:** clean بعد از کامیت.

### [2026-09-07 ~07:40 UTC] — ایجنت Arena — Remediation Part 2: Setup UI پزشک/برنامه (P1) + چاپ نسخه (P12)

- **فاز/محدوده:** تأیید کارفرما برای Part 2 از زنجیره رفع ایرادات ممیزی — دو قلم: بحرانی P1 و عملیاتی P12.
- **اقدامات:**
  - **P1 (راه‌اندازی):** `ClinicianRepository` جدید (فهرست/create/update با چک ۱:۱ صریح + RuntimeException — چون wpdb روی UNIQUE خطا نمی‌اندازد؛ قید DB لایه دوم Race) + صفحه «پزشکان و برنامه» (tools, `cpms_config` + Nonce): لیست/ثبت/ویرایش/غیرفعال‌سازی (حذف فیزیکی ممنوع — FK)، پیوند ۱:۱ کاربر با Select، ویرایش ۷ روز برنامه هفتگی با فرم آرایه‌ای (بدون فرم تودرتوی نامعتبر؛ دکمه «ذخیره روز» per-row؛ حذف روز با فرم hidden مجزا) + استثناهای تعطیلی/مرخصی/بستن — همه از مسیر `ScheduleService` (Audit SCHEDULE_* + بازتولید Slot خودکار). Audit جدید: `CLINICIAN_CREATED/UPDATED/STATUS_CHANGED`.
  - **P12 (چاپ نسخه):** `ClinicalService::prescriptionForPrint()` — `cpms_rx_read` + `requireOwnVisit` (ماتریس 4.3) + Audit `PRESCRIPTION_PRINTED`؛ انتخاب آخرین نسخه غیرواقعی یا rx صریح (rx ویزیت دیگر → 404)؛ خروجی کامل (اقلام/بیمار/MRN/سن/جلالی/شکایت اصلی/پزشک/کلینیک). صفحه مخفی `cpms-prescription-print` با CSS چاپ + واترمارک draft/voided. دکمه «🖨️ چاپ» per-rx در داشبورد پزشک (CFG.can_rx + print_url).
  - تست‌ها: `ClinicianRepositoryTest` (۳) + `PrescriptionPrintTest` (۵) — شامل 404 مالکیت پزشک دیگر و 403 منشی.
  - Docs: report-remediation-part2.md، user-guide (راه‌اندازی از UI + چاپ)، CHANGELOG (ادامه 1.0.1).
- **تصمیمات درون‌فازی:** ① Deactivate-only برای clinician؛ ② برنامه هفتگی از ScheduleService نه SQL مستقیم؛ ③ چاپ فقط پزشکِ خودش؛ ④ «ذخیره روز» = update/create خودکار با u_sched_day؛ ⑤ هر دو قلم CRITICAL ممیزی اکنون بسته — باقی Partها = IMPORTANT/UX.
- **موارد باز:** Part 3 پیشنهادی = UI گزارش‌ها (P4)؛ Part 4 = MariaDB/WP matrix + i18n + JS/CSS؛ Part 5 = پورتال بیمار (تصمیم محصول). **توقف تا تأیید کارفرما.**
- **وضعیت tree:** clean بعد از کامیت.

#### پیوست Part 2 — رفع شکست‌های راند اول CI (ریشه‌یابی از کامنت PR)
- **باگ واقعی تولیدی (کشف تست جدید):** `ClinicalService::voidPrescription` — `$reason` در `use` closure تراکنش نبود → هر ابطال نسخه Warning/Fatal (خط ۴۳۴). هیچ تست قبلی ابطال نسخه را نپوشانده بود. Fix + تست.
- `ClinicianRepository::listAll` — `table('users')` پیشوند cpms_ می‌گرفت (`{wp}_cpms_users` ناموجود) → `dbPrefix().'users'` (الگوی BookingService).
- تست `testUserLinking...` — دو assert با Semantics جابه‌جا نوشته شده بود (خودِ تست اشتباه بود، نه کد) → اصلاح + مستندسازی Semantics در کامنت.
- in-session debug workaround: لاگ job از API مسدود است (results-receiver) — جزئیات شکست از کامنت خودکار PR خوانده شد (مکانیزم موجود ci.yml «Post failures to PR»).

### [2026-09-07 ~09:30 UTC] — ایجنت Arena — F1 Remediation گروه 1: رفع F1-1 (P0) — باگ واحد پنجره در RateLimiter::cleanup
- **فاز/محدوده:** تأیید کارفرما برای ۷ رفع F1 با ترتیب مصوب؛ گروه 1 = F1-1. پروتکل: پیاده‌سازی ← تست ← commit ← push به شاخهٔ feature ← تأیید SHA remote ← گزارش.
- **اقدامات:**
  - **Migration `2026_09_07_0009`:** `cpms_rate_limits.window_sec INT UNSIGNED NOT NULL DEFAULT 3600` (additive، guard با SHOW COLUMNS). پیش‌فرض 3600 = همان واحدی که کد قدیم فرض می‌کرد → رفتار ردیف‌های legacy خنثی.
  - **`RateLimiter::hit()`:** `window_sec` در INSERT ذخیره + `ON DUPLICATE KEY UPDATE window_sec = VALUES(window_sec)` (self-heal ردیف‌های پیش از Migration).
  - **`RateLimiter::cleanup()`:** cutoff مستقل از واحد — `DELETE … WHERE window_id * window_sec < (now - olderThanSec)` (به‌جای `intdiv(time()-olderThanSec, 3600)`).
  - **تست‌های رگرسیون جدید (Integration، روی MySQL واقعی CI):** `testDailyOtpLimitSurvivesCleanup` (سناریوی دقیق OtpService: 3 hit با windowSec=86400 → cleanup(2*86400) هم‌سان Job روزانه → ردیف زنده می‌ماند + `window_sec=86400` + تلاش 4ام همان روز block)؛ `testCleanupKeepsLiveWindowsOfEveryUnit` (86400/3600/60)؛ `testCleanupRemovesExpiredWindowsOfEveryUnit` (ردیف‌های 4 روز پیش با هر واحد حذف می‌شوند). تست قدیمی `testCleanupRemovesOldWindows` حفظ شد (پوشش مسیر legacy با پیش‌فرض 3600).
- **تصمیمات درون‌فازی:** ① حذف ستون در `down()` عمداً no-op (زدن ستون = بازگشت به باگ)؛ ② `VALUES()` در ON DUPLICATE حفظ شد (سازگار MariaDB — alias-syntax در MariaDB نیست)؛ ③ `cleanup()` soft (execute) باقی ماند — شکست پاک‌سازی دوره‌ای نباید tick را شکند (رفتار قبلی).
- **تست محلی (php-wasm PHP 8.5):** lint 3 فایل ✓ + Unit suite 285/0F (همان baseline). **تست Integration: CI (PR push).**
- **وضعیت:** commit + push + SHA remote در ادامه این لاگ ثبت می‌شود.

#### پیوست گروه 1 — رفع شکست راند اول CI (ریشه‌یابی از کامنت خودکار PR)
- **ریشه:** شکست همهٔ Jobهای WP (Integration + Pilot + Closure روی همهٔ runtimes) در یک نقطهٔ واحد بود: `FAIL schema-0008 — 2026_09_07_0009`. Gateها و `MigrationTest` نسخهٔ فعلی schema را **پین** کرده بودند (`2026_09_07_0008`) — نقطهٔ همگام‌سازی طراحی‌شده برای هر Migration جدید.
- **رفع (تضعیف Gate نه، به‌روزرسانی پین):** `schema-0009` در closure-gate (۲×probe + restore-drill grep)، pilot-gate (fresh-install check + upgrade-path check + echo نمایشی) و `MigrationTest` (`LATEST_VERSION` + افزودن `rollbackOne()=0009` به ابتدای زنجیرهٔ rollback در دو تست Preflight/Upgrade — down()ِ 0009 عمداً no-op است و re-migrate آن idempotent).
- **تست‌ها:** Unit 285/0F (php-wasm) + lint ✓؛ Integration/Gates در راند بعدی CI.

#### پیوست گروه 1 (ب) — باگ واقعی دوم: probe اشتباه در Migration 0009 (کشف از 9 شکست Integration راند 2)
- **شواهد:** 9 شکست (7×RateLimiterTest + OtpFlowTest + RestBookingTest) دقیقاً مطابق رفتار «جدولِ 3 ستونه بدون window_sec»: INSERT چهارستونه soft-fail، DELETE با شرط window_sec حذف صفر؛ در حالی که MigrationTest سبز بود (FKها روی جدول واقعی) → ردیف window_sec واقعاً هرگز ساخته نشده بود.
- **ریشه:** `CpmsDb::query()` فقط **bool** برمی‌گرداند؛ در up()ِ 0009 probe با `query("SHOW COLUMNS …")` نوشته شده بود → `empty(true) === false` → شرط «ستون موجود است» همیشگی → **ALTER هیچ‌وقت اجرا نمی‌شد** ولی version با موفقیت ثبت می‌شد (به همین دلیل همهٔ Gateها — که فقط version را پین می‌کنند — سبز ماندند!). دقیقاً همان کلاس خطایی که F1-2 (silent-fail migration) هدف دارد.
- **رفع:** probe با `fetchRow()` + `$col === null` (الگوی استاندارد ماست: 0004/0005/0006) + تست جدید `testWindowSecColumnAddedByMigration` (وجود ستون + int unsigned + default 3600). اسکن کل src: نمونهٔ دیگری از این الگوی نادرست وجود ندارد.
- **درس برای گروه 2 (F1-2):** strict mode روی MigrationRunner عیناً همین کلاس شکست (SQL ناموفق + version ثبت‌شده) را در آینده مسدود می‌کند.

### [2026-09-07 ~12:00 UTC] — ایجنت Arena — F1 Remediation گروه 2: رفع F1-2 (P0) — Migration Fail-loud
- **فاز/محدوده:** گروه 2 از ۷. پیاده‌سازی + تست محلی انجام شد؛ push/CI در انتظار بازیابی اتصال GitHub (توکن GH_TOKEN اعتبار خود را از دست داد — 401 Bad credentials).
- **اقدامات:**
  - **`CpmsDb`:** `setStrict(bool)` + `ensureNoSqlError()` (در Strict mode: `$wpdb->last_error` غیرخالی → `RuntimeException` با پیام خطای SQL) — فراخوانی در انتهای query/execute/fetchRow/fetchAll/fetchValue/insert/update/delete. پیش‌فرض soft (رفتار سایر مسیرها دست‌نخورده).
  - **`MigrationRunner`:** در `migrate()` و `rollbackOne()` — `setStrict(true)` قبل از transaction و `setStrict(false)` در `finally`. چون INSERTِ version داخل همان closure تراکنش است، در throw: نه version ثبت می‌شود (rollback) و نه Migrationهای بعدی اجرا می‌شوند.
  - **تست رگرسیون جدید (Integration):** `testFailingMigrationAbortsAndIsNotRecorded` — MigrationRunner با دایرکتوری temp + Migration آگاهانهٔ شکست‌خورد (`SELECT * FROM جدولِ ناموجود`): assert RuntimeException با «SQL error» + version در `applied()` نیست + Migration خوبِ بعدی اجرا نشده.
- **ارتباط با گروه 1:** دقیقاً همین کلاس خطا در Migration 0009 رخ داده بود (ALTER بی‌صدا رد شد + version ثبت شد)؛ از این پس چنین شکستی در CI/Production fail-loud است.
- **تست محلی (php-wasm 8.5):** lint 3 فایل ✓ + Unit 285/0F. **Integration: CI (در انتظار push).**

### [2026-09-07 ~13:35 UTC] — ایجنت Arena (شاخهٔ `arena/01a07c01-doctor`) — F1 Remediation گروه 3: رفع F1-4 — Audit تغییرات Settings (قبل/بعد + updated_by)
- **رخداد ورودی (گزارش صادقانه):** پیاده‌سازی قبلی گروه 3 (کامیت `06d5498` روی `arena/01a07b2e-doctor`) در هیچ‌جا موجود نبود — نه در workspace جدید (shallow clone با ۱ کامیت)، نه در هیچ ref ریموت (`git ls-remote`)، و فایل `patches/0001-feat-F1-4-*.patch` نیز در ریپو نبود (سندباکس قبلی persist نشده بود). Remote شاخه `arena/01a07b2e-doctor` روی `8d5715d` (همان گروه 2) بود. **تصمیم:** پیاده‌سازی مجدد F1-4 طبق پروتکل و scope تأییدشده PR #5 («Settings audit (before/after + updated_by) + tests») روی شاخهٔ نشست فعلی. این جلسه به شاخهٔ `arena/01a07c01-doctor` قفل است (محیط Agent) — push/PR از همین شاخه.
- **مبنا:** `git reset --hard origin/main` (`a16e245` = merge گروه‌های 1+2) تا delta PR فقط گروه 3 باشد.
- **اقدامات:**
  - **`Settings::set()`** (نقطهٔ واحد همهٔ نوشتن‌ها — ۱۰ call-site): خواندن مقدار مؤثر قبل (ردیف یا `DEFAULTS`) + مقایسه JSON بعد از نوشتن → تغییر مؤثر = Audit `SETTING_UPDATE` (اکشن مرجع audit-strategy §2؛ `resource_type=setting`) با before/after به شکل `{setting, value}` + actor (`updated_by` + نقش WP از `get_userdata`؛ بدون کاربر = actor null → «system»).
  - **مستثناها (ضد سیل Audit ۱۰ساله):** ① تغییر no-op (مقدار جدید = مقدار مؤثر فعلی) — ردیف/updated_by بازنویسی می‌شود ولی Audit نمی‌گیرد؛ ② کلیدهای Runtime/telemetry `RUNTIME_KEYS` = `jobs.last_tick_at` (هر Tick!)، `backup.last_run_at`، `sms.last_test` — OpLog دارند.
  - **قرار امنیتی Vault:** کلید Credential-دار `sms.auth` (`sealed` AES-256-GCM + last4) — رویداد + actor ثبت می‌شود اما مقدار با `[redacted:credentials]` جایگزین می‌شود (حتی Ciphertext به Audit ۱۰ساله کپی نمی‌شود — سیاست «Secret به Audit تعلق ندارد»).
  - **ساختار `{setting, value}` به‌جای کلیدِ خودِ Setting در before/after:** Sanitize فعلی Audit کلیدهای مطابق ممنوعه‌ها (`otp`, `code`, …) را کامل حذف می‌کند — با ساختار ثابت، شناسهٔ Setting (به‌عنوان «مقدار») زنده می‌ماند؛ دقتِ خود Sanitize در F1-8 (گروه 7) اصلاح می‌شود و این ساختار همچنان درست می‌ماند (بدون وابستگی به رفتار فعلی).
  - **Wiring:** `App::settings()` اکنون `self::audit()` تزریق می‌کند (پارامتر اختیاری → سازگار با همهٔ سازنده‌های قبلی؛ بدون وابستگی حلقوی — AuditLogger فقط db+op).
  - **updated_by واقعی:** `SystemPage::backupSave/updateSettings` اکنون `get_current_user_id()` پاس می‌دهند (تا دیروز system ثبت می‌شد)؛ SmsService از قبل `$userId` داشت.
- **تست رگرسیون جدید (Integration — MySQL CI):** `SettingsAuditTest` (۶ تست): before/after+updated_by+نقش (Default 3→5)، no-op بدون Audit، سه کلید Runtime بدون Audit، actor سیستم، Credentials redacted (ciphertext هرگز در after_json نیست)، سلامت Hash Chain بعد از رکوردهای Setting.
- **Docs:** audit-strategy §2 (شرح قانون جدید + مستثناها)، settings-reference v1.4، CHANGELOG 1.0.2.
- **تصمیمات درون‌فازی:** ① Audit بعد از نوشتن Setting (خارج از transaction) — `CpmsDb::transactional` تو-در-تو پشتیبانی نمی‌کند و AuditLogger خودش transaction دارد؛ الگوی موجود سرویس‌ها هم «Audit بعد از Commit» است؛ ② no-op = refresh ردیف بدون Audit (آگاهانه — eventِ تغییر مؤثر ملاک است)؛ ③ `sms.last_test` telemetry است نه Config.
- **تست محلی (php-wasm 8.2):** lint فایل‌های تغییر (Settings/App/SystemPage/SettingsAuditTest) ✓. Unit suite به Settings/App دست ندارد (grep: صفر ارجاع). **Integration: CI (PR).**
- **وضعیت:** commit + push + PR + CI در ادامه این لاگ ثبت می‌شود.

#### پیوست گروه 3 — push + تأیید SHA remote + CI
- **Session-branch نکته:** این نشست به شاخهٔ `arena/01a07c01-doctor` قفل است (محیط Agent)؛ درخواست push به `arena/01a07b2e-doctor` قابل اجرا نبود — و چون `06d5498`/patch هر two موجود نبودند، خروجی واقعی همان پیاده‌سازی مجدد روی این شاخه شد.
- **کامیت:** `d42ef0d` (feat(F1-4)) از مبنای `a16e245` (merge گروه‌های 1+2).
- **Push:** ✅ — Remote SHA: **`d42ef0d9c40e614d775dcc9c7e07b7c417898975`** = local HEAD (تأیید با `git ls-remote`).
- **PR:** #7 (`arena/01a07c01-doctor` → `main`) — «F1 Remediation (cont. 2) — group 3/7: settings audit (F1-4)».
- **CI (روی `d42ef0d`): ✅ سبز ۳/۳** — CI/pull_request run **34128017332** (Unit PHP 8.1–8.4 + Integration WP6.7/MySQL8 = success؛ step «Post failures to PR» skip = صفر شکست) + Pilot/Staging Readiness Gate run **34128001933** (8m34s، success) + Closure Gate run **34128001870** (success). لاگ خام jobها از results-receiver مسدود است (محدودیت شناخته‌شدهٔ محیط) — Evidence: conclusion رسمی GitHub.
- **Tree:** clean. بعد از این docs-commit، CI نهایی روی HEAD ثبت می‌شود.

### [2026-09-07 ~14:55 UTC] — ایجنت Arena (شاخهٔ `arena/01a07c01-doctor`) — F1 Remediation گروه 4: رفع F1-3 — PHPStan در CI (سطح هم‌تراز واقعیت) + docs sync
- **فاز/محدوده:** گروه 4 از 7. «PHPStan added to CI (level matched to real code state) + docs sync» — بدون تضعیف Gate.
- **روش تعیین سطح:** PHP CLI در sandbox نیست (apt مخازن ناقص، باینری استاتیک در GitHub release موجود نبود) → **Probe روی CI**: workflow موقت با اجرای سطح‌های 0..8 و انتشار خروجی در کامنت PR (الگوی «Post failures to PR» — لاگ job از API مسدود است). ۴ راند (رفع constraint stubs `^7.0`، حذف `scanConstants` حذف‌شده در PHPStan 2، رفع artifact ویرایش موازی در انتهای HandwritingService — درس: ویرایش‌های موازی edit_file روی یک فایل ممنوع).
- **باگ‌های واقعی که خود تحلیل پیدا کرد (رفع شد):**
  ① `Admin/SystemPage` — ارجاع به `SystemHealthService::HOST_SUPPORTED*` بدون import درست → کلاسِ `ClinicCore\Admin\SystemHealthService` ناموجود → **Fatal در رندر صفحه «CPMS (سیستم)»** (F10). رفع: import `ClinicCore\Application\System\SystemHealthService`.
  ② `Rest/OtpController:80` — فراخوانی `OtpException::getData()` که وجود نداشت → **Fatal در پاسخ REST برای کد OTP غلط/منقضی** (مسیر رایج کاربر؛ تست‌ها service-level بودند و نمی‌گرفتند). رفع: `getData()` به `OtpException` اضافه شد.
  ③ `OtpService::audit()` — ۵ آرگومان صدا می‌شد، ۴ پارامتر می‌گرفت → meta «remaining» در `OTP_VERIFY_FAIL` بی‌صدا drop. رفع: `$meta` اختیاری + ادغام در after_json (Audit غنی‌تر).
- **لینت/تایپ:** حذف `use`های بلااستفاده (Booking/Clinical/Handwriting)، `callable(): T` برای `CpmsDb::transactional`، PHPDocهای ناسازگار (SmsController/BackupManifest).
- **پیکربندی:** `phpstan.neon` — سطح **3** (سطح 0..3 سبز؛ سطح 4 = ۶۷ خطا — بازسازی تایپی گسترده از ~۲۵ فایل، خارج از scope remediation؛ 5..8 بیشتر). `phpstan-bootstrap.php` (ثابت‌های WP/افزونه: ABSPATH، ARRAY_A، MINUTE_IN_SECONDS، CPMS_PLUGIN_DIR، …). require-dev: `phpstan/phpstan ^2.1` + `php-stubs/wordpress-stubs ^7.0` + `szepeviktor/phpstan-wordpress ^2.0`. ci.yml: job سوم «Static Analysis (PHPStan)» مستقل از Unit/Integration. یک ignore هدفمند+مستند: path-check `require_once` مسیرهای runtime WP در WpUpdateBridge (گارد function_exists موجود). Probe workflow حذف شد.
- **Docs sync:** agent-guide §6 (۳ job)، testing-plan §45 (L.6 → L3 فعلی + هدف ارتقا)، engineering-baseline §38 (اشاره به PHPStan L3 در CI)، CHANGELOG 1.0.2.
- **تست محلی:** lint php-wasm 9 فایل ✓ (بعد از رفع artifact). **PHPStan/Integration/Unit: CI.**
- **وضعیت:** commit + push + SHA remote + CI نهایی در ادامه این لاگ ثبت می‌شود.

#### پیوست گروه 4 — push + تأیید SHA remote + CI
- **کامیت‌های گروه 4:** `a90ea61` (ابزار + probe) ← `17816fb` (stubs ^7.0) ← `0cf7d46` (probe v2) ← `d018dfd` (scanConstants) ← `b3d445f` (باگ‌های واقعی + bootstrap) ← `c92e6f2` (رفع artifact ویرایش) ← **`2cebb07` (قفل سطح 3 + حذف probe + docs sync)**.
- **Push:** ✅ — Remote SHA: **`2cebb07e0221bb5a00e8a02f55d80fad1c0a2b8a`** = local HEAD (تأیید `git ls-remote`).
- **CI (روی `2cebb07`): ✅ سبز ۳/۳** — CI/pull_request run **34130498852** = **۶/۶ job سبز** (Unit PHP 8.1–8.4 + Integration WP6.7/MySQL8 + **Static Analysis/PHPStan L3** — اولین اجرای رسمی Gate استاتیک جدید) + Closure Gate run **34130493200** + Pilot/Staging Readiness Gate run **34130493287** (8m33s).
- **Tree:** clean. ادامه: گروه 5 (F1-5/F1-6 — oplog retention + JobQueue::fail race guard) طبق ترتیب مصوب، در انتظار دستور کارفرما (خواستهٔ این نشست فقط تا گروه 4 بود).

#### پیوست — قطع اتصال GitHub در پایان نشست (گزارش صادقانه)
- بلافاصله بعد از push `0c8c487` و تأیید SHA ریموت، توکن GitHub (gh + git credential) باطل شد (401 Bad credentials — همان الگوی شناخته‌شدهٔ محیط Agent در گروه 2). کامیتِ این یادداشت محلی است؛ بعد از اتصال مجدد GitHub توسط کارفرما push می‌شود.
- **شواهد CI روی `0c8c487` (HEAD نهایی):** CI = **موفق ۶/۶** (run 34131390001 — شامل Static Analysis/PHPStan L3) + Closure Gate = موفق (run 34131386018) — هر دو قبل از قطع، سبز دیده و تأیید شد. Pilot Gate (run 34131385987) در لحظهٔ قطع در حال اجرا بود؛ وضعیت نهایی‌اش از API خوانده نشد — روی `2cebb07` (کد یکسان؛ دلتا فقط ۲ فایل Markdown) Pilot سبز بود (run 34130493287، 8m33s). بعد از اتصال مجدد، کارفرما/ایجنت بعدی می‌تواند وضعیت این run را با `gh run view 34131385987` تأیید کند.

### [2026-09-07 ~17:30 UTC] — ایجنت Arena — F1 Remediation گروه 5: F1-5 (oplog retention) + F1-6 (JobQueue::fail race guard)
- **F1-5:** Setting جدید `retention.oplog_days` (پیش‌فرض **۹۰** — مصوب کارفرما) + Handler `OpLogCleanupHandler` (الگوی OtpCleanupHandler؛ days در هر اجرا از Settings خوانده می‌شود) + `'cleanup.oplog' => 1` در RECURRING_JOBS + ثبت در dispatcher. `cpms_operational_logs` جدول hot بدون Retention بود؛ Audit جداست و ۱۰ساله می‌ماند (audit-strategy §5/§6). Doc: settings-reference v1.5.
- **F1-6:** `JobQueue::fail(..., ?string $workerId = null)` — با workerId: هر دو UPDATE (retry/final) شرط `AND locked_by = %s` می‌گیرند؛ صفر ردیف → `JOB_FAIL_SKIPPED_LOCK_LOST` + return بدون تغییر (زامبی دیگر نمی‌تواند Status/Retry بازنویسی کند). با null: رفتار legacy (bin/cpms jobs retry مستقیماً SQL می‌زند و fail() صدا نمی‌زند — verify شد؛ تنها فراخواننده = JobsDispatcher که اکنون workerId می‌دهد). شمارش ردیف با `execute():int` نه `query():bool` (درس F1-1).
- **تست رگرسیون (Integration):** `JobQueueTest` +۳ (`testFailWithLostLockDoesNotTouchJob` — قفل چرخیده به w2، failِ w1 هیچ تغییری نمی‌دهد؛ `testFailWithOwnerWorkerSchedulesRetry`؛ `testFailFinalWithOwnerWorkerMarksFailed`) + `OpLogRetentionTest` (۴). مسیر legacy fail بدون workerId هم توسط تست قدیمی `testFailRetriesWithBackoffThenFails` پوشش می‌ماند.
- **تست محلی:** lint php-wasm ۷ فایل ✓. **Integration/Unit: CI.**
- **وضعیت:** commit + push + SHA + CI در ادامه ثبت می‌شود.

### [2026-09-07 ~18:00 UTC] — ایجنت Arena — F1 Remediation گروه 6: F1-7 — قفل یکپارچهٔ runTick (WP-Cron = CLI)
- **ریشه:** GET_LOCK فقط در `bin/cpms jobs tick` بود (docblock خودش ادعای «بدون Duplicate Runner» داشت!) و WP-Cron (`cpms_jobs_tick` → `App::runTick`) بدون قفل Tick می‌کرد → دو SAPI هم‌زمان.
- **رفع:** قفل داخل `App::runTick()` — `SELECT GET_LOCK('cpms_jobs_tick', 0)` (ثابت جدید `App::TICK_LOCK`؛ نام همان قفل قبلی برای سازگاری)؛ Skip → return **-1**؛ آزادسازی در `finally` (`RELEASE_LOCK`). helper جدید `App::isTickLocked()` با `IS_FREE_LOCK` (docblock: داخل خود Tick هم true برمی‌گرداند — برای پیام CLI بعد از Skip است). `bin/cpms` دیگر قفل محلی ندارد و همان پیام قبلی را با isTickLocked() چاپ می‌کند. نکته F1-1: GET_LOCK/RELEASE/IS_FREE با پارامتر prepare (%s) — نه string interpolation.
- **تست رگرسیون (Integration):** `TickLockTest` (۳ تست) با **اتصال دوم** `new \wpdb(...)` + set_prefix (الگوی SlotCapacityOneHundredWayTest): ① قفل بیرونی → runTick=-1 + Job queued می‌ماند + isTickLocked=true؛ ② بدون قفل → اجرا + آزادسازی قفل از دید اتصال دوم (IS_FREE_LOCK=1)؛ ③ بازتاب قفل بیرونی در isTickLocked قبل/بعد از Release. tearDown قفل را آزاد می‌کند (ایمنی تست بعدی).
- **سازگاری:** تست قدیمی `JobQueueTest::testRunTickReschedulesAndProcessesRecurringJobs` با قفل جدید هم پاس می‌شود (قفل روی همان اتصال گرفته/آزاد می‌شود). WP-Cron hook فقط return value را نادیده می‌گیرد.
- **تست محلی:** lint php-wasm (App.php، bin/cpms، TickLockTest) ✓. **Integration: CI.**
- **وضعیت:** commit + push + SHA + CI در ادامه ثبت می‌شود.

### [2026-09-07 ~18:25 UTC] — ایجنت Arena — F1 Remediation گروه 7: F1-8 (دقت sanitize) + F1-10 (docblock فالبک StateMachine)
- **F1-8:** `AuditLogger::sanitize` از substring-regex (`/(otp|code|…)/i`) به **تطبیق دقیق نام کلید (case-insensitive)** تغییر کرد — کلیدهای بی‌خطرِ شبیه (`http_code`/`status_code`/`failure_code`) دیگر حذف نمی‌شوند و رکوردهای HTTP/خطا در Audit معنا پیدا می‌کنند. فهرست ممنوعه‌ها گسترده‌تر و صریح شد (`auth_code`, `verification_code`, `passwd`, `access_token`, `refresh_token`, `id_token`, `session_token`, `client_secret`, `apikey`, `authorization`, …). Masking (`MASK_KEYS` از قبل exact) و Truncation و Hash Chain دست‌نخورده. نکتهٔ سازگاری: ساختار `{setting, value}` در Audit تنظیمات (F1-4) همچنان معتبر و بهینه است — شناسهٔ Setting اکنون به‌عنوان «مقدار» هم محفوظ می‌ماند. یادداشت خارج از scope: `OpLogger::sanitize` الگوی substring دارد اما به‌جای حذف، `[masked]` می‌گذارد (آسیب کمتر) — تغییرش در این گروه نیست و به‌عنوان مشاهده ثبت شد.
- **F1-10 (فقط docblock + تست):** قواعد Fallback در `StateMachine` صریح شد — fail-loud برای (from,event) نامعلوم؛ fallback به Candidate بدون محدودیت برای actor خارج از فهرست‌ها (فهرست خالی = همه)؛ ترتیب ارجحیت match؛ نکتهٔ امنیتی (مجوز واقعی در Service). رفتار کد هیچ تغییری نکرد. تست‌های مستندکننده Unit: `testActorFallbackToUnrestrictedCandidate` + `testNoFallbackWhenOnlyRestrictedCandidatesExist`.
- **تست محلی:** lint php-wasm ۵ فایل ✓. **Integration/Unit: CI (راند نهایی).**
- **وضعیت:** commit + push + SHA + CI نهایی (سه pipeline) در ادامه ثبت می‌شود. پس از سبزی: **STOP** — انتظار تأیید کارفرما (طبق پروتکل؛ F2 شروع نمی‌شود).

#### پیوست — شواهد نهایی گروه‌های 4(تکمیل)–7 و STOP
- **گروه 4 (تکمیل):** `463330e` (probe src+bin) → `8db0084` (نهایی: `phpstan.neon.dist`، level=3 بدون baseline — probe: صفر خطا @0..3 روی src+bin، ۷۲ خطا @4؛ job key `phpstan`؛ تصحیح AC-9 در report-f1.md).
- **گروه 5:** `ce751ba` (+`35977f5` docs) — F1-5 (cleanup.oplog + retention.oplog_days=90) + F1-6 (fail race guard). CI run **34139668303** سبز ۶/۶.
- **گروه 6:** `3a403f3` — F1-7 (قفل یکپارچهٔ runTick + isTickLocked + تست اتصال دوم). CI run **34139964706** سبز ۶/۶.
- **گروه 7:** `88183a6` — F1-8 (sanitize exact-match؛ http_code/failure_code حفظ) + F1-10 (docblock فالبک + تست‌های Unit). CI روی HEAD نهایی: run **34140229256** سبز ۶/۶ + Closure **34140225798** سبز + Pilot **34140225799** سبز (۸ دقیقه).
- **پاک‌سازی:** workflow موقت probe حذف شد (در 88183a6+این کامیت).
- **جمع تست‌های جدید این نشست:** SettingsAuditTest (۶) + JobQueueTest (۳+) + OpLogRetentionTest (۴) + TickLockTest (۳) + AuditChainTest (۱+) + StateMachineTest (۲+) = ۱۹+ تست رگرسیون.
- **STOP طبق پروتکل:** هر ۷ گروه انجام/سبز شد — منتظر تأیید کارفرما برای merge PR #7. F2 شروع نمی‌شود.

### [2026-09-07 ~19:30 UTC] — ایجنت Arena (شاخهٔ `arena/01a07cdc-doctor`) — Hotfix نقص‌های نصب واقعی + گیت Real WordPress Acceptance (PR #8)
- **ماموریت کارفرما:** بازتولید سه نقص گزارش‌شدهٔ نصب روی WordPress واقعی (D1: Tools→CPMS (سیستم)/Health = Critical Error؛ D2: CPMS (فنی)→شمارش جداول=0؛ D3: MigrationRunner ممکن است شکست SQL را موفق ثبت کند) + فیکس حداقلی + گیت CI واقعی (ZIP رسمی→WP تمیز→نصب→Migration→DB verification→Browser login→System/Health/Technical/Doctor/Secretary) با دو prefix `wp_`/`clinic_` بدون تضعیف تست.
- **بازتولید (قبل از فیکس — Evidence در Commit Commentها روی main~commits شاخه):** D1 = STILL_PRESENT (قالب `wp-die-message` + «There has been a critical error» روی هر دو prefix)؛ D2 = STILL_PRESENT (`UI=0 / DB=41` روی هر دو prefix)؛ D3 = FIXEDِ قبلی (پروب migration خراب: RuntimeException + عدم ثبت version).
- **ریشه‌ها/فیکس:**
  1. **D1** — `SystemPage::render()` سه فراخوان سرویس (license statusMeta / health run / backup listBackups) را بدون guard انجام می‌داد؛ کلاس‌شکست واقعی: wp-content غیرقابل‌نوشتن برای PHP → `ProtectedBackupStore::ensureGuards()` → `BackupException` → Fatal کل صفحه. فیکس: guard درون‌بخشی + نمایش خطای درون‌صفحه‌ای (صفحهٔ وضعیت = آخرین نقطهٔ دید، هرگز Fatal نشود). رگرسیون: `SystemAdminPagesTest` (۲ تست؛ بازتولید دقیق با «فایل به‌جای پوشهٔ بکاپ»).
  2. **D2** — `SettingsAdmin::tableCount()` از `LIKE 'cpms_%'` بدون prefix به `$wpdb->prefix . 'cpms_%'` (prepared — الگوی SystemHealthService). مانع در-suite تست: جداول TEMPORARY در WP Test Suite در information_schema دیده نمی‌شوند → قفل قطعی = گیت E2E (دو prefix).
  3. **REST 404 روی Permalink ساده (کشف با Console Gate مرورگر):** هلپر JS `api()` در ۵ صفحهٔ مدیریتی path حاوی `?query` را به `rest_url()` می‌چسباند — با permalink ساده (`index.php?rest_route=…`) پارامتر دوم باید با `&` ضمیمه شود وگرنه `rest_no_route` 404 (پولینگ صف/اعلان‌ها/خلاصهٔ مالی/جستجوی بیمار/…). فیکس مرکزی: `apiUrl()` در هر ۵ صفحه (DoctorDashboard/SecretaryQueue/SecretaryFinance/DoctorHandwriting/SmsSettings). Pilot با permalink «قشنگ» این را ماسک می‌کرد.
- **دام‌های زیرساختی که حین ساخت گیت رفع شد (Honesty):** YAML colon-in-scalar در name جاب؛ double-backslash در `wp eval` داخل single-quote؛ pipe-masking با tee (وضعیت خروجی واقعی گم می‌شد → RC capture صریح)؛ فایل‌های افزونهٔ نصب‌شده root-owned → تزریق probe با sudo؛ لاگ خام Job و artifact blob در این محیط مسدودند (sandbox proxy EOF) → Evidence به‌صورت **Commit Comment** منتشر می‌شود + Artifactها در UI گیت‌هاب موجودند. پروب TEMP DEBUG بعد از ریشه‌یابی حذف شد. deny-probe (403 عمدی) روی صفحهٔ بدون watcher منتقل شد تا گیت Console تضعیف نشود.
- **کامیت‌ها:** `cf97dba` (گیت Acceptance) → `85ed9bc` (YAML fix) → `a340cb2` (رفع باگ‌های خود گیت) → `9d9b97a`→`b7ca5e1`→`901c192`→`364ceeb`→`37e69f6` (تقویت evidence) → **`df6567a` (فیکس محصول: D1+D2+REST)** → این لاگ.
- **CI نهایی روی `df6567a`: ✅ همه سبز** — Real WP Acceptance push run **34150553934** (wp_ و clinic_ هر دو **25/25 PASS**؛ actual=41=ui روی هر دو؛ System/Health رندر؛ منوهای نقش‌ها؛ پروب D3؛ منوی Administrator فنی P-3 + Deny 403؛ بدون Fatal در لاگ‌ها) + CI/PR **34150968679** (Unit 8.1–8.4 + Integration + PHPStan) + Acceptance PR **34150968686** + Closure **34150553982** (۵/۵) + Pilot **34150554156** (Release/Upgrade/Staging/Responsive سبز). Artifacts: `rwp-acceptance-wp_` / `rwp-acceptance-clinic_`.
- **PR #8** — باز برای تأیید کارفرما؛ **ایجنت merge نکرد ≻ STOP طبق پروتکل.**

### [2026-09-08 ~22:15 UTC] — ایجنت Arena (شاخهٔ `arena/01a082db-doctor`، PR #11 DRAFT) — بستن OD-9 (مرز ذخیره‌سازی بکاپ) + PHASE 2 PRE-IMPLEMENTATION CHECK — STOP
- **ماموریت کارفرما:** (۱) verify handoff Phase 1A (`79cce4b`)؛ (۲) بستن OD-9 طبق تصمیم مالک (گزینهٔ C سخت‌گیرانه): ریشهٔ بکاپ فعال باید بیرون DocumentRoot باشد — Fail-Closed، بدون silent fallback — ولی Restore/DR نباید deadlock شود؛ (۳) گیت OD-9 با ۴ workflow سبز؛ (۴) فقط PRE-IMPLEMENTATION CHECK فاز ۲ (بدون پیاده‌سازی) + گزارش Gate و STOP.
- **پیاده‌سازی OD-9 (کامیت `3531d5d`):** `ProtectedBackupStore` با سازندهٔ private + `::active()` (fail-closed، realpath ⇒ symlink هم رد؛ کد `CLINIC_BACKUP_STORAGE_INSIDE_WEBROOT`) + `::legacySource()` (فقط‌خواندنی: createDir/ensureGuards/delete رد، listIds بدون گارد). `App::backupService()` پیکربندی ناامن را صریحاً downgrade می‌کند (مسیر عوض نمی‌شود؛ کش برداشته شد). `BackupService`: تفکیک مبدأ/مقصد — `resolveSourceStore()` (فعال→خصوصی→legacy) + `safetyDestinationStore()` (Safety Backup فقط به مقصد امن؛ مبدأ legacy هرگز مقصد نیست؛ نبود مقصد امن = توقف قبل از گام مخرب) + preflight با `integrity_warnings`/`legacy_unverified`/`source`/`source_root` + Audit RESTORE_APPLIED گسترده. `App::ensurePrivateStorage()`: مهاجرت idempotent ریشهٔ ناامن/legacy (copy→verify sha256→rename→verify→remove؛ تعارض بدون overwrite؛ Setting دست‌نخورده). `SystemHealthService`: inside-webroot = FAIL (قبلاً WARNING). `BackupRunHandler`: ثبت errorCode صریح.
- **تست:** `tests/Integration/BackupStorageBoundaryTest.php` — ۱۴ متد (default خارج webroot، safe path، inside-webroot رد، symlink رد، downgrade صریح، legacy readonly، رزولوشن سرویس، redirect Safety Backup، restore مسیر عادی، tampered رد، legacy_unverified، مهاجرت idempotent، تعارض، Health FAIL). مهاجرت با بکاپ واقعی seed می‌شود (نه manifest ساختگی) و restoreApply با artifact «توقف قبل از DDL» ایزوله شده — مسیر مخرب کامل = Restore Drill سطح OS در Pilot Gate.
- **خطای میانی و طبقه‌بندی:** CI run 34282802365 (روی `3531d5d`) قرمز — یک Error در تست تعارض: `mkdir(dirname($dst))` والد را می‌ساخت نه دایرکتوری بکاپ. طبقه‌بندی **D (نقص تست)**؛ fix یک‌خطی در `cd29473`؛ سایر اجراها (Closure/Real-WP روی همان SHA و Pilot با push جدید) سبز بودند.
- **شواهد نهایی روی `cd29473` (هر ۵ اجرا سبز):** CI **34283426837** (PHPStan lvl 3 + Unit 8.1–8.4 + Integration WP6.7/MySQL8؛ ۴۸۱ تست) · Real-WP PR **34283426829** · Real-WP push **34283425385** · Closure (شامل destructive restoreApply) **34283425388** · Pilot/Staging (شامل Restore Drill) **34283425398**. **OD-9 = CLOSED** (`docs/phase-reports/report-od9-closure.md`؛ drift-register/roadmap/handoff/security-model §۵-۴/runbook §۷/error-codes به‌روز).
- **PHASE 2 PRE-IMPLEMENTATION CHECK (`docs/phase-reports/phase2-pre-implementation-gate-report.md`، ۱۷ بند، همهٔ اعداد re-verify):** ۴۱ جدول/۹ migration/۳۹ FK تأیید؛ 🔍 تصحیح‌ها: ۲۶ جدول clinic_id (نه ۲۵ — `sms_messages` در نگاشت ب-۵ جا افتاده + نوع ستونش INT است نه BIGINT)؛ ۲۲ FK لازم (نه ۲۱)؛ سومین UNIQUE شکننده = `u_clinician_user` (نقض AD-05/M:N)؛ سرشماری hardcode: ۵۲ اجرایی + ۳ default-param در ۲۴ فایل — 🔍 **رانش فاز 1A: +۱ hardcode اجرایی جدید در `OtpService::resolveUser` (نقض AD-13 — در بدهی Phase 2 ثبت شد)**؛ یافتهٔ دو منبع TZ (wizard فقط setting می‌نویسد، ردیف clinics هرگز UPDATE نمی‌شود) — باید پیش از M-04 تعیین تکلیف شود. Q6/Q11/Q12 باز اما غیرمسدودکننده. **رأی: فاز ۲ آمادهٔ شروع مشروط به بند ۱۷ گزارش.**
- **محدودیت محیط:** sandbox فاقد PHP — عدد PHPUnit فقط از GitHub Actions با run-id؛ لاگ خام jobها در این محیط EOF می‌دهد (شواهد از check-run/PR comments استخراج شد).
- **STOP طبق دستور:** PR #10 همچنان OPEN + DRAFT (لمس نشد)؛ PR #11 برای CI DRAFT باز شد (merge نشد)؛ بدون tag/release/version-bump/merge. Phase 2 implement نشد. انتظار تأیید مالک.

### [2026-09-09 ~09:30 UTC] — ایجنت Arena (شاخهٔ `arena/01a082db-doctor`) — FINAL PRE-PHASE-2 GATE — STOP
- **ماموریت کارفرما (۱۰ بند):** verify اتصال/هویت repo؛ تصحیح رکورد عددی ۲۶/۲۲؛ تحقیق رگرسیون `clinic_id=1` + fix کوچک؛ تصمیم Source of Truth زمانی؛ طراحی Clinician/User/Membership؛ حکم معنایی SMS؛ اعتبارسنجی Migration Plan؛ تست‌پلن؛ مرز 1B/3؛ گزارش ۱۷بندی. **بدون هیچ implementation فاز ۲.**
- **§۱ هویت:** اتصال GitHub برگشت (401 دیروز گذرا؛ بدون workaround). شاخه = `arena/01a082db-doctor`؛ نام `arena/01a082d2-doctor` **در remote وجود ندارد** (ls-remote: ۱۰ شاخه؛ خطای خوانش «b»→«d2» گزارش شد). HEAD = `9d6cf41` == remote؛ هر ۵ گیت آن سبز (CI 34283426837 · Real-WP 34284357086/34284347413 · Closure 34284347483 · Pilot 34284347475).
- **§۲ تصحیح:** بازشماری مستقل — **۲۶ جدول clinic_id (فهرست/نوع هر یک)، ۴ FK، ۲۲ بدون FK**؛ `cpms_sms_messages` تنها INT (و DEFAULT 1؛ DEFAULT-1ها = ۳ جدول). علت خطای census قبلی: جمع‌بندی فقط migration 0001 را شمرد. بلوک «🔴 تصحیح نهایی» در `phase0.5-target-model.md` (ب-۵) + ماتریس کامل در **د-۶** جدید. تاریخچه Phase 0 بازنویسی نشد.
- **§۳ رگرسیون AD-13:** git blame — خط جدید Phase 1A در **`findExistingUser`** است (کامیت `4c16009`، OD-8)، نه `resolveUser` (آن از 8087b42 بود — تصحیح اتریبوشن گزارش قبلی خودم). Fix (`bbc1e83`): helper واحد `findActivePatientIdByMobile()` با `clinic_id = %d` + `Settings::clinicId()` (getter جدید؛ بدون مفهوم Scope ساختگی)؛ insert لینک هم به کلینیک پیکربندی‌شده؛ **OtpService = صفر hardcode؛ سرشماری ۵۲→۴۹**. تست جدید با کلینیک دوم واقعی + دو بیمار هم‌موبایل. B-15 دفترچه 1B تصحیح؛ **B-21 بسته شد (OD-7/OD-9)**.
- **§۴ TZ:** نقشهٔ کامل read/write — ستون clinics فقط seed می‌شود و تنها reader عملیاتی‌اش `Settings::clinicTimezone()` با ۳ مصرف‌کننده (یادآوری ×۲ + اعلان) است؛ setting ویزارد **هیچ مصرف‌کنندهٔ عملیاتی ندارد** ⇒ انتخاب اپراتیر بی‌اثر (نقص ثبت‌شده). پیشنهاد: `locations.timezone` = SoT عملیاتی؛ clinics.timezone = default واگذاری seeding؛ نگاشت M-04 = setting(معتبر) → ستون → Asia/Tehran + preflight واگرایی؛ hotfix ویزارد پیشنهاد شد (اجرا نشد — خارج از mandate).
- **§۵/§۸ Cardinality:** `u_clinician_user` **حفظ می‌شود** (تصحیح رأی قبلی): AD-05 دربارهٔ Membership است؛ ERD مصوب `WP_USER ||--o| CLINICIAN` یک پروفایل به‌ازای کاربر می‌خواهد. سه مفهوم تفکیک: WP identity / Clinician profile (یک به‌ازای user؛ `clinic_id` = «کلینیکِ خانه»، نه مرز مجوز) / Membership+clinician_locations (M:N واقعی). کار Phase 2 = Query-level (فهرست پزشکان کلینیک از Membership)، نه شکستن ایندکس.
- **§۶ SMS:** tenant-owned عملیاتی (PII + زمینهٔ بالینی در متن/vars_json) — FK به clinics **توجیه معنایی دارد** (پس از INT→BIGINT + حذف DEFAULT 1 در M-08b)؛ location_id لازم نیست؛ نبود retention = تصمیم باز مالک.
- **§۷ Migration:** د-۶ (ماتریس ۲۶ جدول + M-08b جدید + M-09=۲۲ FK + تصمیم سه UNIQUE با test plan پنج‌بندی + ترتیب کامل) — versioned-forward، drop/recreate ممنوع، CI upgrade-path الزامی.
- **§۸ تست‌پلن:** ۱۵ دسته (از جمله 🆕 tripwire معماری شمارش hardcode با snapshot=۴۹؛ ممنوعیت mock isolation — fixture ردیف واقعی دوم).
- **§۹ مرز 1B/3:** از ۱۷ قلم مجوزی (B-01..B-17): Phase 2 = ScopeContext + فیلتر Repository + schema (B-02..B-06, B-08, B-09/10-مشروط, B-11, B-14, B-16, B-17)؛ Phase 3 = enforce سیاستی/ماتریس/Break-Glass (B-01-enforce, B-07, B-12-سیاست, B-15-جریان). Phase 2 به Role Management تبدیل نمی‌شود.
- **گیت‌های fix:** CI `34286105777` سبز (Integration با تست جدید) · Closure `34286100991` سبز · Real-WP/Pilot — نتیجه در `final-pre-phase2-gate-report.md` بند ۱۵.
- **رأی:** ⏳ NOT READY تا (۱) سبزی هر ۵ گیت `bbc1e83` و (۲) تصویب مالک بر «کلینیکِ خانه/UNIQUE» و «Source of Truth زمانی». جزئیات: `docs/phase-reports/final-pre-phase2-gate-report.md` (۱۷ بند).
- **STOP طبق دستور** — بدون merge/tag/تغییر main؛ PR #10 دست‌نخورده؛ untrackedهای محافظت‌شده لمس نشد.

### [2026-09-12 ~08:40 UTC] — ایجنت Arena (شاخهٔ `arena/01a094c5-doctor`) — بازیابیِ تداومِ تمیز + چک‌پوینتِ مستندات/طراحی (فقط مستندات) — STOP
- **چرا این نشست:** نشستِ قبلی به شاخهٔ `arena/01a092ab-doctor` قفل بود و آن شاخه از طریق merge `68fbd405` ancestry نامطلوبِ `30435673`/`151d7e1` را وارد تاریخچه کرده بود. این نشست **از همان topology ادامه نداد**؛ شاخهٔ تازه مستقیماً از `origin/main` = `bdb135e9b3eff9db9fbe104c33bc6c30850c5263` شروع شد (working tree تمیز؛ بدون untracked).
- **راستی‌آزماییِ پیش از نوشتن:** `origin/main` هنوز **دقیقاً** `bdb135e9` (پیش‌روی نکرده بود) · PR باز = فقط **#13 (OPEN + DRAFT، base `arena/01a086ca-doctor`)** که **دست‌نخورده** ماند · هیچ PR معادلی برای این محتوا وجود نداشت · آخرین migration = **`0020`** (`2026_09_09_0020_idempotency_clinic_scope.php`) · کامیتِ مصوبِ مستندات `4c1ab657` با `git fetch origin <sha>` گرفته و بررسی شد: **والد = دقیقاً `bdb135e9`**، **دقیقاً ۸ فایل و همگی مستندات** (CHANGELOG.md · docs/README.md · ADR-0031 · project-phase-taxonomy.md · c10-performance-evidence.md · phase2-state.md · project-current-state.md · roadmap.md)، **بدون کد/تست/workflow/schema/migration**.
- **کامیت ۱ (`84a3975`) — انتقالِ تمیز:** `git cherry-pick -x 4c1ab657` بدون هیچ conflict؛ **tree یکسان** با `4c1ab657` (هر دو `f3c687f6`) ⇒ محتوای **C10 = CLOSED (تصمیم صریح مالک 2026-09-12، فقط دامنهٔ محدودشدهٔ بازبینی شواهد؛ بدون ادعای NFR/بار/مقیاس‌پذیری/آمادگی تجاری/Phase 17/بستن Phase 2)** و **ADR-0031 §۸ / AD-17 (یک هسته، سه توپولوژی استقرار)** عیناً منتقل شد. **`68fbd405`/`30435673`/`151d7e1` وارد ancestry نشدند** (با `git merge-base --is-ancestor` برای هر سه بررسی شد)؛ شاخهٔ قدیم merge نشد؛ تاریخچه بازنویسی نشد.
- **کامیت ۲ (`49ed1c3`) — جهتِ طراحیِ مصوبِ Phase 2 (پیاده‌سازی‌نشده):** سند کانونیِ جدید **`docs/architecture/phase2-tenant-context-remediation-design.md`** با برچسبِ صریح **APPROVED DESIGN DIRECTION — NOT YET IMPLEMENTED**: (A) tenant contextِ صریح/اعتبارسنجی‌شده/ایزولهٔ per-job برای Jobهای پس‌زمینه (Organization مشتق از Clinic و بدون تکرارِ بی‌توجیه؛ Location معمولاً مشتق از آبجکت عملیاتیِ مرجع و **نه** افزودن `location_id` به هر Job؛ برقراری توسط Dispatcher؛ پاک‌سازی در `finally`؛ بدون وابستگی به WP کاربرِ جاری/REST؛ `A → B → A` بدون نشتِ Settings؛ حفظ scope در retry؛ fail-closed برای scope مخدوش و نوعِ ناشناخته؛ ممنوعیتِ حدسِ مالکیت) + طبقه‌بندیِ الزامیِ انواع (T/S/W) · (B) **بدون allowlist نهاییِ system-wide**؛ `backup.run`/`license.refresh`/`cleanup.oplog` **عمداً UNRESOLVED**؛ **ممنوعیت مطلقِ `clinic_id = 0` و Clinic مصنوعی** · (C) timezone عملیاتی = **Location** (چند Location با timezone متفاوت؛ Clinic timezone هرگز override نمی‌کند؛ fail-closed؛ `Asia/Tehran` فقط مقدار سازگاریِ کنترل‌شده؛ Clinic timezone = legacy/default تا تصمیم deprecation) · (D) رزولوشنِ پیکربندیِ SMSِ Clinic B مستقل از کاربر/context/Job قبلی/ترتیب اجرا/instanceِ Clinic دیگر، با ترجیحِ انتزاعِ `SettingsFactory`/`SmsConfigResolver` · (E) انتسابِ tenant در لاگِ عملیاتی + **ستون `clinic_id` به‌تنهایی authorization نیست** + **بدون ادعای نشتیِ اثبات‌نشده** (راستی‌آزمایی: `cpms_operational_logs` امروز **هیچ خواننده/UI/API‌ای ندارد**) · (F) قواعدِ backfillِ Jobهای legacy (فقط provenance قطعی؛ «یک Clinic بودنِ نصب» کافی نیست؛ مبهم‌های pending/processing ⇒ fail-closed؛ بدون Clinic ID حدسی؛ idempotent/recoverable) · **§۸ = ۶ سوالِ بازِ «پیش از Migration `0021`» (همه OPEN)** *(🔴 تاریخی — روایتِ همان کامیت؛ اکنون **هیچ شمارهٔ migration آینده‌ای رزرو نیست** و §۸ بر پایهٔ تصمیماتِ مالک 2026-09-12 به‌روز شد)* · **§۹ = مشخصاتِ ۱۲ تستِ RED ‏(RT-1..RT-12) فقط ثبت شد — هیچ تستی نوشته/اجرا نشد و همهٔ تست‌های موجود حفظ شدند** *(🔴 تاریخی — اکنون **RT-1..RT-14**؛ ورودیِ بعدیِ همین §۱۰ را ببینید)*. لینک‌ها: `docs/README.md` (جدول مراجع + نقشهٔ اسناد؛ و نشانه‌گذاریِ سطرِ کهنهٔ «2026-09-11 / C10 = NOT STARTED» به‌عنوان superseded — **بدون حذفِ محتوا**)، `background-jobs.md` §۵، `phase2-state.md` (اشاره در اقلامِ به‌تعویق‌افتادهٔ Jobs/SMS/timezone)، `project-current-state.md` (Linked canonical docs)، `CHANGELOG.md`.
- **کامیت ۳ (`c575aec`) — آشتی‌دهیِ «۵۴»:** **`۵۴` = سرشماریِ تاریخیِ Phase 0** و **بدون بازنویسی** حفظ شد؛ **وضعیتِ جاریِ راستی‌آزمایی‌شده:** نقضِ tenant-default در runtime فعال = **۰** · Tripwire `{"files_scanned": 173, "hardcodes": 0, "suspects": 1, "allowlist_entries": 0}` + ۵۹/۵۹ self-test · allowlist = **`[]`** · تنها suspect = `SystemClinicResolver.php:52` (sanction‌شده/fail-closed) · الگوی grepِ تاریخی امروز ۷ برخوردِ **متنی/کامنتی** (+۱ عبارت داخل Migrationِ اجراشدهٔ `0020`) دارد و **۰** tenant-defaultِ runtime · هر ۳ Default-Parameter حذف شده‌اند و `DEFAULT 1` سطحِ schema از هر ۳ جدول با Migration `0016` برداشته شد. محلِ ثبت: **§D‑1 جدید در `project-current-state.md`** (جدولِ شاهد + عبارتِ کانونیِ مجاز) + یادداشتِ «وضعیتِ جاری» در `phase0.5-target-model.md` §الف‑۶ (اعداد/جداول/فرمان‌های تاریخی دست‌نخورده) + تصریحِ یک‌خطیِ AD-13 در همین راهنما.
- **پاکسازیِ کوچکِ آینده (ثبت شد، انجام نشد):** docblockِ `src/Infrastructure/Repository/ClinicianRepository.php:17` («همه کوئری‌ها clinic_id=1») **راستی‌آزماییِ مستقل: کهنه** — کلاس `int $clinic_id` صریح می‌گیرد (`listAll():30`، `create():76`) و literalِ tenant ندارد. چون مأموریت **فقط‌مستندات** بود، **هیچ فایل PHP‌ای تغییر نکرد**؛ این قلم cleanupِ فقط‑کامنتیِ آینده است.
- **اعتبارسنجیِ محلیِ این نشست:** Tripwire (scan + ۵۹ self-test) ✅ · بررسیِ لینک‌های نسبیِ Markdown روی فایل‌های تغییرکرده ✅ (تنها لینکِ شکستهٔ گزارش‌شده = `project-current-state.md:398 → phase2-state.md` که **پیش‌ازاین نشست هم وجود داشت** و عمداً دست نخورد) · بدون تغییر در `.github/workflows`، `phpstan.neon.dist`، `phpcs.xml.dist`، `phpunit.xml` یا هر ابزار/تستی. **اجرای واقعیِ PHPUnit/PHPStan/WPCS فقط در GitHub Actions** (sandbox فاقد PHP) — run IDها در PR ثبت می‌شود.
- **ممنوع‌های رعایت‌شده:** بدون کد محصول · بدون تست · بدون تغییر workflow · بدون schema · **بدون Migration `0021`** · بدون تغییر `main` · بدون merge · بدون force-push · بدون `reset --hard` · بدون بازنویسی تاریخچه · بدون tag/release/version-bump · **End Gate فاز ۲ شروع/تعریف/پاس نشد** · Phase 3 / Phase 17 همچنان **NOT STARTED** · Phase 2 = **IN PROGRESS** · PR #13 دست‌نخورده · شاخهٔ قدیمیِ Arena بسته/حذف نشد.
- **STOP طبق دستور** — پس از pushِ شاخه و بازکردنِ **یک Draft PR** به `main` (بدون merge)، منتظر تصمیم مالک: بستنِ ۶ سوالِ §۸ پیش از هر Migration، و تصویبِ جداگانهٔ هر پیاده‌سازی.

### [2026-09-12 ~12:10 UTC] — ایجنت Arena (شاخهٔ `arena/01a094c5-doctor`، PR #26 DRAFT) — بازبینیِ مستقلِ معماری + اصلاحاتِ مستنداتیِ C-1..C-9 (فقط مستندات) — STOP
- **چرا این نشست:** بازبینیِ **فقط‌خواندنیِ** Draft PR #26 و سندِ کانونیِ `docs/architecture/phase2-tenant-context-remediation-design.md` + حلِ شاهد-محورِ شش سوالِ §۸. **حکمِ مالک: `B — NEEDS SMALL DOCUMENTATION CORRECTIONS BEFORE ACCEPTANCE`** ⇒ اصلاحاتِ مستنداتی اعمال شد؛ **merge/Ready نشد**.
- **راستی‌آزماییِ پیش از نوشتن:** `origin/main` = **`bdb135e9`** (بدون پیش‌روی) · headِ PR #26 = **`0670216`** (بدون تغییرِ غیرمنتظره) · `mergeable_state=clean` · **۲۱/۲۱ check success** روی همان SHA · PR #13 = OPEN + DRAFT (دست‌نخورده) · آخرین migration = **`0020`** (‏`0021` وجود ندارد) · working tree تمیز.
- **یافتهٔ اصلی (must-fix = C-1):** گزارهٔ §۵-D-2 («Secret/API key هرگز در `cpms_settings` ذخیره نمی‌شود») **خلافِ کد و خلافِ `ADR-0025:44-45`** بود. واقعیتِ راستی‌آزمایی‌شده: credentialِ SMS **per-Clinic** در `cpms_settings.sms.auth` به‌صورت **sealed (AES-256-GCM)** ذخیره می‌شود (`SmsService.php:348`, `:646-660`, `:667+`؛ `CredentialVault.php:7-15`) و **plaintext** هرگز ذخیره/نمایش/لاگ نمی‌شود (`Settings.php:145-147`, `:239-243`). **کلیدِ Vault سطحِ نصب است** ⇒ **رمزنگاری به‌خودیِ خود مرزِ tenant نیست**؛ ایزولاسیون فقط با **resolveِ درستِ Clinic پیش از خواندن/رمزگشایی** برقرار می‌شود.
- **سایر اصلاحات:** **C-2** ترتیبِ ردیف‌های §۱ (‏۱۳/۱۴) · **C-3** مسیرهای wp-admin برای ست‌شدنِ `ScopeContext` (`ClinicianAdminPage.php:413/456/491/509`) · **C-4** ثبتِ سابقهٔ کاریِ `report.export` به‌عنوانِ ردیفِ ۱۵ §۱ (persistِ `clinic_id` در `payload_json` + اعتبارسنجیِ fail-closed + bind + restore در `finally`) · **C-5** تصحیحِ طبقهٔ `appt.reminder` از T به **W** (جاروی سراسری با انتسابِ per-row؛ **بدونِ ادعای sharding**) · **C-6** ثبتِ منبعِ سومِ timezone (`setup.clinic.timezone`) و **قطعِ اتصالِ راستی‌آزمایی‌شده** با `clinicTimezone()` (که فقط `cpms_clinics.timezone` را می‌خواند و هیچ نویسندهٔ production‌ای ندارد) · **C-7** افزودنِ **C-9/C-10** (`slots.generate` = `gmdate('Y-m-d')`ِ UTC؛ `visits.no_show` = cutoffِ UTC روی مقادیرِ محلیِ `slot_date/slot_time`) · **C-8** افزودنِ **E-7** (ایندکسِ موجود `(level, created_at)` به‌دلیلِ leftmost-prefix، `DELETE`ِ فقط-`created_at` را پوشش نمی‌دهد؛ حذفِ امروز **بی‌کران**) · **C-9** تعیینِ **منبعِ حقیقتِ registry = `App::dispatcher()`/`RECURRING_JOBS`** (‏۱۵ نوع) و ثبتِ driftِ `background-jobs.md`.
- **§A-3 جدید:** جدولِ طبقه‌بندیِ **T/S/W** برای **۱۵ نوعِ واقعیِ ثبت‌شده** با تفکیکِ اجباریِ «رفتارِ جاریِ runtime» از «طبقهٔ هدف»، ستون‌های وابستگیِ Settings / دسترسیِ tenant-sensitive / لزومِ `clinic_id` / مجاز بودنِ `NULL`. طبقهٔ `backup.run` و `license.refresh` **مشروط به §۸-۱** ثبت شد. **`NULL` هرگز خودکار «system» نیست.**
- **§۸ بر پایهٔ تصمیماتِ مالک:** ۸-۱ 🟢 **جهتِ مصوب** = انتزاعِ اختصاصی روی **WordPress Options** با ترجیحِ `autoload=no` (سابقهٔ بومی: `cpms_otp_pepper` با `add_option(..., '', 'no')`) — **بدونِ انتقالِ فله‌ای بر پایهٔ prefix**، **بدونِ جابه‌جاییِ وضعیتِ ساخت‌یافتهٔ license** (جدول‌های `0008`)، **فهرستِ کلیدها OPEN** · ۸-۲ ✅ **installation-wide** (رفتارِ قابلِ اجرا + شاهدِ تاریخیِ `settings-reference.md:5`/`agent-guide.md:658`) **با مرزِ ضدِتعمیم** (سایرِ کلیدهای retention/purge جداگانه تصمیم می‌گیرند) · ۸-۳ 🟢 **مدلِ طراحیِ مصوب** · ۸-۴ ⛔ **schema/migration NOT AUTHORIZED** (فقط جهت؛ **راهبردِ «NULLِ دو-دوره‌ای» کانونی/نهایی نشد**) · ۸-۵ ⏳ **OPEN — NOT MEASURED** (نمونهٔ پرس‌وجو با `$db->table()`، چون prefixِ وردپرس محیط‌محور است) · ۸-۶ ✅ **resolved برای انواعِ جاری** (snapshotِ `location_id` مسیرِ تغییر ندارد؛ reschedule ردیفِ جدید می‌سازد؛ `cpms_locations` نویسندهٔ production ندارد) + **زنجیرهٔ اشتقاقِ Location برای آبجکتِ بدونِ `location_id` همچنان OPEN**.
- **تست‌ها:** RT-1..RT-12 **حفظ** و با شاهد تدقیق شدند (RT-2 تفکیکِ تاریخِ محلی از quiet hours · RT-3 سنجشِ **هویتِ credential** نه فقط provider ID · RT-4/RT-14 fail-closedِ per-job و ایزولاسیونِ blast radius · RT-6 پوششِ `App::$settings/$dispatcher/$smsService/providers` · RT-7 authoritative Location · RT-8 بدونِ side-effectِ جزئی · RT-9 شاملِ `slots.generate`/`visits.no_show` · RT-10 evidence-driven · RT-11 تفکیکِ «سمتِ نوشتنِ امروز» از «خوانندهٔ آینده» · RT-12 روی ۱۵ نوعِ واقعی) + **RT-13** (پیکربندیِ سطحِ نصب بدونِ contextِ Clinic و بدونِ `clinic_id=0`/Clinic جعلی) و **RT-14** (ایزولاسیونِ شکستِ per-job درونِ tick) افزوده شد. **RT-15 اضافه نشد** (بدونِ شاهدِ semanticِ جداگانه). **هیچ تستی نوشته/اجرا/ضعیف نشد.**
- **drift:** `docs/drift-register.md` **بخش ۹** اضافه شد (‏۹-A تناقضِ داخلیِ `settings-reference.md` دربارهٔ Secretِ SMS · ۹-B جدولِ §۲ `background-jobs.md` در برابرِ registry واقعی · ۹-C docblockِ کهنهٔ `ClinicianRepository.php:17`) — **بدونِ تغییرِ هیچ فایلِ PHP** و بدونِ بازنویسیِ اسنادِ تاریخی.
- **ممنوع‌های رعایت‌شده:** بدون کد محصول · بدون تست · بدون تغییر workflow/CI · بدون schema · **بدونِ هر migration** · **بدونِ رزروِ شمارهٔ migration آینده** (هر عبارتِ ارجاع‌دهنده به شمارهٔ migrationِ بعدی، به «پیش از هر migration آینده» تبدیل شد) · بدون ادعای نشتِ `operational_logs` (هیچ خوانندهٔ production‌ای ندارد) · بدون `clinic_id=0` / Clinic مصنوعی / حدسِ مالکیتِ tenant · بدون ادعای NFR/مقیاس‌پذیری · بدون merge/Ready/force-push/tag/release/version-bump · PR #13 دست‌نخورده · End Gate فاز ۲ شروع/پاس نشد · Phase 3 / Phase 17 = NOT STARTED · Phase 2 = IN PROGRESS · C10 = CLOSED (با دامنهٔ محدودشده).
- **اعتبارسنجیِ این نشست:** Tripwire (`hardcodes: 0`, `allowlist_entries: 0`, `suspects: 1` = `SystemClinicResolver.php:52`) + **۵۹ self-test سبز** ✅ · بررسیِ لینک‌های نسبیِ Markdown ✅ · diff = **فقط docs + CHANGELOG** ✅ · CI پس از push در گزارشِ نهایی ثبت شد.
- **STOP طبق دستور** — پس از pushِ همان شاخه و DRAFT باقی‌ماندنِ PR #26؛ منتظر تصمیم مالک برای پذیرش/ادغام و برای بستنِ مواردِ OPEN باقی‌مانده (فهرستِ کلیدهای سطحِ نصب، جزئیاتِ schema/ایندکسِ لاگ عملیاتی، اندازه‌گیریِ دادهٔ legacy، زنجیرهٔ اشتقاقِ Location برای آبجکت‌های بدونِ `location_id`).

### [2026-09-13 09:40 UTC] — ایجنت Arena — T3 GREEN implementation slice (PR #28؛ اعتبارسنجی CI در انتظار)
- **فاز/محدوده:** فقط Phase 2 / T3 برای `appt.reminder`؛ PR #28 عمداً **OPEN + DRAFT** باقی می‌ماند. Phase 3 شروع نشده و هیچ merge/Ready/force-push/reset/migration انجام نشده است.
- **مبنای راستی‌آزمایی‌شده پیش از نوشتن:** `origin/main=a93d20ab1dfd81d2873ea345918e950f17a944d9`؛ PR #28 و شاخهٔ `arena/01a09667-doctor` در `6081c7392a0062e923b9036fcf31ad302ef96c65` و tree محلی clean بود. RED تاریخی مورد قبول: CI `34747206969` روی همان SHA.
- **اقدامات:** `ApptReminderHandler` به W-sweep محدود با join ساختاری Appointment→Location، زمان‌مرجع UTC ثابت، پنجرهٔ محافظه‌کارانهٔ تاریخ، eligibility نهایی per-Location، keyset cursor و continuation پایدار در `cpms_jobs.payload_json` تبدیل شد. payload continuation فقط `{continuation:true, version:1, reference_utc, cursor:{slot_date,slot_time,id}}` است و malformed payload با `JOB_PAYLOAD_INVALID` fail-closed می‌شود. `App::dispatcher()` فقط DI scope-neutral جدید را تزریق می‌کند؛ `JobQueue`/`JobsDispatcher`/`SmsService`/quiet-hours تغییر نکردند. تست‌های T3 برای mismatch، payload، fixed reference، cursor، prefix ردشدنی، continuation یکتا و exhaustion افزوده شد؛ مستند `background-jobs.md` نیز قرارداد و proof پنجره را ثبت می‌کند.
- **اعتبارسنجی محلی:** `git diff --check` موفق؛ lint WASM PHP 8.2 برای ۴ فایل PHP تغییرکرده موفق. PHPUnit/Integration/PHPStan/WPCS/Tripwire/Real-WP/Closure/Pilot در sandbox اجرا نشده‌اند؛ شواهد دقیق پس از commit/push و CI ثبت می‌شود.
- **کامیت‌ها:** هنوز ساخته نشده (در انتظار review نهایی diff و validation CI).
- **وضعیت tree:** dirty؛ فقط ۵ فایل tracked مرتبط با T3 تغییر کرده‌اند، به‌علاوه helper lint خارج از repo در `/home/ubuntu/doctor/lint-php.mjs` که باید قبل از commit حذف شود.

### [2026-09-16 21:48 UTC] — ایجنت Arena — Phase 4 Slice 3: WalkIn حرفه‌ای مشترک
- فاز/محدوده: فقط  در مسیر واقعی ؛ بدون تغییر migration/authorization/queue semantics.
- اقدامات: RED معتبر روی کامیت تست‌تنهای  و run  (یک شکست دقیق: 404  به‌علت استفاده از Clinic خانه A زیر Scope موثق B)؛ سپس Clinic عملیات از Scope موثق، مشارکت با ، مالکیت بیمار/Visit/settings/audit هم‌Clinic؛ ۶ تست پذیرش مثبت/منفی. fixture مستقیم Finance نیز Scope صریح موجود خود را برقرار می‌کند. CI evidence برای نمایش testcaseهای Slice 3 در JUnit افزوده شد.
- کامیت‌ها:  تست RED؛  کد+کنترل‌ها؛  Scope صریح fixture؛  شاهد اجرای testcaseها (شاخه ).
- CI:  روی  = SUCCESS؛ Integration دقیقاً ۸۷۸ تست/۱۰۵۴۴ assertion و suite جدید ۶ تست/۱۹۷ assertion، بدون failure/error/skip. run میانی  صریحاً FAILURE بود (۸ fixture مالی بدون Scope) و با  اصلاح شد؛ سبزی آن run ادعا نمی‌شود.
- تصمیمات درون‌فازی: REST همیشه Scope صریح RestClinicContext را مصرف می‌کند؛ no-scope چندکلینیکی fail-closed؛ Clinic خانه فقط داده سازگاری و نه tenant عملیات REST؛ auto-enqueue و Audit از Clinic Visit/Scope B.
- موارد باز/تحویل: تست Integration محلی به‌علت نبود PHP/WP/MySQL اجرا نشد؛ lint سه فایل PHP با PHP-WASM و Tripwire ۵۹/۵۹ محلی سبز بود. وضعیت نهایی Gateهای exact-head پس از commit این لاگ باید از GitHub بازیابی شود.
- وضعیت tree: در زمان ثبت این ورودی، تغییر فقط همین لاگِ append-only است؛ پس از commit باید clean شود.

### [2026-09-16 21:49 UTC] — ایجنت Arena — تصحیح ورودی بلافاصلهٔ قبل (آسیب shell-expansion)
- **تصحیح صریح:** در ورودی بلافاصلهٔ قبل، backtickهای Markdown به‌علت heredoc بدون quote توسط shell اجرا شدند و چند شناسه/نام از متن حذف شد؛ آن متن ناقص را نباید شاهد دانست. دادهٔ صحیح و کامل این ورودی است.
- فاز/محدوده: فقط `VisitService::walkIn()` در مسیر واقعی `POST /clinic/v1/visits/walk-in`؛ بدون تغییر migration/authorization/queue semantics.
- اقدامات: RED معتبر روی کامیت تست‌تنهای `0bb8bad` و run `35153195839` (یک شکست دقیق: 404 `CLINIC_NOT_FOUND` به‌علت استفاده از Clinic خانه A زیر Scope موثق B)؛ سپس Clinic عملیات از Scope موثق، مشارکت با `MembershipRepository::clinician_participates_in()`، مالکیت بیمار/Visit/settings/audit هم‌Clinic؛ ۶ تست پذیرش مثبت/منفی. fixture مستقیم Finance نیز Scope صریح موجود خود را برقرار می‌کند. CI evidence برای نمایش testcaseهای Slice 3 در JUnit افزوده شد.
- کامیت‌ها: `0bb8bad` تست RED؛ `463a456` کد+کنترل‌ها؛ `f771b6c` Scope صریح fixture؛ `8e1dad4` شاهد اجرای testcaseها (شاخه `arena/01a0ac20-doctor`).
- CI: `35154085312` روی `8e1dad4` = SUCCESS؛ Integration دقیقاً ۸۷۸ تست/۱۰۵۴۴ assertion و suite جدید ۶ تست/۱۹۷ assertion، بدون failure/error/skip. run میانی `35153752130` صریحاً FAILURE بود (۸ fixture مالی بدون Scope) و با `f771b6c` اصلاح شد؛ سبزی آن run ادعا نمی‌شود.
- تصمیمات درون‌فازی: REST همیشه Scope صریح RestClinicContext را مصرف می‌کند؛ no-scope چندکلینیکی fail-closed؛ Clinic خانه فقط دادهٔ سازگاری و نه tenant عملیات REST؛ auto-enqueue و Audit از Clinic Visit/Scope B.
- موارد باز/تحویل: تست Integration محلی به‌علت نبود PHP/WP/MySQL اجرا نشد؛ lint فایل‌های PHP تغییرکرده با PHP-WASM و Tripwire ۵۹/۵۹ محلی سبز بود. وضعیت نهایی Gateهای exact-head پس از commit این لاگ باید از GitHub بازیابی شود.
- وضعیت tree: در زمان ثبت این ورودی، تغییر فقط لاگ append-only است؛ پس از commit باید clean شود.

### [2026-09-17 02:40 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0ac5a-doctor`) — Phase 4 Slice 4: حل clinician صف برای حرفه‌ای مشترک
- **فاز/محدوده:** فقط `VisitService::queueScopeClinicianId()` روی مسیرهای واقعی `GET /clinic/v1/doctor/today`، `GET /clinic/v1/queue` و `GET /clinic/v1/rt/queue`. Slice فقط خواندنی است؛ walkIn/checkIn/transition/checkout، AuthorizationService، Scope establishment، Booking، Schedule، migrationها، ServiceOffering و pricing دست‌نخورده‌اند. پارامتر مردهٔ `clinician_id` روی `/secretary/today` عمداً در این slice اصلاح نشد.
- **مبنای راستی‌آزمایی‌شده پیش از ویرایش:** `main = 0a2f31c61d8bba60ba5e4a134e527b3db8326f44` (هم local HEAD و هم `git ls-remote`)، PR باز = ۰، آخرین migration = `2026_09_09_0020_idempotency_clinic_scope` (بدون ساخت یا رزرو ۰۰۲۱).
- **VALID RED (شاهد واقعی):** کامیت تست‌تنهای `21f7c3d` روی run `35160194475` — `Tests: 891, Assertions: 11159, Failures: 3` و هر سه شکست فقط در suite جدید و دقیقاً روی قرارداد هدف: doctor = 200 با صف/آمار خالی (`Failed asserting that an array contains 180`)، staff = `Failed asserting that 404 is identical to 200` با کد `CLINIC_NOT_FOUND`، و rt/queue = فید خالی. ده کنترل منفی/همجوار در همان run سبز بودند (رفتار موجود حفظ شده).
- **دو تلاش ناموفقِ قبل از RED معتبر (گزارش صادقانه — این‌ها RED نبودند):**
  ① `a0a7038` / run `35159625216` = **شکست fixture** نه RED: `cpms_visits.location_id` با Migration 0013‏ NOT NULL + FK `fk_visit_location` است (Migration 0015 آن را nullable نکرد) ⇒ درج مستقیم Visit خطای foreign key داد. رفع در `f406e4d` (ساخت Location صریح برای هر Clinic).
  ② `f406e4d` / run `35159920036` = **باگ assertion خودِ تست** نه RED: آرگومان‌های `assertGreaterThan`/`assertLessThan` جابه‌جا (actual, expected) پاس شده بودند. رفع در `21f7c3d` بدون تغییر معنا.
- **تغییر محصول (`0d2abfa`):**
  - پریمیتیو جدید `MembershipRepository::active_clinician_id_for_wp_user(int $wp_user_id): ?int` — حل «هویت یکتای فعال» فقط با `wp_user_id` و **بدون فیلتر Clinic**؛ بدون `ORDER BY`/`LIMIT 1`/«ردیف اول»: یک پرس‌وجوی تجمعی (`COUNT(*)`, `MIN(id)`, `MAX(id)`) همیشه دقیقاً یک ردیف دارد و `COUNT(*)=1 AND MIN(id)=MAX(id)` یکتایی ساختاری `u_clinician_user` را باز-راستی‌آزمایی می‌کند.
  - **تفکیک شکست DB از نبودِ مشروع:** چون `COUNT(*)` همیشه ردیف می‌دهد، `fetchRow() === null` فقط یعنی خودِ پرس‌وجو شکست خورده ⇒ `RuntimeException` (کنوانسیون موجود همین Repository) ⇒ در REST به `CLINIC_INTERNAL_ERROR` (500) می‌رسد و هرگز «پزشک ندارد»/صف خالیِ بی‌صدا نمی‌شود. `n = 0` ⇒ نبودِ مشروع (بدون ردیف یا ردیف غیرفعال) ⇒ null.
  - `queueScopeClinicianId()`: doctor = حل هویت یکتای فعال → نبود ⇒ ۰ (رفتار ADR-0030) → `clinician_participates_in(identity, trustedClinic)` → false ⇒ ۰ → در غیر این صورت همان هویت. staff = بدون پارامتر ⇒ null (بدون تغییر)؛ با پارامتر ⇒ فقط اگر فعال و مشارکت‌کننده در Clinic مورد اعتماد، وگرنه همان 404 `CLINIC_NOT_FOUND`. از `MembershipRepository` موجود استفاده می‌شود و منطق عضویت تکرار نشده است.
- **برخورد واقعی با تست موجود (مهم):** run `35160454285` (پس از پچ محصول) سه RED را سبز کرد اما `ClinicTenantIsolationTest::testDoctorQueueScopeNeverUnionsClinics` را شکست. این یک تضاد قراردادیِ اجتناب‌ناپذیر است نه flake: آن تست یک پزشک با عضویت ACTIVE در هر دو Clinic A و B، یک هویت clinician (خانه A) و یک Visit در هر Clinic می‌سازد و انتظار «صف خالی زیر context موثق B» را داشت — با این توجیهِ درون‌کدی که «پروفایل پزشک در B وجود ندارد»، یعنی `clinicians.clinic_id` به‌عنوان مرز دامنه. دو fixture از نظر ساختاری یکسان‌اند، پس هیچ پیاده‌سازی نمی‌تواند هر دو را برآورده کند. انتظارِ کهنه در `82a421f` تصحیح شد: خاصیت ضد-union **در هر دو جهت** صریح asserts می‌شود (ویزیت B دیده می‌شود، ویزیت A دیده نمی‌شود، دقیقاً یک ردیف) و هیچ assertion حذف/سست نشد.
- **کنترل‌های منفی (همه در suite جدید و همه سبز روی main و پس از پچ):** پزشک بدون هویت ⇒ ۲۰۰ با صف/آمار خالی؛ هویت غیرفعال ⇒ همان‌طور؛ مشارکت suspended در B ⇒ REST موجود `403 CLINIC_SCOPE_UNAVAILABLE` و در سطح سرویس (با Scope صریح B) صف خالی؛ فیلتر کارکنی با clinician بدون مشارکت/معلق/ناموجود/غیرفعال ⇒ `404 CLINIC_NOT_FOUND`؛ منشی بدون فیلتر ⇒ کل صف Clinic B و هرگز Clinic A؛ بازیگر غیرمجاز در B ⇒ `403 CLINIC_PERMISSION_DENIED`؛ هویت یکتا و Clinic خانه دست‌نخورده + صفر جهش Visit؛ پزشکِ Clinic خانه در context A ⇒ فقط ویزیت‌های خودش (نه همکار).
- **مرز tenant صف (بازبینی کد):** `queueFor`/`statsFor`/`eventsSince`/`lastEventId` در `VisitRepository` بدون تغییر باقی ماندند و هر چهار همچنان `clinic_id = %d` بی‌قیدوشرط + clinician اختیاری دارند؛ tenant صف همچنان از `queueClinicId()`/`App::scope()` می‌آید و `clinician_id` فقط تنگ‌تر می‌کند.
- **اعتبارسنجی محلی:** PHP/Composer/MySQL در sandbox نصب نیستند و مخازن apt مسدودند ⇒ **Integration/Unit محلی NOT RUN**؛ پکیج‌های php-wasm هم باینری لازم را نداشتند. آنچه محلی اجرا شد: Tripwire (`59/59` self-test سبز، scan = `hardcodes: 0, suspects: 1` = همان suspect قدیمی `SystemClinicResolver.php:52`) و پارس‌شدنِ syntactic فایل‌های تغییرکرده با پارسر PHP (با کنترل منفی: فایل عمداً خراب = PARSE FAIL).
- **CI روی HEAD نهایی این slice (`82a421f48d6bcda5bb0f789ab2a6d3285c7be19f`):** run `35160862027` = SUCCESS؛ Integration ترمینال `OK (891 tests, 11179 assertions)` با `errors=0 warnings=0 failures=0 skipped=0`؛ suite جدید `Phase4Slice4QueueSharedProfessionalTest` = ۱۳ تست / ۶۳۳ assertion / صفر شکست؛ `Phase4Slice3WalkInSharedProfessionalTest` = ۶/۱۹۷ و suiteهای Phase 3 Slice 6 سبز. دانلود artifact لاگ/JUnit از sandbox مسدود است ⇒ شواهد per-test برای `ClinicTenantIsolationTest`/`DoctorQueueScopeTest` در دانهٔ تک‌تست **NOT RETRIEVED** (شاهد موجود: aggregate صفر شکست + ناپدیدشدنِ همان تک‌شکستِ run قبلی).
- **Git/PR:** فقط کامیت forward-only روی `arena/01a0ac5a-doctor`؛ PR **#62 DRAFT** (بدون Ready/merge/force-push/reset/rebase/tag/release و بدون تغییر main).
- **وضعیت tree:** در زمان ثبت این ورودی تغییر فقط همین لاگ append-only است؛ پس از commit باید clean شود. وضعیت Gateهای exact-head برای کامیتِ این لاگ پس از push از GitHub بازیابی می‌شود.

### [2026-09-17 07:08 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0acc1-doctor`، PR #63 DRAFT) — Phase4 Clinic Profile Canonicalization — FIX 3 ACCEPTANCE BLOCKERS — STOP
- **فاز/محدوده:** فقط PR #63 — Clinic Profile Canonicalization — سه مانع پذیرش (حذف فایل گزارش، تقویت تست updated_at، بازگردانی escaping تصادفی). بدون تغییر معماری ClinicProfileService / ClinicRepository / AuthorizationService / Location / timezone / FinanceService / Pricing / ServiceOffering / migrations. بدون 0021.
- **مبنای راستی‌آزمایی‌شده پیش از ویرایش:** `origin/main = b0f7f7015cd2b8a4674213a15fe17fe2ed5754c7` (هم local و هم `git ls-remote`)، PR #63 = OPEN + DRAFT، head = `f11025faaa10830538f4736c28904d6dcf0ad6ed` (مطابق expected)، base = `b0f7f7015cd2b8a4674213a15fe17fe2ed5754c7` (مطابق expected)، open PRs = فقط #63، working tree پس از `git checkout HEAD -- .` تمیز و منطبق بر head.
- **VALID RED (شاهد واقعی — صادقانه):** run `35167649955` و `35166751584` روی کامیت‌های اولیهٔ این شاخه — `Phase4ClinicProfileCanonicalizationRedTest::testWizardSaveShouldUpdateCanonicalAndDownstreamButCurrentlyDoesNot` با پیام `RED EVIDENCE: cpms_clinics(A).name should equal operator-entered value after wizard save, but remains old` — Expected `'کلینیک جدید A - RED TEST'` Actual `'کلینیک قدیمی A'` — اثبات می‌کرد wizard به `setup.clinic.*` می‌نوشت و `cpms_clinics` قدیمی می‌ماند. این RED معتبر مبنای GREEN شد.
- **تلاش‌های ناموفق میانی قبل از GREEN نهایی (گزارش صادقانه — این‌ها RED نبودند، بلکه شکست‌های fixture/test):**
  ① `ClinicScope::__construct()` private — `Call to private ClinicCore\Application\Scope\ClinicScope::__construct()` در `Phase4ClinicProfileTest::testNegativeD_SuspendedMembershipDenied` — رفع با `ClinicScope::forClinic()->withOrganization`.
  ② `updated_at` با دقت `.000` — همان ثانیه می‌ماند — `assertNotEquals` شکست — موقتاً به `notEmpty` تضعیف شده بود (ضعیف) — اکنون با blocker 2 به شکل قوی deterministic بازگردانده می‌شود.
  ③ audit COUNT — `cpms_audit_log` (مفرد) به‌جای `cpms_audit_logs` (جمع) و ستون `target_id` به‌جای `resource_id` — `null` برمی‌گشت — رفع با جدول جمع و `resource_id` و `?? '0'`.
  ④ `FinanceException: دسترسی لازم را ندارید` در `receipt()` — `cpms_manager` فاقد `INVOICE_READ` — نیاز به grant در `cpms_membership_capabilities` + WP cap `cpms_invoice_read` + `cpms_config` — رفع با `grantCapability` + `grantWpCap`.
  ⑤ `SetupWizardTest` — انتظارات قدیمی dual-write `setup.clinic.*` + timezone fallback — خلاف canonical — بازنویسی برای چک `cpms_clinics` canonical و حفظ تاریخی `setup.clinic.*`.
  ⑥ Static Analysis — `Anonymous function has an unused use $current/$createdAt/$actorUserId` در `ClinicProfileService.php:168` — رفع با حذف `use`های بلااستفاده.
  ⑦ `PHASE4_CLINIC_PROFILE_REPORT_FA.md` — فایل evidence تولیدشدهٔ بدون درخواست — باید حذف شود (blocker 1).
  ⑧ `renderHealth()` — `;font-weight:600;\"&gt;` (backslash اضافی) به‌جای `;font-weight:600;"&gt;` — رگرسیون تصادفی escaping (blocker 3).
- **تغییر محصول در این نشست (فقط سه قلم مجاز):**
  - **Blocker 1:** حذف `PHASE4_CLINIC_PROFILE_REPORT_FA.md` از PR (نه جابجایی، نه کپی، نه ساخت فایل گزارش دیگر). شواهد حذف: `git status` → `D PHASE4_CLINIC_PROFILE_REPORT_FA.md` و `git diff --stat` بدون آن فایل.
  - **Blocker 2:** تقویت تست `updated_at` — در `Phase4ClinicProfileTest::setUp()` مقدار اولیهٔ `cpms_clinics.updated_at` برای Clinic A/B به `'2020-01-01 00:00:00.000'` (clearly earlier valid timestamp) ست می‌شود — بدون `sleep` — سپس در `testHappyPathUpdatesCanonicalAndPreservesInvariants` مقدار `before.updated_at` چک می‌شود که دقیقاً `'2020-01-01 00:00:00.000'` است و `after.updated_at !== before.updated_at` و `notEmpty` — اثبات deterministic تغییر timestamp روی تغییر واقعی. سایر assertionها تضعیف نشدند. اگر تست محصول واقعی را نشان دهد، STOP قبل از تغییر product code (در این مورد محصول درست کار می‌کند — `nowUtcSql()` مقدار جدید می‌دهد).
  - **Blocker 3:** بازگردانی escaping در `src/Admin/CpmsSetupWizard.php::renderHealth()` — خط `echo '<span style=\"color:' . esc_attr($cls) . ';font-weight:600;\\\">'` (غلط) به `echo '<span style=\"color:' . esc_attr($cls) . ';font-weight:600;\">'` (درست) — مطابق base `origin/main:450` — `od -c` تأیید: `;   "   >   '` (بدون backslash). فقط همین یک خط، بدون تغییر UI اطراف.
- **معماری محصول بدون تغییر (تأیید):** `ClinicProfileService` — trusted `trustedClinicId` + CONFIG auth + atomic validation + preserve invariants + no-op + audit only on change + fail-closed — دست‌نخورده؛ `ClinicRepository::find/updateProfile` — دست‌نخورده؛ `AuthorizationService`/`Location`/`timezone`/`FinanceService`/Pricing/ServiceOffering دست‌نخورده؛ بدون migration جدید (آخرین `0020`).
- **کامیت‌های این شاخه (تا قبل از این لاگ):** `dfef387` (GREEN اولیه) → `c9dd91a` (fix private ctor/audit/updated_at/INVOICE_READ) → `caab63a` (fix audit plural + WP caps) → `f11025f` (افزودن گزارش فارسی — حذف در همین نشست) — این لاگ.
- **CI روی head قبلی `caab63a` (آخرین head سبز قبل از گزارش):** run `35169312043` = SUCCESS — `Integration (WP 6.7 + MySQL 8) pass 1m53s` + `Static Analysis (PHPStan) pass 44s` + Unit 8.1-8.4 + WPCS + Tripwire + Real WP Acceptance (wp_/clinic_) + Closure (5/5) + Upgrade path + Release Artifact + Responsive smoke — همه سبز، جز Staging Gate که در لحظهٔ چک pending بود. شواهد per-test برای `Phase4ClinicProfileTest`/`Phase4ClinicProfileCanonicalizationRedTest` در JUnit aggregate صفر failure داشت، اما دانلود لاگ خام از sandbox مسدود است ⇒ **per-test PASS برای تک‌تست updated_at در این run NOT RETRIEVED** (شاهد موجود: aggregate green + عدم وجود failure در کامنت PR برای آن SHA).
- **وضعیت exact-head جدید پس از این کامیت اصلاحی:** در زمان ثبت این ورودی، کامیت اصلاحی هنوز push نشده ⇒ **head جدید full SHA = pending/not-yet-known** و **exact-head check run IDs = pending/not-yet-known** — ادعای سبزی آینده نمی‌شود. پس از push، با `git rev-parse HEAD` و `gh pr checks 63` بازیابی می‌شود و در گزارش نهایی ثبت می‌شود. از ایجاد کامیت‌های حلقوی صرفاً برای به‌روزرسانی run ID خودِ همین کامیت لاگ خودداری شد.
- **اعتبارسنجی محلی:** PHP/Composer/MySQL در sandbox نصب نیستند ⇒ **Integration/Unit محلی NOT RUN**؛ آنچه محلی اجرا شد: `git diff --stat` — فقط سه فایل مجاز تغییر (حذف گزارش + `CpmsSetupWizard.php` یک خط + `Phase4ClinicProfileTest.php` تقویت updated_at) + همین لاگ append-only؛ `php -l` با WASM برای فایل‌های تغییرکرده **NOT RETRIEVED** (محیط فاقد PHP)؛ Tripwire محلی **NOT RUN**.
- **Git/PR:** فقط کامیت forward-only روی `arena/01a0acc1-doctor`؛ PR #63 همچنان **DRAFT** (بدون Ready/merge/close/delete branch/tag/release)؛ بدون `reset --hard`/`git clean`/`rebase`/`force-push`؛ push به همین شاخه.
- **وضعیت tree:** در زمان ثبت این ورودی، تغییر فقط همین لاگ append-only + سه اصلاح مجاز است؛ پس از commit باید clean شود. وضعیت Gateهای exact-head برای کامیتِ این لاگ پس از push از GitHub بازیابی می‌شود.

### [2026-09-17 07:26 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0acc1-doctor`، PR #63 DRAFT) — CORRECTION: VALID RED evidence for Clinic Profile — STOP
- **تصحیح صریح:** ورودی قبلی `[2026-09-17 07:08 Asia/Tehran]` در بخش VALID RED به‌صورت مبهم هر دو `35166751584` و `35167649955` را زیر VALID RED قرار داده بود — آن سطر نادقیق/مبهم بود و اصلاح می‌شود.
- **VALID RED فقط:** run `35166751584`، commit `1c94e65` (test-only commit) — canonical Clinic profile پس از مسیر موفق موجود Wizard قدیمی stale ماند — شاهد واقعی RED.
- **NOT VALID RED:** run `35167649955`، commit `dfef387` — این یک اجرای failing پس از GREEN اولیه بود (2 errors + 6 failures) با نقص‌های fixture/test شامل private `ClinicScope` constructor، timestamp same-second assertion، و انتظارات قدیمی `SetupWizardTest` — RED معتبر نیست.
- **دامنهٔ این تصحیح:** فقط همین گزارهٔ شواهدی را supersede می‌کند؛ سایر حقایق ثبت‌شدهٔ قبلی بدون تغییر باقی می‌مانند مگر اینکه مستقلاً نقض شوند.
- **عدم افزودن شواهد گمانه‌ای:** هیچ run ID دقیق برای head جدید فعلی قبل از وجود واقعی اضافه نمی‌شود.
- **Git/PR:** بدون تغییر کد محصول/تست/workflow/migration — فقط همین لاگ append-only.

### [2026-09-17 08:32 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0adaf-doctor`، PR #64 DRAFT) — تنها blocker: attach کردن WP user موجود به Clinic هدف — IMPLEMENTED / EVIDENCE-CLOSED PENDING FINAL LOG HEAD
- **دامنهٔ دقیق:** فقط مسیر Staff Management برای اینکه operator مجاز در Clinic B حساب WordPress موجود/حرفه‌ای را به Clinic B attach کند؛ بدون ساخت WP user یا clinician identity دوم. Schedule/Booking/WalkIn/Queue/AuthorizationService architecture/Location CRUD/pricing/geography/specialty/Patient Portal/migration دست‌نخورده ماندند؛ migration جدید وجود ندارد و آخرین migration همچنان `0020` است.
- **راستی‌آزماییِ پیش از کدنویسی:** `origin/main` و `main` روی `30c4eee3fe3ae33b3b18054d0f6de70fd71875a3` بودند؛ PR بازِ مرتبط پیش از شروع وجود نداشت. شروع ثبت‌شدهٔ نشست: `START_TEHRAN=2026-09-17 04:46:23 Asia/Tehran`.
- **RED معتبر:** test-only head `5ae1c6e06102863472ca0759062d3f7060601c4d`، run `35183271154`، aggregate `Tests: 906, Assertions: 11384, Failures: 1.`؛ شکست دقیق `Phase4ExistingStaffOnboardingTest::testClinicBStaffPathAttachesExistingUserWithoutCreatingIdentity`: خطای واقعیِ مسیر create، `Sorry, that username already exists!`، در برابر انتظار خطای خالی. این RED از missing-method/parse/setup failure نبود و blocker را در product/admin contract نشان داد.
- **پیاده‌سازیِ forward-only:** در `StaffManagementPage`، فرم nonceدار `mode=attach_existing` فقط شناسهٔ پایدار WP و نقش Clinic-scoped می‌گیرد؛ `save()` آن را parse می‌کند؛ `attachExistingUser()` authentication/current operator را کنترل، Clinic درخواست‌شده را از persistence با `authorizeStaffWrite()` و CONFIG/مجوز scoped دوباره authorize، role را به allowlist CPMS محدود، user nonexistent و administrator را fail-closed می‌کند، و فقط `MembershipService::create_membership()` را صدا می‌زند. هیچ `wp_insert_user()`، تغییر `u_clinician_user`، ساخت clinician، merge با mobile، تغییر home Clinic یا تغییر membership قبلی ندارد. active/suspended duplicate از همان service به conflict قطعی برمی‌گردد و silently reactivate/duplicate نمی‌شود؛ audit attach نیز ثبت می‌شود.
- **تست‌های اضافه‌شده:** `tests/Integration/Phase4ExistingStaffOnboardingTest.php` مسیر واقعی `StaffManagementPage::upsertUser()` با `mode=attach_existing` را پوشش می‌دهد: حفظ WP identity/role/capability، حفظ membership و clinician/home-Clinic قبلی، دقیقاً یک identity، Clinic-A-only authorization، no Clinic authorization، target raw/zero/fixed Clinic rejection، nonexistent WP user، active duplicate، suspended duplicate و administrator؛ نقش disallowed نیز در مسیر negative fixture کنترل می‌شود. تست‌ها فقط زیرساخت موجود را مصرف می‌کنند و migration/CI تغییر نکرده است.
- **اصلاح حین اعتبارسنجی:** اجرای نخست روی `0480b80` در testِ authorization موجود به‌علت نمایش global user choices، markerهای Clinic B را در renderِ Clinic A نشت می‌داد؛ این production leak با commit `f6ac779` حذف شد: UI اکنون stable WP ID را می‌گیرد و فهرست global user/name/email تولید نمی‌کند. PHPStan annotation همان run نیز یک import missing برای `MembershipException` را نشان داد و در همان fix اضافه شد. این دو مورد fixture failure نبودند؛ اصلاح محصول بودند.
- **GREEN دقیقِ قابل‌بازیابی برای pre-log head:** commit `f6ac779e205dac724e99277256d5ee26566bf05b` روی شاخه push شد. CI run `35183825260` = **SUCCESS**: terminal `OK (912 tests, 11507 assertions)`، JUnit root `tests=912, assertions=11507, errors=0, warnings=0, failures=0, skipped=0`; PHPStan، WPCS، Tenant Tripwire و Unitهای PHP 8.1/8.2/8.3/8.4 نیز pass شدند. Closure Gate run `35183822682` = success (runtime PHP 8.1/8.3/8.4، compat WP، restoreApply همگی pass)؛ Real WP Acceptance run `35183825249` = success برای prefixهای `clinic_` و `wp_`؛ Pilot/Staging Gate run `35183822616` = success (Release Artifact، Upgrade path، Staging Gate و Responsive smoke).
- **مرز شواهد:** artifact خام JUnit/log از sandbox با `EOF` قابل download/retrieve نبود و completion comment suite جدید `Phase4ExistingStaffOnboardingTest` را به‌صورت per-suite چاپ نکرد؛ بنابراین **per-test/per-suite PASS برای این class = NOT RETRIEVED** و از aggregate green استنتاج نمی‌شود. فقط aggregate GREEN بالا و تغییر count (`906` RED → `912` GREEN) ثبت شده است. اجرای PHPUnit/PHPStan/WPCS محلی **NOT RUN** است چون PHP/Composer/MySQL در sandbox موجود نیستند؛ `git diff --check` محلی VERIFIED است.
- **Git/PR/Gates:** دو commit forward-only این نشست `0480b80` و `f6ac779` هستند؛ فقط به `arena/01a0adaf-doctor` push شده‌اند. PR #64 همچنان **OPEN + DRAFT** و **بدون Ready/merge/close/delete/force-push/rebase/reset/clean/tag/release**؛ `main` تغییر نکرده است. پس از این append-only log، exact head و check runهای نهایی هنوز **PENDING / NOT RETRIEVED** هستند و نباید با شواهد pre-log اشتباه شوند.

### [2026-09-17 09:20 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0adaf-doctor`، PR #64 DRAFT) — FIX ONLY: enumeration parity for existing-user attach
- **پیش‌شرط زنده:** PR #64 همچنان OPEN + DRAFT، base/main=`30c4eee3fe3ae33b3b18054d0f6de70fd71875a3` و head پیش از این blocker=`64b88db6bf6e95742821156fe5bf7aabd8c8a495` بود. local checkout ابتدا روی base با همان محتوای دقیقِ head پراکنده/unstaged بود؛ با stash موقتِ non-destructive و fast-forward امن به همان head همگام شد؛ بدون reset/clean/rebase/force-push. main در این نشست تغییر نکرده است.
- **Focused RED معتبر:** commit test-only=`1e8a1b899a84911a5b3de9f69cb5869fa20a1fea`، CI run=`35187126304`، integration=`Tests: 913, Assertions: 11525, Failures: 1.`؛ failure دقیق در `Phase4ExistingStaffOnboardingTest::testAuthorizedTargetUserRejectionsAreEnumerationNeutralAndUnauthorizedLookupStaysScoped`: برای nonexistent پیام `کاربر WordPress موجود یافت نشد.` و برای administrator پیام `حساب administrator از مسیر مدیریت پرسنل قابل افزودن نیست.` بود. این شکست regression واقعیِ parity است، نه setup/parse failure؛ DB warningهای انتهای log علت failure این تست نبودند.
- **حداقل fix:** فقط `StaffManagementPage::attachExistingUser()` تغییر کرد؛ شاخهٔ nonexistent و administrator یک rejection خنثی و یکسان می‌دهند: `حساب انتخاب‌شده برای افزودن به این کلینیک قابل استفاده نیست.` Authorization قبل از `get_userdata()`، MembershipService، success path، duplicate/suspended semantics، role mutation، identity و audit architecture تغییر نکردند.
- **تست focused:** همان test اکنون equality پیام و شکل خروجی (`error/generated/user_id`)، zero membership mutation، حفظ global administrator role، و authorization-before-target-lookup برای nonexistent و administrator را assert می‌کند؛ negativeهای قبلی تضعیف نشدند.
- **Audit correction:** هیچ شواهد مستقیمی در این نشست retrieve نشد که `cpms_audit_logs.clinic_id` برای attach برابر Clinic B باشد. فقط وجود `clinic_id` در metadata ثبت‌شدهٔ مسیر قبلی شناخته شده است؛ ستون ممکن است به‌دلیل اتکای logger موجود به `App::scope()` مقدار `NULL` داشته باشد. این موضوع blocker این task نیست و audit architecture عمداً تغییر نکرد.
- **کامیت کد:** `6b8aa86e98c5ca26a0be2e3a54043a2b59449027` (`fix: neutralize existing-user enumeration`) روی همان branch push شد. پس از append این entry، head/checkهای نهایی هنوز pending هستند و نباید ادعای GREEN pre-log ساخته شود.
- **اعتبارسنجی محلی:** `git diff --check` سبز؛ PHPUnit/PHPStan/WPCS محلی **NOT RUN** چون PHP/Composer/MySQL در sandbox موجود نیستند. per-test artifact خام از sandbox در این مرحله **NOT RETRIEVED**؛ شواهد RED بالا از PR completion comment مستقیماً بازیابی شد.
- **محدوده:** فقط `StaffManagementPage.php`، `Phase4ExistingStaffOnboardingTest.php` و همین entry append-only تغییر می‌کنند؛ بدون migration/0021، workflow، AuthorizationService، MembershipService، Location، Schedule، Booking، Visit، Queue، Pricing یا ServiceOffering.

### [2026-09-17 10:39 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0ae2c-doctor`، بدون PR) — PHASE4 «افزودن WP user موجود به Clinic دیگر»: STOP بدون ویرایش — کار از قبل در main موجود است
- **LIVE REVERIFY:** task این نشست انتظار داشت main روی baseline `30c4cee3fe3ae33b3b18054d0f6de70fd71875a3` (merge PR #63) باشد. راستی‌آزمایی زندهٔ GitHub API: main زنده = `ad5a2617ebc474fbb6d0e973ecd8cb036c60f85b` (merge PR #64 در 2026-09-17 06:03:03 UTC). main نسبت به baseline موردانتظار جلو رفته و delta آن دقیقاً همین slice درخواستی است ⇒ مطابق قاعدهٔ STOP خودِ task، بدون هیچ ویرایش کد/تست/workflow/migration متوقف شدم. PR باز: صفر (مطابق انتظار). آخرین migration: `2026_09_09_0020_idempotency_clinic_scope.php` — بدون 0021. شروع ثبت‌شدهٔ نشست: `START_TEHRAN=2026-09-17 10:32:45 Asia/Tehran`. `git status` پیش از این append: clean روی `ad5a261`.
- **کار موجود (بازیابی مستقیم):** PR #64 «Phase 4: attach existing WP staff to another Clinic» (MERGED) — فایل‌ها: `src/Admin/StaffManagementPage.php` (+107/−1)، `tests/Integration/Phase4ExistingStaffOnboardingTest.php` (+497 جدید)، `docs/agent-guide.md` (+21). کامیت‌های main بعد از baseline: `5ae1c6e` (test-only RED)، `0480b80` (feat)، `f6ac779` (fix: حذف نشت lookup کاربران cross-clinic)، `64b88db` (docs)، `1e8a1b8` (test-only RED enumeration parity)، `6b8aa86` (fix: پیام‌های rejection خنثی)، `47fe469` (docs، head نهایی PR).
- **مسیر محصول موجود (بدون تغییر در این نشست):** `admin_post_cpms_staff_save` → `StaffManagementPage::save()` (is_user_logged_in + check_admin_referer) → `upsertUser(mode=attach_existing)` → `attachExistingUser()`: کنترل actor جاری، `authorizeStaffWrite()` (TrustedClinicEstablisher + مجوز CONFIG/Scoped از persistence — clinic_id ارسالی فقط selector)، allowlist نقش CPMS، `get_userdata(existing_user_id)` (شناسهٔ پایدار WP؛ بدون merge موبایل؛ administrator رد می‌شود)، و تنها membership-write = `App::membership_service()->create_membership()`. active/suspended duplicate = conflict قطعی از همان service (بدون reactivate/duplicate خاموش). audit: `STAFF_USER_ATTACHED`.
- **پوشش تست موجود (۸ متد):** happy path (حفظ WP identity/role/capability، حفظ membership A و clinician/home-Clinic، دقیقاً یک identity) + negativeهای A–F: بدون مجوز Clinic، فقط-A نتواند B را تغییر دهد، WP user ناموجود، duplicate active، duplicate suspended، نقش غیرمجاز + enumeration parity. با کنترل‌های منفی task مطابقت دارد.
- **شواهد CI بازیابی‌شده (completion comments PR #64 + GitHub API؛ بدون اجرای محلی):** RED معتبر: run `35183271154` روی commit test-only `5ae1c6e06102863472ca0759062d3f7060601c4d` — `Tests: 906, Assertions: 11384, Failures: 1.` در `Phase4ExistingStaffOnboardingTest::testClinicBStaffPathAttachesExistingUserWithoutCreatingIdentity` با Expected `''` / Actual `'Sorry, that username already exists!'` — یعنی مسیر create واقعی موجود user را attach نکرد. GREEN نهایی pre-merge: run `35187343756` روی head `47fe469b1892d364835ce84b2693855bc03bbc6c` = success؛ terminal `OK (913 tests, 11541 assertions)`؛ JUnit root: `tests=913 assertions=11541 errors=0 warnings=0 failures=0 skipped=0`. پس از merge روی main `ad5a261`: run `35188168950` = success (Integration 1m46s + PHPStan + Unit 8.1/8.2/8.3/8.4 + WPCS + Tenant Tripwire) و تمام ۱۹ check-run روی کامیت merge = success.
- **مرز صداقت شواهد:** per-test/per-suite PASS برای `Phase4ExistingStaffOnboardingTest` در runهای GREEN = **NOT RETRIEVED** (completion comment فقط selection suiteهای دیگر را چاپ می‌کند؛ لاگ خام از sandbox قابل بازیابی نبود). aggregate green مبنای ادعای اجرای تک‌تست قرار نگرفت. اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox نیستند — با `which php composer mysql` راستی‌آزمایی شد).
- **یادداشت اصلاحی همین نشست:** نخستین تلاش برای append با ابزار ویرایش، به‌اشتباه header ورودی 09:20 را جایگزین کرد؛ بلافاصله با بازگردانی محتوای کامیت‌شدهٔ همان فایل (تنها تغییر uncommitted همین خطای خودم بود؛ `git diff` پیش از اصلاح فقط همین فایل را نشان می‌داد) و append صحیح در انتهای فایل اصلاح شد. هیچ ورودی دیگری تغییر یا حذف نشد.
- **اقدام این نشست:** فقط همین ورودی append-only (الزام governance) + commit forward-only روی `arena/01a0ae2c-doctor` و push به همین شاخه. بدون ساخت PR (شرط STOP؛ هیچ تغییر محصول/تستی برای review وجود ندارد)، بدون Ready/merge/close/delete branch/tag/release/force-push/reset/clean/rebase؛ main مستقیماً تغییر داده نشد.

### [2026-09-17 11:46 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0ae2c-doctor`، PR #65 DRAFT) — PHASE4 «شعبه‌ها (Location master data): ساخت + ویرایش name/timezone» — IMPLEMENTED / GREEN
- **LIVE REVERIFY:** main زنده = `ad5a2617ebc474fbb6d0e973ecd8cb036c60f85b` (مطابق انتظار)، PR باز = ۰، آخرین migration = `0020` (بدون 0021)، working tree clean. کار Location-management موجود نبود (فقط `PrimaryLocationResolver` فقط-خواندنی + seed مهاجرت 0011؛ هیچ صفحهٔ admin/REST/سرویس/ریپوی Location روی main نبود) ⇒ **PRE-EXISTING RED NOT AVAILABLE** (طبق استثنای مصوب task، boundary + تست‌های قراردادی با هم ساخته شدند و رفتار داخل boundary جدید test-first هدایت شد). شروع: `START_TEHRAN=2026-09-17 11:08:33 Asia/Tehran`.
- **مرز پیاده‌سازی (کوچک‌ترین سطح قابل‌نگهداری، هم‌راستا با StaffManagementPage/ClinicProfileService):** `admin_post_cpms_location_save` → `LocationAdminPage::save()` (is_user_logged_in + `check_admin_referer`) → `upsertLocation()` (مرز خالص: TrustedClinicEstablisher — clinic_id ارسالی فقط selector — + مجوز CONFIG scoped) → `LocationService` (CONFIG را دوباره احراز می‌کند؛ validation کامل قبل از mutation؛ parity برای ناموجود/خارجی؛ IANA-only بدون fallback؛ audit با trusted clinic_id صریح) → `LocationRepository` (create همیشه `is_primary=0`/`is_active=1` را قطعی می‌کند؛ update فقط name/timezone/updated_at) → DB. فایل‌های جدید: `src/Admin/LocationAdminPage.php`، `src/Application/Location/LocationService.php`، `src/Application/Location/LocationException.php`، `src/Infrastructure/Repository/LocationRepository.php`، `tests/Integration/Phase4LocationMasterDataTest.php` (۱۴ تست از مسیر واقعی admin boundary). تغییر یافته‌ها: `src/Bootstrap/App.php` (ثبت صفحه + locationRepository/locationService)، `.github/workflows/ci.yml` (فقط selection شواهد per-suite برای suite جدید — الگوی مستقر Slice3/4؛ بدون تغییر گیت‌ها)، همین لاگ. بدون migration/0021؛ Schedule/Booking/WalkIn/Queue/Pricing/ServiceOffering/Organization/geography/Clinic Profile/PrimaryLocationResolver دست‌نخورده.
- **RED معتبر داخل boundary جدید (مطابق شرط‌های مصوب):** کامیت `0fda84b` — CI run `35197860173`: `Tests: 927, Assertions: 12130, Failures: 1.` — تنها شکست: `Phase4LocationMasterDataTest::testNoOpUpdateIsExplicitAndProducesNoSuccessAudit` با «identical name+timezone must be reported as an explicit no-op — Failed asserting that false is true.» (خط ۲۸۹). bootstrap/ثبت/fixture/احراز هویت/establishment/دسترسی به سرویس واقعی همگی موفق بودند و assertion رفتاری شکست خورد — پیاده‌سازی اولیهٔ واقعی (نه stub ردکننده) همیشه update+audit می‌زد. ۱۳ تست دیگر همان run سبز بودند؛ warningهای DB در tail مربوط به تست‌های دیگر (VisitNoShow*/SecurityHardening) و از قبل موجود بودند.
- **GREEN:** کامیت `a993cee6759695b7abb22aee5108e0ed0ec2e5f5` (تشخیص صریح no-op: مقادیر یکسان ⇒ return noop=true بدون update/updated_at/audit موفق). CI run `35198192935` = success: terminal `OK (927 tests, 12135 assertions)`؛ JUnit root: `tests=927 assertions=12135 errors=0 warnings=0 failures=0 skipped=0`. **شاهد per-suite/per-test مستقیم** (کامنت completion PR #65): `Phase4LocationMasterDataTest tests="14" assertions="594" errors="0" warnings="0" failures="0" skipped="0"` + هر ۱۴ testcase با شمارش assertion جدا (از جمله `testNoOpUpdateIsExplicitAndProducesNoSuccessAudit` با ۳۸ assertion و `testUpdateTimezoneDoesNotRewriteHistoricalOperationalRows` با ۵۴ assertion).
- **گیت‌های exact-head (`a993cee`):** CI `35198192935` = success (Integration 1m53s + PHPStan + WPCS + Unit 8.1/8.2/8.3/8.4 + Tripwire)؛ Closure Gate `35198187315` = success (runtime 8.1/8.3/8.4 + WP 6.4-6.6 + restoreApply)؛ Pilot/Staging `35198187110` = success (Upgrade path + Staging + Release Artifact + Responsive)؛ Real WP Acceptance `35198192940` = success (prefixهای `clinic_` و `wp_`). تمام ۱۹ check-run روی همین SHA = success.
- **صداقت شواهد:** اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox نیستند). Tenant Tripwire محلی = **PASS** (python3 موجود بود: `production tenant hardcode = 0`). `git diff --check` محلی = سبز. تست مستقیم nonce/CSRF در لایهٔ `save()` (که redirect/exit می‌کند) با زیرساخت موجود Integration ممکن نیست — همان الگوی مستقر StaffManagementPage؛ مرز خالص post-nonce (`upsertLocation`) تست شده است. fault-injection برای «query failure → fail-closed» تست مستقیم ندارد؛ به‌صورت code-level (`assertNoSqlError` + نگاشت RuntimeException→QUERY_FAILED) برقرار است. تغییر ci.yml صرفاً selection شواهد بود (نه رفتار گیت) و در PR مستند شده است.
- **Git/PR:** دو کامیت forward-only (`0fda84b`، `a993cee`) + همین لاگ؛ فقط به `arena/01a0ae2c-doctor` push شد. PR #65 = **OPEN + DRAFT** (بدون Ready/merge/close/delete branch/tag/release/force-push/reset/clean/rebase)؛ main دست‌نخورده. این slice «final technical slice candidate» فاز ۴ است؛ تصمیم نهایی پذیرش/ادامه با مالک است.


### [2026-09-17 13:15:50 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0aec1-doctor`) — Phase 4 documentation closure reconciliation
- فاز/محدوده: فقط آشتی‌دهیِ مستندات کانونی وضعیت Phase 4؛ بدون بازگشاییِ تحلیل فنی و بدون شروع Phase 5.
- **LIVE PIN:** `origin/main = 70ace204d1524a8f5e83d33c67c1a09b7543e7a3`، open PRs = `0`، آخرین migration = `0020`، شاخهٔ نشست = `arena/01a0aec1-doctor`.
- اقدامات: roadmap، current-state، taxonomy، drift register، README و جدول جاری همین راهنما به‌روز شدند؛ closure فقط به PRهای merge‌شدهٔ **#59–#65** و دامنهٔ bounded آن‌ها ارجاع می‌دهد. `clinic.phone` stale Patient Portal consumer، ServiceOffering، تصمیم‌های specialty/geography، و M-01..M-05 به‌عنوان fixed/implemented ادعا نشدند؛ Location timezone و membership cross-Clinic بدون تغییر تصمیمی حفظ شدند.
- کامیت‌ها: **PENDING — SHA در گزارش نهایی ثبت می‌شود**.
- CI: اجرای محلی PHPUnit/PHPStan/WPCS **NOT RUN**؛ exact-head checks تا پس از ساخت Draft PR **NOT RETRIEVED**.
- تصمیمات درون‌فازی: Phase 4 = **CLOSED / TECHNICALLY COMPLETE (BOUNDED)**؛ Phase 3 = **COMPLETED / FROZEN**؛ Phase 5 = **NOT STARTED**؛ هیچ migration (از جمله `0021`) ساخته/تغییر/رزرو نشد.
- موارد باز/تحویل به ایجنت بعد: deferred/open Master Data drift و هر شواهد اجراییِ not retrieved همان‌طور که هست باقی می‌ماند؛ این entry خودش append-only است.
- وضعیت tree: **در زمان ثبت این entry، منتظر commit**.

### [2026-09-17 18:44 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0afef-doctor`، PR DRAFT) — Phase 5 Slice 1: «ثبت Location فاکتور از Visit و تثبیت رفتارهای مالی موجود»
- **LIVE REVERIFY پیش از کار:** `origin/main = 86cd5ef0479e74a9f1e86f7474196a30e1bc5221` (مطابق انتظار)، open PRs = `0`، آخرین migration = `0020` (بدون 0021)، شاخهٔ نشست = `arena/01a0afef-doctor`، working tree clean. شروع: `START_TEHRAN=2026-09-17 18:44:58 Asia/Tehran`.
- **دامنه:** کوچک‌ترین برش Phase 5 — فقط `invoice.location_id` از `visit.location_id` در زمان صدور فاکتور. Location هرگز از payload گرفته نمی‌شود. `invoiceView()` مقدار ذخیره‌شده را برمی‌گرداند. هیچ migration جدید، تغییر schema، ServiceOffering، pricing per-professional، per-location tariff، `services.location_id` NOT NULL، `is_active` policy، backfill، discount workflow، tax/VAT، multi-currency، prepayment، packages، UI redesign یا broad audit انجام نشد.
- **Invariantهای محافظت‌شده:** ۱) Clinic فقط از trusted server context؛ ۲) payload منبع location_id نیست؛ ۳) invoice.location_id فقط از Visit معتبر؛ ۴) بدون first-row/default/fake Location؛ ۵) cross-Clinic service_id fail-closed؛ ۶) manual unit_price override حفظ؛ ۷) fallback به service.price حفظ؛ ۸) snapshot تاریخی مستقل از service.price بعدی؛ ۹) بدون backfill فاکتور تاریخی؛ ۱۰) بدون سیاست جدید is_active/location_id غیرNULL.
- **تغییرات محصول (FinanceService.php — ۸ سطر اضافه):** `issueInvoice()`: استخراج `location_id` از Visit row (قبلاً `findForUpdate` + `rowBelongsToTrustedClinic` شده) و ارسال به `InvoiceRepository::insert()`؛ `invoiceView()`: بازگرداندن `location_id` (nullable برای فاکتورهای تاریخی). بدون تغییر `InvoiceRepository` (ستون `location_id` از migration 0015 وجود دارد و `$row +=` مقدار صریح را حفظ می‌کند).
- **تست جدید (Phase5InvoiceLocationFromVisitTest.php — ۷ متد):** ۱) سناریوی اصلی: Clinic با ۲ Location، Visit در Location دوم، `invoice.location_id === visit.location_id`؛ ۲) payload location_id نادیده گرفته شود؛ ۳) manual unit_price override snapshot؛ ۴) fallback به service.price؛ ۵) snapshot immutability پس از تغییر service.price؛ ۶) cross-Clinic service_id fail-closed؛ ۷) فاکتور تاریخی NULL backfill نشود.
- **RED/GREEN:** اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox موجود نیستند؛ با `which php composer mysql` تأیید). RED معتبر runtime در CI = **NOT RETRIEVED** (پیش از push). GREEN = **PENDING** تا پس از CI exact-head.
- **Git/PR:** کامیت forward-only `ac9fe35` + همین لاگ؛ فقط به `arena/01a0afef-doctor` push. Draft PR **PENDING**. بدون Ready/merge/close/delete branch/tag/release/force-push/reset/clean/rebase؛ main دست‌نخورده.

### [2026-09-17 19:28 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0b016-doctor`) — Phase 5 documentation closure reconciliation (bounded، فقط مستندات)
- **فاز/محدوده:** فقط آشتی‌دهیِ مستندات کانونی با وضعیت واقعی Phase 5. **بدون هیچ تغییر PHP / test / workflow / migration / schema / configuration؛ بدون ServiceOffering؛ بدون تغییر `u_service_code`؛ بدون `0021`؛ بدون شروع Phase 6؛ بدون جزئیات محصولی جدید.**
- **LIVE REVERIFY پیش از کار:** repo root = `/home/user/doctor` · شاخهٔ نشست = `arena/01a0b016-doctor` · local HEAD = `e063b42` · working tree clean (بدون untracked) · `origin/main = e063b42260cb5ab740acf48dcf73c6c67b11fef6` (مطابق انتظار) · open PRs = `0` · **PR #67 = MERGED** (merge = `e063b42`، 2026-09-17T15:36:54Z؛ head = `fa86e41e`؛ ۳ فایل تغییریافته) · آخرین migration = `0020` · `0021` وجود ندارد. شروع: `START_TEHRAN=2026-09-17 19:28:08 Asia/Tehran`.
- **اقدامات:** `roadmap.md` (سطر Phase 5 + ارجاع سطر Phase 4)، `project-current-state.md` (بلوک checkpoint سرصفحه + جدول §C + سطر «Integrated main checkpoint»)، `drift-register.md` (نُت بستن Phase 5 + سطر O-06 + ردیف نگاشت «Pricing/تعرفه») و همین راهنما (جدول وضعیت بالا + همین entry) به‌روزرسانی شدند؛ در `docs/README.md` و `docs/governance/project-phase-taxonomy.md` فقط ادعای کهنهٔ «Phase 5 شروع نشده» با annotation تاریخی/به‌روزرسانی حداقلی اصلاح شد. هیچ فایل خارج از `docs/` تغییر نکرد.
- **تصمیمات درون‌فازی:** **Phase 5 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** بر پایهٔ PR #67 ادغام‌شده؛ pricing پایه **Clinic-scoped** و کافی برای جریان تأییدشدهٔ فعلی V1؛ manual `unit_price` override و fallback به `service.price` و snapshot آیتم‌های فاکتور حفظ‌شده‌اند؛ `is_active` server-side rejection **قراردادِ اثبات‌شدهٔ محصول نیست**؛ سیاست مالیات/VAT = **REQUIRES LEGAL VERIFICATION**؛ **هیچ migration اضافی برای Phase 5 توجیه نمی‌شود**. **هیچ فرضِ تک‌Clinic/تک‌Location مستند نشد**؛ تعرفهٔ متفاوتِ «همان Service» در Locationهای مختلفِ یک Clinic **الزامِ سختِ اثبات‌شدهٔ V1 نیست** و تعلیق آن بر پایهٔ **نبودِ الزامِ اثبات‌شده** است، نه فرضِ تک‌Location — **پشتیبانی چند Location به‌تعویق نیفتاده است** (معماری مرجع `Organization → Clinic → Location`). در صورت الزامِ تأییدشدهٔ آینده = تصمیمِ scoped جداگانه با بازبینی schema/uniqueness.
- **شواهد PR #67 (محدودیت دائمی — ثبت صادقانه، بدون تبدیل NOT RETRIEVED به PASS):** historical **VALID RED = NOT AVAILABLE** (هیچ workflow-run روی کامیت پیاده‌سازی `ac9fe35abbd3ec4217b5801f581d8316b5217b17` ثبت نشده — `total_count = 0`؛ بنابراین «RED معتبر» در دسترس نیست)؛ **direct named execution proof برای `Phase5InvoiceLocationFromVisitTest` = NOT RETRIEVED** (کامنتِ شواهدِ Integration همان PR فقط suiteهای Phase-3 Slice-6 / Phase-4 Slice-3 / Phase-4 Slice-4 / Phase-4 Location را نام می‌برد؛ شمارش‌های aggregate — ۹۳۴ تست / ۱۲۱۶۶ assertion — شاهدِ اجرای یک تستِ نام‌دار نیستند). exact-head: ۱۹/۱۹ check-run موفق روی `fa86e41e`؛ **post-merge** روی `e063b42`: check-runs = 19/19 success و ۴ workflow-run موفق (CI `35241393668` · Real WP `35241393522` · Pilot/Staging `35241393533` · Closure `35241393493`).
- **کامیت‌ها:** **PENDING — SHA نهایی در گزارش تحویل ثبت می‌شود** (شاخه `arena/01a0b016-doctor`).
- **CI:** اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox موجود نیستند)؛ exact-head checks مربوط به این PR مستندات = تا پیش از ساخت Draft PR **NOT RETRIEVED**.
- **موارد باز/تحویل به ایجنت بعد:** قلمِ بازِ O-06 («تعرفهٔ متفاوت per-Location برای همان Service») تا الزامِ تأییدشدهٔ آینده — **بدون فاز/بدون migration** — باز می‌ماند؛ Phase 6 شروع نشده است؛ entry قبلیِ Phase 5 Slice 1 (خطوط پیشین) **append-only و دست‌نخورده** می‌ماند و «GREEN = PENDING» آن در همین entry با شواهد post-merge فوق **تکمیل** (نه ویرایش) می‌شود.
- **وضعیت tree:** **در زمان ثبت این entry، منتظر commit.**

### [2026-09-18 01:25 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0aebc-doctor`، PR DRAFT) — حفظ دو الزام صریح مالک (documentation-only)
- **فاز/محدوده:** فقط **ثبت ماندگارِ** دو الزام صریح مالک در اسناد کانونی — **بدون هیچ تغییر PHP / test / workflow / migration / schema / UI / config** و **بدون پیاده‌سازی** هیچ‌یک از دو الزام؛ بدون بازطراحی roadmap؛ بدون ایجاد سند جدید.
- **LIVE PIN پیش از کار (بازبینی مستقل):** `origin/main = 7d4a0c1c16a42fb3dc171a35fdd567651f960ed3` (شامل merge **PR #70** — Phase 6 Slice 2) · open PRs = `#71` (DRAFT) · آخرین migration = `0020` (`0021` وجود ندارد) · شاخهٔ نشست = `arena/01a0aebc-doctor` · working tree در شروع = clean. برای پایه‌گذاریِ بدون‌تعارض، `git fetch origin main` + `merge --ff-only` انجام شد (فقط metadata محلی؛ بدون force/rebase/rewrite).
- **الزام ۱ — «مدت نوبت/اسلات قابل‌پیکربندی» (`FR-3.10`، فاز مالک Phase 6):** نمونه‌های الزامی ۱۵/۲۰ دقیقه و سایر مقادیر معتبرِ اپراتور؛ **بدون** تحمیل یک مقدار سراسری بر همهٔ پزشکان/کلینیک‌ها/شعبه‌ها. **شواهد as-built (بازرسی کد):** `cpms_schedule.appointment_duration_min` (`SMALLINT UNSIGNED NOT NULL DEFAULT 20` — Migration `0001`؛ هر ردیفِ برنامه = Clinic+Location+Clinician+روز+شیفت)، اعتبارسنجی **۵..۲۴۰** در `ScheduleService`، فرم wp-admin (`ClinicianAdminPage` فیلد مدت، min 5/max 240) و REST (`POST/PUT /config/schedules`)، مصرف در `SlotsGenerateHandler` و `DurationResolver` (ADR-0017: Override نوبت ← Service ← برنامهٔ پزشک ← پیش‌فرض کلینیک + Snapshot در زمان رزرو). ⇒ **نیازی به معماری «مدت» جدید نیست**؛ باقی‌مانده = UX/پیش‌فرض‌های تجاری (Phase 20).
- **الزام ۲ — «ویزارد راه‌اندازی هدایت‌شدهٔ پس از نصب» (`FR-23.1`/`FR-23.2`، فاز مالک Phase 20):** الزام تجاری مالک برای راه‌اندازی هدایت‌شدهٔ اولین اجرا (نه صرفاً مستندات). **چک‌پوینت الزامی ثبت‌شده:** «Before final commercial Setup Wizard implementation, obtain owner product/UX direction for exact wizard steps.» مراحل/صفحه‌ها/پیش‌فرض‌ها/UX **تأیید نشده‌اند** و نباید اختراع شوند.
- **تفکیک صریح (بدون ادعای تکمیل):** یک **ویزارد فنی** وجود دارد — `CpmsSetupWizard` (`src/Admin/CpmsSetupWizard.php`، slug `cpms-wizard`، cap `cpms_config`، ۱۲ گام، resumable با `setup.current_step`، تست `SetupWizardTest`، مستند در `docs/admin-ux-plan.md` §Chunk B) — که **Organization/Clinic/Location جدید نمی‌سازد** و فقط Clinic موجودِ scope را می‌خواند/به‌روزرسانی می‌کند. ⇒ «ویزارد فنی موجود» **≠** «تکمیل الزام تجاری آنبوردینگ»؛ ادعای تکمیل = **NOT AVAILABLE** (شاهد اجرایی/پذیرش تجاری وجود ندارد).
- **فایل‌های تغییریافته (فقط `docs/`):** `docs/srs/SRS.md` (بردار `FR-3.10` + بخش جدید §3.23 با `FR-23.1`/`FR-23.2`)، `docs/roadmap/roadmap.md` (سطرهای Phase 6 و Phase 20 + نُت حفظ الزامات)، `docs/agent-guide.md` (همین entry).
- **فازبندی:** مدت نوبت → **Phase 6 (Scheduling Engine)**؛ نهایی‌سازی تجاری ویزارد/آنبوردینگ → **Phase 20 (UI/UX & Commercial Release)**. هیچ تغییری در کار فعال فعلی و هیچ migration/`0021` رزرو نشد.
- **CI:** اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox موجود نیست)؛ exact-head checks پس از ساخت Draft PR در گزارش تحویل ثبت می‌شود.
- **موارد باز/تحویل به ایجنت بعد:** پیش از پیاده‌سازی تجاری ویزارد، **جهت‌گیری محصولی/UX مالک** الزامی است (`FR-23.2`)؛ برای مدت نوبت هیچ کار فیچر جدیدی لازم نیست مگر UX آنبوردینگ (Phase 20).
- **وضعیت tree:** در زمان ثبت این entry، تغییرات آمادهٔ commit روی `arena/01a0aebc-doctor`؛ شمارهٔ PR و SHA نهایی در گزارش تحویل ثبت می‌شود.

### [2026-09-18 01:27 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0b130-doctor`، PR #71 DRAFT) — Phase 6 Slice 3: «Location صریح و معتبر برای create برنامه»
- **LIVE REVERIFY پیش از کار:** `origin/main = 7d4a0c1c16a42fb3dc171a35fdd567651f960ed3` (= merge PR #70 / Slice 2)، open PRs = `0` (پیش از ساخت)، آخرین migration = `0020` (بدون 0021)، شاخهٔ نشست = `arena/01a0b130-doctor`.
- **قرارداد هدف:** create برنامه در قرارداد مدرن `location_id` صریح می‌خواهد؛ Location باید واقعی + فعال + متعلق به Clinic معتبرِ Scope باشد (هرگز از payload/ردیف پزشک/Location ارسالی Clinic گرفته نمی‌شود)؛ بیگانه/ناموجود/غیرفعال ⇒ پاریتِ 404 `CLINIC_NOT_FOUND` «محل یافت نشد» (عدم شمارش)؛ نبود فیلد ⇒ 400 `CLINIC_VALIDATION_FAILED` `errors.location_id=required` — هیچ جایگزینی بی‌صدا با Primary/اولی/خانه. قاعدهٔ تکراری در محدودهٔ (Clinic+Location+clinician+weekday) — همان روز هفته در Location دیگر مجاز است؛ **Multi-shift فعال نشد**. view مقدار ذخیره‌شدهٔ `location_id` را برمی‌گرداند؛ Slotها Location و timezone محلیِ آن را به‌ ارث می‌برند.
- **RED (TDD، واقعی در CI — محلی PHP/MySQL نبود):**
  - RED اولیه **run 35276874340** @ `496a97c4` (فقط تست): ۹۵۴ تست — T1–T7 با امضای نقصِ کلاسیک **B** (جایگزینی بی‌صدا Primary 181≠182؛ نبود field در view؛ پیش‌بررسی تکراری Clinic-wide؛ چیدمان ردیف 190≠191؛ نبود fail-closed برای Location بیگانه/ناموجود) + ۲ نقص **D** سمت تست که شناسایی و ثبت شدند: (۱) `$this->TZ_PRIMARY` به‌جای `self::TZ_PRIMARY` در preconditions (error)؛ (۲) dispatchهای هماهنگ‌شدهٔ `RestScheduleTest` بدون `X-CPMS-Clinic-Id` → مرز REST، selectorِ Location بدون selectorِ Clinic را 422 می‌گیرد (`Location cannot establish Clinic.`).
  - **VALID RED پاک: run 35278155692** @ `4c458008` — `Tests: 954, Assertions: 12735, Failures: 7, Errors: 0` — دقیقاً و فقط T1–T7 (B) شکست خوردند؛ bootstrap/fixture/مسیر محصول سالم؛ `testFixtureTopologyPreconditions` و T8 و `RestScheduleTest` و بقیهٔ ۹۴۷ تست سبز.
- **GREEN:**
  - commit محصول **`6a823ed`**: `ScheduleService::create()` (ترتیب: clinician → مشارکت در Clinic معتبر → بازهٔ روز → `location_id` الزامی → `LocationRepository::findActiveForClinic` (real+active+clinic match، پاریت 404) → پیش‌بررسی تکراری Location-aware → درج با `location_id` صریح)؛ `ScheduleRepository::create()` بدون فالبک (guard آشکار — ستون NOT NULL از 0013)؛ `findByClinicianDayInClinic` حذف شد (مرده پس از سوییچ) و `findByClinicianDayInClinicAndLocation` جایگزین شد؛ `scheduleView()` field `location_id` دارد؛ REST: آرگومنت `location_id` اختیاری (اجرای قرارداد در سرویس تا 404-parityِ physician بیگانه حفظ شود)؛ wp-admin: جدول برنامه = ماتریس (روز × محل) فقط با Locationهای Clinic معتبر، ردیف موجود Location ثابت دارد (update جابه‌جا نمی‌کند)، `saveSchedules` جست‌وجو با (clinician, day, clinic, location) و create با `location_id`؛ `App::scheduleService()` LocationRepository تزریق می‌کند.
  - **run 35278862253** @ `6a823ed`: `Tests: 954, Failures: 0, Errors: 1` — تنها error = نقص **D** سمت تست: assertion جدیدِ `testWpAdminScheduleCreateForOwnClinicSucceeds` کلید `location_id` را می‌خواند ولی probe `fetchScheduleRowForClinicianDay` آن ستون را SELECT نمی‌کرد → commit `8c153c1`.
  - **GREEN نهایی: run 35279185117** @ `8c153c16` — terminal summary (completion-guard): **`OK (954 tests, 12771 assertions)`**؛ JUnit: `errors=0 failures=0 skipped=0`. اجرای نام‌دار (کامنت شواهد همان run): `Phase6ScheduleExplicitLocationTest` **9/9** (261 assertion — هر ۹ testcase نام‌شده: preconditions/T1–T8) + سوت‌های رگرسیون: `RestScheduleTest` 8/8 · `Phase4Slice1ProfessionalMultiClinicParticipationTest` 8/8 · `ScheduleRegenerationClinicScopeRedTest` 2/2 · `SchedulingLocationLocalBoundaryRedTest` 9/9 · `SlotsGenerateLocationLocalTemporalRedTest` 2/2. exact-head CI checks روی `8c153c16`: Integration · PHPStan · Tenant Tripwire · Unit (8.1/8.2/8.3/8.4) · WPCS — همه success.
- **محدودیت‌های رعایت‌شده:** بدون migration (0021 ساخته/رزرو نشد)؛ بدون جدول/وابستگی/لایهٔ سرویس جدید؛ `cpms_clinician_locations` الزامِ authorization نشد؛ `PrimaryLocationResolver` برای مسیرهای خواندنیِ Booking (Appointment/VisitRepository) ماند و فقط از create برنامه برداشته شد؛ Phase 20 UI ساخته نشد؛ Phase 7 شروع نشد؛ CHANGELOG طبق عرف ریپو (سطر Phase برای برش‌ها افزوده نمی‌شود) دست‌نخورده.
- **Docs:** `docs/api/api-contract.md` — سطر G1 به‌روزرسانی (location_id الزامی در POST + view + قاعدهٔ تکراری Location-aware)؛ `docs/api/error-codes.md` بدون تغییر (کدهای `CLINIC_VALIDATION_FAILED`/`CLINIC_NOT_FOUND` مستند بودند).
- **Git/PR:** کامیت‌ها forward-only: `496a97c` (تست RED) → `4c45800` (تصحیح D) → `6a823ed` (GREEN محصول) → `8c153c1` (تصحیح D) → همین entry (docs). Draft PR **#71** باز است (draft)؛ بدون Ready/merge/close/delete branch/tag/release/force-push/reset/clean/rebase؛ main دست‌نخورده.
- **تحویل به ایجنت بعد:** برش بعدی Phase 6 = **multi-shift** (هنوز فعال نیست — single-row-per-(Location,weekday) فعلاً قرارداد است)؛ entry قبلی‌ها append-only و دست‌نخورده ماندند.

### [2026-09-18 01:51 Asia/Tehran] — تکمیل entry Phase 6 Slice 3 (تأیید exact-head نهایی)
- **exact head نهایی:** `ca7b484e7092f74429bba11a3f532ba4a6709aee` (کامیت docs = آخرِ entry قبلی) — CI run **`35279505286`** @ همان head: `completed/success`؛ completion-guard **`OK (954 tests, 12771 assertions)`**؛ اجرای نام‌دار (کامنت شواهد 5721802943): `Phase6ScheduleExplicitLocationTest` 9/9 (261 assertion) · `Phase4Slice1ProfessionalMultiClinicParticipationTest` 8/8 · `RestScheduleTest` 8/8 · `ScheduleRegenerationClinicScopeRedTest` 2/2 · `SchedulingLocationLocalBoundaryRedTest` 9/9 · `SlotsGenerateLocationLocalTemporalRedTest` 2/2 — همه 0 error/0 failure.
- **وضعیت پایانی:** Draft PR **#71** (OPEN/draft، head = `ca7b484e`، ۵ کامیت از main جلو) کامل و ready-to-review است؛ بدون merge/Ready/tag/release/force-push. برش بعدی = multi-shift Phase 6.

### [2026-09-18 01:58 Asia/Tehran] — اصلاح entry تکمیلی Phase 6 Slice 3 (head دقیق + سلسلهٔ کامیت‌های docs)
- entryِ «01:51» یک اسنپ‌شات در لحظهٔ نگارش بود و خطِ «exact head نهایی: ca7b484e» را می‌تواند گمراه‌کننده خواند؛ **اصلاح دقیق:**
  - آخرین کامیتِ **حاوی کد = `8c153c1`** (GREEN — run 35279185117).
  - کامیت‌های پس از آن **فقط docs** هستند: `ca7b484` (entry + api-contract) → `b342116` (entry تکمیلی) → `511ac96` (اصلاح timestamp) → همین entry.
  - بازتأیید CI روی headهای docs (همه بدون هیچ تغییر کد): run **35279505286** @ `ca7b484e` — `completed/success`، completion-guard `OK (954 tests, 12771 assertions)`، کامنت شواهد 5721802943؛ run **35281643731** @ `511ac96` — `completed/success`.
- **وضعیت پایانی واقعی:** Draft PR **#71** (OPEN/draft) — head = آخرین کامیتِ docs-only؛ CI روی head هر لحظه سبز است؛ آمادهٔ review؛ بدون merge/Ready/tag/release/force-push. برش بعدی = multi-shift Phase 6.

### [2026-09-18 03:00 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0b1a3-doctor`، Draft PR #73) — Phase 6 Slice 4: «multi-shift با رد قطعی همپوشانی»
- **LIVE REVERIFY پیش از کار:** repo root = `/home/user/doctor` · شاخهٔ نشست = `arena/01a0b1a3-doctor` · local HEAD = `c0867b4` (= `origin/main`) · tree clean · open PRs = `0` · آخرین migration = `0020` (بدون 0021).
- **اصلاح تحلیل مدیر (با شاهد):** مدیر ۲ تست منسوخ نام برده بود؛ بازرسی زنده نشان داد **۳** تست قرارداد قدیمی «یک ردیف در هر روز» را کد می‌کردند — سومی `RestScheduleTest::testScheduleCrudAndSlotGeneration` (بلوک dup با 14:00-18:00 پس از 09:00-12:00 و کامنت «the single-row rule») بود که با همان اصلِ حفظ invariant به‌روزرسانی شد. بقیهٔ تحلیل (u_sched_slot چندشیفته، LIMIT 1 در `ScheduleService::create:108`، نبود ORDER BY، کامنت کهنهٔ handler، گارد admin) عیناً تأیید شد.
- **تأیید ادعای (f) روی ژنراتور زنده:** `SlotGenerator::grid` هر Slot را با `t + step <= endSec` داخل پنجرهٔ شیفت خودش تولید می‌کند ⇒ گرید پایهٔ دو شیفت نامتقاطع هیچ `slot_time` مشترکی ندارد (تعیین‌پذیر بدون ORDER BY). ظرافتِ گزارش‌شده: extraهای `open_override` در سطح (clinician, clinic, date) خوانده و به‌ازای هر ردیف بازتولید می‌شوند — رفتار پیشین، بدون exception در تست‌های جدید، خارج از scope این برش (بدون دست‌کاری query طبق بند f).
- **قرارداد هدف:** چند شیفت نامتقاطع در یک (Clinic, Location, clinician, weekday) مجاز؛ همپوشانی ACTIVEها در create و update با دلیل متمایز `overlapping_shift` (زیر `CLINIC_VALIDATION_FAILED`/400، کلید `start_time,end_time`) رد می‌شود؛ تکرار دقیق start_time همچنان `duplicate_schedule_day`؛ مرز مماس مجاز؛ ردیف غیرفعال مانع نیست و فعال‌سازیِ داخل همپوشانی رد می‌شود؛ update ردیفِ خودش را مستثنا می‌کند؛ Location سطر تغییرناپذیر ماند (خارج از whitelist).
- **تغییرات محصول (بدون migration — آخرین همچنان 0020):** `ScheduleService` (حذف پیش‌بررسی تک‌ردیفه + `assertNoShiftConflict` در create/update با مقایسهٔ بازه‌ای `s1<e2 && s2<e1`)؛ `ScheduleRepository` (یابندهٔ LIMIT 1 حذف و `listByClinicianDayInClinicAndLocation` جایگزین شد — پوشش با کلید 0014)؛ `ClinicianAdminPage::saveSchedules` (تطابق چندردیفه ⇒ خطای صریح «چند برنامه… هیچ تغییری ذخیره نشد» و بدون نوشتن)؛ `SlotsGenerateHandler` (فقط سطر ۱۸: کلید زندهٔ u_slot)؛ `ci.yml` (فقط بلوک additive شواهد Slice 4).
- **RED معتبر در CI:** run **35286496400** @ `bfe7d6e` (فقط تست) — completion-guard: `Tests: 963, Assertions: 12991, Failures: 10` (0 error) — دقیقاً ۱۰ شکست کلاس-B موردانتظار: M1/M2(دلیل اشتباه)/M4/M5/M6/M7/M8 + T4 + بلوک Phase4Slice1 + بلوک RestScheduleTest؛ M3 و preconditions و ۹۵۳ تست دیگر سبز. اجرای محلی = NOT RUN (PHP/MySQL در sandbox نیست)؛ لاگ job = NOT RETRIEVED (محدودیت blob) — شواهد از کامنت‌های PR (5722565341/5722565574) خوانده شد.
- **GREEN:** کامیت محصول (همین برش)؛ نتیجهٔ exact-head پس از CI در گزارش PR ثبت می‌شود. Invariantهای ۱-۸ در بدنهٔ PR #73 صورت و با تست اثبات می‌شوند.
- **Git/PR:** forward-only روی `arena/01a0b1a3-doctor`؛ Draft PR **#73** باز؛ بدون Ready/merge/force-push/reset/rebase. Entryهای قبلی append-only و دست‌نخورده.

### [2026-09-18] — Phase 6 Slice 5 explicit wp-admin schedule-row identity (in progress)
- **LIVE INSPECTION (VERIFIED FACT):** repository root is `/home/user/doctor`; authoritative `main` and local HEAD are `8be47522c8c60a51394659ac987497b9d49c0433`; no open PR was returned; latest migration remains `0020`.
- **CORRECTION (VERIFIED FACT):** the repository’s plugin and test roots are nested under `clinic-practice-management/`, not the repository root. The director’s overwrite claim and Slice-4 multi-row refusal guard were confirmed in `src/Admin/ClinicianAdminPage.php`.
- **TEST-FIRST EVIDENCE:** test-only commit `bd9351e` defines explicit-row edit, render-all-rows/add-row, and foreign/mismatched-row fail-closed contracts. Local execution is **NOT RUN**: PHP and Composer dependencies are unavailable in the sandbox (`php: command not found`, `vendor/bin/phpunit: No such file or directory`), so no valid local RED is claimed.

### [2026-09-18 08:34 Asia/Tehran] — ایجنت Arena (شاخهٔ `arena/01a0b2d4-doctor`، Draft PR #75) — Phase 6 Slice 6: «دامنه‌بندی Location برای استثناهای برنامه»
- **LIVE REVERIFY پیش از کار:** repo root = `/home/user/doctor`؛ شاخهٔ نشست = `arena/01a0b2d4-doctor`؛ local HEAD = `main` = `e388729244b4a5a1a339f5c0212bd466b71b37fa` (merge PR #74 / Slice 5)؛ tree clean؛ open PRs = `0`؛ آخرین migration = `0020` (بدون 0021).
- **قرارداد هدف (از قبل‌موجود در 0015 — هیچ migration جدیدی ساخته/رزرو نشد):** `cpms_schedule_exceptions.location_id` nullable + FK `fk_schedexc_location`؛ `NULL` = همهٔ Locationهای Clinic معتبر، عدد = فقط همان Location. نوشتن: نبود/تهی ⇒ NULL، مقدار ⇒ واقعی+فعال+مالکیت Clinic معتبر وگرنه fail-closed؛ تولید: query استثنا به‌ازای هر ردیف برنامه `(location_id IS NULL OR location_id = <row>)`؛ view: `location_id` nullable؛ wp-admin: selector «همهٔ محل‌ها» + Locationهای فعال Clinic و ستون محل در فهرست؛ REST: آرگومان اختیاری؛ delete و SlotGenerator دست‌نخورده.
- **اصلاح تحلیل مدیر (با شاهد زنده):** (۱) ادعای «REST باید همان پاکت 404 سرویس را برگرداند» نادرست است: در مرز REST، `location_id` **هم‌زمان selector مکانیِ درخواست** است (`RestClinicContext::extractIds` + `TrustedClinicEstablisher::verifiedScope`) و بیگانه/ناموجود/غیرفعال هر سه پیش از رسیدن به سرویس با **یک پاکت یکسان 403 `CLINIC_SCOPE_UNAVAILABLE`** رد می‌شوند (پاریت حفظ می‌شود، افشای وجود نمی‌شود). پیشینهٔ همین رفتار در Slice 3 (`Phase6ScheduleExplicitLocationTest` T5) فقط `not 200` + پاریت را assert می‌کند. نسخهٔ نخستِ assertionهای REST در سوئیت من یک نقص **D** بود (انتظار 404 از مرز) و در `7b4a801` تصحیح شد — قرارداد 404 parity در سطح **سرویس** (و مسیر wp-admin) assert می‌شود. (۲) گیت WPCS روی خطوط >۱۰۰ کاراکتر حساس نیست: PR #74 (Slice 5) ۲۵ خط بالای ۱۰۰ بایت (تا ۴۶۰) افزود و `WPCS (changed code)` = SUCCESS (run `35305875331` / job `105477847882`).
- **RED معتبر (TDD، فقط تست):** commit `248fe90` → CI run **`35308554603`**، job Integration `105485705953` — `FAILURES! Tests: 978, Assertions: 13498, Failures: 9` — دقیقاً ۹ نقص کلاس **B** موردانتظار: R1 (leave گره‌خورده به A2، A1 بسته می‌شد)، R2 (blocked گره‌خورده)، R3 (open_override گره‌خورده)، R5 (Location بیگانه بی‌صدا نادیده + نوشتن ردیف)، R6 (ناموجود)، R7 (غیرفعال)، R8 (نبود `location_id` در view)، R9 (فرم wp-admin بدون selector)، R9-write (انتخاب اپراتور ذخیره نمی‌شد). R4/R10/`testFixtureTopologyPreconditions`/قرارداد delete در همین run سبز بودند (نیمهٔ no-regression). لاگ job/artifact = **NOT RETRIEVED** (blob → EOF، محدودیت کلاس C) — شواهد از کامنت‌های PR #75 خوانده شد.
- **GREEN نهایی:** commit محصول **`9424937`** (۵ فایل، +۱۴۵/−۱۲) — `ScheduleService` (هلپر `optionalLocationIdForTrustedClinic` + درج + op-log + `exceptionView` با `location_id` nullable)، `ScheduleRepository` (`EXCEPTION_CREATE_FIELDS` += `location_id`)، `SlotsGenerateHandler` (SELECT دامنه‌مند + `(location_id IS NULL OR location_id = %d)`؛ `SlotGenerator` دست‌نخورده)، `ClinicianAdminPage` (selector + ستون محل + forward در handler)، `ScheduleController` (آرگومان اختیاری integer با اعمال قرارداد در Service). سپس commit **`7b4a801`** (تصحیح D تست + بلوک additive شواهد JUnit).
  - run میانی `35308906265` @ `9424937`: `Tests: 978, Assertions: 13539, Failures: 3` — هر سه، همان نقص D سمت تست (نه محصول): ۴۰۳ مرز در برابر انتظار ۴۰۴.
  - **run نهایی CI `35309183691` @ `7b4a801` = completed success**؛ completion-guard: **`OK (978 tests, 13564 assertions)`**؛ JUnit root: `errors=0 warnings=0 failures=0 skipped=0`. شواهد نام‌دار (کامنت PR همان run): سوئیت جدید `Phase6ScheduleExceptionLocationTest` **13 تست / 452 assertion / 0 failure** (هر ۱۳ testcase نام‌شده) + رگرسیون‌ها همه ۰ خطا: `C7PreIntegrationBoundaryTest` 22/359 · `C7ScheduleObjectIdIsolationTest` 3/42 · `Phase4Slice1ProfessionalMultiClinicParticipationTest` 8/228 · `Phase6ScheduleExplicitLocationTest` 9/264 · `Phase6ScheduleMultiShiftTest` 11/326 · `RestScheduleTest` 8/67 · `ScheduleRegenerationClinicScopeRedTest` 2/41 · `SchedulingLocationLocalBoundaryRedTest` 9/295 · `SlotsGenerateLocationLocalTemporalRedTest` 2/82 · `SlotsGenerateM2WiringRedTest` 1/43. سایر jobهای همان run: Tenant Tripwire · Unit 8.1/8.2/8.3/8.4 · PHPStan · WPCS — همه success.
- **Indexability (کد، بدون اجرای EXPLAIN):** query استثنا همچنان با prefix `(clinician_id, date)` روی `idx_sched_exc (clinician_id,date,type)` می‌رود؛ `clinic_id` از قبل residual filter بود و شرط جدید `(location_id IS NULL OR location_id = ?)` روی همان مجموعهٔ باریک‌شده ارزیابی می‌شود ⇒ مسیر دسترسی تغییر نمی‌کند و **index جدید توجیه ندارد**. EXPLAIN واقعی = NOT RUN (MySQL در sandbox نیست).
- **محدودیت‌ها:** بدون migration/schema/جدول/ایندکس/سرویس/وابستگی جدید؛ بدون تغییر قرارداد Slices 3–5، کلیدهای uniqueness، pricing/booking/visit؛ `SlotGenerator` بدون تغییر معنایی؛ `listExceptions()` (Clinic-scoped) دست‌نخورده؛ `docs/api/api-contract.md` عمداً دست‌نخورده ماند (خارج از فهرست مجاز این برش) — به‌عنوان follow-up مستند شد؛ تست‌ها خارج از scope گیت WPCS.
- **Git/PR:** forward-only: `248fe90` (تست RED) → `9424937` (GREEN محصول) → `7b4a801` (تصحیح D + شواهد CI) → همین entry (docs). Draft PR **#75** باز است (draft؛ بدون Ready/merge/force-push/reset/rebase/tag/release). Entryهای قبلی append-only و دست‌نخورده.
- **تحویل به ایجنت بعد:** برش بعدی Phase 6 = دامنهٔ Location برای سایر مصرف‌کننده‌های `location_id` که هنوز NULL-محورند (نمونه: `services`/`invoices`/`payments`/`settings` که در 0015 ستون گرفتند ولی هیچ‌کدام هنوز خوانده نمی‌شوند) — و در صورت لزوم هم‌راستاسازی پیام مرز REST با پاکت سرویس (تصمیم محصولی، خارج از این برش).

### [2026-09-18 10:40 UTC] — ایجنت Arena (شاخهٔ `arena/01a0b3fe-doctor`، Draft PR) — Phase 6 documentation closure (bounded، فقط مستندات)
- **فاز/محدوده:** فقط آشتی‌دهیِ مستندات کانونی با وضعیت واقعی Phase 6 پس از ادغام Slice 7. **بدون هیچ تغییر `src/` / تست / workflow / migration / schema / runtime configuration؛ `ScheduleController.php` دست‌نخورده؛ بدون ساخت یا رزرو `0021`؛ بدون فایل گزارش جدید در ریشه.**
- **LIVE REVERIFY پیش از کار (VERIFIED FACT):** repo root = `/home/user/doctor` · شاخهٔ نشست = `arena/01a0b3fe-doctor` · local HEAD = `bd5e6a1819a838648dbdcbc6914c0d8b24bba38b` = `origin/main` · working tree clean (بدون untracked) · open PRs = `0` · آخرین migration = `0020` (`clinic-practice-management/src/Migrations/` = ۲۰ فایل `0001`..`0020` + `MigrationRunner.php`؛ `0021` وجود ندارد). این دقیقاً همان نقطهٔ شروعِ موردانتظار مدیر بود ⇒ STOP لازم نشد.
- **راستی‌آزمایی مستقلِ شواهد Slice 7 (GitHub API، نه حافظهٔ چت):** PR #76 = MERGED 2026-09-18T09:59:44Z، merge = `bd5e6a1819a838648dbdcbc6914c0d8b24bba38b`، head = `bd840163…`، والدین `fc0a598e…` + `bd840163…` · check-runهای merge SHA = **19/19 `completed`/`success`** · چهار workflow روی merge SHA همه `completed`/`success`: CI `35332404611` · Real WP `35332404609` · Pilot/Staging `35332404592` · Closure `35332404644` · exact-head `bd840163` = 19/19 success و Integration run `35329496603` = `OK (983 tests, 13851 assertions)` با `Phase6ScheduleShiftConcurrencyRedTest` = ۵ تست / ۲۹۰ assertion / ۰ شکست.
- **VALID RED پذیرفته‌شده (HISTORICAL EVIDENCE، بازخوانی‌شده):** `fca63d3a21a7918c950d2cf07bf43343fe0c20ef` — CI run `35327220676` = `completed/failure` با `Tests: 983, Assertions: 13622, Failures: 3`: case A (دو ردیف فعال همپوشان `08:00-12:00` و `10:00-14:00` باقی ماند)، case C و case D (دو ردیف ACTIVE همپوشان پس از race) و positive control E سبز ⇒ سیگنال محصول، نه نقص harness.
- **تلاش‌های INVALID RED (بدون بازنویسی):** `091fae1c5c59142d7bc4d60aeb2ae8954e52ed28` — run `35318716174`: `ParseError: unexpected identifier "START"` و **صفر تست اجراشده** (junit موجود نیست) ⇒ INVALID. `c8b9592056307fea8213d131e135131198508674` — run `35320273122`: `Tests: 983 … Failures: 3` اما هر سه شکست **غیرمحصولی** بودند (`no child may crash or surface a raw (non-product) failure: … Undefined array key "id"` در A/B + شکستِ positive control E با یک ردیف) ⇒ سیگنال معتبر محصول نیست و **INVALID باقی می‌ماند**.
- **استثناهای ثبت‌شده (بدون پنهان‌سازی):** (۱) **فرآیندی Slice 7:** ایجنت write پس از VALID RED باید متوقف می‌شد اما پیش از مجوز مدیر وارد GREEN (`0202a517…`) شد — ثبت به‌عنوان استثنای تاریخیِ انضباط فرآیند/شواهد، **غیربلاکر**، و بدون اثر بر اعتبار شواهد نهاییِ مستقلِ پذیرفته‌شده. (۲) **Slice 5:** کامیت تست‌تنهای `bd9351e75407ec0449204733d3b5894fd7d33aa8` در همان SHA **`total_count = 0` check run** دارد (راستی‌آزمایی‌شده با API) ⇒ RED گذشته‌نگر ساخته نشد؛ فقط استثنای تاریخی شواهد. (۳) **main قبلی `fc0a598e739d6e4951de3167bbc4509e0d9c5378`:** ۱۸ success + **۱ cancelled** (`Responsive smoke`؛ Pilot/Staging run `35310058979` = `cancelled`) ⇒ **هرگز PASS نبود** و با شواهد موفقِ بعدی روی `bd5e6a18` تضاد ندارد.
- **بازبینی کد برای دو قلم hardening (VERIFIED FACT، بدون تغییر کد):** **ج** — `ScheduleService::requireClinicianWithinTrustedClinic()` وقتی `explicitTrustedClinicId()` (یعنی `ScopeContext::tryGet()`) تهی است فقط `requireClinician()` را صدا می‌زند؛ ولی مسیرهای تولیدی wp-admin پیش از سرویس Scope صریح برقرار می‌کنند (`ClinicianAdminPage::saveSchedules()` → `authorizeAdminWrite()` → `App::replaceExplicitScope($scope)`) و مرز REST جداگانه scope می‌شود ⇒ آن شاخهٔ legacy از این مسیرها قابل دسترس نیست و `clinicians.clinic_id` مرجع tenant نیست ⇒ **deferrable hardening، نه بلاکر امنیتی فعلی production**؛ حفظ برای اسلایس hardening برنامه‌ریزی‌شدهٔ C7. **د** — در `SlotsGenerateHandler` مسیر settings (`horizonForClinic()`) `1..365` را clamp می‌کند ولی مسیر payload فقط `<= 0` را رد می‌کند (بدون سقف بالایی)؛ هیچ producer تولیدی `horizon_days` را ست نمی‌کند و مسیر payload خارجی راستی‌آزمایی نشد ⇒ **deferrable hardening** با clamp سقف بالایی در C7 — **در این slice پیاده‌سازی نشد**.
- **قلم الف (فقط ثبت، بدون ویرایش منبع):** کامنت مسیر استثنا در `src/Rest/ScheduleController.php` (~سطر ۱۳۱) «۴۰۴ محل یافت نشد» را توصیف می‌کند، درحالی‌که مرز scope در حال اجرا — `RestClinicContext::extractIds` + `TrustedClinicEstablisher::verifiedScope` — برای Location بیگانه/ناموجود/غیرفعال `403 CLINIC_SCOPE_UNAVAILABLE` با `reason=location` پیش از رسیدن به سرویس برمی‌گرداند. **رفتار محصول درست است**؛ این فقط بدهی کامنت است و اصلاحش به نخستین برش محصولیِ مجازی که آن فایل را لمس کند موکول شد.
- **پوشش نیازمندی‌ها:** `FR-3.1`، `FR-3.2`، `FR-3.3`، `FR-3.4`، `FR-3.6`، `FR-3.7`، `FR-3.8`، `FR-3.9`، `FR-3.10` حفظ شد؛ **`FR-3.5` (تقویم/Slotهای آزاد پیش از Login) متعلق به Phase 8 است و به‌عنوان پوشش Phase 6 ادعا نشد.**
- **فایل‌های تغییریافته (فقط `docs/`):** `docs/roadmap/roadmap.md` (سطر Phase 6) · `docs/project-current-state.md` (بلوک checkpoint جدید + سطر §C + پاراگراف checkpoint + راستی‌آزمایی schema) · `docs/governance/project-phase-taxonomy.md` (سطر Phase 6 + bullet ‏live main) · `docs/drift-register.md` (یادداشت بستن Phase 6 + سطر نگاشت فاز مالک + O-03/O-04 که **باز می‌مانند**) · `docs/README.md` (بلوک به‌روزرسانی 2026-09-18) · `docs/api/api-contract.md` (اصلاح دو عبارت کهنهٔ G1/G1b: multi-shift اکنون فعال است و پاکت واقعی مرز REST) · `docs/agent-guide.md` (همین entry + سطر وضعیت فاز).
- **CI:** اجرای محلی PHPUnit/PHPStan/WPCS = **NOT RUN** (PHP/Composer/MySQL در sandbox نیستند)؛ هیچ شواهد runtime جدیدی ساخته/ادعا نشد. شواهد ثبت‌شده در بالا همه **بازخوانیِ GitHub API** از اجراهای تاریخیِ موجودند. exact-head checks همین PR مستندات = پس از push در گزارش تحویل ثبت می‌شود.
- **Git/PR:** forward-only؛ بدون `reset --hard` / `clean` / `rebase` / force-push / history rewrite؛ فقط یک Draft PR به `main` (بدون merge/Ready/tag/release). entryهای قبلی **append-only و دست‌نخورده**.
- **موارد باز/تحویل به ایجنت بعد:** (۱) اسلایس hardening برنامه‌ریزی‌شدهٔ C7 = قلم‌های ج + د (clamp سقف بالایی `horizon_days`)؛ (۲) اصلاح کامنت `ScheduleController.php` در نخستین برش محصولی که آن فایل را لمس می‌کند؛ (۳) ردیف‌های drift ‏O-03/O-04 باز می‌مانند؛ (۴) `CHANGELOG.md` از 2026-09-12 به‌روز نشده (بدهیِ پیش‌موجود — بستن‌های Phase 3/4/5 هم در آن ثبت نشده‌اند) و در این closure دست‌نخورده ماند.
- **وضعیت tree:** پس از کامیت = clean.

### [2026-09-18 11:35 UTC] — تکمیل entry ‏Phase 6 documentation closure (شواهد exact-head همین PR مستندات)
- **هدف:** تکمیلِ (نه ویرایشِ) entry قبلی — ثبت نتیجهٔ واقعی گیت‌ها روی head خودِ این PR مستندات، طبق قاعدهٔ append-only.
- **head دقیق:** `8d886cefe5a3512333be8ed0c45f6af2715737f3` (شاخهٔ `arena/01a0b3fe-doctor`، Draft PR **#77**، base = `main`). والد = `bd5e6a1819a838648dbdcbc6914c0d8b24bba38b` (= `origin/main` در زمان شروع).
- **CI (VERIFIED FACT، بازخوانی GitHub API):** روی همان head **۱۹/۱۹ check run = `completed`/`success`** (بدون pending/cancelled/failure)؛ چهار workflow: CI `35335151338` (`pull_request`) · Real WordPress Acceptance `35335151457` (`pull_request`) · Closure Gate `35335078179` (`push`) · Pilot/Staging Readiness Gate `35335078346` (`push`) — همه `completed`/`success`. `WPCS (changed code)` = success (هیچ فایل PHP تغییر نکرد).
- **محدوده (بازبینی کامل diff):** `git diff --name-only origin/main..HEAD` = فقط ۷ فایل زیر `docs/`؛ **۰** فایل خارج از `docs/`. بدون تغییر `src/`/tests/workflows/migrations/schema/runtime configuration؛ `ScheduleController.php` دست‌نخورده؛ `0021` ساخته/رزرو نشد.
- **اجرای محلی:** PHPUnit/PHPStan/WPCS محلی = **NOT RUN** (PHP/Composer/MySQL در sandbox نیستند)؛ هیچ شواهد runtime جدیدی ادعا نشد. یک لینک نسبیِ شکسته در `docs/project-current-state.md` (`](phase2-state.md)` ← مسیر درست `phase-reports/phase2-state.md`) **از قبل روی `main` وجود داشت** و عمداً دست‌نخورده ماند (خارج از دامنهٔ این closure).
- **وضعیت tree:** پس از کامیت = clean. Draft PR **#77** بدون Ready/merge باقی می‌ماند (پذیرش مستقل).

### [2026-09-18 12:20 UTC] — تصحیحِ محدود: انتسابِ شکست‌های `c8b95920` (‏supersede؛ بدون ویرایشِ entry منتشرشده)
- **قاعدهٔ حاکم (بازخوانی زندهٔ §9 همین فایل):** «**Append-only** — ورودی قبلی را هرگز ویرایش/حذف نکن». بنابراین entry منتشرشدهٔ `[2026-09-18 10:40 UTC]` **دست‌نخورده** می‌ماند و این entry **فقط** همان یک انتساب را supersede می‌کند؛ هیچ محتوای دیگری از closure فاز ۶ تغییر نمی‌کند.
- **عبارتِ فراتر از شاهد در entry قبلی:** «هر سه شکست **غیرمحصولی** بودند». این یک **استنتاج** بود، نه شاهدِ بازیابی‌شده؛ عبارتِ `(non-product)` که در همان entry نقل شد، **برچسبِ assertion خودِ تست** است (`no child may crash or surface a raw (non-product) failure`) و نه حکمِ داوری.
- **شواهدِ تأییدشده (بازخوانی کامنت PR #76 برای run `35320273122` روی `c8b9592056307fea8213d131e135131198508674`):** سوئیتِ موردنظر **اجرا شد** — `Tests: 983, Assertions: 13649, Failures: 3`؛ سه تستِ شکست‌خورده `testConcurrentOverlappingCreatesLeaveExactlyOneActiveRow` (A)، `testConcurrentExactDuplicateCreatesKeepStableContract` (B) و `testConcurrentNonOverlappingCreatesBothSucceed` (positive control نامتقاطع) بودند؛ خروجی‌های fatal کودکانِ fork‌شده حاوی `PHPUnit\Framework\Error\Warning: Undefined array key "id"` بود و positive control شکست خورد.
- **اثبات‌نشده (NOT RETRIEVED):** مبدأ/انتسابِ آن warning بازیابی **نشده** است. در نتیجه، این شکست‌ها **نمی‌توانستند** به‌طور معتبر به قراردادِ همزمانیِ **محصول** منتسب شوند. **نه نقصِ محصول ادعا می‌شود و نه نقصِ harness** — و در این تصحیح هیچ بررسیِ تازه‌ای برای کشفِ مبدأ انجام نشد.
- **نتیجه (بدون تضعیف):** `c8b9592056307fea8213d131e135131198508674` **INVALID RED باقی می‌ماند**. `091fae1c5c59142d7bc4d60aeb2ae8954e52ed28` (‏`ParseError`، صفر تستِ اجراشده) و VALID RED پذیرفته‌شدهٔ `fca63d3a21a7918c950d2cf07bf43343fe0c20ef` (run `35327220676`) **بدون تغییر** می‌مانند.
- **فایل‌های همین تصحیح (فقط `docs/`):** `docs/roadmap/roadmap.md`، `docs/project-current-state.md`، `docs/README.md` (اصلاحِ مستقیمِ عبارت — اسنادِ وضعیت/roadmap که §9 آن‌ها را append-only نکرده است) + `docs/agent-guide.md` (همین entry الحاقی). **بدون** تغییر `src/`/tests/workflows/migrations/schema/runtime configuration؛ آخرین migration همچنان `0020`.
- **وضعیت tree:** پس از کامیت = clean. Draft PR **#77** بدون Ready/merge.

### [2026-09-18 17:25 UTC] — ایجنت Arena (شاخهٔ `arena/01a0b557-doctor`، Draft PR #79) — Phase 7 Slice 1 / FR-4.6 — RED → GREEN (پیشنهاد جایگزین‌های CLINIC_SLOT_TAKEN)
- **فاز/محدوده:** آغاز Phase 7 Slice 1 — FR-4.6 (اگر Slot بین انتخاب و تأیید پر شود: پیام واضح + Slotهای نزدیک خالی). سیاستِ محصولیِ **Owner-issued** (نه متن خام SRS): روی باختِ `CLINIC_SLOT_TAKEN` در دو سایتِ پذیرفته‌شده، دادهٔ الحاقیِ `nearby_slots` — حداکثر ۵ جایگزینِ آزادِ همان تاریخِ محلی/همان Location/همان Clinic/همان Clinician از ردیفِ persisted اسلاتِ باخته، با ترتیب قطعیِ تثبیت‌شده، `[]` در نبودِ جایگزین، بدون تغییر کد/پیام/409، بدون فیلد اضافه؛ شکلِ entry همان شکلِ availability: `{time, capacity_left, duration_min, slot_id, location_id, date}`. کلیدِ `nearby_slots` پیش از این روی main وجود نداشت (grep: صفر) و طبق اصطلاح FR-4.6/شفاهیِ قرارداد انتخاب شد.
- **LIVE REVERIFY شروع:** repo root `/home/user/doctor` · شاخهٔ `arena/01a0b557-doctor` @ `4b322d9dc8839e1efd66d712aedc06d9f22cd175` = `origin/main` · open PRs = 0 · آخرین migration = `0020` · tree clean.
- **RED (شواهد پذیرفته‌شدهٔ رکورد):** head `81d06619c63e23212c209bf8d30ff9ddd45dfaa6` — CI run `35370158310` = `completed/failure`، `Tests: 995, Assertions: 14160, Failures: 3` و هر سه شکست دقیقاً در نبودِ کلید `nearby_slots`: A) `testHoldRaceLossSuggestsUpToFiveSameDateSameLocationFreeAlternatives` (سایت واقعی `atomicHold` — پاکت/پیام/rollback سپس نبودِ کلید)؛ B) `testConfirmFinalAtomicClaimLossSuggestsAlternativesFromPersistedLosingSlot` (مسیر واقعی `confirm()` به سایتِ `atomicClaim` — پیام اختصاصیِ سایت «اسلات در لحظه نهایی پر شد» + hold→`released` سپس نبودِ کلید)؛ C) `testHoldRaceLossWithNoEligibleAlternativeYieldsEmptyAlternativesList`. A/B/C = VALID RED. **صداقت دربارهٔ B:** fixture ترتیبیِ قطعی است (hold فعال + `held_count=0` توسط یک UPDATE مستقیم — همان وضعیت گذرای race واقعی confirm-vs-expiry)؛ این تست شاخهٔ واقعیِ نهاییِ `atomicClaim` را در runtime اجرا می‌کند اما **بازتولیدِ runtime یک race هم‌زمان واقعی نیست** — همین‌طور گزارش شد، بیش از این ادعا نشد.
- **تلاش تاریخی (بدون بازنویسی):** `b690e6c550f726a0329da51fbd37519e307f5c5e` — run `35369425482`: B VALID RED (شکست در نبودِ کلید)؛ A و C به‌دلیل خطای محاسبهٔ fixture در خودِ تست (شمارشِ «free» پیش از holdِ برنده → `7≠6` / `1≠0`) = **INVALID RED کلاس D**؛ با کامیتِ تست-تنهای `81d0661` اصلاح شد (شمارشِ eligibility در لحظهٔ باخت). نویزِ ازپیش‌موجودِ `wptests_cpms_medical_files` در لاگ Integration از fixture های `VisitNoShow*` روی main است (نام واقعی جدول `cpms_medical_attachments`) — از fixture این برش حذف شد.
- **GREEN (کامیت محصول):** `dfb37d41eba75c8543abea574f18858370aacf0d` — فقط `src/Application/Booking/BookingService.php` (۸۸+/۲−): (۱) سایتِ hold: همان throw + دادهٔ `nearby_slots` از `$slot` (ردیف persisted همان مسیر)؛ (۲) سایتِ `atomicClaim` در confirm: همان پیامِ اختصاصی + `nearby_slots` از ردیفِ `FOR UPDATE` که مسیر واقعی به آن رسیده؛ (۳) متد خصوصیِ `nearbySlotsForLosingSlot()`: فقط زمینهٔ trusted ردیفِ باخته (clinic/clinician/location/تاریخ محلی)؛ بازاستفادهٔ `SlotRepository::availabilityCandidates()` با پنجرهٔ تک‌تاریخ `[date, date]` (is_open=1 + ظرفیت آزاد واقعی + ترتیب قطعی) سپس فیلترِ همان-Location + حذفِ خودِ اسلاتِ باخته + `filterNotLocallyPast` (fail-closed) + سقف ۵ — همه پیش از ساختِ دادهٔ خطا؛ نبودِ زمینه → `[]` بدون حدس؛ هر خطای غیرمنتظره → `[]` (پاکتِ CLINIC_SLOT_TAKEN هرگز به خطای دیگر تبدیل نمی‌شود؛ قرارداد wpdb: خطای query = نتیجهٔ خالی)؛ فقط-خواندنی (هیچ mutation). روش جدید Repository/abstraction ساخته نشد. RED دست‌نخورده (diff صفر مقابل `81d0661`)؛ بدون schema/migration (آخرین = `0020`)؛ بدون تغییر CI-gate.
- **CI (VERIFIED FACT):** RED `81d0661` → run `35370158310` = failure با فقط ۳ شکستِ نام‌برده‌شده (بقیهٔ ۷ job سبز). GREEN `dfb37d4` → run `35371699567` = **`success`** (هر ۸ job: Tripwire/Unit 8.1–8.4/PHPStan/WPCS/Integration) + سه workflow دیگر روی همان head همه `success`: Real WP `35371699587` · Closure `35371694729` · Pilot/Staging `35371694688`. جمعِ Integration: `OK (995 tests, 14175 assertions)`، ۰ failure/error/warning/skip. **شواهد per-test junit برای سوئیتِ نام‌برده بازیابی نشد** (دانلود artifact از sandbox مسدود — blob EOF)؛ استناد = فهرستِ شکست‌های نام‌دار در run RED (هویت سوئیت) + `OK` کلِ همان ۹۹۵ تست در run GREEN + دلتای ۱۵+ assertion (تکمیلِ همان ۳ تست).
- **تصمیمات درون‌فازی:** کلیدِ `nearby_slots` طبق راهنمای مدیر؛ بدون primitive جدید Repository (primitive موجود با پنجرهٔ تک‌تاریخ کفایت می‌کند)؛ degrade به `[]` مطابق «پیشنهاد ثانویه است»؛ بدون fallback روز بعد/Location دیگر؛ CHANGELOG دست‌نخورده (عرف ریپو برای برش‌ها).
- **موارد باز/تحویل به ایجنت بعد (بدهی مستندات برای بستنِ Slice — انجام نشده در این برش طبق دستور):** آشتی‌دهیِ اسناد: C7 ج/د اکنون پیاده‌سازی‌شده‌اند؛ `bin/cpms slots generate --days=N` تولیدکنندهٔ تأییدشدهٔ `horizon_days` است؛ اسناد کهنهٔ خلاف این نیاز به اصلاح دارند (entryهای append-only منتشرشده بازنویسی نمی‌شوند)؛ همچنین مکانیزمِ نمایش `nearby_slots` در UI (خارج از مرز سرویس) و فراخوانیِ REST-level test در صورت نیازِ قرارداد API.
- **وضعیت tree:** clean پس از هر کامیت. Draft PR **#79** بدون Ready/merge/force-push/reset/rebase — forward-only: `b690e6c` → `81d0661` → `dfb37d4` → همین entry.

### [2026-09-18 19:25 UTC] — ایجنت Arena (شاخهٔ `arena/01a0b5c1-doctor`) — closure-only مستندات: C7 hardening + Phase 7 Slice 1 / FR-4.6 (آشتی‌دهیِ اسناد؛ بدون تغییر محصول)
- **فاز/محدوده:** بستنِ فقط-مستندات (docs-only) برای (۱) کارِ C7 hardeningِ ادغام‌شده (PR #78) و (۲) Phase 7 Slice 1 / FR-4.6 (PR #79): آشتی‌دهیِ عباراتِ کهنهٔ **جاری** در اسناد وضعیت/roadmap + همین entryِ append-only. هیچ کد/تست/workflow/migration/schema/runtime configuration تغییر نکرد؛ آخرین migration = `0020` (بدون 0021).
- **LIVE REVERIFY شروع (VERIFIED FACT):** repo root = `/home/user/doctor` · local branch = `arena/01a0b5c1-doctor` @ `757d7424332d87cdd3d1894ee39f9f5f01bf0a33` = `origin/main` (fetch زنده) · open PRs = ۰ · آخرین migration = `2026_09_09_0020_idempotency_clinic_scope.php` · tree clean (بدون untracked).
- **شواهدِ راستی‌آزمایی‌شده (GitHub API، نه حافظهٔ چت):** PR #78 = MERGED 2026-09-18T15:07:23Z، merge = `4b322d9dc8839e1efd66d712aedc06d9f22cd175`؛ post-merge روی همان merge SHA: هر ۴ workflow لازم terminal-success (CI `35360479404` · Real WP `35360479402` · Pilot/Staging `35360479660` · Closure `35360479487`) + **۱۹/۱۹ check run success**. PR #79 = MERGED 2026-09-18T18:12:54Z، merge = `757d7424332d87cdd3d1894ee39f9f5f01bf0a33`؛ post-merge روی همان merge SHA: CI `35378860137` · Closure Gate `35378860056` · Real WP Acceptance `35378860138` · Pilot/Staging `35378860185` — همه success + **۱۹/۱۹ check run success**. RED: `81d06619c63e23212c209bf8d30ff9ddd45dfaa6` → CI run `35370158310` = failure (A/B/C = VALID RED پذیرفته‌شده). تلاش تاریخی: `b690e6c550f726a0329da51fbd37519e307f5c5e` → CI run `35369425482` (B در آن تلاش valid؛ A/C = INVALID RED کلاس D به‌دلیل محاسبهٔ fixture — بازنویسی نشد). GREEN محصول: `dfb37d41eba75c8543abea574f18858370aacf0d`.
- **بازبینی درایِ درخت (بدون تغییر کد):** (ج) `list()`/`listExceptions()` در `requireClinicianWithinTrustedClinic()` بدون Scope صریحِ معتبر fail-closed‌اند (`CLINIC_NOT_FOUND`/404؛ کوئریِ بدون دامانه اجرا نمی‌شود)؛ (د) `SlotsGenerateHandler`: `horizon_days` عددی خارج از `1..365` ⇒ warning `SLOTS_GEN_SKIP_INVALID_HORIZON` + skip هر-پزشک (fail-closed؛ sweep ادامه می‌یابد)؛ `ScheduleService::regenerate()` فقط `['source' => 'manual']` enqueue می‌کند (بدون horizon_days)؛ `App::scheduleRecurringJobs()` payload تهی (بدون horizon_days)؛ **`bin/cpms slots generate --days=N` مقدار را می‌گذارد** (producer واقعیِ CLI اپراتور/سرور)؛ مسیر job-enqueue خارجیِ REST راستی‌آزمایی نشده است (ادعای سراسری ساخته نشد)؛ `ScheduleController.php` در PR #78 لمس نشد (بدهیِ کامنتِ الف پابرجاست).
- **تصمیمات درون‌فازی:** (۱) سیاستِ `nearby_slots` (حداکثر ۵ / همان تاریخ محلی / همان Location باخته / همان Clinic+clinician از زمینهٔ persisted اسلات باخته / ترتیب قطعی / `[]` در نبودِ واجد شرایط / کد-پیام-409 دست‌نخورده) **سیاستِ محصولیِ Owner-issued** است — **متمایز از متن اصلی SRSِ FR-4.6** و در هیچ سندی به‌عنوان عبارت SRS ثبت نشد. (۲) fixture قطعیِ B شاخهٔ واقعیِ `confirm()`/`atomicClaim` را در runtime اجرا می‌کند اما **بازتولیدِ runtimeِ race همزمان واقعی نیست** — اسناد تازه دقیقاً به همین شکل ثبت شد. (۳) C7 ج/د = CLOSED (PR #78)؛ شاخه‌های سازگاریِ no-scopeٔ باقی‌ماندهٔ نوشتن‌های موجودیت (`requireSchedule/requireExceptionForTrustedClinic`) پس از بازسازیِ جداگانهٔ post-PR#78 = **NO CHANGE JUSTIFIED** (فراخوان‌های نوشتنِ production REST/wp-admin Scope صریح برقرار می‌کنند؛ هیچ فراخوانِ فعلیِ src/bin/job بدون Scope یافت نشد؛ مصرف‌کنندهٔ سازگاریِ VERIFIED وابسته به شاخه نیست) — **نه patchِ C7ِ دیگری الزامی**. (۴) Phase 7 = STARTED/IN PROGRESS؛ Slice 1 / FR-4.6 = CLOSED (BOUNDED)؛ **Phase 7 کامل بسته نیست؛ هیچ FR-4.xِ دیگر بسته ادعا نمی‌شود؛ Slice 2 ساخته/ادعا نشد.**
- **تکمیل (نه ویرایش) entryهای پیشین:** بدهیِ مستنداتِ تحویل‌شده از entryهای `[2026-09-18 10:40 UTC]` (خروجیِ «اسلایس hardening برنامه‌ریزی‌شدهٔ C7 = قلم‌های ج + د») و `[2026-09-18 17:25 UTC]` (خروجیِ «آشتی‌دهیِ اسناد: C7 ج/د اکنون پیاده‌سازی‌شده‌اند … اسناد کهنهٔ خلاف این نیاز به اصلاح دارند») در همین closure انجام شد؛ entryهای منتشرشده **append-only و دست‌نخورده** ماندند و این entry فقط supersede/تکمیل می‌کند، بازنویسی نمی‌کند.
- **فایل‌های تغییرکرده (فقط `docs/`):** `docs/roadmap/roadmap.md` (ردیف Phase 6: عبارتِ hardening کهنه ← وضعیتِ C7-closed + واقعیتِ producerها؛ ردیف Phase 7: ⏳ ← STARTED/Slice 1 CLOSED) · `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `757d7424` + demoteِ بلوکِ Phase 6 به historical + نوتِ supersede روی عبارتِ «Deferred hardening» + ردیفِ Phase 7 در جدول §C + بندِ «Integrated main checkpoint» + بندِ Schema) · `docs/README.md` (نوتِ supersede روی جملهٔ hardeningِ بلوکِ Phase 6 + بلوکِ تازهٔ «به‌روزرسانی 2026-09-18 (C7 hardening + Phase 7 Slice 1 / FR-4.6)») · `docs/drift-register.md` (ردیفِ Schedule + نوتِ supersede در Phase 6 closure note + نوتِ تازهٔ C7/Phase 7) · `docs/governance/project-phase-taxonomy.md` (پینِ current live main + جملهٔ C7 + ردیفِ crosswalkِ Phase 7) · `docs/agent-guide.md` (ردیف‌های Phase 6/7 در جدول + همین entry). **بدون** تغییر `src/`/tests/workflows/migrations/schema/runtime configuration؛ `CHANGELOG.md` دست‌نخورده (عرفِ ریپو + بدهیِ ثبت‌شدهٔ پیش‌موجود).
- **Git/PR:** forward-only روی `arena/01a0b5c1-doctor` (شاخهٔ همین نشست، که روی authoritative main بود)؛ بدون `reset --hard`/`clean`/rebase/force-push/history rewrite. **یک Draft PR** به `main` (بدون Ready/merge/tag/release).
- **شواهدِ بازیابی‌نشده (NOT RETRIEVED):** PHP/Composer/MySQL در sandbox نیستند ⇒ هیچ اجرای محلیِ PHPUnit/`php -l`/tripwire ادعا نشد؛ همهٔ شواهدِ اجرایی از GitHub Actions/Remote است.
- **وضعیت tree:** در زمان ثبتِ این entry، تغییر فقط همین اسنادِ docs-only است؛ پس از commit باید clean شود. وضعیتِ Gateهای exact-head برای کامیتِ این docs پس از push از GitHub بازیابی می‌شود (PENDING — نباید با شواهدِ pre-log اشتباه گرفته شود).

### [2026-09-19 03:23 UTC] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0b7ac-doctor`) — closure-only مستندات: Phase 7 Slice 2 (آشتی‌دهیِ اسناد پس از بستنِ فنیِ PR #81؛ بدون تغییر محصول)
- **هویت ایجنت:** این نشست یک ایجنت نوشتن/گیت‌هابِ **جدید** است — نه ایجنتِ ادغام‌کنندهٔ PR #81 و نه هیچ ایجنتِ بازنشستهٔ پیشین. شاخهٔ نشست = `arena/01a0b7ac-doctor` (جدید؛ شاخهٔ `arena/01a0b642-doctor` متعلق به کارِ فنیِ PR #81 است و لمس نشد).
- **فاز/محدوده:** بستنِ فقط-مستندات (docs-only) برای **Phase 7 Slice 2** (کارِ فنی ادغام‌شده با **PR #81**): آشتی‌دهیِ عباراتِ کهنهٔ **جاری** در اسناد وضعیت/roadmap/governance/agent-guide + همین entryِ append-only. **هیچ کد/تست/workflow/migration/schema/runtime configuration تغییر نکرد**؛ آخرین migration = `0020` (بدون 0021). بدون تغییرِ `CHANGELOG.md` (عرف ریپو)، بدون پاک‌سازیِ لینک/آرایشی، بدون دست‌زدن به مشاهدهٔ ازپیش‌موجودِ کهنهٔ اشاره‌گرِ T8/no-show در این کارِ مستندات.
- **LIVE REVERIFY شروع (VERIFIED FACT — GitHub/Git زنده، نه حافظهٔ چت):** `origin/main = e60c62428e6109e7182d04d3266f0d15b12b0d2f` (مطابق انتظار؛ = merge **PR #81**، MERGED 2026-09-19T03:08:27Z) · open PRs = **۰** · working tree در شروع = **clean** (بدون untracked) · آخرین migration = `2026_09_09_0020_idempotency_clinic_scope.php` (۲۰ فایل `0001..0020`؛ `0021` وجود ندارد) · هیچ PRِ documentation-closureِ دیگری باز نبود ⇒ تکراری ساخته **نشد**.
- **شواهدِ راستی‌آزمایی‌شده (GitHub API):** PR #81 = MERGED؛ merge = `e60c62428e6109e7182d04d3266f0d15b12b0d2f`. **post-merge روی merge SHA:** هر ۴ workflow لازم `completed/success` — CI `35417703302` · Real WordPress Acceptance `35417703278` · Closure Gate `35417703283` · Pilot/Staging Readiness Gate `35417703280` — و **۱۹/۱۹ check run موفق** روی همان merge SHA (بازیابی زنده). **تاریخچهٔ شواهد:** پذیرفته‌شده‌ی نهاییِ RED = `29bd8c36a263fc25b62a0f29554a7ce3224415a3` («test(phase7-slice2): RED hygiene + explicit concurrency evidence classification») — REDِ دقیق‌سر: CI run `35398347887` = `completed/failure` در همان head (check runهای همان SHA: ۱۸ موفق + ۱ شکست)؛ کامیت محصول GREEN = `6c16e296d73eb6caf7271d49634de58ca0e6b1c6` («feat(phase7-slice2): GREEN — I-3 HAS_ACTIVE_VISIT + check-in serialization on the appointment row»).
- **حفظِ صادقانهٔ تاریخچهٔ فرآیند (بدون بازنویسی):** تلاشِ نخستِ RED `14319d0fd38dae9fd55ff36e13b301a658934c9d` دارای **شکست‌های جانبی ناشی از آلودگی صف تست** بود؛ آن‌ها **نقص‌های بهداشتیِ تست/هارنس** بودند و **پیش از RED نهاییِ پذیرفته‌شده اصلاح شدند**؛ آن تلاشِ نخست به‌عنوان شاهدِ پاک بازنویسی **نمی‌شود**.
- **واقعیتِ فنیِ ثبت‌شده در اسناد (درایِ درخت، بدون تغییر کد):** برش ۲ اینورتِ **I-3** را هنگام وجودِ **واقعاً فعالِ Visit** (ویزیتِ متصل در یکی از وضعیت‌های زندهٔ `ACTIVE_STATUSES` با `active = 1` — مبنای ثبت‌شده در `VisitMachine`) برای **T5 لغوی بیمار**، **T6 لغوی کارکنان** و **T7 جابه‌جایی** اعمال می‌کند: رد با **`HAS_ACTIVE_VISIT`** / **HTTP 409** (`BookingException::of(..., 409)` در `BookingService::assertNoActiveVisit()` — بررسی روی نوبتِ قفل‌شده، پیش از هر تغییر). **چک‌این با همان معماری قفلِ موجودِ ردیف نوبت در برابر این تغییرات سریال می‌شود.** اشاره‌گر کهنهٔ `active_visit_id` به‌تنهایی واقعاً فعال تلقی **نمی‌شود**. فایل‌های محصولِ ادغام‌شده با PR #81: `BookingService.php` · `VisitService.php` · `VisitMachine.php` + سوئیت Integration `Phase7Slice2ActiveVisitInvariantRedTest.php` (۸ تست، شامل دو تست همزمانیِ واقعی با `pcntl_fork`). هیچ تغییر migration/schema لازم نبود.
- **فایل‌های تغییرکرده (فقط `docs/`؛ شناسایی با جست‌وجوی زنده، نه فهرستِ ازپیش‌فرض):** `docs/project-current-state.md` (بلوک checkpoint تازهٔ `e60c6242` در صدر + تاریخی‌کردنِ بلوک `757d7424` + پاراگرافِ «This file describes…» + ردیف Phase 7 جدول §C + بندِ «Integrated main checkpoint» + الحاقِ راستی‌آزماییِ مجددِ `0020` در بند Schema) · `docs/roadmap/roadmap.md` (ردیف Phase 7 در §۰: افزودنِ بستنِ Slice 2 + «no Slice 3») · `docs/governance/project-phase-taxonomy.md` (ردیف crosswalkِ Phase 7 + پینِ «Current live main» روی `e60c6242`) · `docs/README.md` (annotation تاریخی روی بندِ 2026-09-18 + بلوکِ تازهٔ «به‌روزرسانی 2026-09-19») · `docs/drift-register.md` (نُتِ تازهٔ «Current Phase 7 Slice 2 closure note» — نُت‌های پیشین تاریخی و دست‌نخورده) · همین راهنما (ردیف Phase 7 در جدول وضعیت + همین entry). **entryهای منتشرشدهٔ پیشین در هیچ فایلی بازنویسی نشدند.**
- **موارد کهنهٔ عمداً دست‌نخورده:** (۱) عبارتِ «no Slice 2» در نُتِ بستنِ قبلیِ `drift-register.md` و بلوکِ 2026-09-18 `README` — متنِ تاریخیِ درست برای چک‌پوینتِ خود؛ فقط با نُتِ تازه/annotation جایگزین (نه بازنویسی) شد. (۲) اشاره‌گرِ کهنهٔ ازپیش‌موجودِ **T8/no-show** در `docs/state-machines/appointment.md` (ادعای شمولِ T8 در I-3) — به دستورِ صریح، خارج از دامنهٔ این کارِ مستندات. (۳) نبودِ ردیفِ `HAS_ACTIVE_VISIT` در `docs/api/error-codes.md` — سندِ قراردادِ API، خارج از جنسِ اسنادِ وضعیت/roadmap/governance/agent-guide؛ افزودنش در این بستنِ محدود لازم نبود. (۴) `CHANGELOG.md` — عرفِ ریپو برای برش‌ها (دست‌نخورده از بستن‌های پیشین).
- **تصمیمات درون‌فازی:** (۱) **Phase 7 = STARTED/IN PROGRESS می‌ماند**؛ فقط Slice 2 بستهٔ فنی (BOUNDED) اعلام شد؛ **هیچ ادعای تکمیلِ Phase 7 و هیچ Slice 3 ساخته/ادعا نشد.** (۲) ادعاهای بستنِ Slice 1 (PR #79) بدون تغییر حفظ شدند. (۳) تفکیکِ «شواهدِ پس‌از‌ادغام روی merge SHA» از «شواهدِ دقیق‌سر» رعایت شد؛ شواهدِ تاریخی جایگزین شواهدِ جاری نشد. (۴) هیچ گزارشِ زمانی (تایمینگ) ارائه نشد.
- **تکمیل (نه ویرایش) وضعیت‌های پیشین:** عبارت‌های «no Slice 2 exists or is claimed» در اسناد جاری با وضعیتِ جدید جایگزین شدند؛ عبارت‌های مشابه در متن‌های تاریخی (نُت‌های بستنِ پیشین) به‌صورت تاریخی حفظ شدند.
- **شواهدِ بازیابی‌نشده (صداقت):** اجرای محلیِ PHPUnit/PHPStan/WPCS/`php -l` = **NOT RUN** (PHP/Composer/MySQL در این sandbox نیستند)؛ هیچ شاهدِ محلی‌ای ادعا نشد. شواهدِ دقیق‌سرِ خودِ این کامیتِ مستندات = تا پیش از ساخت Draft PR **NOT RETRIEVED** و پس از push بازیابی می‌شود (باید با شواهدِ بالا که متعلق به `e60c6242` است اشتباه گرفته نشود).
- **Git/PR:** forward-only روی `arena/01a0b7ac-doctor`؛ بدون `reset --hard`/`clean`/rebase/force-push/history rewrite؛ **یک Draft PR** به `main`؛ بدون Ready/merge/tag/release. SHAی کامیت و شمارهٔ PR در گزارش تحویلِ همین نشست ثبت می‌شود.
- **وضعیت tree:** در زمان ثبتِ این entry، تغییر فقط همین اسنادِ `docs/` است؛ پس از commit باید clean شود.

### [2026-09-19 12:37 UTC] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0b998-doctor`) — closure-only مستندات: Phase 7 bounded closure (آشتی‌دهیِ اسناد پس از بستنِ فنیِ PR #83 + PR #85؛ بدون تغییر محصول)
- **هویت ایجنت:** این نشست یک ایجنت نوشتن/گیت‌هابِ **جدید** است — نه ایجنتِ ادغام‌کنندهٔ PR #85، PR #83 یا هیچ PRِ پیشین. شاخهٔ نشست = `arena/01a0b998-doctor` (روی authoritative main؛ شاخه‌های `arena/01a0b8a0-doctor` و `arena/01a0b8a0`/`arena/01a0b7f7-doctor` متعلق به کارِ فنیِ PR #85/#83 بودند و لمس نشدند).
- **فاز/محدوده:** بسته‌شدنِ **محدود (BOUNDED)** و فقط-مستندات (docs-only) برای **Owner Roadmap Phase 7 — Appointment Engine** بر روی کارِ ادغام‌شده: زیرساخت‌های موجودِ `FR-4.x` (slot holds با TTL + انقضای خودکار، idempotency، atomic claim) + **PR #79** (Slice 1 / FR-4.6) + **PR #81** (Slice 2 — I-3/چک‌این) + **PR #83** (FR-5.3 جابه‌جاییِ منشی/کارکنان) + **PR #85** (FR-5.5 no-show دستی + خودکار). آشتی‌دهیِ عباراتِ کهنهٔ **جاری** در اسناد وضعیت/roadmap/governance/README/drift/agent-guide + همین entryِ append-only. **هیچ کد/تست/workflow/migration/schema/runtime configuration تغییر نکرد**؛ آخرین migration = `0020` (بدون 0021). بدون تغییرِ `CHANGELOG.md` (عرفِ ریپو)، بدون پاک‌سازیِ لینک/آرایشی، بدون بازنویسیِ تاریخ، بدون ایجادِ برشِ محصولِ تازه.
- **LIVE REVERIFY شروع (VERIFIED FACT — Git/remote/PR زنده، نه حافظهٔ چت):** `origin/main = d7484ceddf698898483122cf52bb5abb55718744` (= merge **PR #85**، MERGED 2026-09-19T12:04:04Z؛ مطابق انتظارِ تاریخیِ درخواست — main به‌طور مادی تغییر نکرده بود) · open PRs = **۰** (هیچ PRِ closure/docsِ دیگری باز نبود ⇒ تکراری ساخته **نشد**) · working tree در شروع = **clean** (بدون untracked) · آخرین migration = `2026_09_09_0020_idempotency_clinic_scope.php` (۲۰ فایل `0001..0020`؛ `0021` وجود ندارد/رزرو نشده).
- **شواهدِ راستی‌آزمایی‌شده (GitHub API — بازیابی زنده):** PR #83 = MERGED 2026-09-19T07:19:56Z؛ merge = `cf1ace137922d84b7042841e68629aedfb8334a8`؛ head = `54612ab8fce4bf5d449dcdcb458dfb842964f1aa` (exact-head: **۱۹/۱۹ چک `success`**)؛ **post-merge روی merge SHA:** هر ۴ workflow لازم terminal-success — CI `35429028253` · Real WordPress Acceptance `35429028209` · Closure Gate `35429028230` · Pilot/Staging Readiness Gate `35429028233` — و **۱۹/۱۹ check run `success`**. PR #85 = MERGED 2026-09-19T12:04:04Z؛ merge = `d7484ceddf698898483122cf52bb5abb55718744` (= main جاری)؛ head = `2bbb6085c31e11e12e76a0d26a6b5a8589999c06` (exact-head: **۱۹/۱۹ چک `success`**)؛ **post-merge روی merge SHA:** CI `35441813653` · Real WordPress Acceptance `35441813668` · Closure Gate `35441813683` · Pilot/Staging Readiness Gate `35441813696` — همه terminal-success و **۱۹/۱۹ check run `success`**. PR #84 (merge `8c05c831aea6806038521bfb11fb6bcc5aa49cce`) = فقط CI/tooling (pinِ downloadِ release-zip به workflow run جاری). PR #79 (merge `757d7424…`) و PR #81 (merge `e60c6242…`) = شواهدِ پیشینِ ثبت‌شده، بدون تغییر.
- **واقعیتِ فنیِ ثبت‌شده (درایِ درخت، بدون تغییر کد):** (۱) **FR-5.3 (PR #83):** هستهٔ مشترکِ reschedule + REST `POST /appointments/{id}/reschedule` (namespace مرسومِ `clinic/v1`) با `cpms_appt_reschedule` و Clinic scope معتبر — confirmed → rescheduled + یک جایگزین + پیوند دوطرفه؛ شمارنده‌های slot؛ fail-closed در نبودِ authorization و بین-کلینیک؛ `HAS_ACTIVE_VISIT`/409 در برابر Visit واقعاً فعال (اشاره‌گر کهنه مسدود نمی‌کند)؛ Idempotency-Key الزامی/replay؛ audit + اعلانِ داخلی + SMS جابه‌جایی. **سیاستِ Owner-issued (نه عبارتِ اصلی SRSِ FR-5.3):** staff از مهلتِ ۲۴ ساعتهٔ بیمار و min-leadِ مقصدِ بیمار معاف است؛ جابه‌جاییِ موفقِ staff هم اعلانِ داخلی و هم SMS جابه‌جایی/تغییر می‌فرستد؛ سیاستِ بیمار (B5) دست‌نخورده. (۲) **FR-5.5 (PR #85):** سطح دستی REST `POST /appointments/{id}/no-show` از طریق nonce → قابلیت `cpms_appt_no_show` → Clinic scope صریحِ معتبر (دلیل الزامی؛ `no_show_at`؛ دلیل در audit `APPOINTMENT_NO_SHOW`؛ 403/404/403 fail-closed؛ Visit واقعاً فعال = 409 `HAS_ACTIVE_VISIT`؛ تکرار از terminal = 409) + sweep موجودِ خودکار پس از Grace Period (پیش‌فرض ۳۰ دقیقه، قابل تنظیم؛ timezone موقعیت = منبع زمانِ عملیاتی) + **اصلاحِ اشاره‌گر کهنه** (stale `active_visit_id` به Visitِ غیرفعال/terminal دیگر sweep را مسدود نمی‌کند و در تکمیلِ موفق پاک می‌شود؛ Visit واقعاً فعال همچنان مسدود می‌کند — I-3) + **سازگاریِ concurrency/ER-06** (چک‌این هرگز Visit واقعاً فعال را به نوبتِ terminalِ `no_show` متصل نمی‌کند — race واقعیِ test در سوئیتِ ادغام‌شده). فایل‌های محصولِ ادغام‌شده: PR #83 → `BookingService.php` + `BookingController.php` + سوئیت `Phase7SliceStaffRescheduleRedTest.php`؛ PR #85 → `BookingService.php` + `VisitService.php` + `VisitRepository.php` + `BookingController.php` + سوئیت `Phase7Fr55NoShowRedTest.php` (۱۲ تست). هیچ‌کدام بدون migration/schema. (۳) **FR-5.4 bounded non-claim:** SRSِ FR-5.4 از `pending` با TTL استفاده می‌کند و «تأیید پزشک» را **مثالِ توضیحی** است؛ معماریِ فعلی `pending` نوبت را persistent نمی‌کند — ظرفیتِ تأییدنشده = slot holds با TTL + انقضای خودکار (`cpms_slot_holds` + `HoldsExpireHandler`)؛ create کارکنان به‌عمد create→pending→confirm را در یک operation جمع می‌کند و مستقیم `confirmed` ذخیره می‌کند (کامنتِ N-2 در `BookingService.php`، راستی‌آزمایی‌شده) ⇒ **هیچ workflow تأییدِ پزشکی ادعا نمی‌شود** (ساخته‌نشده؛ بدون scope محصولیِ آیندهٔ مالک‌رهبری‌شده نباید اختراع شود)؛ **job انقضای نوبتِ persistent-pending وجود ندارد**.
- **صداقتِ شواهد:** عنوانِ PR #85 عبارتِ پیش‌از‌ادغام «DRAFT — TEST-ONLY RED» را حفظ کرده و تاریخِ Git بازنویسی **نشد**؛ اسناد دقیقاً همین را ثبت می‌کنند: حالتِ نهاییِ merged = پیاده‌سازیِ GREEN محصول (با ۱۹/۱۹ چک‌های merge SHA ثابت) و عبارتِ عنوان به‌عنوان وضعیتِ merged گزارش **نمی‌شود**. تفکیکِ «post-merge روی merge SHA» از «exact-head» در همه‌جا رعایت شد. هیچ گزارشِ زمانی (تایمینگ) ارائه نشد.
- **فایل‌های تغییرکرده (فقط `docs/`؛ شناسایی با جست‌وجوی زنده، نه فهرستِ ازپیش‌فرض):** `docs/project-current-state.md` (بلوکِ checkpoint تازهٔ `d7484ce` در صدر + تاریخی‌کردنِ بلوکِ `e60c6242` + پاراگرافِ «This file describes…» + ردیفِ Phase 7 جدول §C + بندِ «Integrated main checkpoint» + الحاقِ راستی‌آزماییِ مجددِ `0020` در بندِ Schema) · `docs/roadmap/roadmap.md` (ردیفِ Phase 7 در §۰: 🚧 ← ✅ **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** + ثبتِ بسته‌شدنِ PR #83/#85 + bounded non-claimِ FR-5.4) · `docs/governance/project-phase-taxonomy.md` (ردیفِ crosswalkِ Phase 7 + پینِ «Current live main» روی `d7484ce` + جملاتِ Phase 7 در همان bullet) · `docs/README.md` (annotation تاریخی روی بلوکِ 2026-09-19 Slice 2 + بلوکِ تازهٔ «به‌روزرسانی 2026-09-19 (Phase 7 — bounded documentation closure)») · `docs/drift-register.md` (نُتِ تازهٔ «Current Phase 7 bounded closure note» — نُت‌های پیشین تاریخی و دست‌نخورده) · همین راهنما (ردیفِ Phase 7 در جدول وضعیت + همین entry). **entryهای منتشرشدهٔ پیشین در هیچ فایلی بازنویسی نشدند.**
- **موارد کهنهٔ عمداً دست‌نخورده (تاریخی، نه کهنهٔ جاری):** (۱) عبارت‌های «Phase 7 ... IN PROGRESS / NOT complete / no Slice 3» در متن‌های تاریخی (بلوک‌های `757d7424` و `e60c6242` در current-state، نُت‌های پیشینِ drift-register، بلوکِ 2026-09-18 README، entryهای پیشینِ همین راهنما شامل entryهای 2026-09-18/03:23) — متنِ تاریخیِ درست برای چک‌پوینتِ خود؛ فقط با نُت/annotationِ تازه جایگزین شدند، بازنویسی نشدند. (۲) ردیفِ **O-05** در drift-register (فازِ مالک = Phase 7) — این closure هیچ ردیفِ drift را resolve/promote نمی‌کند. (۳) `CHANGELOG.md` — عرفِ ریپو برای برش‌ها (دست‌نخورده). (۴) `docs/state-machines/appointment.md` و `docs/api/error-codes.md` — اسنادِ قرارداد/مدل؛ خارج از دامنهٔ این آشتی‌دهیِ وضعیت (دقیقاً همان مرزِ ثبت‌شدهٔ entryِ 03:23).
- **تصمیمات درون‌فازی:** (۱) Phase 7 از IN PROGRESS به **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** تغییر کرد — فقط بر مبنایِ کارِ ادغام‌شده (موجودیِ FR-4.x + PR #79/#81/#83/#85)؛ (۲) **هیچ فازِ دیگری کامل اعلام نشد**؛ (۳) FR-5.4 **بدون اغراق** ثبت شد (bounded non-claim — نه ادعای doctor-approval و نه ادعای وجودِ job انقضای pending)؛ (۴) سیاستِ staff-reschedule دقیقاً به‌عنوان **owner-issued** (نه SRS) ثبت شد؛ (۵) هیچ برشِ محصولِ تازه‌ای ساخته/ادعا نشد.
- **شواهدِ بازیابی‌نشده/اجرا‌نشده (صداقت):** اجرای محلیِ PHPUnit/PHPStan/WPCS/`php -l` = **NOT RUN** (PHP/Composer/MySQL در این sandbox نیستند)؛ هیچ شاهدِ محلی‌ای ادعا نشد. شواهدِ دقیق‌سرِ خودِ کامیتِ مستنداتِ این PR = تا پیش از push **NOT RETRIEVED** و پس از push/ساخت Draft PR بازیابی و در گزارش تحویل ثبت می‌شود (باید با شواهدِ `d7484ce`/`cf1ace13` که بازیابی‌شده‌اند اشتباه گرفته **نشود**).
- **Git/PR:** forward-only روی `arena/01a0b998-doctor`؛ بدون `reset --hard`/`clean`/rebase/force-push/history rewrite؛ **یک Draft PR** به `main`؛ بدون Ready/merge/tag/release.
- **وضعیت tree:** در زمان ثبتِ این entry، تغییر فقط همین اسنادِ `docs/` است؛ پس از commit باید clean شود.

### [2026-09-19 19:11 UTC] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0b9e6-doctor`، Draft PR #87) — Phase 8 Slice 1 (FR-3.5 / UC-01): RED → **GREEN** — سطح عمومیِ آنونیمِ مرورِ نوبت
- **هویت ایجنت:** این نشست یک ایجنت **جدید** است — نه ایجنتِ ادغام‌کنندهٔ PR #88 یا هر PRِ پیشین. شاخهٔ نشست = `arena/01a0b9e6-doctor`؛ کار فقط روی همین شاخه، forward-only.
- **فاز/محدوده:** **Phase 8 — Patient Public Booking**، فقط **Slice 1** = «مشاهدهٔ آنونیم و read-onlyِ پزشکان و نوبت‌های آزادِ یک Clinicِ صریح». این سطح ownerِ FR-3.5 / UC-01 است (پیش از این هیچ‌کس آن را claim نکرده بود — `docs/roadmap/roadmap.md` §0 و `docs/drift-register.md`). **Phase 8 کامل اعلام نشد** و هیچ مستندِ closureِ فازی نوشته نشد؛ فقط حداقل راهنمای اپراتور (`docs/user-guide.md` §4.1).
- **LIVE REVERIFY شروع (VERIFIED FACT — Git/remote/PR زنده، نه حافظهٔ چت):** checkout محلی در شروعِ این GREEN روی `97efdb45` **کهنه** بود و فایلِ RED به‌صورت untracked حاضر بود؛ `origin/main` = `e00cec8dedd3245eb0c7dbfe772775c04e9af1f8` (جلو رفته بود، چون **PR #88** — `fix: make REST bootstrap and booking settings multi-Clinic safe`، شاخهٔ `arena/01a0ba45-doctor` — در 2026-09-19T17:52:42Z **MERGED** شده بود). PR باز = فقط **#87** (OPEN، `isDraft=true`، mergeable) با `headRefOid = 3eb12451ef017982fe2490bfcfc4a3dfa00f2458`. **PR جدیدی ساخته نشد** (دستورِ صریح مالک).
- **آشتی‌دهیِ شاخه (بدون بازنویسی تاریخ):** فایلِ untrackedِ محلی با `git show 3eb1245:<path>` بایت‌به‌بایت **یکسان** تأیید شد، سپس نسخهٔ پشتیبان گرفته و `git merge --ff-only 3eb1245` اجرا شد ⇒ HEAD محلی = **`3eb1245`** = همان **accepted synchronized RED head**. هیچ `reset --hard` / `git clean` / rebase / force-push / history-rewrite / empty-commit انجام نشد.
- **شواهدِ REDِ پذیرفته‌شده (مربوط به `3eb1245`، نه HEAD جدید):** Integration = **۱۰۵۰ تست / ۱۶۵۰۶ assertion / ۱۳ شکستِ عمدی / ۰ خطا / ۰ warning / ۰ skipped**. تفکیکِ ۲۳ متدِ این سوئیت: ۱۳ RED (T1–T5، T7–T10، T18، T19، T22، T23) + ۹ positive control (T11–T17، T20، T21) + T6 که **فقط GREEN guard** است (assertionهایش فقط «نبود» را می‌سنجد و در HEAD کهنه trivially پاس می‌شد — این تصحیحِ طبقه‌بندی پیش‌تر در `c04c74f` ثبت شده و دست‌نخورده ماند).
- **پیاده‌سازیِ GREEN (کامیت `6fad3e4bb85f8bc3c24eacf84cbb6168960c6b19`):** مکانِ متمرکزِ فرانت‌اند `ClinicCore\Frontend\*` (طبق ADR: بدون انباشتِ HTML/JS/CSS در Bootstrap) — `src/Frontend/PublicBookingShortcode.php` (ثبت + رندرِ shortcode `[cpms_public_booking clinic_id="N"]`) و `src/Frontend/PublicBookingAssets.php` (انکیوِ شرطی) + `assets/css/cpms-public-booking.css` و `assets/js/cpms-public-booking.js`. در `src/Bootstrap/App.php` فقط **۲۵ خط الحاقی و صفر حذفی** (import + یک فراخوانیِ `register()`).
- **قاعدهٔ Clinic (AD-13):** Clinic **فقط** از attribute صریح خوانده می‌شود و باید عددیِ مثبت **و** ردیفِ پایدارِ واقعیِ `cpms_clinics` باشد. نبودِ attribute / غیرعددی / `<= 0` / ناموجود ⇒ **یک** وضعیتِ بستهٔ `root data-state="error"` با enumeration parity (بدونِ `data-clinic-id`، بدونِ هیچ پزشک، و **بدونِ انتشارِ هیچ قراردادِ runtime** ⇒ مرورگر هیچ درخواستی نمی‌زند). هرگز: Clinic با کمترین id، «تنها Clinic»، Scope محیطی، کاربر جاری، یا جغرافیای درخواست. Clinicِ معتبرِ بدونِ پزشکِ فعال ⇒ `root data-state="empty"` (جدا از `error`). لیستِ پزشکان **server-rendered** است و فقط `is_active = 1`های همان Clinic (با defence-in-depth روی `clinic_id`).
- **قراردادِ runtime (D-5):** `rest_root = rtrim(rest_url('clinic/v1'),'/')`، `availability_path = '/availability'`، `quote_path = '/booking/quote'`، `clinic_id`، و `state_vocabulary` با پوششِ هر پنج وضعیتِ قرارداد — همه با `wp_json_encode(JSON_HEX_TAG|JSON_HEX_AMP|…)`. **هیچ REST endpoint جدیدی ثبت نشد**؛ فقط A1 و A4 موجودِ `permPublic()`.
- **مرزِ امنیت/PHI/سیاست:** سطح read-only و آنونیم است و هیچ دادهٔ هویت/تماسِ بیمار (patient_id، mrn، mobile، national_id، first_name، last_name) نه خوانده و نه منتشر می‌شود؛ تنها دادهٔ شخصیِ نمایشی `full_name` پزشک است. درخواست‌های JS عمداً `credentials: 'omit'` هستند (واقعاً آنونیم؛ بدون سطحِ CSRF). **سرور authoritative می‌ماند:** ظرفیت / min-lead / افق / سیاست tenant در JS پیاده یا تکرار **نشده**؛ برچسبِ Jalali (`days[].jalali`) و ساعتِ wall-clock محلیِ Location (`slots[].time`) **verbatim** نمایش داده می‌شوند (بدون `Date`، `toLocaleString` یا هر بازمحاسبهٔ زمانی). JS فقط verdictِ سرور را به `data-state` نگاشت می‌کند و پیامِ خودِ سرور را relay می‌کند. همهٔ متن‌های فارسی و skeletonها سمتِ سرور escape می‌شوند (`<p data-message>` + `<template data-template>`) و JS با `cloneNode()` + `textContent` پر می‌کند (بدون `innerHTML`، بدون رشتهٔ فارسی در JS).
- **assetها (D-6):** vanilla، صفر وابستگی، بدون build step/CDN/framework؛ handleهای `cpms-public-booking` **فقط** برای صفحه‌ای که سطح را رندر می‌کند انکیو می‌شوند؛ `CpmsAssets`/`cpms-admin` **دست‌نخورده** (هیچ اشتراک handle/file/hook).
- **تغییرِ پذیرفته‌شده در تستِ RED (class D — گزارش‌شده، بدون تضعیف):** `primeRestServerSingleton()` (فراخوانی در setUp + متدِ خصوصی) و نُتِ کهنهٔ «ARCHITECTURAL RISK» در هدر **حذف/به‌روز** شدند، چون **PR #88** REST bootstrap را scope-neutral کرد و A1/A4 را به Settingsِ **Clinicِ پایدارِ همان پزشک** (`BookingService` اکنون `SettingsFactory` می‌گیرد و `settingsFor($clinicId)` را per-operation صدا می‌زند) منتقل کرد ⇒ آن priming **inert** شده بود و دیگر نه چیزی را میخ می‌کرد و نه می‌توانست suite بعدی را آلوده کند. **راستی‌آزماییِ مکانیکی (نه ادعا):** با مقایسهٔ نسخهٔ `HEAD` و نسخهٔ جدید پس از حذفِ خطوطِ کامنت، **تنها** تفاوتِ کدِ اجراییِ کلِ فایل همان ۱۰ خطِ حذف‌شده است؛ هر **۲۳** متدِ تست و **همهٔ** assertionها بایت‌به‌بایت یکسان‌اند (هیچ assertion تضعیف/اضافه/بازنویسی/جابه‌جا/skip/حذف نشد). کنترل‌های A1/A4 (T11–T17) همچنان روی معماریِ واقعیِ ادغام‌شدهٔ #88 اجرا می‌شوند — و اکنون شاهدِ **قوی‌تری** هستند چون دیگر به pre-primingِ مصنوعی وابسته نیستند. ضمناً این ویژگی به‌طور مستقل توسط `tests/bin/multi-clinic-rest-bootstrap-probe.php` (که با #88 به CI اضافه شد و روی همین شاخه هم اجرا می‌شود) در یک فرآیندِ **process-fresh** چند-Clinicه گیت می‌شود.
- **پشتیبانیِ test-infrastructure برای Responsive harness (class D، محدود):** هارنسِ موجود (`bin/pilot-responsive.py`) ساختاراً نمی‌توانست به صفحهٔ shortcode برسد چون هر چهار ورودیِ `PAGES` پیش از `goto` Login می‌کردند. **یک** ورودیِ آنونیمِ **اختیاری** با گیتِ `RESP_PUBLIC_PAGE` افزوده شد: وقتی این متغیر تنظیم نباشد رفتارِ فایل **دقیقاً** همان پیشین است (همان ۴ صفحه/همان Login/همان معیارها/همان خروجی) ⇒ صفر ریسک برای گیتِ موجود. وقتی تنظیم باشد، روی Chromiumِ واقعی بررسی می‌شود: رندرِ آنونیم بدون redirect به Login، صحتِ پیوندِ `data-clinic-id`، `dir=rtl`/`lang=fa`، لیستِ server-rendered پزشکان، بارِ هر دو assetِ سطح، **A1 ⇒ 200**، انتخابِ نوبت ⇒ **A4**، گذارِ وضعیتِ قابل‌مشاهده، نبودِ PHI/affordanceِ خارج‌از‌دامنه در زیردرختِ سطح، و نبودِ overflow افقی **هم پیش و هم پس از** تعامل. fixtureِ همان job هم یک ردیفِ برنامهٔ فعال + نوبت‌های آزاد + یک Pageِ واقعیِ published با خودِ shortcode می‌سازد و Clinic id را از ردیفِ پایدارِ پزشک **derive** می‌کند (نه literal). Workflow بازطراحی نشد؛ Pilot gate دست‌نخورده است.
- **یک نکتهٔ معماریِ ثبت‌شده (تصمیمِ درون‌فازی):** `PublicBookingShortcode::register()` در `App::boot()` عمداً **پیش از** گاردِ `$booted` فراخوانی می‌شود. دلیل فنی: `add_shortcode()` در `$GLOBALS['shortcode_tags']` ثبت می‌کند که جزو `$wp_filter` نیست؛ اگر چیزی در چرخهٔ زندگیِ وردپرس (یا سوئیت تست) آن را بازنشانی کند، گاردِ `$booted` مانعِ ثبتِ مجدد می‌شد و سطح از دسترسِ بیمار می‌افتاد. خودِ ثبت idempotent است (`add_shortcode` همان کلید را بازنویسی می‌کند و `add_action` با callback/priority یکسان در وردپرس dedupe می‌شود)، پس هیچ هوکِ تکراری ساخته نمی‌شود.
- **شواهدِ اجرا‌نشده/بازیابی‌نشده (صداقت):** اجرای محلیِ PHPUnit / PHPStan / WPCS / `php -l` = **NOT RUN** (هیچ PHP/Composer/MySQL در این sandbox نیست و نصب ممکن نشد؛ هیچ شاهدِ محلیِ PHP ادعا نمی‌شود). آنچه محلی انجام شد فقط بررسی‌های **غیر-PHP** و کاملاً برچسب‌دار است: parse با tree-sitter (syntax، نه semantics)، بررسیِ مکانیکیِ arity همهٔ ۲۶ فراخوانیِ `sprintf` (۰ خطا)، `node --check` روی JS، یک harnessِ jsdom با fetchِ stub که **۷۲** assertionِ رفتاریِ JS را روی ۱۰ سناریو (A1/A4 bookable · policy_rejected · unavailable · empty · خطای شبکه · CLINIC_NOT_FOUND · بدنهٔ غیر-JSON · سطحِ بسته با صفر درخواست · گاردِ in-flight · تعویضِ پزشک) سبز کرد — که در همان harness **یک باگِ واقعی** (`fill()` وقتی خودِ گرهِ کلون‌شده حامل `data-fill` بود) پیدا و اصلاح شد — و یک شبیه‌سازیِ Python از regex/contractهای خودِ سوئیت RED روی markupِ رونویسی‌شده (۰ شکست). **هیچ‌کدام شاهدِ سوئیتِ واقعی نیستند؛ CI شاهدِ یکتاست.** شواهدِ دقیق‌سرِ خودِ این کامیت‌ها = تا پیش از push **NOT RETRIEVED**.
- **Git/PR:** forward-only روی `arena/01a0b9e6-doctor`؛ بدون `reset --hard`/`clean`/rebase/force-push/history-rewrite/empty-commit. **PR جدید ساخته نشد** — همان Draft PR #87؛ **Draft باقی ماند** (Ready نشد) و **merge نشد**؛ بدون tag/release.
- **وضعیت tree:** در زمانِ ثبتِ این entry، کامیتِ `6fad3e4` (پیاده‌سازی) انجام شده و فقط همین فایلِ `docs/agent-guide.md` تغییرِ staged-نشده دارد؛ پس از کامیتِ مستندات باید clean شود.

### [2026-09-19 19:45 UTC] — همان ایجنت (شاخهٔ `arena/01a0b9e6-doctor`، Draft PR #87) — افزودنِ شاهدِ قابل‌بازیابی برای Phase 8 Slice 1 (فقط زیرساختِ شاهد/CI، class D)
- **چرا:** هر چهار workflow روی سرِ GREEN `125dd8f4` **موفق** شدند و **۱۹/۱۹** check run سبز است، ولی دو شاهدِ **الزامیِ** این slice از sandbox **قابل بازیابی نبود**: (۱) تفکیکِ سوئیتِ Phase 8 Slice 1 در کامنتِ «Integration completion evidence» — چون بخش‌های per-suiteِ آن step در `ci.yml` **hardcoded** برای Phase3/Phase4/Phase6/C7 نوشته شده بودند و پیش از وجودِ Phase 8 شکل گرفته بودند؛ (۲) خروجیِ Responsive smoke — چون فقط در `$GITHUB_STEP_SUMMARY` و artifact می‌رفت و stepِ «Debug dump به PR» با `if: failure()` در موفقیت **skip** می‌شد. دانلودِ لاگ/artifact در این sandbox ممکن نیست: `api.github.com` در دسترس است ولی hostِ مقصدِ redirect یعنی `*.blob.core.windows.net` در سطح TLS مسدود است (`SSL_ERROR_SYSCALL`) — همان محدودیتی که خودِ repo در کامنتِ `ci.yml` و در نامِ آن step مستند کرده است («لاگ Blob از Sandbox در دسترس نیست»).
- **چه شد (کاملاً الحاقی — ۳۴ خط اضافه، **صفر** خط حذف):** در `ci.yml` دو بخشِ `Phase8 Slice 1 Public Booking Browse suite/testcases` دقیقاً با همان الگوی موجود (`grep '^testsuite ' / '^testcase '` روی junit) پیش از `rm -f "$SPLIT"` اضافه شد؛ در `pilot-gate.yml` یک stepِ `if: always()` به jobِ `responsive` اضافه شد که همان خلاصهٔ `PASS|FAIL` (به‌همراه `INFO`ها و محتوای `/tmp/resp-public-page.env`) را با همان الگوی موجودِ یافتنِ PR (`gh pr list --head … --state open`) به PR post می‌کند. **هیچ محصول، assertion، آستانه، معیارِ گیت یا ترتیبِ stepِ موجود تغییر نکرد**؛ YAML هر دو فایل پس از تغییر parse شد.
- **دامنه:** این تغییر فقط **کانالِ گزارش‌دهی** را کامل می‌کند تا شواهدِ لازم از طریق API خوانده شوند. نه Phase 8 را کامل اعلام می‌کند، نه scope را گسترش می‌دهد، نه زیرساختِ PR #88 را باز می‌کند، نه PR جدیدی می‌سازد؛ PR #87 همچنان **Draft** است و Ready/merge نشده.

### [2026-09-19 20:15 UTC] — همان ایجنت (شاخهٔ `arena/01a0b9e6-doctor`، Draft PR #87) — رفعِ **تنها** blockerِ پذیرشِ مستقل: URLِ A1 زیر Plain permalinks
- **defect (واقعی، نه سلیقه‌ای):** JS سطح عمومی A1 را با `rest_root + availability_path + '?clinician_id=' + N` می‌ساخت. سرور `rest_root` را از `rest_url('clinic/v1')` منتشر می‌کند و در **Plain permalinks** (که وردپرس کاملاً پشتیبانی می‌کند) شاخهٔ `else` در `get_rest_url()` اجرا می‌شود: `<home>/index.php` + `add_query_arg('rest_route', '/clinic/v1')`. نتیجهٔ الحاق **دو** `?` بود: `index.php?rest_route=/clinic/v1/availability?clinician_id=N`. چون `rest_route` یک query var عمومی است (`$wp->add_query_var('rest_route')` در `rest-api.php`) و `rest_api_loaded()` روی `untrailingslashit($wp->query_vars['rest_route'])` route می‌کند، وردپرس route را `/clinic/v1/availability?clinician_id=N` می‌دید، هیچ handler مطابقت نمی‌کرد و `rest_no_route` (۴۰۴) برمی‌گشت؛ `clinician_id` هم هرگز param نمی‌شد (`serve_request()` ⇒ `set_query_params(wp_unslash($_GET))`). یعنی کلِ جریانِ مرورِ آنونیم روی یک پیکربندیِ پشتیبانی‌شده از دسترس بیمار خارج بود.
- **رفعِ محصول (کامیت `47d7b939b2e01a6378bf8dfd4fd4c469a14d1417`) — ۶ خطِ اجرایی:** استفاده از **همان الگوی موجودِ ریپو**، یعنی `apiUrl()` که پیش‌تر در `DoctorDashboardPage` / `DoctorHandwritingPage` / `SecretaryFinancePage` / `SecretaryQueuePage` / `SmsSettingsPage` ship شده (`if root has '?' and path has '?' → path.replace('?','&')`)، نه اختراعِ الگوی جدید. راستی‌آزمایی مقابلِ **شکل‌های واقعیِ `get_rest_url()` در WP 6.7**: pretty ⇒ `…/availability?clinician_id=N`؛ plain ⇒ `…rest_route=/clinic/v1/availability&clinician_id=N`؛ index-permalink ⇒ `…/availability?clinician_id=N`. هر سه به routeِ `/clinic/v1/availability` با `clinician_id` به‌عنوان paramِ واقعی می‌رسند. **هیچ URL استقرار hardcode نشد**؛ ریشه همان چیزی است که سرور منتشر کرده. A4 چون POST بدون query در URL است در هر دو حالت درست بود ⇒ **رفتارش تغییر نکرد** و فقط یک کامنت آن invariant را ثبت کرد.
- **دامنهٔ دست‌نخورده (عمداً):** A1/A4 semantics، منطقِ tenant، قراردادِ shortcode، طراحیِ UI، rate limiting، defaultها، login/OTP و migration/schema. مشاهده‌های **غیر-blockerِ** پذیرش هم عمداً دست‌نخورده ماندند: quote detailِ کهنه هنگامِ بارِ A4 بعدی، fetch timeout/AbortController، جمله‌بندیِ `available:false`، و تکرارِ `printLateStyle`. هیچ مرورِ گستردهٔ دوباره‌ای انجام نشد و Phase 8 هم گسترش نیافت.
- **شاهدِ regression (کوچک‌ترین پوشش برای همین defect؛ هیچ تستِ قراردادیِ پذیرفته‌شده بازنویسی یا تضعیف نشد):**
  - **مسیرِ مرورگرِ واقعی زیر Plain permalinks** (گزینهٔ preferred): `real-wp-acceptance.yml` وردپرسِ تمیز 6.7.2 را با `wp core install` بالا می‌آورد و **هیچ** `wp rewrite structure` اجرا نمی‌کند ⇒ `permalink_structure` خالی ⇒ همان Plain. افزونه از **ZIP رسمیِ ریلیز** نصب می‌شود. یک stepِ fixture جدید برنامهٔ فعال + نوبت‌های آزاد + یک Pageِ published با خودِ shortcode می‌سازد و Clinic id را از fixtureِ صریحِ موجود (که خودش از slugِ پایدارِ `default` حل می‌شود) **derive** می‌کند — نه literal. `bin/rwp-acceptance.py` سپس URLِ **واقعیِ** صادرشده از مرورگر را می‌گیرد و با همان قاعدهٔ خودِ وردپرس حل می‌کند و assert می‌کند: حالتِ permalink واقعاً plain است (از `rest_root` منتشرشده ⇒ شاهد self-authenticating)، دقیقاً یک `?`، route == `/clinic/v1/availability`، `clinician_id` یک paramِ واقعی، `rest_route` آلوده نشده، A1 HTTP 200 (۴۰۴ یعنی همان blocker)، رسیدنِ panel به `selectable`، رندرِ نوبت‌ها، route و HTTP 200 برای A4، گذارِ وضعیتِ قابل‌مشاهده، و نبودِ خطای console/page. **کاملاً الحاقی: ۱۶۴ خط، صفر حذف.** گیتِ ایزولاسیونِ BrowserContext همچنان **PASS** با نقشهٔ persona→context **یکسان** (contextها ۱۱→۱۲، جریان‌های احراز هویت ۱۳→۱۳ بدون تغییر — چون این سطح عمداً آنونیم است و login نمی‌کند).
  - **سمتِ Pretty permalinks:** jobِ responsive با `wp rewrite structure '/%postname%/'` اجرا می‌شود و پیش‌تر هم سطح عمومی را در ۶ viewport exercised می‌کرد و `a1_url`/`a4_url` را **گرفتار** می‌کرد ولی هیچ‌وقت assert یا منتشر نمی‌کرد. همان حلِ routeِ وفادار به وردپرس + assertionها به `bin/pilot-responsive.py` اضافه شد، به‌همراهِ یک خطِ `PASS` اختصاصی که URLهای تولیدشده را در شاهدِ قابل‌بازیابی چاپ می‌کند. ۵۲ الحاق و تنها خطِ حذف‌شده همان `detail.update()` است که بسط یافته؛ رفتار وقتی `RESP_PUBLIC_PAGE` تنظیم نباشد بدون تغییر است.
- **راستی‌آزماییِ محلی (برچسب‌دار؛ CI شاهدِ معتبر است — در این sandbox هیچ PHP/Composer/MySQL نیست):** `node --check`؛ سوئیتِ jsdomِ موجود با **۷۲** assertion روی همان فایلِ ship‌شده بدون تغییر پاس شد (ریشهٔ pretty دست‌نخورده)؛ یک بررسیِ متمرکزِ سه‌شکلی که هر URL تولیدشده را از رونویسیِ `rest_api_loaded()`/`serve_request()` عبور می‌دهد؛ و **کنترلِ منفی**: اجرای همان بررسی روی فایلِ **پیش از رفع** (`8b4c9f0`) defect را بازتولید کرد (route آلوده، `clinician_id=undefined`، `rest_no_route`، panel هرگز selectable نشد — ۷ FAIL) ⇒ پوششِ جدید واقعاً این blocker را می‌گیرد. `tenant-tripwire.py --test` = ۵۹/۵۹ و اسکنِ تولید = ۰ hardcode با همان **یک** suspectِ از پیش موجود؛ هر دو هارنس `py_compile` شدند؛ هر چهار YAMLِ workflow parse شدند؛ PHPِ استخراج‌شده از fixture هم parse شد.
- **Git/PR:** forward-only روی `arena/01a0b9e6-doctor`؛ بدون `reset --hard`/`clean`/rebase/force-push/history-rewrite/empty-commit. **PR جدیدی ساخته نشد**؛ PR #87 **Draft** باقی ماند (Ready نشد، merge نشد). عنوان/بدنهٔ PR عمداً به‌روز **نشد** — طبق دستور، reconcile کردنِ metadata می‌تواند درست پیش از Ready/Merge انجام شود.


### [2026-09-20 (UTC)] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0bf86-doctor`) — closure-only مستندات: Phase 8 — reconciliationِ پس‌از‌ادغامِ PR #89 (Slice 2) + ثبتِ Slice 1 (PR #87)؛ بدون تغییر محصول
- **هویت ایجنت:** ایجنت نوشتن/گیت‌هابِ **جدید** — نه ایجنتِ ادغام‌کنندهٔ PR #89 و نه هیچ ایجنتِ دیگر؛ به ایجنتِ در‌جریانِ PR #90 دست‌زدگی **ندارد** و آن PR/شاخه اصلاً لمس نشد. شاخهٔ نشست از authoritative main زنده باز شد.
- **LIVE REVERIFY شروع (VERIFIED FACT — GitHub/Git زنده، نه حافظهٔ چت):** `origin/main = 440ba7e7d07579036128f288f502d390d27c98d3` (= merge PR #89؛ مطابق انتظارِ تاریخی — main به‌طور مادی تغییر نکرده بود) · PR #89 = MERGED 2026-09-20T09:24:19Z؛ parents `41225398…` (پدر ۱) + `70d033cc…` (پدر ۲) · open PRs = **۱**: draft PR #90 `arena/01a0be60-doctor` @ `c17c965d32ec056f3e53be280173dc5182ef1c2e` base `main` (بدون تغییر؛ هیچ cherry-pick از آن انجام **نشد**) · هیچ PRِ docs-closureِ تکراری باز نبود ⇒ تکراری ساخته **نشد** · آخرین migration روی main = `2026_09_20_0021_otp_tokens_clinic_binding.php` (۲۱ فایل `0001..0021`)؛ `0022` فقط روی درختِ PR #90 است.
- **شواهدِ post-merge روی merge SHA `440ba7e…` (بازیابی زنده):** چهار workflow لازمِ push/main همه completed/success — CI `35502216463` · Real WordPress Acceptance `35502216468` · Closure Gate `35502216456` · Pilot/Staging Readiness Gate `35502216452` — + **۱۹/۱۹ check run `success`**. Exact-head #89: ۱۹/۱۹ روی `70d033cc…`. Exact-head #87: ۱۹/۱۹ روی `0643e2b5…`؛ post-merge #87 روی `41225398…`: CI `35468244421` · Real WP `35468244416` · Closure `35468244423` · Pilot/Staging `35468244448` (push eventها؛ یک اجرای schedule-رویدادِ Pilot به‌عنوان شواهدِ closure استفاده **نشد**).
- **حقایقِ ثبت‌شده:** Phase 8 Slice 1 = CLOSED (BOUNDED — PR #87)؛ Slice 2 = CLOSED/MERGED (PR #89)؛ **Phase 8 = IN PROGRESS — NOT complete**؛ Slice 3 فقط draft PR #90 (بدون هیچ ادعای accept/merge)؛ سیاست‌های Slice 2 (Hold-Timing؛ حداقل‌هویتِ Patient جدید با gate نام در B2) دقیقاً به‌عنوان **سیاستِ Owner-issued متمایز از عبارتِ اصلی SRS** ثبت شد (مرجع: `decisions/2026-09-20-phase8-slice2-owner-decisions.md`)؛ migration `0021` روی main با مجوزِ صریحِ مالک ساخته شد؛ `0022` migrationِ main **نیست**.
- **فایل‌های تغییرکرده (فقط `docs/`؛ محدود به همین closure):** `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `440ba7e` در صدر + تاریخی‌کردنِ بلوکِ `d7484ce` + ردیفِ Phase 8 جدول §C + پاراگرافِ «This file describes…» + آشتی‌دهیِ ادعایِ زندهٔ «Schema: current version`0020`/no 0021`» با حفظِ زنجیرهٔ تاریخ + الحاقِ راستی‌آزماییِ `0021`) · `docs/roadmap/roadmap.md` (ردیفِ Phase 8: ⏳ ← 🚧 IN PROGRESS با ادعاهایِ bounded بالا؛ اصلاحِ «= current main» در ردیفِ Phase 7 به حالتِ چک‌پوینتِ خودش) · `docs/governance/project-phase-taxonomy.md` (جداسازیِ ردیفِ Phase 8 از «Phase 8، 10..20 = NOT STARTED» + re-pinِ «Current live main» + اصلاحیهٔ الحاقیِ migration + جملهٔ وضعیتِ Phase 8) · `docs/README.md` (annotationِ تاریخیِ بلوکِ Phase 7 + بلوکِ تازهٔ «به‌روزرسانی 2026-09-20») · `docs/drift-register.md` (الحاقِ نُتِ تاریخ‌دارِ Phase 8؛ نُت‌های پیشین تاریخی و دست‌نخورده) · همین راهنما (ردیفِ Phase 8 در جدول وضعیتِ صدر + همین entry). **هیچ entry تاریخیِ تاریخ‌داری بازنویسی نشد؛ فقط بلوک‌های زندهٔ وضعیت اصلاح/الحاق شدند.**
- **عمداً دست‌نخورده (تاریخی، نه کهنهٔ جاری):** عباراتِ «0021 exists not/remains 0020» در بلوک‌ها/ردیف‌های تاریخ‌ scopedِ فازهای ۲..۷ (current-state، roadmap، taxonomy، README، drift-register) — درستِ همان چک‌پوینت‌ها بودند و حفظ شدند؛ `CHANGELOG.md` (توس توسط خودِ PR #89 انجام شد؛ عرفِ مخزن)؛ هیچ کد/تست/workflow/schema/asset تغییری نکرد؛ هیچ migrationِ جدیدی رزرو **نشد**؛ شماره‌های migration دست‌نخورده.
- **شواهدِ این کامیتِ docs:** اجرای محلیِ PHPUnit/PHPStan/WPCS = NOT RUN (محیط sandbox نیست؛ عرف: CI شاهدِ معتبر)؛ شواهدِ exact-head خودِ این PR پس از push بازیابی و در PR ثبت می‌شود — با شواهدِ `440ba7e` اشتباه گرفته **نمی‌شود**؛ NOT RETRIEVED ≠ PASS.
- **Git/PR:** forward-only روی `arena/01a0bf86-doctor` (از همان `440ba7e…` باز شد)؛ بدون rebase/reset/clean/force-push/history-rewrite/empty-commit؛ بدون حذفِ شاخهٔ مبدأ؛ **یک PRِ docs-only**؛ merge با همان سبکِ merge-commitِ مخزن (پس از terminal-success بودنِ گیت‌های لازمِ exact-head طبقِ دستورالعملِ closure).

### [2026-09-20 (UTC)] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0bfc5-doctor`) — closure-only مستندات: Phase 8 Slice 3 — reconciliationِ پس‌از‌ادغامِ PR #90 + بستنِ bounded فاز ۸؛ بدون تغییر محصول
- **هویت ایجنت:** ایجنت نوشتن/گیت‌هابِ **جدید** — نه ایجنتِ ادغام‌کنندهٔ PR #90 و نه هیچ ایجنتِ دیگر؛ به PR #90/#91 دست‌زدگی ندارد و هیچ‌کدام اصلاً لمس نشدند. شاخهٔ نشست با ff به authoritative main زنده (`9abd3379…`) رسید و کد محصول تغییر نکرد.
- **LIVE REVERIFY شروع (VERIFIED FACT — GitHub/Git زنده، نه حافظهٔ چت):** `origin/main = 9abd3379a02a320705cadac5229726bbd1b202cb` (= merge PR #90؛ مطابق انتظار — main تغییر مادی نکرده بود) · PR #90 = MERGED 2026-09-20T17:04:39Z؛ والدین `7873d46e…` + `0f044eb518d1e9a3c91f48a49a05df4f5c62ba83` (the exact accepted PR #90 head) · open PRs = ۰ (هیچ PRِ docs-closureِ تکراری باز نبود ⇒ تکراری ساخته نشد) · آخرین migration روی main = `2026_09_20_0022_slot_holds_patient_binding.php` (۲۲ فایل `0001..0022`) · exact-head #90 روی `0f044eb5…` = ۱۹/۱۹ چک success.
- **شواهدِ post-merge #90 روی merge SHA `9abd3379…` (بازیابی زنده، همه push→main):** چهار workflow لازم همه completed/success — CI `35524710717` · Real WordPress Acceptance `35524710714` · Closure Gate `35524710737` · Pilot/Staging Readiness Gate `35524710697` — + **۱۹/۱۹ check run `success`**. PR-head evidence جای شواهدِ post-merge استفاده نشد.
- **حقایقِ ثبت‌شده:** Phase 8 = **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** — Slice 1 = CLOSED (PR #87)؛ Slice 2 = CLOSED (PR #89)؛ Slice 3 = CLOSED (PR #90)؛ PR #88 همچنان fixِ hardeningِ غیربرشی؛ **این بستن release/go-live/V1-complete/commercial نیست**؛ هیچ قلمِ deferred بازی (`booking.max_future_days` 30-vs-60؛ A1 min-lead visibility؛ anonymous A1/A4 rate-limit؛ cross-Clinic OTP cooldown/attempt coupling؛ Patient Portal / Phase 9؛ full patient profile؛ بدهیِ فنیِ نامرتبط) به فاز ۸ کشیده نشد؛ تفکیک‌های Owner-issued از عبارتِ اصلی SRS (Hold-Timing؛ حداقلِ نام‌ها؛ OTPِ مقید به Clinic؛ reuseِ قطعیِ WP-user؛ سیاستِ Slice 3) حفظ شد؛ متنِ تاریخیِ SRS و چک‌پوینت‌های تاریخ‌دار بازنویسی نشد.
- **فایل‌های تغییرکرده (فقط `docs/`؛ محدود به همین closure):** `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `9abd3379` در صدر + تاریخی‌شدنِ بلوکِ `440ba7e` + ردیفِ Phase 8 جدول §C + پاراگرافِ «This file describes…» + «Integrated main checkpoint» میانی + آشتیِ Schema با `0022` و الحاقِ زنجیرهٔ راستی‌آزمایی) · `docs/roadmap/roadmap.md` (ردیفِ Phase 8 §۰: 🚧 IN PROGRESS ← ✅ CLOSED/TECHNICALLY COMPLETE (BOUNDED)) · `docs/governance/project-phase-taxonomy.md` (ردیفِ Phase 8 → CLOSED (TECHNICAL; BOUNDED) + re-pinِ «Current live main» + اصلاحیهٔ الحاقیِ migration) · `docs/README.md` (annotationِ تاریخیِ بلوکِ Slices 1+2 + بلوکِ تازهٔ «به‌روزرسانی 2026-09-20 (Phase 8 Slice 3)») · `docs/drift-register.md` (الحاقِ نُتِ تاریخ‌دارِ Slice 3؛ نُت‌های پیشین تاریخی و دست‌نخورده) · همین راهنما (ردیفِ Phase 8 در جدولِ صدر + همین entry). **هیچ entry تاریخیِ تاریخ‌داری بازنویسی نشد؛ فقط بلوک‌های زندهٔ وضعیت اصلاح/الحاق شدند.**
- **عمداً دست‌نخورده (تاریخی، نه کهنهٔ جاری):** عبارت‌های «0021-latest / 0022 not on main» در بلوک‌های checkpoint-scoped تاریخی (current-state، taxonomy، README، drift-register) — درستِ چک‌پوینتِ خودشان و حفظ‌شده؛ `CHANGELOG.md` (عرفِ مخزن برای برش‌ها؛ این PR docs-only است)؛ هیچ کد/تست/workflow/schema/asset/migration تغییری نکرد؛ شماره‌های migration دست‌نخورده.
- **شواهدِ این کامیتِ docs:** اجرای محلی PHPUnit/PHPStan/WPCS = NOT RUN (بدون PHP/Composer/MySQL در sandbox؛ عرفِ مخزن: CI شاهدِ معتبر)؛ شواهدِ exact-head خودِ این PR پس از push بازیابی و پیش از Ready/Merge بررسی می‌شود — با شواهدِ `9abd3379` اشتباه گرفته نمی‌شود؛ NOT RETRIEVED ≠ PASS.
- **Git/PR:** forward-only روی `arena/01a0bfc5-doctor` (ff به همان `9abd3379…`، بدون rebase/reset/clean/force-push/history-rewrite/empty-commit)؛ بدون حذفِ شاخهٔ مبدأ؛ **یک PRِ docs-only**؛ بدون tag/release؛ merge با همان سبکِ merge-commitِ مخزن (پس از terminal-success بودنِ گیت‌های لازمِ exact-head طبقِ دستورالعملِ closure).

### [2026-09-20 (UTC)] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0c064-doctor`، Draft PR #94 — **merge نشد**) — زیرساختِ CI: رفعِ نقصِ اثبات‌شدهٔ «added-lines» گیتِ WPCS (class D) + افشای وضعیتِ واقعیِ استانداردها (class A)
- **هویت/دامنه:** ایجنت نوشتن/گیت‌هابِ جدید؛ کار فقط «زیرساخت» — بدونِ تغییر در PHP/JS/CSS محصول، تست‌ها، migrationها و بدونِ هیچ بخشی از Phase 9. **PR #93 و شاخه‌اش اصلاً لمس نشدند** (head در پایانِ کار دقیقاً همان `d440f2aafe41e82b1110e58b06ef09afba322960` است).
- **LIVE REVERIFY شروع (GitHub/Git زنده، نه حافظهٔ چت):** `main = 2263ed673a8fdf4ab91e6a7fffae6d82252e4324` (مطابقِ انتظار) · open PRs = فقط **#93** (Draft، head `d440f2aa…`) · `defaults.run.working-directory: clinic-practice-management` در ci.yml:27 · توکنِ خرابِ added-lines فقط در ci.yml:398 · **هیچ PR/شاخهٔ دیگری این نقص را رفع نکرده است** (بررسیِ همهٔ ۹۵+ تیپِ ریموت: هر ۹۲ تیپی که این step را دارند همان توکنِ معیوبِ یکسان را دارند؛ هیچ `:(top)`/اسکریپتِ کمکی/`bin/**wpcs*` در هیچ شاخه‌ای وجود ندارد) · `#93` زیرِ `.github/` فقط `pilot-gate.yml` را تغییر می‌دهد (ci.yml دست‌نخورده).
- **بازتولیدِ زندهٔ نقص (نه استنتاج):** گامِ discovery با cwd=plugin ۹۶ فایلِ PHPِ in-scope می‌دهد (مسیرها root-relative با پیشوندِ `clinic-practice-management/`؛ `sed` آن‌ها را برای phpcs strip می‌کند). گامِ added-lines همان پیشوند را **داخلِ همان پوشه** به git می‌دهد؛ `git diff` مسیرها را نسبت به CWD تفسیر می‌کند ⇒ `clinic-practice-management/clinic-practice-management/…` ⇒ **بدونِ match**؛ و pathspecِ نامطابق برای `git diff` خطا نیست (rc=0، خروجی صفر، stderr خالی — عملاً راستی‌آزمایی شد) و statusِ pipeline از `awk` می‌آمد. اندازه‌گیریِ زنده با `WPCS_BASELINE=f23e0c5…`: فرمِ تاریخی **۰** کلیدِ added-line روی ۹۶ فایل؛ فرمِ اصلاح‌شده **۱۵۲۰۹** کلید (برابرِ مجموعِ `git diff --numstat`). **سنِ نقص:** `defaults.run.working-directory` در `5a44e7f` (2026-09-05) و imported stepِ added-lines با همان pathspecِ غلط در `67e1208` (2026-09-09) آمد و `merge-base --is-ancestor` تأیید می‌کند اولی ancestorِ دومی است ⇒ **این گیت از لحظهٔ تولد، هیچ خطی جمع نکرده است** (پس سبزهای قبلیِ WPCS از لحاظِ سیاستِ added-lines اعتبارِ retroactive ندارند).
- **اصلاح (کم‌ترین ریشهٔ پایدار):** `bin/wpcs-changed-lines.py` تنها منبعِ هر دو artifact (`/tmp/wpcs-files.txt` در دامنهٔ مسیرِ working-directory برای phpcs و `/tmp/wpcs-added.txt` در همان دامنه برای فیلترِ added-lines)؛ هر فرمانِ git در top-levelِ مخزن و با مسیرهای repo-root-relative اجرا می‌شود و دو دامنه یک‌بار صریح map می‌شوند (بدونِ hardcodeِ نامِ افزونه: `git rev-parse --show-prefix`). **Fail-closed:** baselineِ غیرقابل‌resolve، diffِ خالی/با headerِ بیگانه (pathspec حل نشده)، و ناسازگاریِ `@@` با `git diff --numstat` ⇒ rc=1 + `::error::`. تغییراتِ حذفی/متادیتا-فقط مشروع می‌مانند (numstat 0 / parsed 0) ⇒ خالیِ «اثبات‌شده» هرگز با خرابیِ collector اشتباه نمی‌شود (بدونِ `test -s` بی‌قید). ci.yml: اجرای self-test در jobِ wpcs + جایگزینیِ دو stepِ inline با اسکریپت + آینه‌کردنِ شواهدِ self-test/collection روی PR (لاگ‌های job از API خواندنی نیستند).
- **سیاست بدونِ تغییر:** همان `WPCS_BASELINE`، همان فیلترِ in-scope، همان فیلترِ added-lines، همان sniffs. راستی‌آزماییِ مکانیکیِ scope: لیستِ فایل‌های فرمِ قدیم و جدید **بایت‌به‌بایت یکسان** است (۹۶ فایل).
- **اثباتِ رگرسیونِ قطعی (`--test`، مخزن‌های gitِ مصنوعی؛ بدونِ PHP/شبکه):** ۴/۴ PASS — (۱) فایلِ PHPِ تغییرکرده با یک خطِ ناقضِ add‌شده واقعاً در diffِ inspected حاضر است و متادیتای non-empty با شمارهٔ خطِ درست تولید می‌شود، در حالی که فرمِ تاریخیِ pathspec همان جمع‌آوریِ خالی را بازتولید می‌کند؛ (۲) تغییرِ صرفاً حذفی (تنها و در مجموعهٔ مختلط) هرگز false-fail نمی‌دهد؛ (۳) خرابیِ collector/path (baselineِ بی‌resolve، فایلِ خارج از working-directory، pathspecِ حل‌نشده، ناسازگاریِ numstat) هرگز به PASS تبدیل نمی‌شود؛ (۴) «هیچ تغییرِ in-scope» همان no-op سبز باقی می‌ماند. خروجیِ self-test در CI **قابلِ‌بازیابی** است (کامنتِ شواهد روی PR #94، run `35534433407`). شاهدِ واقعیِ مستقل (کلوْنِ scratch، فقط‌خواندنی روی headِ #93): collector برای `src/Admin/PatientPortalPage.php` **۱۴۰** کلیدِ added-line می‌بیند (`numstat_added=140 collected=140`) در حالی که فرمِ تاریخی صفر می‌دهد.
- **گیت‌های exact-head روی `bd24ca6483897d076ff8565c7ee6cc720dc73860` (بازیابیِ زنده):** CI `35534433407` = **completed/failure** · Real WP `35534433380` = success · Closure `35534423996` = success · Pilot/Staging `35534424084` = success · check-runها = **۱۸ success + ۱ failure**. jobِ ناموفق: `WPCS (changed code)` = `106140784287`؛ annotationهای قابلِ‌بازیابی: `12497 WPCS violation(s) on added lines:` و `Process completed with exit code 1.` و `PR comment failed`.
- **طبقه‌بندی: A (وضعیتِ محصول/استانداردها) — نه D.** دلیل: collector اثبات‌شده درست است (برابریِ `numstat_added == collected` برای **هر ۹۶ فایل** در کامنتِ شواهدِ همان run؛ لیستِ فایل‌ها یکسان با فرمِ قدیم؛ منطقِ intersect دست‌نخورده) ⇒ ۱۲٬۴۹۷ کلیدِ منطبق، نقض‌های **واقعیِ phpcs** روی خطوطِ **واقعاً addشدهٔ** دلتای پس‌از‌baseline در main است که پیش‌تر با مجموعهٔ خالی ماسک می‌شد. جزئیاتِ per-sniff **NOT RETRIEVED** (لاگِ job: blob→EOF؛ annotation به یک خط کوتاه می‌شود؛ کامنتِ نقض‌ها به‌دلیل حجم > سقفِ GitHub ارسال نشد) — این یک محدودیتِ کلاس C است، نه ادعای سبزی.
- **تصمیم (قاعدهٔ پذیرش):** طبق دستور، merge فقط با terminal-success بودنِ گیت‌های لازم مجاز است؛ WPCS روی exact-head **قرمز** است و رفعِ آن به تغییرِ کدِ محصول یا تغییرِ سیاستِ baseline/ruleset نیاز دارد که هیچ‌کدام در دامنهٔ این PR نیست ⇒ **PR #94 عمداً Draft و merge‌نشده ماند** (بدون Ready/merge/tag/release؛ بدون حذفِ شاخه). نقطهٔ تصمیم با مالک است: (الف) رفعِ نقض‌های پس‌از‌baseline، یا (ب) تصمیمِ صریحِ Owner برای baseline/سیاست. **#93 نه sync شد و نه ادعای انطباقش می‌شود.**
- **مرزهای صداقت/اجرا:** PHPUnit/PHPStan/phpcs محلی = **NOT RUN** (PHP/Composer در sandbox نیست؛ apt/packagist مسدود). تلاشِ runnerِ محلیِ php-wasm (`@php-wasm/node` 3.1.54 + tarballهای codeload) تا مرحلهٔ اجرای PHP و mount پیش رفت اما runtimeِ wasm امکانِ spawn ندارد و اجرای phpcs در بودجهٔ sandbox تمام نشد ⇒ مشخصه‌سازیِ per-sniff **NOT RETRIEVED**. شاهدِ معتبر = CI. Git: forward-only؛ بدونِ rebase/reset/clean/force-push/empty-commit؛ شاخهٔ مبدأ حفظ شد.

### [2026-09-20 (UTC)] — همان ایجنت (شاخهٔ `arena/01a0c064-doctor`، Draft PR #94) — **ادامهٔ #94**: bumpِ ratchetِ WPCS به `2263ed67…` + ثبتِ governance؛ بدونِ هیچ تغییرِ محصول
- **LIVE REVERIFY شروع (VERIFIED FACT):** `main = 2263ed673a8fdf4ab91e6a7fffae6d82252e4324` · `#93 = OPEN/DRAFT @ d440f2aafe41e82b1110e58b06ef09afba322960` · `#94 = OPEN/DRAFT @ ed24c6b15e8c41c24f9d2ad6571e00edd591d9a3` · open PRs = فقط #93 و #94 · collector/self-testهای #94 **بدونِ تغییر** (diff از `bd24ca6` = خالی؛ ۴/۴ PASS روی همین head) · آخرین migration = `0022` · working tree clean.
- **مبنای سیاست (Owner-approved، به‌عنوان ورودیِ این نشست):** «کدِ جدید/دست‌خورده نباید نقضِ جدید وارد کند»؛ بدهیِ legacy نه mass-format می‌شود و نه الزاماً پاک است؛ bumpِ baseline دستی + مستند است. **تغییرِ این نشست:** لنگرِ ratchet از `f23e0c50049f6f3e91f2425005ce8db08067af73` به `2263ed673a8fdf4ab91e6a7fffae6d82252e4324` منتقل شد — همان mainِ authoritative و مبنای #93/#94.
- **راستی‌آزماییِ عددِ «کامیت‌های انباشته»:** دستورِ این نشست «553» را فرض گرفته بود؛ اندازه‌گیریِ زنده روی main: `git rev-list --count f23e0c5…2263ed6` = **۵۶۵** (۴۸۶ غیرِ merge + ۷۹ merge، بازهٔ 2026-09-09 → 2026-09-20). در ثبتِ governance **عددِ راستی‌آزمایی‌شده** نوشته شد (NOT 553)؛ این تناقضِ کوچک صرفاً در شمارش است و به stateِ زنده/دامنهٔ fix دخالت نمی‌کند.
- **فایل‌های تغییرکرده (فقط زیرساخت/governance):** `.github/workflows/ci.yml` (تنها `WPCS_BASELINE` + کامنتِ ارجاع به بخشِ مستند) · `docs/drift-register.md` (بخشِ تازهٔ **§۸-۱-۱** به‌عنوان ثبتِ durable) · همین راهنما (همین entry). **هیچ sniff حذف/تضعیف نشد، هیچ مسیرِ محصولی exclude نشد، هیچ violation‌ای suppress نشد، گیت اختیاری نشد، رفتارِ fail-closed حفظ شد، هیچ PHPِ تاریخی فرمت نشد، هیچ فایلِ محصول/تست/migration تغییر نکرد، #93 دست‌نخورد.**
- **اثباتِ لازم (A–C):** (A) self-testهای collector = ۴/۴ PASS (زنده در CIِ exact-head، کامنتِ شواهد)؛ (B) اثباتِ زنده‌بودنِ collector: خطِ ناقضِ add‌شدهٔ مصنوعی در خودِ regression proof شناسایی می‌شود، حالتِ حذفی معتبر می‌ماند، و مسیرِ شکسته/حل‌نشده نمی‌تواند بی‌صدا PASS شود؛ (C) دلتای #94 نسبت به baselineِ جدید فاقدِ PHPِ محصولیِ in-scope است ⇒ انتظار می‌رود WPCS یک **no-op سبزِ راستی‌آزمایی‌شده** باشد (نه سبزِ کاذبِ تاریخی): collector روی همین دلتا باید `0` فایلِ in-scope و `0` خطِ added گزارش کند و این حالت طبقِ قرارداد فقط وقتی معتبر است که collection اثبات‌شده کامل باشد.
- **پیگیریِ لازم:** پس از این commit، شواهدِ exact-head (CI + Real WP + Closure + Pilot/Staging و ۱۹/۱۹ check-run) بازیابی می‌شود؛ در صورتِ terminal-success: Ready + merge با سبکِ merge-commitِ مخزن (بدونِ squash/rebase/حذفِ شاخهٔ مبدأ) و سپس شواهدِ post-merge روی merge SHA.
### [2026-09-20 (UTC)] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0c0e4-doctor`) — closure-only مستندات: Phase 9 Slice 1 (PR #93) — reconciliationِ پس‌از‌ادغام
- **هویت ایجنت:** ایجنت نوشتن/گیت‌هابِ **جدید** — نه ایجنتِ ادغام‌کنندهٔ PR #93 و نه ایجنتِ PR #94؛ به PR #93/#94 دست زده نشد. دامنه = **فقط مستندات**؛ بدونِ شروعِ Phase 9 Slice 2 و بدونِ ممیزیِ گستردهٔ مستندات.
- **LIVE REVERIFY شروع (VERIFIED FACT — GitHub/Git زنده، نه حافظهٔ چت):** `origin/main = a593958521386d459f76c5efa839d9962e1f67fc` (= merge PR #93؛ مطابق انتظار — main materially حرکت نکرده بود)؛ **open PRs = ۰**؛ **PR #93 = MERGED** در 2026-09-20T21:52:03Z با merge SHA `a593958521386d459f76c5efa839d9962e1f67fc`؛ والدین `b6c34c116807c99105aa603fc71ee220475a47a7` (= merge PR #94) + `9e7058cf9768ccb4dfce56eafbfb2c85f5e7c9bb` (= accepted headِ PR #93)؛ **هیچ closure PR موجودی #93 را پوشش نمی‌داد** (آخرین closureهای docs = PR #91 و PR #92 برای Phase 8).
- **شواهدِ post-merge #93 روی merge SHA `a593958…` (بازیابی زنده، همه push→main):** چهار workflow لازم همه `completed/success` — CI **`35539966134`** · Real WordPress Acceptance **`35539966131`** · Closure Gate **`35539966142`** · Pilot/Staging Readiness Gate **`35539966158`**؛ به‌علاوهٔ exact-head روی `9e7058cf…` = **۱۹/۱۹ check run success**.
- **راستی‌آزماییِ دامنهٔ محصولی (از خودِ درخت، نه از عنوانِ PR):** فایل‌های PR #93 = `pilot-gate.yml` · `assets/js/cpms-patient-portal.js` · `bin/pilot-slice-portal-cancel.py` · `src/Admin/PatientPortalPage.php` · `tests/Integration/Phase9Slice1PatientPortalSelfCancelRedTest.php`. هیچ فایلِ migration و هیچ ثبتِ `register_rest_route` جدیدی وجود ندارد؛ `PatientPortalPage.php` همان مسیرِ **موجودِ B4** یعنی `POST clinic/v1/appointments/{id}/cancel` را مصرف می‌کند (عبارتِ «cancel route config» در عنوانِ PR = همان آبجکتِ configِ سمت کلاینت `cancel_path`/`nonce`، نه REST route جدید). آخرین migration روی main = **`2026_09_20_0022_slot_holds_patient_binding.php`** (۲۲ فایل `0001`..`0022`؛ بدون `0023`).
- **حقایقِ ثبت‌شده:** Phase 8 = **CLOSED / TECHNICALLY COMPLETE (BOUNDED)** (بدون تغییر)؛ **Phase 9 — Patient Portal = STARTED / IN PROGRESS**؛ **Slice 1 = CLOSED (TECHNICAL; BOUNDED) via PR #93**؛ **Phase 9 کامل نیست** و **Patient Portal به‌طور کامل ادعا نمی‌شود**؛ **PR #94** = fixِ زیرساختِ CI + governanceِ WPCS ratchet — **برشِ فاز نیست** و governanceِ ratchet آن (مبنای `2263ed67…`) **بدون تغییر حفظ شد**. صداقتِ شواهد: عنوانِ PR #93 عبارتِ پیش‌از‌ادغامِ «test(red) … (RED)» را حفظ کرده — تاریخِ Git بازنویسی نمی‌شود؛ حالتِ نهاییِ ادغام‌شده شامل پیاده‌سازیِ GREEN محصول است.
- **فایل‌های تغییرکرده (فقط `docs/`؛ محدود به همین closure):** `docs/roadmap/roadmap.md` (ردیف Phase 9: `⏳` ← STARTED/IN PROGRESS + Slice 1 CLOSED) · `docs/governance/project-phase-taxonomy.md` (ردیف Phase 9 در جدولِ فازها؛ مثالِ نام‌گذاری؛ «Current live main» ← `a593958…` با demote شدنِ `9abd3379…`؛ خلاصهٔ حاکمِ §۸) · `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `a593958` در صدر + تاریخی‌سازیِ بلوکِ Slice 3؛ روایتِ «This file describes…»؛ rule of thumbِ §B؛ ردیفِ تازهٔ Phase 9 در جدولِ §C + اشاره‌گرهای `= current main`؛ سطرِ **Schema**) · `docs/agent-guide.md` (ردیف Phase 8 + ردیفِ تازهٔ Phase 9 در جدولِ فازها؛ و همین entryِ append-only) · `docs/README.md` (بلوکِ dated تازهٔ Phase 9 Slice 1 + noteِ 🔴 تاریخی روی بلوکِ Slice 3).
- **عمداً دست‌نخورده (تاریخی، نه کهنهٔ جاری):** همهٔ بلوک‌های checkpoint-scoped تاریخی و entryهای append-only قبلی؛ SHAهای تاریخی بازنویسی نشدند؛ تفکیکِ سیاست‌های **Owner-issued** (Hold-Timing؛ حداقلِ هویتِ بیمارِ جدید؛ FR-5.3؛ `nearby_slots`) حفظ شد؛ سابقهٔ بسته‌شدنِ Phase 8 حفظ شد؛ روایتِ «Remaining C6 (at `3fc5a54`)» در §K فقط **annotation** گرفت (بازنویسی نشد).
- **`CHANGELOG.md` عمداً تغییر نکرد:** عرفِ زندهٔ مخزن **الزام نمی‌کند** هر برشِ محدودِ محصول یک ورودیِ Unreleased بگیرد — جدیدترین ورودیِ CHANGELOG مربوط به **2026-09-12** است و **صفر** ورودی/ارجاع برای Phase 3 (PRs #49–#57)، Phase 4 (#59–#65)، Phase 5 (#67)، Phase 6 (#69–#76)، Phase 7 (#79/#81/#83/#85) و Phase 8 (#87/#89/#90) دارد. افزودنِ ورودی فقط به‌خاطرِ فقدانِ ورودیِ Slice-1 مطابق دستور **ممنوع** است.
- **شواهدِ این کامیتِ docs:** اجرای محلی PHPUnit/PHPStan/WPCS = NOT RUN (بدون PHP/Composer/MySQL در sandbox؛ عرفِ مخزن: CI شاهدِ معتبر است)؛ شواهدِ exact-headِ خودِ این closure پس از push بازیابی می‌شود.
- **Git/PR:** forward-only روی `arena/01a0c0e4-doctor` (از همان `a593958…` = live main باز شد)؛ بدون rebase/reset --hard/clean/force-push/history-rewrite/empty-commit؛ بدون tag/release؛ بدون حذفِ شاخهٔ مبدأ؛ دقیقاً **یک** PR فقط-مستندات.

### [2026-09-21 (UTC)] — ایجنت Arena (شاخهٔ `arena/01a0c36e-doctor`) — ثبتِ قراردادِ نهاییِ معماریِ پورتال (OWNER-ISSUED) + اثباتِ فنیِ پوستهٔ مستقل (پیش از Phase 9 Slice 3)
- **دامنه:** architecture-decision + technical-proofِ bounded — **نه** پیاده‌سازیِ Slice 3، نه محتوای Patient Portal، نه Staff Portal، نه migration/`0023`، نه REST/auth/token جدید، نه framework/build tooling کلاینتی، بدون تغییرِ ترتیب/وضعیتِ roadmap، بدون هیچ تغییرِ `src/`.
- **LIVE REVERIFY شروع (VERIFIED FACT — Git/GitHub زنده):** `main = origin/main = 1669c10e0f41c90f70001208f33a08c2f25ba7a4` (head شاخه هم همین) · open PRs = **۰** · آخرین migration = **`0022`** (۲۲ فایل `0001`..`0022`؛ بدون `0023`) · roadmap: Phase 9 = IN PROGRESS، Slices 1–2 = CLOSED · owner policies موجود در `docs/decisions/` · هیچ پیاده‌سازیِ Phase 9 Slice 3 شروع نشده · working tree clean.
- **ثبتِ تصمیم (یک محل — بدون تکرار):** `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` ← **بخش دوم** = قرارداد نهایی مالک «Three Environments + One CPMS Core + Shared WordPress Authentication Foundation + Independent CPMS Portals + Hybrid Rendering + Server-side Authorization» + **تصمیم نهاییِ روتینگ/ارائه** (Pageِ CPMS-owned + رهگیریِ `template_include` + قالبِ standaloneِ مالکِ کل سند؛ Page+shortcode alone = INSUFFICIENT؛ بدون rewrite framework). برچسبِ صریحِ OWNER-ISSUED PRODUCT/ARCHITECTURE POLICY مجزا از SRS تاریخی. ADR جدید عمداً ساخته نشد («یک محلِ موجودِ owner-decision» طبق دستور + اولویتِ سندیِ «تصمیمات نهاییِ کارفرما» در precedence policy).
- **اثبات فنی (NON-PRODUCTION، fixture-only، هرگز در release ZIP):** `tests/Fixtures/standalone-shell/` (رهگیر + قالبِ standalone + دو تمِ کنترلیِ مادّاً متفاوتِ alpha/beta) · `tests/Integration/StandaloneShellProofTest.php` (اثباتِ in-processِ A–H) · `bin/rwp-shell-proof.py` (اثباتِ مرورگریِ واقعی: ماتریسِ {Plain, Pretty} × {alpha, beta} با positive-control، نشستِ وردپرس + REST موجودِ `wp/v2/users/me` با/بدون nonce، logout واقعی، و برابریِ contract fingerprint) · اتصال به گیت‌های موجود: Real WP Acceptance (نصبِ فیکسچر + اجرا + انتشارِ لاگ در commit comment) و ci.yml (شاهدِ junit برای `StandaloneShellProofTest` — الحاقی، بدون تغییرِ معیار). قراردادِ ایزولاسیونِ کانتکست محلی راستی‌آزمایی شد (acceptance-isolation-check روی `rwp-shell-proof.py` = PASS).
- **ادعای سبز: در این کامیت نیست** (NOT RUN ≠ PASS؛ Inference ≠ fact). PHPUnit/PHPStan/WPCS/CI محلی = **NOT RUN** (بدون PHP/Composer/MySQL در sandbox؛ عرفِ مخزن: CI شاهدِ معتبر است). شواهدِ exact-head (Integration · Units · Tenant Tripwire · PHPStan · WPCS · Real WP Acceptance · Closure Gate · Pilot/Staging + check-runها) پس از ساختِ Draft PR بازیابی و **در بدنهٔ همان PR** با run-id ثبت می‌شود. برچسب‌های صادقانهٔ NOT VERIFIED در سند ثبت شد (runtimeِ fixture روی WP 6.4/6.5/6.6 و PHP 8.1/8.3/8.4 = NOT RUN؛ غیر از Chromium و غیر از Apache = NOT VERIFIED).
- **عمداً دست‌نخورده:** `docs/roadmap/roadmap.md` (بدون تغییرِ ترتیب/وضعیت — طبق دستور)، `docs/README.md`، `docs/project-current-state.md`، `docs/governance/project-phase-taxonomy.md`، `CHANGELOG.md`، `PatientPortalPage.php` و هر `src/`، migrationها، RESTها. محتوای تصمیم عمدتاً در همان یک سند است؛ این entry فقط روزنامهٔ کار است (بدون تکرارِ متنِ تصمیم).
- **Git/PR:** forward-only روی `arena/01a0c36e-doctor`؛ دقیقاً **یک Draft PR**؛ بدون rebase/reset --hard/clean/force-push/history-rewrite/empty-commit؛ بدون Ready/merge تا تکمیلِ شواهد؛ شاخهٔ مبدأ حفظ می‌شود.

### [2026-09-21 (UTC)] — همان ایجنت (شاخهٔ `arena/01a0c36e-doctor`، Draft PR #98) — fixِ هارنسِ proof (کلاس D) پس از گیتِ اولِ exact-head
- **گیت‌های head اول `6a04d484a425040e1edc60c27a259899f48f11ec` (بازیابیِ زنده):** CI `35589618372` = success (شامل Integration `OK (1106 tests, 18956 assertions)` و خودِ proof suite: `StandaloneShellProofTest` = **5 tests / 77 assertions / 0 failures**) · Pilot/Staging `35589577656` = success · Closure Gate `35589577631` = success · Real WP Acceptance `35589618403` = **failure** (هر دو leg ماتریس) ⇒ 19 check-run = ۱۷ success + ۲ failure.
- **طبقه‌بندیِ شکست: D (نقصِ تست/هارنس) — نه A/B/C.** Root cause بازیابی‌شده از کامنتِ evidence همان run (لاگِ debug.log): `PHP Fatal error: Uncaught Error: Call to undefined function wp_get_current_user() in .../mu-plugins/cpms-standalone-shell-proof-template.php:25` — بارگذارِ mu-plugins وردپرس **هر فایلِ `.php` ریشهٔ** `wp-content/mu-plugins/` را خودکار اجرا می‌کند؛ کپیِ کنارِ همِ «قالب» در ریشه باعث شد قالبِ proof زودتر از `pluggable.php` به‌عنوانِ «افزونهٔ must-use» اجرا شود → fatal در bootstrapِ هر wp-cli (قدمِ `wp user create`ِ خودِ step) → step پیش از نوشتنِ shell-proof.log مرد + Log gate عمداً fatal را گرفت. محصول (`src/`) بی‌تقصیر — صفر تغییرِ محصول در این PR.
- **fix (همین commit):** (۱) استقرارِ درستِ WordPress-native: قالب در `mu-plugins/cpms-shell-proof-fixture/` (زیرپوشه‌ها خودکار اجرا نمی‌شوند) + lookupِ دومسیره در رهگیر؛ (۲) fail-safe صریح در سرِ قالب (`function_exists('wp_get_current_user')` — بی‌صدا return در includeِ زودهنگام)؛ (۳) کامنتِ workflow شرحِ این قاعده. تلاشِ اول **بازنویسی نمی‌شود** — همان‌طور که هست ثبت شد. شواهدِ سرِ جدید پس از push در بدنهٔ PR #98 ثبت می‌شود (ادعای سبز بدون run-id در هیچ commit نمی‌آید).

### [2026-09-21 (UTC)] — همان ایجنت (شاخهٔ `arena/01a0c36e-doctor`، Draft PR #98) — fixِ دومِ probe (کلاس D — سلکتورِ login)
- **گیتِ head دوم `de93b97a7472aa46a2d5439fc922151bb371cede` (بازیابیِ زنده):** CI `35590482259` = success · Pilot/Staging `35590478571` = success · Closure `35590478476` = success · Real WP `35590482263` = **failure** — فقط step «Technical proof» در هر دو leg (Log gate این‌بار **پاک** = fixِ قبلی مؤثر بود). شواهدِ کاملِ proof در کامنتِ evidence همان run (id `201293665`/`201293699` روی merge SHA `d946888…`): **19 PASS / 1 FAIL** در خانهٔ اولِ ماتریس — positive-control تم alpha، نبودِ leak در home، سندِ کاملِ پوسته، fingerprint کامل، `anonymous`، self-authِ Plain (`users_me_url=…index.php?rest_route=/wp/v2/users/me`)، و `CPMS-SHELL-PROOF-RUNTIME-LOADED` همه PASS؛ تنها شکست: `Page.fill: waiting for locator("#user_password")`.
- **طبقه‌بندی: D (نقصِ probe) — نه A/B/C.** علت: شناسهٔ فیلدِ گذرواژهٔ `wp-login.php` در وردپرس **`#user_pass`** است (نه `#user_password`) — الگویِ canonical همین در `rwp-acceptance.py` است. محصول بی‌تقصیر؛ بقیهٔ ۱۹ ادعا در همان خانه اجرا و سبز شدند. fix: سلکتور به `#user_pass` + الگویِ `wait_for_url` همچون harness موجود. تلاش‌ها بازنویسی نمی‌شوند؛ شواهدِ head نهایی در بدنهٔ PR #98.

### [2026-09-21 (UTC)] — همان ایجنت (شاخهٔ `arena/01a0c36e-doctor`، Draft PR #98) — fixِ سومِ probe (کلاس D — .htaccess در حالتِ Pretty)
- **گیتِ head سوم `fee61b06ac1c2d08f637c02f08db2d78fa8894de` (بازیابیِ زنده):** CI `35591618735` = success · Pilot/Staging `35591614734` = success · Closure `35591614739` = success · Real WP `35591618840` = **failure** فقط در step «Technical proof» (Log gate پاک). شواهدِ کامل در کامنتِ evidence همان run (id `201294885`/`201294917`): **ماتریسِ Plain در هر دو تم ۱۰۰٪ PASS** شامل login واقعی (`cpms_patient`)، نشانِ نشستِ وردپرس، nonce `wp_rest`، REST موجودِ `wp/v2/users/me` با کوکی+nonce (۲۰۰ + `id=6` همان هویت)، کنترلِ منفیِ بدون nonce (۴۰۱)، logout واقعی → `anonymous`، و ثباتِ کاملِ fingerprint زیر نشست؛ Pretty شروع شد (positive-control هر دو تم PASS) ولی `/cpms-shell-proof/` و `/wp-json/...` → **۴۰۴ خودِ Apache** (بدنهٔ `<!DOCTYPE HTML PUBLIC "-//IETF//DTD HTML 2.0//EN">` در اثباتِ REST) — یعنی rewrite هرگز به WP نرسید.
- **طبقه‌بندی: D (نقصِ probe/محیطِ تست) — نه A/B/C.** علت: `wp rewrite flush --hard` در این محیط `.htaccess` مؤثر نمی‌نویسد — همان تلهٔ **مستندشدهٔ خودِ ریپو** در `pilot-gate.yml` («wp-cli --hard گاهی htaccess نمی‌نویسد؛ قواعد استاندارد WP مستقیم»). fix: نوشتنِ مستقیمِ بلاکِ استانداردِ WP در `.htaccess` برای حالتِ Pretty (دقیقاً الگویِ pilot-gate؛ AllowOverride All و mod_rewrite در همین job از قبل فعال‌اند) + بازسازیِ وضعیتِ اولیهٔ فایل + سخت‌گیریِ چکِ `pretty_permalink_self_authenticated` (پیکربندیِ خالی دیگر PASS کاذب نمی‌گیرد — یک PASS کاذبِ همین head شناسایی و بسته شد) + چکِ صریحِ `permalink_structure` در هر حالت. محصول بی‌تقصیر. تلاش‌ها بازنویسی نمی‌شوند.

### [2026-09-21 (UTC)] — همان ایجنت (شاخهٔ `arena/01a0c36e-doctor`، Draft PR #98) — رفعِ چهارم (class D: چکِ خطایِ صفحه، هم‌تراز با حدِّ `is_cpms_url` ریپو)
- **گیتِ head چهارم `7b91ebb929fae7c8765296fd425a6b982a8ff308` (بازیابیِ زنده):** CI `35594316166` = success · Pilot `35594313141` = success · Closure `35594313015` = success · Real WP `35594316163` = failure. **job `prefix wp_` همه‌ی استپ‌ها success؛ job `prefix clinic_` فقط در استپِ proof شکست.** شواهدِ کامنت (merge SHA `5689540ea6cc`): `wp_` leg = **141 passed / 0 failed** شاملِ `shellproof.contract_equal_across_modes_and_themes` («1 fingerprint from 4 cells» = گزاره‌های G+H با فین‌گرپرینتِ برابرِ ۴ سلول) و `permalink_structure_expected` هر دو mode؛ `clinic_` leg = **140 passed / 1 failed** — فقط `shellproof.no_page_errors` با `Cannot read properties of undefined (reading 'serialize')` در context بیمارِ سلولِ `pat-plain-cpms-proof-theme-beta`.
- **طبقه‌بندی: D (چکِ harness پهناورتر از سطحِ ادعایِ قرارداد) — نه A/B/C.** منبعِ آن خطایِ JS **NOT RETRIEVED** است (در `assets/js` هیچ `.serialize` نیست؛ URL صفحه در آن build ثبت نمی‌شد؛ در ۱ از ۸ context بیمار دیده شد؛ آرتیفکت/لاگِ خام پشتِ blob بازیابی نشد). طبقِ انضباطِ **موجودِ خودِ ریپو** (چکِ `browser.no_console_errors` در `rwp-acceptance.py` فقطِ صفحاتِ CPMS را گیت می‌کند — `is_cpms_url`) گیت به سطحِ ادعا تنظیم شد: `shellproof.no_page_errors_on_shell_pages` فقط خطایِ صفحاتِ shell را قرمز می‌کند؛ **همه‌ی** خطاها (با URL) در `logs/shell-proof-page-errors.log` + چکِ اطلاعاتیِ افشاشدهٔ `non_shell_page_errors_informational` می‌مانند؛ نه نقصِ محصول ادعا می‌شود، نه پنهان می‌شود، نه assertion برای پنهان‌کردنِ باگ عوض شد (کلاسِ چک با «سطحِ ادعایِ قراردادِ پوسته» هم‌تراز شد — خطاهایِ صفحاتِ میزبانِ وردپرس خارج از قراردادِ مستندِ Part 2-G هستند و این PR مجاز به تغییرِ Product/Portal نیست). listenerها از این commit URL صفحه را ثبت می‌کنند.
- **تلاش‌ها بازنویسی نمی‌شوند.** محصول بی‌تقصیر.

### [2026-09-21 (UTC)] — ایجنت تازهٔ Arena (نوشتن/گیت‌هاب؛ شاخهٔ `arena/01a0c47d-doctor`) — closure-only مستندات: Phase 9 Slice 3 (PR #99) — reconciliationِ پس‌از‌ادغام
- **هویت ایجنت:** ایجنت نوشتن/گیت‌هابِ **جدید** — نه ایجنتِ PR #98 و نه ایجنتِ PR #99؛ به PR #98/#99 دست زده نشد. دامنه = فقط مستنداتِ current-state؛ **بدونِ** تغییرِ کد محصول/تست/workflow/asset/schema/migration/configuration/runtime؛ **بدونِ** شروعِ Slice بعدی؛ **بدونِ** بازبینیِ مجددِ معماری/محصول.
- **LIVE REVERIFY پیش از هر write (VERIFIED FACT — Git/GitHub زنده):** `main = origin/main = 8a324e59341851f5ced01ed37ecd5e69c725f30a` (= "Merge pull request #99 from bia2on2on/arena/01a0c3f2-doctor"؛ والدین `352a093649265063fc055585fed7bacafbb37453` (= merge PR #98) + `8d6231c19d3caccb94194836040f575a1566d345` (= accepted headِ PR #99)) · open PRs = **۰** · PR #99 = **MERGED 2026-09-21T14:53:05Z**، head `8d6231c1…`، merge `8a324e59…` · post-merge روی همان merge SHA: CI `35615159555` · Real WordPress Acceptance `35615159562` · Closure Gate `35615159508` · Pilot/Staging Readiness Gate `35615159515` — همه `push`→`main`، `completed`/`success` + ۱۹/۱۹ check run success · exact-headِ `8d6231c1…`: ۱۹/۱۹ check run success (CI `35608010833` · Real WP `35608010848` · Closure `35608005843` · Pilot/Staging `35608005844`) · آخرین migration = **`2026_09_20_0022_slot_holds_patient_binding.php`** (۲۲ فایل `0001`..`0022`؛ **بدون `0023`**) · هیچ closure PR موجودی #99 را پوشش نمی‌داد (آخرین closureها: #95 = Slice 1، #97 = Slice 2) · working tree clean. main مادّاً جابه‌جا نشده بود؛ ادامه داده شد.
- **حقایقِ ثبت‌شده:** **Phase 9 — Patient Portal = IN PROGRESS؛ کامل نیست**؛ Slice 1 = CLOSED (PR #93)؛ Slice 2 = CLOSED (PR #96)؛ **Slice 3 = CLOSED / TECHNICALLY COMPLETE (BOUNDED) via PR #99** — پوستهٔ مستقلِ CPMS Patient Portal اکنون کدِ production روی main است (CPMS-owned Page + `template_include` مالکِ افزونه + قالبِ standaloneِ تمام‌سند طبق قراردادِ PR #98)؛ Patient Portal دیگر wp-admin را به‌عنوان UI عملیاتی به‌کار نمی‌برد؛ Theme فعال مالکِ ارائهٔ پوسته نیست؛ **بدونِ REST endpoint جدید، بدونِ سیستم auth جدید، بدونِ migration**؛ **تأیید بصریِ مالک برای پوستهٔ Slice 3 = COMPLETE در accepted head** (اظهارِ مالک در دستورِ همین closure — PR #99 هیچ review یا کامنتِ انسانیِ جداگانه‌ای ندارد؛ منبعِ ادعا صادقانه ثبت شد)؛ **قرارداد/اثباتِ معماریِ PR #98 همچنان تصمیمِ حاکم است** (بدون تکرار/بازنویسی)؛ **هیچ ادعای release/go-live/V1-complete**؛ هیچ Slice بعدی شروع نشده.
- **فایل‌های تغییرکرده (فقط `docs/`؛ شناسایی با جست‌وجوی زندهٔ نشانگرهای CURRENT/LIVE کهنه):** `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `8a324e59` در صدر + تاریخی‌شدنِ بلوکِ `c18c18f4`؛ به‌روزرسانیِ نشانگرهای «current main / current live main / current integrated main checkpoint / Current live truth» که هنوز `a593958`/`c18c18f4` را جاری می‌خواندند: پاراگرافِ «This file describes…»، یادداشتِ ابهام‌زداییِ Phase 9، ردیفِ Phase 9 در جدولِ فازها، پاراگرافِ «Integrated main checkpoint:»، خطِ Schema، annotationِ «Current live truth») · `docs/roadmap/roadmap.md` (ردیف Phase 9: Slice 2 «current main» → superseded؛ افزودنِ Slice 3 CLOSED (BOUNDED)) · `docs/governance/project-phase-taxonomy.md` (ردیفِ Phase 9، «Current live main»، خلاصهٔ Phase 9) · `docs/agent-guide.md` (ردیفِ Phase 9 + همین entry) · `docs/README.md` (بلوکِ «به‌روزرسانی» تازه + annotationِ 🔴 تاریخی روی بلوکِ Slice 2) · `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` (**فقط یک annotationِ کوچکِ وضعیت** زیرِ بندِ Readiness که «Slice 3 همچنان شروع نشده» می‌گفت — سیاست/قرارداد بازنویسی/تکرار نشد).
- **موارد کهنهٔ عمداً دست‌نخورده (تاریخی، نه کهنهٔ جاری):** بلوک‌های «Previous integrated main checkpoint (historical…)» و عبارت‌های «= current main» برای `d7484ce…` در متن‌های Phase 7 (چک‌پوینتِ خودشان؛ closureهای #91/#92/#95/#97 هم دست نزدند — cosmetic cleanup ممنوع)؛ عبارت‌های تاریخیِ SRS؛ ترتیبِ roadmap.
- **`CHANGELOG.md` عمداً تغییر نکرد:** همان عرفِ ثبت‌شدهٔ بلوک‌های Slice 1/2 — جدیدترین ورودی 2026-09-12 است و Phase 3..9 (PRs #49–#99) هیچ ورودی ندارند؛ عرفِ زنده الزام نمی‌کند.
- **شواهدِ این کامیتِ docs:** اجرای محلی PHPUnit/PHPStan/WPCS = NOT RUN (بدون PHP/Composer/MySQL در sandbox؛ عرفِ مخزن: CI شاهدِ معتبر است)؛ شواهدِ exact-headِ خودِ این closure پس از push بازیابی و در بدنهٔ PR ثبت می‌شود؛ NOT RUN ≠ PASS؛ PR-head ≠ post-merge.
- **Git/PR:** forward-only روی `arena/01a0c47d-doctor` (از همان `8a324e59…` = live main باز شد)؛ بدون rebase/reset --hard/clean/force-push/history-rewrite/empty-commit؛ بدون tag/release؛ بدون حذفِ شاخهٔ مبدأ؛ دقیقاً **یک** PR فقط-مستندات؛ پس از merge این closure، ایجنت از کارِ WRITE بازنشسته می‌شود و Slice بعدی را شروع نمی‌کند.

### [2026-09-21 17:26 UTC] — Arena agent (branch `arena/01a0c4ae-doctor`, Draft PR #101) — Phase 9 Slice 4 write-rejection correctness fix
- **Scope:** one A-class correctness blocker in `PatientService::updateMe()` only; no selector, route, profile-field, mobile, booking, ScopeContext, Organization Identity, migration/schema, UI, or other Phase-9 behavior changed.
- **Live pre-write pin:** local/remote/PR #101 head = `2e816f44dbefd0c5601e61f31478bcaa8f60ed49`; PR remains OPEN + DRAFT to `main` `3c1db8250f24cff509b7a544317c01043c1e1244`; the only open PR is #101; latest migration is `0022`; no `0023` exists.
- **Root cause:** `CpmsDb::update()` casts wpdb `false` and legitimate `0` affected-row results to the same integer `0`; `updateMe()` ignored that outcome and could read/audit/return success after a rejected UNIQUE write.
- **Action:** immediately after the existing write, the profile path now treats nonempty immediate `$wpdb->last_error` as failure. A post-failure same-Clinic national-ID lookup maps an actual competing record to established `CLINIC_VALIDATION_FAILED`/400; another rejected write is fail-closed with sanitized `CLINIC_INTERNAL_ERROR`/500. A zero-row write with empty `last_error` remains a valid idempotent/no-change save; affected-row count is deliberately not used as a failure predicate.
- **Regression:** added one guard to the existing Slice-4 Integration class. It proves a same-value profile save remains 200, then deterministically injects a same-Clinic competing national ID after the pre-check and before the real wpdb UPDATE. The actual UNIQUE constraint rejects the selected write; assertions require canonical 400 validation, no raw duplicate detail, unchanged selected state, and no successful `PATIENT_PROFILE_UPDATED` audit. This is deterministic branch evidence, **not** a runtime reproduction of a concurrent race.
- **Local validation:** PHP 8.1 WASM syntax lint passed for the two changed PHP files; `git diff --check` passed. Native PHP/Composer/MySQL/PHPUnit/PHPStan/WPCS are unavailable in this sandbox; exact-head GitHub gate evidence is pending push.
- **Commits/CI:** pending at time of this append; this append-only entry is included with the narrow fix.
- **Tree:** dirty only with the narrow service/test fix and this required append-only agent log; no migration files changed.

### [2026-09-21 (UTC)] — Arena agent (branch `arena/01a0c516-doctor`) — closure-only مستندات: Phase 9 Slice 4 BACKEND FOUNDATION (PR #101) — reconciliationِ پس‌از‌ادغام
- **هویت ایجنت:** ایجنت نوشتن/گیت‌هابِ **جدید** — نه ایجنتِ ادغام‌کنندهٔ PR #101؛ دامنه = **فقط مستندات (ONE bounded documentation-only closure PR)**؛ بدون لمسِ کد، تست‌ها، workflowها، assetها، migration/schema، پیکربندی یا رفتار زمان اجرا؛ بدون شروعِ Profile UI یا فاز بعدی؛ بدون ادعای release/go-live/V1/تجاری.
- **LIVE REVERIFY پس‌از‌ادغامِ PR #101 (VERIFIED FACT — Git/GitHub زنده):**
  - PR #101 MERGED at `2026-09-21T17:40:46Z` با head `d0580bebccdeee66d492a4b1607c7c223c4b8ff2` به `main`.
  - exact merge commit SHA روی `origin/main` = `28e7fbf61ec4e783f81dc3ecfd7ba92e6a2b8380` (والدین: parent 1 = `3c1db8250f24cff509b7a544317c01043c1e1244` [merge PR #100]، parent 2 = `d0580bebccdeee66d492a4b1607c7c223c4b8ff2` [accepted head PR #101]).
  - open PRs در زمان شروع = ۰.
  - چهار workflow لازم پس‌از‌ادغام روی exact merge SHA `28e7fbf61ec4e783f81dc3ecfd7ba92e6a2b8380`:
    - CI `35633544036` = `completed` / `success` (همهٔ ۶ job موفق).
    - Real WordPress Acceptance `35633543948` = `completed` / `success`.
    - Closure Gate `35633543996` = `completed` / `success`.
    - Pilot/Staging Readiness Gate `35633544127` = `completed` / `success`.
    - همهٔ ۱۹/۱۹ check runs روی exact merge SHA موفق.
  - وضعیت Schema: ۲۲ فایل migration (`0001`..`0022`)، آخرین migration = `2026_09_20_0022_slot_holds_patient_binding.php`؛ بدون `0023`.
- **حقایقِ ثبت‌شده:**
  - **Phase 9 — Patient Portal = IN PROGRESS؛ کامل نیست.**
  - Slices 1–3 = CLOSED (بدون تغییر).
  - **Slice 4 BACKEND FOUNDATION = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #101**.
  - قرارداد محصولیِ Slice 4: (1) بیمار فقط پرونده‌های فعالِ کلینیکی پیوندیافته با حساب کاربری جاری وردپرس را می‌بیند/انتخاب می‌کند؛ (2) `link_id` فقط selector است نه منبع اختیار (authority)؛ (3) N>1 پروندهٔ فعال بدون selector صریح با `422 CLINIC_SELECTION_REQUIRED` fail-closed می‌شود؛ (4) هیچ fallback پیش‌فرضی به ردیفِ اول/primary وجود ندارد؛ ۱ پیوند خودکار resolve می‌شود؛ ۰ یا selector نامعتبر با `404 CLINIC_NOT_FOUND` fail-closed می‌شود؛ (5) هیچ ادغام cross-Clinic یا پیونددهی خودکار رخ نمی‌دهد؛ (6) هویت موبایل/ورود کاربر read-only است؛ (7) کد ملی Clinic-scoped در ذخیره‌سازی، اعتبارسنجی‌شده، در پروفایل قابل ویرایش و الزام رزرو نیست؛ برخورد کد ملی در همان کلینیک پیش از تغییر/audit با `400 CLINIC_VALIDATION_FAILED` رد می‌شود؛ (8) همگام‌سازی هویت سازمان (Org Identity) خارج از این برش است؛ (9) **Slice 4 Patient Profile UI پیاده‌سازی‌نشده / pending باقی است**.
  - قراردادهای معماریِ پیشین (`docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md`) تصمیم حاکم باقی می‌مانند.
- **فایل‌های تغییرکرده (فقط `docs/`):**
  - `docs/project-current-state.md`
  - `docs/roadmap/roadmap.md`
  - `docs/governance/project-phase-taxonomy.md`
  - `docs/README.md`
  - `docs/agent-guide.md`
  - `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md`
- **Git/PR:** forward-only روی `arena/01a0c516-doctor` (شاخه‌بندی از `28e7fbf61ec4e783f81dc3ecfd7ba92e6a2b8380` = live main)؛ ایجاد یک PR فقط-مستندات؛ تجمیع پس از سبز شدن بررسی‌های سرشاخه؛ تأیید ران‌های پس‌از‌ادغام؛ و بازنشستگی دائمی از کار WRITE.


### [2026-09-23] — PR #105 same writer — authorized Phase 9 My Visits GREEN
- Live pre-write main `ed7db7acfa56129035d762e8388c86fded8b7c24`; draft #105 at accepted VALID RED `960b1f50d6f76342bb3bd0be50e7afefdde236cc`; clean tree; only open PR #105; migration 0022 unchanged.
- Owner authorized GREEN on the same branch `arena/01a0cb00-doctor`, no merge. Reuse C5/C6 and Profile's existing public resolver (no extraction/new policy); add only Visits section/nav and vanilla read-only list/detail state. Preserve bounds/visibility/audit; no migration, new endpoint, framework, prescriptions-list or Files UI.
- Accepted PHPUnit assertions unchanged. Existing browser journey extended to 390/768/1366, sole-record detail, canonical nonce rejection, screenshots/health. D: asynchronous browser context assertions now wait for rendered text instead of racing response headers; same content assertions, no weakening.
- Native PHP setup unavailable (C: Debian network); PHP WASM lint passes, JS syntax and Python AST parse pass. Exact-head CI/browser results and one bounded post-GREEN self-review will be recorded on #105; pending is not PASS. No merge/phase closure claimed.

- GREEN first-head evidence `dba80b7`: CI `35788630376` Integration **OK (1141 tests, 22197 assertions)**, no errors/warnings/skips. A: changed-line WPCS findings corrected narrowly, existing public/named-argument and domain-property spelling retained with targeted naming annotations. Local WASM PHPCS 3.13.5/WPCS 3.3.0 scan of touched lines now clean; no dependencies added to the repository.
- D: Pilot `35788625373` stopped in older Slice-1/2 config allowlist before the Visits fixture. Its accepted-key list lacked the two newly contracted safe route templates. Test-only fix admits exactly `visits_path`/`visit_detail_path` and asserts `/visits`/`/visits/{id}`; authority-key bans unchanged. Visits browser evidence NOT RUN at that intermediate head; no claim of success.


### [2026-09-23] — NEW WRITER — bounded documentation-only closure for merged PR #105 (Phase 9 “My Visits”)
- **Live pre-write verification (all retrieved from GitHub, not assumed):** authoritative `main` = `f09a81c132d545d1d1657029632a8b069515dd26` (= the expected PR #105 merge SHA; `origin/main` re-fetched and unchanged); **open PRs = 0**; **PR #105 = MERGED** (MERGED 2026-09-23T04:37:56Z, branch `arena/01a0cb00-doctor`); merge parents = `ed7db7acfa56129035d762e8388c86fded8b7c24` (previous main = merge of PR #104) + `3e7d2103044ef6282953f04dea9f8c47bb18443f` = **the exact accepted Jalali-corrected head** (`fix(portal): display paired Jalali dates in My Visits`). Working tree clean including untracked files before writing. No product code, test, workflow, migration, schema or dependency file was touched.
- **Merge-SHA (exact-head) required workflow families — all `push`→`main`, all `completed`/`success`, retrieved live:** CI `35819115348` · Real WordPress Acceptance `35819115341` · Closure Gate `35819115342` · Pilot/Staging Readiness Gate `35819115349`; **19/19 check runs `completed`/`success`** on `f09a81c…`; no queued/in_progress/cancelled run counted as PASS.
- **Migration:** latest remains `2026_09_20_0022_slot_holds_patient_binding.php` (22 migration files `0001`..`0022` under `clinic-practice-management/src/Migrations/`; no `0023`). **PR #105 added no migration** (its 12 changed files include no `src/Migrations/` path).
- **حقایقِ ثبت‌شده (bounded closure claims):**
  - **Phase 9 — Patient Portal = IN PROGRESS؛ کامل نیست** (NOT a release / go-live / V1 / commercial-completeness claim; the Patient Portal as a whole is not claimed complete).
  - Previous completed Patient Portal slices remain CLOSED: Slices 1–3 via PR #93 / #96 / #99, Slice 4 BACKEND FOUNDATION via PR #101, Slice 4 Patient Profile UI via PR #103 — unchanged, preserved as historical blocks.
  - **My Visits = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #105** (merge `f09a81c132d545d1d1657029632a8b069515dd26`; accepted head `3e7d2103044ef6282953f04dea9f8c47bb18443f`).
  - Accepted patient-record selector semantics now active for **C5** `GET /clinic/v1/visits` and **C6** `GET /clinic/v1/visits/{id}`: `link_id` is **selector-only** (never tenant/patient authority; the authenticated WP user remains the sole authority); **0 ⇒ canonical safe not-found `404 CLINIC_NOT_FOUND`**؛ **1 ⇒ auto-resolution allowed**؛ **N>1 بدون selector ⇒ `422 CLINIC_SELECTION_REQUIRED`**؛ **selector نامعتبر/بیگانه/غیرفعال ⇒ `404` غیرِشماریِ یکسان (non-enumerating)**؛ **هیچ fallback به primary/اولین در C5/C6 وجود ندارد**.
  - **Jalali/Shamsi:** تاریخ‌های ویزیتِ قابل‌رؤیتِ بیمار به‌ صورت **Jalali/Shamsi** نمایش داده می‌شوند (فیلدِ افزایشیِ جفت‌شدهٔ `visit_jalali = ClinicCore\Domain\Time\Jalali::formatYmd(visit_date)` روی ردیف‌های C5 و شیء `visit` در C6) در حالی که **معنایِ خامِ بک‌اند/ذخیره‌سازیِ `visit_date` همان `Y-m-d` گرگوری و بدون تغییر است**؛ بدون موتور تقویم دوم، بدون وابستگی `Intl`، بدون تغییر storage/timezone/Location/UTC.
  - **Next defined Patient Portal capability = “My Prescriptions” (per the owner-policy ordering Profile/Visits/Prescriptions/Files) and it remains NOT IMPLEMENTED / pending** — not started, not scoped; **no future contract is invented**.
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):**
  - `docs/project-current-state.md` — new top integrated-main checkpoint block for `f09a81c…` + historical demotion of the `9909ce1…` block; §C Phase 9 row (My Visits closure + selector semantics + Jalali + migration 0022 + next = My Prescriptions pending); the §C “Integrated main checkpoint” paragraph; the §J “This file describes the current integrated main checkpoint …” paragraph; the §Schema re-verification sentence; and the “current live truth” annotation inside the preserved historical C6 block.
  - `docs/roadmap/roadmap.md` — Phase 9 row tail only.
  - `docs/agent-guide.md` — این ورودیِ لاگ (append-only؛ بدون بازنویسی ورودی‌های پیشین، از جمله ورودیِ PR #105 همان نویسنده).
- **اسنادِ کهنه‌ای که لمس نشد (خارج از دامنهٔ این closure و ثبت‌شده برای شفافیت):** خلاصهٔ جدولِ Phase 9 در همین فایل (سطرِ `> | Phase 9 | Patient Portal |`) و `docs/governance/project-phase-taxonomy.md` (سطر Phase 9 + بندِ «Current live truth») هنوز «Slice 4 Patient Profile UI پیاده‌سازی‌نشده / pending» را می‌گویند — این کهنگی پیش از این closure و متعلق به closure مستنداتِ PR #104 است (PR #104 این دو موضع را به‌روز نکرد). اصلاحِ آن‌ها بازنویسیِ دامنهٔ closure پیشین است و در این بستهٔ مستندات انجام نشد.
- **Git/PR:** forward-only روی `arena/01a0cc97-doctor` (از `f09a81c132d545d1d1657029632a8b069515dd26` = live main شروع شد)؛ بدون rebase / reset --hard / git clean / force-push / بازنویسی تاریخچه / empty commit؛ بدون tag/release؛ without merge.


### [2026-09-23] — NEW WRITER — minimal addendum on PR #106: remaining CURRENT/LIVE Phase 9 statements in this file + `project-phase-taxonomy.md`
- **Same writer, same branch `arena/01a0cc97-doctor`, same PR #106 — no new PR, no merge, forward-only, no rebase/force-push.** Live re-verification before writing: `origin/main` = `f09a81c132d545d1d1657029632a8b069515dd26` (unchanged; = expected PR #105 merge SHA); open PRs = 1 (documentation-only **PR #106**); PR #106 OPEN / MERGEABLE / CLEAN, head at write time `e03f4c845957ed0bd6ea428077ae44409699b5fe`; working tree clean.
- **Scope of this addendum:** only statements that claim to describe the **CURRENT / LIVE** Phase 9 state. Historical checkpoints and older append-only log entries were **not** rewritten.
- **Corrected (exact positions):**
  - `docs/agent-guide.md` **line 29** — the Phase 9 row of the top *current-status* Roadmap table: `merge 28e7fbf… = **current main**` → `main at the Slice-4-backend-foundation closure checkpoint (superseded as live main)`; `**Slice 4 Patient Profile UI پیاده‌سازی‌نشده / pending باقی است**` → Slice 4 Patient Profile UI = CLOSED (BOUNDED) via PR #103 + **My Visits = CLOSED / TECHNICALLY COMPLETE (BOUNDED) via PR #105** (merge `f09a81c132d545d1d1657029632a8b069515dd26` = **current main**; accepted head `3e7d2103044ef6282953f04dea9f8c47bb18443f`; post-merge gates CI `35819115348` · Real WP `35819115341` · Closure Gate `35819115342` · Pilot/Staging `35819115349` + ۱۹/۱۹)؛ and the closing sentence now names **My Prescriptions** as the next defined capability = NOT IMPLEMENTED / pending. **Only this one line changed in this file's non-log content.**
  - `docs/governance/project-phase-taxonomy.md` **line 70** — Phase 9 crosswalk/status row: `= **current main**` (on `28e7fbf…`) → `main at the Slice-4-backend-foundation closure checkpoint (since superseded as live main)`; `**Patient Profile UI پیاده‌سازی‌نشده / pending باقی است**` → Profile UI CLOSED via PR #103 + **My Visits CLOSED via PR #105** (`f09a81c…` = current live main) with the accepted multi-record `link_id` selector semantics and the Jalali-display note; final status cell now: **IN PROGRESS** — Phase 9 not complete, next defined capability (**My Prescriptions**) NOT IMPLEMENTED / pending.
  - `docs/governance/project-phase-taxonomy.md` **line 150** — new **Current live main (re-verified 2026-09-23, after the Phase 9 My Visits closure)** bullet for `f09a81c132d545d1d1657029632a8b069515dd26`; the previous `28e7fbf…` bullet was **retitled** to `Previous live main (historical; re-verified 2026-09-21 … superseded as live main by … PR #103 / PR #104 / PR #105 …)` and **its body is preserved byte-for-byte** (only the bullet label prefix changed).
  - `docs/governance/project-phase-taxonomy.md` **line 160 (was 158)** — §۸ «وضعیت جاری (خلاصهٔ ثابت — حاکم)» Phase 9 bullet: `= current live main` (on `28e7fbf…`) → `main at the Slice-4-backend-foundation closure checkpoint, since superseded as live main`; Profile UI CLOSED via PR #103 + **My Visits CLOSED via PR #105** (`f09a81c…` = current live main) added; closing sentence now names **My Prescriptions** as the pending next capability.
- **Preserved as historical (not touched / not rewritten):** all "Previous integrated main checkpoint (historical)" blocks in `docs/project-current-state.md`; the body of the retitled `Previous live main` bullet in `project-phase-taxonomy.md`; every earlier append-only log entry in this file — including the PR #102 closure entry at line ~1200 (`**Slice 4 Patient Profile UI پیاده‌سازی‌نشده / pending باقی است**`, correct for its own checkpoint) and the previous PR #106 entry at line ~1237 which recorded these two positions as deliberately out of scope. That earlier decision is **superseded by this addendum on the same PR**, and the older entry is left in place per the append-only policy rather than rewritten.
- **Current truth recorded:** Phase 9 = **IN PROGRESS** · Slice 4 backend = **CLOSED** · Slice 4 Patient Profile UI = **CLOSED** · **My Visits = CLOSED / TECHNICALLY COMPLETE (BOUNDED) via PR #105** (merge `f09a81c132d545d1d1657029632a8b069515dd26`) · accepted multi-record `link_id` selector semantics · patient-visible My Visits dates **Jalali** with raw backend/storage dates unchanged · latest migration **`2026_09_20_0022_slot_holds_patient_binding.php`** (PR #105 added no migration) · next defined capability **My Prescriptions = NOT IMPLEMENTED / pending**. **No future contract invented.**
- **Files changed by this addendum (docs-only):** `docs/agent-guide.md` (1 status-table line + this append-only entry) and `docs/governance/project-phase-taxonomy.md` (3 current-state positions). No product code, tests, workflows, schema, migrations, README, CHANGELOG or unrelated docs.


### [2026-09-23] — NEW WRITER — bounded documentation-only closure for merged PR #107 (Phase 9 “My Prescriptions”)
- **Live pre-write verification (all retrieved from GitHub, not assumed):** authoritative `main` = `d0644706324be211e7fe68d1f9f2b8232357f704` (= the expected PR #107 merge SHA; `origin/main` re-fetched and unchanged); **open PRs = 0**; **PR #107 = MERGED** (MERGED 2026-09-23T09:59:20Z, branch `arena/01a0cd28-doctor`); merge parents = `cfc13ab64938666e2587f1cbaf132c8d6d418307` (previous main = merge of PR #106 — Phase 9 My Visits bounded documentation closure) + `d0f390e997fb09e22aa00701170bc01161407153` = **the exact accepted PR #107 head** (`feat(phase9): My Prescriptions list + explicit patient-record selection (GREEN)`). Working tree clean including untracked files before writing. No product code, test, workflow, migration, schema or dependency file was touched.
- **Merge-SHA (exact-head) required workflow families — all `push`→`main`, all `completed`/`success`, retrieved live:** CI `35846142339` · Real WordPress Acceptance `35846142325` · Closure Gate `35846142311` · Pilot/Staging Readiness Gate `35846142296`; **19/19 check runs `completed`/`success`** on `d064470…`; no queued/in_progress/cancelled run counted as PASS.
- **Migration:** latest remains `2026_09_20_0022_slot_holds_patient_binding.php` (22 migration files `0001`..`0022` under `clinic-practice-management/src/Migrations/`; no `0023`). **PR #107 added no migration** (its 11 changed files include no `src/Migrations/` path).
- **حقایقِ ثبت‌شده (bounded closure claims):**
  - **Phase 9 — Patient Portal = IN PROGRESS؛ کامل نیست** (NOT a release / go-live / V1 / commercial-completeness claim; the Patient Portal as a whole is not claimed complete).
  - Previous completed Patient Portal slices remain CLOSED: Slices 1–3 via PR #93 / #96 / #99, Slice 4 BACKEND FOUNDATION via PR #101, Slice 4 Patient Profile UI via PR #103, My Visits via PR #105 — unchanged, preserved as historical blocks.
  - **My Prescriptions = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #107** (merge `d0644706324be211e7fe68d1f9f2b8232357f704`; accepted head `d0f390e997fb09e22aa00701170bc01161407153`).
  - Accepted patient-record selector semantics now active for **C7** `GET /clinic/v1/prescriptions`: `link_id` is **selector-only** (never tenant/patient authority; the authenticated WP user remains the sole authority); **0 ⇒ canonical safe not-found `404 CLINIC_NOT_FOUND`**؛ **1 ⇒ auto-resolution allowed**؛ **N>1 بدون selector ⇒ `422 CLINIC_SELECTION_REQUIRED`**؛ **selector نامعتبر/بیگانه/غیرفعال/cross-clinic ⇒ `404` غیرِشماریِ یکسان (canonical non-enumerating)**؛ **هیچ fallback به primary/اولین در C7 وجود ندارد**.
  - Server-side **`is_patient_visible = 1` constraint and draft exclusion (`status = 'draft'`) strictly enforced**. Patient-visible prescriptions remain **read-only** (no refill/mutation/printing/pharmacy action).
  - **Jalali/Shamsi:** تاریخ‌های نسخهٔ قابل‌رؤیتِ بیمار بر پایهٔ تایم‌زونِ عملیاتیِ Location به‌ صورت **Jalali/Shamsi** نمایش داده می‌شوند (`created_at_jalali = ClinicCore\Domain\Time\Jalali::formatYmd(local_date)`) در حالی که **معنای خامِ بک‌اند/ذخیره‌سازیِ datetime همان گرگوری UTC `Y-m-d H:i:s.000` و بدون تغییر است**؛ بدون موتور تقویم دوم، بدون وابستگی `Intl`، بدون تغییر storage/timezone/Location/UTC.
  - UI display allowlist includes prescription number, safe status, generic and brand name, strength, form, dose, frequency, route, duration, instructions, and Jalali date; internal operational fields (`void_reason`, `is_patient_visible`, `drug_ref_id`, `correction_of_prescription_id`) are suppressed. No Files UI added.
  - **Next defined Patient Portal capability = “My Files” (per the owner-policy ordering Profile/Visits/Prescriptions/Files and wireframe §7) and it remains NOT IMPLEMENTED / pending** — not started, not scoped; **no future contract is invented**.
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):**
  - `docs/project-current-state.md` — new top integrated-main checkpoint block for `d064470…` + historical demotion of the `f09a81c…` block; §C Phase 9 row (My Prescriptions closure + C7 selector semantics + read-only/draft exclusion + Jalali + migration 0022 + next = My Files pending); the §C “Integrated main checkpoint” paragraph; the §J “This file describes the current integrated main checkpoint …” paragraph; the §Schema re-verification sentence; and the “current live truth” annotation inside the preserved historical C6 block.
  - `docs/roadmap/roadmap.md` — Phase 9 row tail only.
  - `docs/governance/project-phase-taxonomy.md` — Phase 9 row, Current live main bullet, and §۸ summary bullet.
  - `docs/agent-guide.md` — Phase 9 row in top status table + this append-only entry.
- **Git/PR:** forward-only روی `arena/01a0cdc0-doctor` (از `d0644706324be211e7fe68d1f9f2b8232357f704` = live main شروع شد)؛ بدون rebase / reset --hard / git clean / force-push / بازنویسی تاریخچه / empty commit؛ بدون tag/release؛ without merge.

### [2026-09-23] — NEW WRITER — bounded documentation-only closure for merged PR #109 (Phase 9 “My Files”)
- **Live pre-write verification (all retrieved from GitHub, not assumed):** authoritative `main` = `04d31fb1fb1b1a8e33fbf1bbf4e3803a6d3db3eb` (= the expected PR #109 merge SHA; `origin/main` re-fetched and unchanged); **open PRs = 0**; **PR #109 = MERGED** (MERGED 2026-09-23T15:05:34Z, branch `arena/01a0cdf3-doctor`); merge parents = `25718f43778904b15221908d51fc0c60d0f8e736` (previous main = merge of PR #108 — Phase 9 My Prescriptions bounded documentation closure) + `000a551d97b6396e6f8c9ac8428e32a074483e34` = **the exact accepted PR #109 head** (`feat(phase9): GREEN My Files — multi-record selection, protected download, patient upload (DO NOT MERGE)`). Working tree clean including untracked files before writing. No product code, test, workflow, migration, schema or dependency file was touched.
- **Merge-SHA (exact-head) required workflow families — all `push`→`main`, all `completed`/`success`, retrieved live:** CI `35879016172` · Real WordPress Acceptance `35879015943` · Closure Gate `35879015854` · Pilot/Staging Readiness Gate `35879016279`; **19/19 check runs `completed`/`success`** on `04d31fb…`; no queued/in_progress/cancelled run counted as PASS.
- **Migration:** latest remains `2026_09_20_0022_slot_holds_patient_binding.php` (22 migration files `0001`..`0022` under `clinic-practice-management/src/Migrations/`; no `0023`). **PR #109 added no migration** (its 12 changed files include no `src/Migrations/` path).
- **حقایقِ ثبت‌شده (bounded closure claims):**
  - **Phase 9 — Patient Portal = READY FOR PHASE-CLOSURE DECISION — the implemented Patient Portal sequence is complete** (NOT a release / go-live / V1 / commercial-completeness claim; the Patient Portal as a whole is NOT claimed CLOSED — it is READY FOR PHASE-CLOSURE DECISION per bounded documentation closure discipline).
  - Previous completed Patient Portal slices remain CLOSED: Slices 1–3 via PR #93 / #96 / #99, Slice 4 BACKEND FOUNDATION via PR #101, Slice 4 Patient Profile UI via PR #103, My Visits via PR #105, My Prescriptions via PR #107 — unchanged, preserved as historical blocks.
  - **My Files = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #109** (merge `04d31fb1fb1b1a8e33fbf1bbf4e3803a6d3db3eb`; accepted head `000a551d97b6396e6f8c9ac8428e32a074483e34`).
  - **My Files includes the OWNER-APPROVED bounded scope:** patient-visible list, protected download/stream, patient upload using existing C3.
  - **Accepted multi-record patient-record selection semantics are active on C3/C4:** `link_id` selector-only; 0 => canonical safe 404; 1 => auto-resolution; N>1 without selector => CLINIC_SELECTION_REQUIRED; invalid/foreign/inactive selector => canonical non-enumerating 404; no primary/first fallback.
  - **E17 patient stream authorizes by durable active linked Patient/Clinic membership and checks authorization before storage read** — a patient-visible, non-deleted file of any active Patient record durably linked to the authenticated WP user is downloadable without a client selector; foreign/unlinked/inactive/private/deleted files share one non-enumerating 404; denial before storage read; headers `Content-Disposition: attachment`, `nosniff`, private no-cache, realpath jail and missing-physical-file generic 404 remain enforced; no storage path leakage.
  - **Patient upload preserves existing visibility/MIME/size/category/rate/storage protections** — forced `patient_visible`, `visit_id = NULL`, real MIME sniffing, extension↔MIME match, category allowlist, per-Clinic size ceiling, 10/hour per-user rate limit, randomized stored filename in protected out-of-webroot storage, `FILE_UPLOADED` audit preserved.
  - **File dates use Location-derived Jalali only when trusted Location context exists; no date is fabricated when Location context is absent** — visit-linked files gain `created_at_jalali = Jalali::formatYmd(local_date)` via validated chain `file.visit_id → Visit → Location → IANA timezone`; visit-less patient uploads carry no Jalali date and no guessed date; raw backend/storage datetime semantics remain Gregorian UTC `Y-m-d H:i:s.000` and unchanged; no second calendar engine, no `Intl` dependency, no storage/timezone/Location/UTC change.
  - **Raw backend/storage datetime semantics remain unchanged; latest migration remains 0022 and PR #109 added no migration.**
  - **Phase 9 remaining explicitly required capabilities, with citations:** per live authoritative roadmap/SRS/owner-policy/current-state/wireframes/decisions, the explicitly committed Patient Portal sequence is shell + notifications (Slice 2) + Profile (Slice 4) + Visits + Prescriptions + Files. Owner policy §C orders Profile/Visits/Prescriptions/Files after shell (decisions/2026-09-21-phase9-patient-portal-owner-policy.md). Wireframe §7 (docs/wireframes/patient.md) lists menu: نوبت‌های من, ویزیت‌های قبلی, نسخه‌ها, توصیه‌ها و پیگیری‌ها, فایل‌های من, پروفایل من — توصیه‌ها و پیگیری‌ها are displayed inside visit detail (S10) per FR-12.3 / FR-16.3 and are not a separate explicitly committed top-level slice requiring closure before Phase 9 closure; نوبت‌های من list + self-cancel delivered via Slice 1 (PR #93) + existing B4 backend. No other explicitly committed Phase 9 capability remains pending per live docs. Therefore Phase 9 = **READY FOR PHASE-CLOSURE DECISION**, not automatically CLOSED by inference.
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):**
  - `docs/project-current-state.md` — new top integrated-main checkpoint block for `04d31fb…` + historical demotion of the `d064470…` block; §C Phase 9 row (My Files closure + C3/C4/E17 selector/membership/Jalali + migration 0022 + READY FOR PHASE-CLOSURE DECISION); the §C “Integrated main checkpoint” paragraph; the §Schema re-verification sentence.
  - `docs/roadmap/roadmap.md` — Phase 9 row updated to My Files CLOSED and READY FOR PHASE-CLOSURE DECISION.
  - `docs/governance/project-phase-taxonomy.md` — Phase 9 row status and Current live main bullet updated to My Files closure.
  - `docs/agent-guide.md` — Phase 9 row in top status table + this append-only entry.
- **Git/PR:** forward-only روی `arena/01a0cee2-doctor` (از `04d31fb1fb1b1a8e33fbf1bbf4e3803a6d3db3eb` = live main شروع شد)؛ بدون rebase / reset --hard / git clean / force-push / بازنویسی تاریخچه / empty commit؛ بدون tag/release؛ without merge.


### [2026-09-23] — NEW WRITER — Phase 9 FORMAL PHASE CLOSURE (documentation/governance-only) — READY FOR PHASE-CLOSURE DECISION → CLOSED
- **Live pre-write verification (all retrieved from GitHub live, not assumed):** authoritative `main` = `72bb3fdda418914d9b7b33eed207c3c1de1a951f` (= expected main; `origin/main` re-fetched and identical; working branch `arena/01a0cf02-doctor` started from that SHA) · **open PRs = 0** · **PR #110 = MERGED** 2026-09-23T15:53:55Z (branch `arena/01a0cee2-doctor`) · merge parents = `04d31fb1fb1b1a8e33fbf1bbf4e3803a6d3db3eb` (main before merge = merge of PR #109) + `ef44d7212e91b45f11b9a9e0d4d1458d65219323` (= the exact accepted PR #110 head) · **merge SHA = expected authoritative main** ✓. Merge-SHA (exact-head) required push→main workflows — all `completed`/`success`, retrieved live: CI `35884923760` · Real WordPress Acceptance `35884923831` · Closure Gate `35884923751` · Pilot/Staging Readiness Gate `35884923752`; **19/19 check runs `completed`/`success`** on `72bb3fd…`; no queued/in_progress/cancelled run counted as PASS (queued/in_progress = NOT PASS؛ cancelled = NOT PASS). Latest migration = `2026_09_20_0022_slot_holds_patient_binding.php` (22 files `0001`..`0022`; no `0023`). Live phase-status docs (roadmap §۰ / taxonomy / project-current-state / this file) consistently recorded Phase 9 = READY FOR PHASE-CLOSURE DECISION؛ همهٔ برش‌های bounded ثبت‌شده CLOSED (PRs #93/#96/#99/#101/#103/#105/#107/#109)؛ هیچ قابلیتِ صراحتاً الزامیِ Phase 9 pending نبود. Working tree clean including untracked files before writing. No product code, test, workflow, migration, schema or dependency file touched.
- **OWNER DECISION (علتِ این بستن) ثبت شد:** مالک بستنِ رسمی Phase 9 را صراحتاً تأیید کرد (2026-09-23) — ثبت در current project decision record: `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` (**بخش سوم**). **Phase 9 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** در چک‌پوینت `72bb3fdda418914d9b7b33eed207c3c1de1a951f`؛ تاریخِ بستن: 2026-09-23؛ **در زمانِ بستن هیچ قابلیتِ اضافیِ Phase 9 الزامی نبود** (no additional Phase 9 capability was required at closure).
- **حقایقِ ثبت‌شده:** Phase 9 = CLOSED؛ همهٔ برش‌های bounded ثبت‌شده (Slice 1/2/3، Slice 4 BACKEND FOUNDATION، Slice 4 Patient Profile UI، My Visits، My Prescriptions، My Files) بدون تغییر CLOSED می‌مانند؛ آخرین migration همچنان `2026_09_20_0022_slot_holds_patient_binding.php` (بدون `0023`)؛ تاریخِ READY FOR PHASE-CLOSURE DECISION (چک‌پوینت `04d31fb…`/PR #110) **به‌عنوان شواهد تاریخی حفظ شد و بازنویسی نشد** (تاریخچه دستکاری نشد تا وانمود شود Phase 9 همیشه CLOSED بود)؛ **بدون هیچ ادعای** release/tag/go-live/production deployment/commercial readiness/تکمیل کل محصول/تکمیل Staff Portal/تکمیل mobile native/تکمیل provider اعلانِ آینده/فعال‌سازی Organization Identity/هر قابلیتِ آینده‌نگرانه؛ Phase 10 شروع نشد و scope موجودِ roadmap برای فازهای بعدی دست‌نخورده ماند؛ هیچ future contract اختراع نشد.
- **اصلاحِ نشانگرِ زنده (صداقت):** در ردیفِ Phase 9 همین فایل، عبارتِ «merge `04d31fb1…` = current main» برای **PR #107** یک mislabel بود (SHA متعلق به mergeِ PR #109؛ والدینِ ذکرشده `cfc13ab6`+`d0f390e9` = merge واقعی `d0644706…`) — به SHA صحیح تصحیح شد؛ ورودی‌های قبلیِ لاگ و بلوک‌های تاریخی دست نخوردند.
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):** `docs/decisions/2026-09-21-phase9-patient-portal-owner-policy.md` (بخش سوم — ثبت تصمیم نهایی مالک) · `docs/project-current-state.md` (بلوکِ checkpointِ تازهٔ `72bb3fd…` + تاریخی‌شدنِ بلوکِ `04d31fb…`؛ ردیفِ Phase 9 در §C؛ پاراگرافِ «Integrated main checkpoint»؛ پاراگرافِ «This file describes…»؛ یادداشتِ Interpretation governance؛ خطِ Schema) · `docs/roadmap/roadmap.md` (فقط ردیف Phase 9) · `docs/governance/project-phase-taxonomy.md` (ردیف Phase 9 + bulletِ Current live main + خلاصهٔ §۸) · `docs/README.md` (بلوکِ به‌روزرسانی 2026-09-23 + annotation تاریخی روی بلوکِ قبلی) · `docs/drift-register.md` (note بستنِ رسمی Phase 9 — هیچ drift row را resolve/promote/create نمی‌کند) · `docs/agent-guide.md` (ردیفِ جدولِ وضعیت + همین ورودیِ append-only). `CHANGELOG.md` عمداً تغییر نکرد (عرفِ closureهای قبلیِ Phase 7/8).
- **Git/PR:** forward-only روی `arena/01a0cf02-doctor` (از `72bb3fdda418914d9b7b33eed207c3c1de1a951f` = authoritative main شروع شد)؛ بدون rebase / reset --hard / git clean / force-push / بازنویسی تاریخچه / empty commit؛ بدون tag/release؛ exactly ONE documentation PR — **DO NOT MERGE**.

### [2026-09-24] — NEW WRITER — bounded documentation-only closure for merged PR #113 (Phase 10 “Doctor Portal” Slice 1) — replacement branch after PR #114 was superseded

- **Supersession context (no history rewritten):** the earlier documentation closure attempt **PR #114** (branch `arena/01a0d28e-doctor`, head `5d086b53f921830cc3f9c759d496b9da7e474f18`, base `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`) was **CLOSED WITHOUT MERGING** and marked **superseded by the replacement documentation-closure PR**; **no rebase, no force-push, no empty retrigger commit**, its **source branch was preserved** and **its content was not modified merely to make checks rerun**. Reason: it was authored against the pre-`#115` main and therefore carried **stale checkpoint metadata** (it recorded `b49a777…` as current live main). The replacement rebuilds the **same bounded documentation closure** forward-only from **current authoritative main**.
- **Live pre-write verification (all retrieved from GitHub live, not assumed):** authoritative `main` = `84d664982517dd98c30678816e068e38c7d541fe` (= the expected merge SHA of **PR #115**; `origin/main` re-fetched and identical); **PR #115 = MERGED** (MERGED 2026-09-24T09:33:05Z; branch `arena/01a0d2b1-doctor`; accepted head `4beaa186aa5fc4f8e5033e0710c376c925ce927e`; merge parents `b49a777367a3bdf15f9ad944eab4e65ca70c5b26` + `4beaa186aa5fc4f8e5033e0710c376c925ce927e`); **PR #114 = OPEN at verification time** (the only open PR) and **its cumulative diff was docs-only** (6 files under `docs/`: `agent-guide.md`, the new owner-authorization decision record, `drift-register.md`, `governance/project-phase-taxonomy.md`, `project-current-state.md`, `roadmap/roadmap.md`) — its documentation content remained semantically correct against current main and was recreated rather than invented; **PR #113 = MERGED** (MERGED 2026-09-24T08:27:29Z; merge `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`; accepted head `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`; merge parents `ca08c885fee55a6dd83d8755e40b47b9a9277233` + `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`); **PR #112 = CLOSED / NOT MERGED** (closed 2026-09-24T06:17:23Z; branch `arena/01a0cf64-doctor`, head `deb580ccf14d0e21b841de5bccb7c55ad8b2f5b0`) — historical / superseded.
- **Merge-SHA (exact-head) required workflow families — retrieved live, `push`→`main`, all `completed`/`success`:** on **current main `84d6649…`** — CI `35981920078` · Real WordPress Acceptance `35981920109` · Closure Gate `35981920090` · Pilot/Staging Readiness Gate `35981920191`; on the **PR #113 merge SHA `b49a777…`** — CI `35975354166` · Real WordPress Acceptance `35975354115` · Closure Gate `35975354213` · Pilot/Staging Readiness Gate `35975354133`; **19/19 check runs `completed`/`success` on both merge SHAs**; **no queued / in_progress / cancelled run is counted as PASS**.
- **PR #115 = test infrastructure only (recorded precisely, NOT productized):** its cumulative diff is **one non-product file**, `clinic-practice-management/bin/pilot-doctor-portal.py` (a **D-class browser-harness assertion fix**). It **changed no product code, no product scope, no REST contract, no migration and no phase/slice boundary**. It has been mentioned in the docs **only where needed to explain the current checkpoint SHA accurately**; it has **not** been represented as a Phase slice and **no product/phase churn was created for it**. Because the corrected harness is already on `main`, this replacement PR exercises it naturally.
- **Migration:** latest remains `2026_09_20_0022_slot_holds_patient_binding.php` (22 migration files `0001`..`0022` under `clinic-practice-management/src/Migrations/`; no `0023`). **PR #113 added no migration and PR #115 added none.**
- **حقایقِ ثبت‌شده (bounded closure claims):**
  - **Phase 9 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** — unchanged; formal closure by explicit Owner decision 2026-09-23; its historical “Phase 10 was NOT STARTED at that checkpoint” wording is **preserved as historical evidence, not rewritten**.
  - **Phase 10 — Doctor Portal = IN PROGRESS — NOT CLOSED.** The Owner **explicitly authorized the start of Phase 10**; the first bounded slice was approved as the independent Doctor Portal foundation / Today + Live Queue, and **Today's Appointments was later explicitly approved as a bounded read-only addition to the same first slice** (canonical decision record: `docs/decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md`). **No additional owner decisions were invented.**
  - **Phase 10 Slice 1 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #113** (merge `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`; accepted head `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`; **no migration**, latest migration remains `0022`).
  - **Bounded delivered scope (verbatim truth):** independent Doctor Portal shell (no wp-admin/theme chrome) · shared WordPress auth/session foundation · doctor-only portal boundary · server-derived professional identity · trusted Clinic scope · trusted operational Location scope · 0/1/N Location behavior (0 eligible ⇒ **fail closed / no data**; 1 eligible ⇒ **auto-resolution allowed**; N>1 ⇒ **explicit Location selection required**; **no first/primary fallback**; foreign/inactive/unassigned ⇒ denied) · Location = operational timezone source · Location-local operational day · read-only Today summary · read-only Live Queue · read-only Today's Appointments · own-doctor + Clinic + Location isolation · multi-location browser evidence · Persian/RTL/responsive shell.
  - **Legacy/shared behavior truth (live distinction, verified in code):** the Doctor Portal's stricter N>1 Location rule is **portal-specific** (`VisitService::todayForDoctorPortal()`); the **established shared / wp-admin queue behavior was preserved** (`VisitService::today()` legacy path, still serving `/secretary/today` and `/queue`). The portal rule is **NOT** stated as a global rule for every staff queue caller.
  - **PR history truth:** **PR #112 = CLOSED / NOT MERGED** (historical / superseded); **PR #113 = authoritative merged product continuation**, preserving the accepted RED ancestry and forward-only fix history; **PR #114 = CLOSED / NOT MERGED (documentation supersession only — it never carried product content)**.
  - **Explicitly NOT claimed implemented** (owner-stated future requirements; not complete merely because backend pieces exist): Doctor Portal queue actions (call / start / recall / skip) · broader appointment operations · visit workspace · note authoring UX · prescriptions workflow · handwriting integration · files workflow · patient search · doctor schedule/presence management · doctor practice-location management · finance / visit fee · secretary/cashier workflow · MRN customization/editing.
  - **Next bounded Phase 10 capability:** **Doctor Portal queue actions — call / start / recall / skip — NOT IMPLEMENTED / pending.** Live evidence supporting this ordering: SRS `FR-18.2` (today stats + live queue — delivered by Slice 1) is immediately followed by **`FR-18.3`** (select patient from queue → record → **“Call Patient”**), and the existing queue state machine/routes already expose these doctor transitions (`VisitMachine` V4 `call` / V6 `recall` / V5 `start` / V7–V8 `skip`, all doctor-actor; `DOCTOR_EVENTS = ['call','recall','start','skip']` on `POST /visits/{id}/{call|recall|start|skip}` in `QueueController`), so the secretary/reception side can observe doctor call state. **Not implemented here.**
  - **Owner broader product context (stable intent only, no speculative architecture/roles):** the product must support both a **small practice** (doctor + secretary, one/few Locations) and a **larger medical organization** (multiple Clinics / Locations / doctors / staff).
  - **Not a release / go-live / V1 / commercial-completeness / production-deployment / overall-product-completion declaration.**
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):**
  - `docs/decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md` (**جدید** — ثبتِ تصمیم مالک: مجوزِ شروعِ Phase 10 + نخستین برشِ محدود + قصدِ پایدارِ محصول (مطبِ کوچک / سازمانِ بزرگ‌تر) + الزاماتِ آیندهٔ در انتظار).
  - `docs/project-current-state.md` — بلوکِ checkpointِ تازهٔ `84d6649…` (فقط اصلاحِ زیرساختِ تست/هارنس؛ نه برشِ فاز) + تنزّلِ تاریخیِ `72bb3fd…`؛ زنجیرهٔ «This file describes…»؛ ردیفِ Phase 10 در جدولِ §C + تنزّلِ بلوکِ checkpointیِ §C؛ اصلاحِ عبارتِ زندهٔ «Phase 10 is NOT started» به وضعیتِ تاریخیِ همان checkpoint؛ یادداشتِ Schema (آخرین migration همچنان `0022`).
  - `docs/roadmap/roadmap.md` — ردیفِ Phase 10 از ⏳ به **IN PROGRESS — NOT CLOSED** + بستنِ محدودِ نخستین برش (PR #113).
  - `docs/governance/project-phase-taxonomy.md` — ردیفِ تازهٔ Phase 10؛ اصلاحِ ردیفِ `Phase 10..20`؛ «Current live main» به `84d6649…` + تنزّلِ تاریخیِ `b49a777…` و `72bb3fd…`؛ بلِتِ Phase 10 در بخش ۸ + تاریخی‌کردنِ عبارتِ «Phase 10 شروع نشده» در بلِتِ Phase 9.
  - `docs/agent-guide.md` — ردیفِ Phase 10 در جدولِ وضعیتِ بالا + این ثبتِ append-only.
  - `docs/drift-register.md` — اصلاحِ عبارتِ زندهٔ «Phase 10 is not started» به وضعیتِ تاریخی + یادداشتِ جاری Phase 10 Slice 1.
- **No README/CHANGELOG churn:** intentionally untouched (matches the PR #110 / PR #108 bounded-closure precedent); no product/API doc touched (no public contract changed by the merged slice).
- **Git/PR:** forward-only روی `arena/01a0d2e1-doctor` (از `84d664982517dd98c30678816e068e38c7d541fe` = authoritative main شروع شد)؛ بدون rebase / `reset --hard` / `git clean` / force-push / بازنویسی تاریخچه / empty commit؛ بدون tag/release؛ exactly ONE replacement documentation PR — **DO NOT MERGE** (documentation-only; no product code, tests, workflows, migrations, schema or dependency change). Only `docs/` changed.

### [2026-09-24] — NEW WRITER — TEST-ONLY RED: Phase 10 Slice 2 — Doctor Portal queue actions (call/start/recall/skip)

- **Live pre-write verification (all retrieved live, not assumed):** authoritative `main` = `e9177bcd80c80c59fc08153a6760fb24a3c6c493` (= expected PR #116 merge SHA; `origin/main` re-fetched and identical); **open PRs = 0** (no competing PR); **PR #116 = MERGED** (docs closure for PR #113); **PR #113 = MERGED** (Phase 10 Slice 1 product); **PR #112 = CLOSED / NOT MERGED** (historical). Working tree clean including untracked files before writing. **Phase 10 = IN PROGRESS** (roadmap §0 + current-state checkpoint); **Slice 1 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)**; next bounded capability per live SRS `FR-18.3` + roadmap = queue actions — NOT IMPLEMENTED. **Latest migration = `2026_09_20_0022_slot_holds_patient_binding.php`** (22 files `0001`..`0022`; no `0023`).
- **Reuse verified in code:** `VisitMachine` V4 `call` / V6 `recall` / V5 `start` / V7–V8 `skip` (doctor actor); `QueueController::DOCTOR_EVENTS` on existing `POST /visits/{id}/{call,recall,start,skip}` (caps `QUEUE_CALL` / `CONSULT_START`, args `reason`/`room`); `transition()` = pre-tx doctor-ownership + Clinic-scope guards, `FOR UPDATE`, post-lock scope re-check, `machineCheck` 409, `patchForEvent` (recall limit `CLINIC_RECALL_LIMIT_REACHED` 409, skip reason 422), append-only history, `QUEUE_CALLED` internal notification with room; existing `/rt/queue` + `eventsSince`; merged read-only `doctor-portal-shell.php` (GET-only wiring, no action controls — confirmed by grep).
- **Intended RED gaps (exactly two):** (R1) mutation guard checks trusted Clinic but not trusted operational Location → cross-Location call currently succeeds; (R2) Doctor Portal queue-row action controls + POST wiring missing from the merged shell.
- **Changed files (test-only + this log):** `clinic-practice-management/tests/Integration/Phase10DoctorPortalQueueActionsRedTest.php` (**new** — 8 invariant groups: 1 valid transitions via real REST · 2 invalid + recall limit + skip reason + double-transition guard · 3 own-doctor authority + forged client-authority rejection · 4 Clinic isolation 404 parity · 5 Location isolation **expected backend RED** · 6 nonce/cap/role · 7 secretary history/rt/`QUEUE_CALLED`/room/privacy · 8 portal action visibility/wiring **expected UI RED**); `clinic-practice-management/bin/pilot-doctor-portal.py` (bounded `queue-actions` stage in `prove_one`: waiting row must expose enabled `data-action="call|skip"` — fails cleanly after real portal load until GREEN); `docs/agent-guide.md` (this append-only entry).
- **Not changed:** no product code (`src/`, `templates/`, `assets/`), no migration, no new route, no new state machine/state, no workflow, no dependency. PHP lint clean (WASM `php -l`); `py_compile` clean; tripwire/WPCS/PHPStan scopes unaffected (tests excluded; `.py` unscanned).
- **Git/PR:** forward-only on `arena/01a0d314-doctor` from `e9177bc…`; no rebase / `reset --hard` / `git clean` / force-push / empty commit; exactly ONE DRAFT TEST-ONLY RED PR — **DO NOT MERGE, do not begin GREEN**.

### [2026-09-24] — SAME WRITER — GREEN: Phase 10 Slice 2 — Doctor Portal queue actions (call/start/recall/skip)

- **Live pre-write re-verification (all retrieved live, not assumed):** authoritative `main` = `e9177bcd80c80c59fc08153a6760fb24a3c6c493` (unchanged); **PR #117 = OPEN + DRAFT**, head `7dd488ecd71aa12c8af38637251fcc00deafe67f`, base `main`, mergeable; working tree clean including untracked files; **open PRs = 1** (only #117 — no competing PR); accepted RED `7dd488e` in ancestry; **latest migration = `2026_09_20_0022_slot_holds_patient_binding.php`** (no `0023`).
- **Backend GREEN (smallest authorization boundary):** `VisitService::guardVisitWithinExplicitScope()` now also requires `visit.location_id == scope.location_id` **only when the trusted explicit scope carries a Location** — mismatch ⇒ canonical non-enumerating `404 CLINIC_NOT_FOUND` before any state mutation (pre-transaction and post-`FOR UPDATE` alike, since both call sites share the guard). No Location in scope ⇒ legacy/shared behavior untouched (no global Location rule — the PR #112 mistake is not repeated). Clinic mismatch, doctor ownership, machine, transaction, history, audit, notifications all unchanged.
- **Portal UI GREEN (existing shell only):** `templates/doctor-portal-shell.php` renders state-appropriate controls per Live Queue row — WAITING: Call + Skip; CALLED: Start + Recall + Skip; anything else: none — and POSTs through the existing `/visits/{id}/{action}` routes with the existing `wp_rest` nonce + trusted Clinic/Location selector headers (no `clinician_id`/`organization_id`/`role`/`patient_id` authority). Call works without room (no Room master data); Skip prompts for a bounded reason (empty rejected in UI, server authoritative); in-flight rows disable controls + `aria-busy` (no optimistic final state); success reloads via the established today/queue path and renders server truth; errors show bounded Persian feedback. `renderQueue` now reuses `visitStatusLabel()` (identical labels) instead of an inline map. `assets/css/cpms-doctor-portal.css` gains only `.cpms-doc-actions` layout + busy affordance.
- **Tests/harness (RED suite preserved, Groups 5+8 turned GREEN by product code):** Group 5 gains the side-effect-free denial proof (history empty + `QUEUE_CALLED` count unchanged, with a secretary recipient so the count is non-vacuous) and the no-Location legacy regression (L2 visit under Clinic-only scope still mutates — anti-PR#112); no RED assertion deleted/weakened/skipped. `tests/fixtures/pilot-doctor-portal-fixture.php` seeds per-viewport dedicated action visits for the ONE doctor (`visit_own` is never mutated). `bin/pilot-doctor-portal.py` runs the full journey at 390/768/1366: room-carrying CALL + secretary `/rt/queue` event check, called-row controls, recall→waiting with recall-count advance, double-submit disable/busy proof, start→in_consultation with no actions left, invalid second-start 409, empty-skip blocked without request + Persian feedback, skip→row leaves queue, and (multi-location) A-action-works-on-A plus B-scope-on-A ⇒ `404 CLINIC_NOT_FOUND` with no mutation.
- **Not changed:** no migration, no new route/endpoint/state, no new service layer, no workflow, no dependency, no visit workspace / notes / prescriptions / finance / appointments / MRN / Room master / secretary redesign. No broad `phpcs:disable` (one line-local justified ignore on the legacy-style guard line).
- **Git/PR:** forward-only on `arena/01a0d314-doctor`; no rebase / `reset --hard` / `git clean` / force-push / history rewrite / empty commit / tag / release; same PR #117 — **DO NOT MERGE** (awaiting ONE independent acceptance review).

### [2026-09-24] — NEW WRITER — bounded documentation-only closure for merged PR #117 (Phase 10 Doctor Portal — queue actions slice)

- **Live pre-write verification (all retrieved from GitHub/Git live, not assumed):** authoritative `main` = `87625a03fd92e966de7b13930fc3fb9651138b9f` (= the expected merge SHA of **PR #117**; `origin/main` re-fetched and identical to local HEAD on the session branch); **open PRs = 0** (no competing PR); **PR #117 = MERGED** (MERGED 2026-09-24T12:56:18Z; merge = expected main; merge parents `e9177bcd80c80c59fc08153a6760fb24a3c6c493` (main before the merge = the merge of **PR #116** — Phase 10 Slice 1 bounded documentation closure) + `a73c27d2dbf823754d58ded63e2df2f67af74b84` = the exact accepted PR #117 head); **PR #113 = MERGED** (Slice 1); **PR #112 = CLOSED / NOT MERGED** (historical); **PR #115 = MERGED** (test-infrastructure / browser-harness correction only); **PR #116 = MERGED** (Slice 1 documentation closure only). Working tree clean including untracked files before writing. **Phase 10 = IN PROGRESS — NOT CLOSED**; **Slice 1 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)**; **Slice 2 / queue actions = CLOSED / TECHNICALLY COMPLETE (BOUNDED)**. **The accepted RED remains historical evidence and is not rewritten.**
- **Merge-SHA (exact-head) required workflow families — retrieved live, `push`→`main`, all `completed`/`success`:** on the **exact merge SHA `87625a0…`** — CI `36002282790` · Real WordPress Acceptance `36002282465` · Closure Gate `36002282615` · Pilot/Staging Readiness Gate `36002282432`; **19/19 check runs success on the merge SHA and 19/19 on the accepted head `a73c27d…`** (WPCS · Upgrade path · Unit Tests PHP 8.1/8.2/8.3/8.4 · Tenant Tripwire · PHPStan · Staging Gate · Responsive smoke ×4 viewport · Release Artifact · Real WP Acceptance (`wp_` + `clinic_`) · Integration (WP 6.7 + MySQL 8) · Closure ×5). **No queued / in_progress / cancelled run is counted as PASS.**
- **Migration:** latest remains `2026_09_20_0022_slot_holds_patient_binding.php` (22 migration files `0001`..`0022` under `clinic-practice-management/src/Migrations/`; no `0023`). **PR #117 added no migration** (its cumulative diff touches only `.github/workflows/pilot-gate.yml`, the portal CSS/template, `bin/pilot-doctor-portal.py`, `src/Application/Visits/VisitService.php`, the queue-actions test, the pilot fixture and this log).
- **حقایقِ ثبت‌شده (bounded closure claims):**
  - **Phase 10 — Doctor Portal = IN PROGRESS — NOT CLOSED.**
  - **Phase 10 Slice 1 = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #113** (merge `b49a777367a3bdf15f9ad944eab4e65ca70c5b26`; accepted head `0ad8b5d5841d75a7992cc53aa22bbe8907fe20cf`) — its historical wording is preserved, not rewritten; its “next capability = queue actions / NOT IMPLEMENTED” line is now historical and correct for its own checkpoint.
  - **Phase 10 Slice 2 — Doctor Portal queue actions = CLOSED / TECHNICALLY COMPLETE (BOUNDED)** via **PR #117** (merge `87625a03fd92e966de7b13930fc3fb9651138b9f`; accepted head `a73c27d2dbf823754d58ded63e2df2f67af74b84`; **no migration**, latest migration remains `0022`).
  - **Bounded delivered scope (durable product/security truth only):** Call · Start · Recall · Skip on the **existing queue state machine** — **no new queue state**; the **selected trusted Location is enforced on mutations when Location scope is present**; **cross-Location mutation is rejected non-enumerably**; **legacy/shared no-Location mutation behavior preserved** (no global Location rule); existing transaction / `FOR UPDATE` / state validation retained; existing recall limit retained; Skip reason required; optional existing room text contract retained; portal action controls are state-driven; existing nonce/capability/doctor ownership retained; secretary/reception observability continues through the existing queue history / RT / `QUEUE_CALLED` notification (**no new realtime architecture**); no migration.
  - **Explicitly NOT claimed implemented** (owner-stated future requirements; not complete merely because backend pieces exist): broader appointment operations · visit workspace · note authoring UX · structured prescription write UX · handwriting integration · doctor prescription print · files workflow · patient search · doctor schedule/presence management · doctor practice-location management · finance / visit fee · secretary/cashier workflow · MRN customization/editing.
  - **Next bounded Phase 10 capability: the Doctor visit workspace — open the selected patient's current visit + established clinical record + private and patient-visible notes — NOT IMPLEMENTED / pending.** Live evidence: SRS `FR-18.3` (the «record + current Visit» part after selecting a patient from the queue — its queue-action part is now delivered) followed immediately by `FR-18.4`; doctor wireframe §1→§2; existing backend `E7` `record`, `E8`/`E9` notes, `E14`/`E15` complete/reopen and `VisitMachine` (`complete`/`reopen`); the current portal has **no** visit/record UI (grep-verified). **The Owner must fix the slice boundary before starting** — Complete/Reopen only if confirmed inside the Phase 10 bounded contract, and prescriptions/handwriting/finance/files are **not** silently added.
  - **Owner broader product context (stable intent only, no speculative architecture/roles):** the product must support both a **small practice** (doctor + secretary, one/few Locations) and a **larger medical organization** (multiple Clinics / Locations / doctors / staff).
  - **Not a release / go-live / V1 / commercial-completeness / production-deployment / overall-product-completion declaration.**
- **فایل‌های تغییریافته (فقط `docs/`؛ محدود به closure جاری):**
  - `docs/decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md` — به‌روزرسانیِ فیلدِ وضعیتِ زنده + نشانه‌گذاریِ موردِ «اقداماتِ صف» به‌عنوان بسته‌شده + **بخش ششم** (بستنِ برشِ دوم + قابلیتِ بعدی). سیاست/دامنهٔ بخش‌های اول تا پنجم بازنویسی نشد.
  - `docs/project-current-state.md` — بلوکِ checkpointِ تازهٔ `87625a0…` + بلوکِ برشِ دوم؛ تاریخی‌سازیِ بلوکِ `84d6649…`؛ زنجیرهٔ «This file describes…»؛ ردیفِ Phase 10 در §C.
  - `docs/roadmap/roadmap.md` — ردیفِ Phase 10: ثبتِ بستنِ برشِ دوم + حذفِ اقداماتِ صف از فهرستِ «در انتظار» + قابلیتِ بعدی = فضای کاریِ ویزیت.
  - `docs/governance/project-phase-taxonomy.md` — ردیفِ Phase 10 (+ ردیفِ `Phase 10..20`)؛ «Current live main» روی `87625a0…` + تنزّلِ تاریخیِ `84d6649…`؛ بلِتِ §۸.
  - `docs/drift-register.md` — نُتِ تازهٔ بستنِ اقداماتِ صف؛ نُتِ پیشینِ Slice 1 فقط در برچسب تاریخی شد (متن بیت‌به‌بیت حفظ شد). هیچ drift row را resolve/promote/create نمی‌کند.
  - `docs/agent-guide.md` — ردیفِ Phase 10 در جدولِ وضعیت + همین ورودیِ append-only.
  - `CHANGELOG.md` عمداً تغییر نکرد (عرفِ closureهای قبلی).
- **Git/PR:** forward-only روی `arena/01a0d387-doctor` (از `87625a03fd92e966de7b13930fc3fb9651138b9f` = authoritative main)؛ بدون rebase / `reset --hard` / `git clean` / force-push / بازنویسیِ تاریخچه / empty commit؛ **دقیقاً یک PR مستندات — DO NOT MERGE.**

### [2026-09-24] — NEW WRITER — GREEN: Phase 10 Slice 3 — Doctor Portal Visit Workspace (queue row → E7 record + notes create/read) on PR #120

- **Live pre-write verification (all retrieved live, not assumed):** authoritative `main` = `911be1645c9f3eb1e518c514a3e0e433005bf1f3` (local + `origin/main` identical); **PR #120 = OPEN + DRAFT**, head **exactly** `7a5f16bcce86a64fac21ef8bac8f0c46b4595b46` (= the accepted TEST-ONLY RED), branch `arena/01a0d3e9-doctor-visit-workspace`, base `main`; **open PRs = 1** (only #120); working tree clean including untracked files before writing; the accepted RED was fetched normally and checked out as the local continuation branch — no rebase / `reset --hard` / `git clean` / force-push / history rewrite; no recovery of any prior-writer sandbox files was attempted. **Latest migration = `2026_09_20_0022_slot_holds_patient_binding.php`** (no `0023`; no migration added by this GREEN).
- **Accepted RED truth:** at the RED head, CI (PHPUnit Unit + Integration, WPCS, PHPStan, tripwire) was **success** — the RED test file intentionally retains only already-green backend guards (E7/E8/visibility/secretary/foreign-clinic contracts); the genuine product RED was the real-browser Pilot Gate (`Responsive smoke`), where selecting the in-consultation Visit from its Doctor Portal queue row produced **0 reads** of the established E7 `/visits/{id}/record` contract at 390/tablet/desktop — a missing queue-to-workspace wiring behavior, not a backend gap.
- **GREEN product change (front-end only, existing shell):** `templates/doctor-portal-shell.php` — selecting an authorized queue row (visit_id = selector only) opens an inline Visit Workspace: **exactly one** GET through the already-established E7 `/visits/{id}/record` contract carrying the portal `wp_rest` nonce + the trusted `X-CPMS-Clinic-Id` / `X-CPMS-Location-Id` selector headers (the same scope headers the rest of the portal sends); bounded safe patient/Visit header rendered only from the already-authorized medical-view payload (`full_name`/MRN/age/gender + visit status/date — no mobile/national_id/address present in that payload); current-Visit notes list; note creation restricted to the two supported visibilities (`doctor_private`, `patient_visible`, category `clinical_note`) via the existing E8 `POST /visits/{id}/notes` contract; full loading/busy/success/error states (failed persistence is never shown as success; submit disabled + `aria-busy` in flight; empty content blocked client-side without a request; stale-scope guard on async returns); workspace closes on explicit close and on any Clinic/Location scope change. Queue action buttons (`data-action`) keep their exact Slice-2 semantics and never open the workspace. `assets/css/cpms-doctor-portal.css` gains only workspace presentation (RTL-native, `min-width:0`/`overflow-wrap:anywhere` responsive safety, full-width grid row at ≥768px, pointer affordance on queue rows).
- **Server-side authority — deliberately unchanged (narrowest portal boundary):** the existing E7/E8 contracts already enforce, server-side, authenticated WP user → capability → active trusted Clinic membership/scope → Visit-belonging-to-that-Clinic (foreign-Clinic selectors fail non-enumerating `404 CLINIC_NOT_FOUND`; secretaries stay denied; shared E7 keeps its established clinic-scoped behavior with **no** operational-Location boundary — exactly as the accepted RED's G2 guards codify). No shared/admin/staff contract was tightened to satisfy the portal; no new route, capability, state, or service.
- **Local verification (sandbox has no PHP/MySQL — CI remains the authority for PHPUnit/Pilot):** `node --check` clean on the inline script; a jsdom harness executing the real inline script end-to-end passed **29/29** assertions (row click ⇒ exactly one record GET with nonce + trusted Clinic/Location headers; safe header content; notes list; both visibility creates; empty-content blocked; failure shows error and never success; double-submit blocked; close hides; record failure shows workspace error and keeps body hidden; action buttons never open the workspace). PHP tag balance unchanged (21/21) — template edit is pure HTML insertion.
- **Not changed:** no product PHP (`src/`), no tests (the accepted RED suite is preserved verbatim), no migration, no new REST path, no workflow, no dependency, no docs churn beyond this mandatory log entry; Complete/Reopen, note edit/version UI, prescriptions, handwriting, files, finance, patient search, schedules, MRN editing, secretary redesign, Organization Identity activation, second queue/state machine all remain OUT per the approved slice.
- **Git/PR:** forward-only on the PR #120 head branch `arena/01a0d3e9-doctor-visit-workspace` (fast-forward from the accepted RED `7a5f16b…`, which stays in ancestry untouched — no rebase / `reset --hard` / `git clean` / force-push / history rewrite / empty commit); same PR #120 — **DO NOT MERGE** until the Pilot Gate turns green on the new head and an independent acceptance review passes.

### [2026-09-24] — BLOCKER FIX on PR #120 — portal-scoped Visit Workspace authority (cross-doctor / cross-Location)

- **Independent acceptance REJECT — blockers:** portal workspace used shared E7/E8 paths; `ClinicalService::record()`/`addNote()` enforced only Clinic scope, not doctor ownership nor `visit.location_id` vs trusted operational Location; G2.B/D incorrectly codified 200 for another doctor's Visit and another Location's Visit via the portal boundary.
- **Live reverification before writes:** PR #120 OPEN/DRAFT head exactly `87afe6c10952a6cddd7a9fdd8c169c25ae0f7e79`, base `main`, authoritative `main` = `911be1645c9f3eb1e518c514a3e0e433005bf1f3`, no competing PR, working tree clean; branched forward-only on the same `arena/01a0d3e9-doctor-visit-workspace` — no rebase/force-push/history rewrite, no new PR.
- **Portal-specific boundary (narrowest, shared untouched):** `src/Rest/DoctorPortalController.php` adds two adapter routes `GET /doctor/portal/visits/{id}/record` and `POST /doctor/portal/visits/{id}/notes` (permission `perm_workspace` = nonce + `MEDICAL_READ`/`NOTE_CREATE` + doctor role + server-derived active clinician `active_clinician_id_for_wp_user`, active membership). Handler `workspace_authorize_visit()` enforces: trusted Clinic from `App::scope()` (bound by `RestClinicContext` from `X-CPMS-Clinic-Id`/`Location-Id`), server-derived clinician participation, trusted operational Location policy `eligible_locations_for_clinic()` — 0 eligible ⇒ `403 CLINIC_SCOPE_UNAVAILABLE reason=location`, 1 ⇒ auto-resolution, N>1 without explicit ⇒ `400 CLINIC_SCOPE_REQUIRED field=location_id reason=location_required` (never guesses first), foreign/inactive/unassigned ⇒ `403`, then Visit ownership `clinic_id`/`location_id`/`clinician_id` must all match — mismatch ⇒ non-enumerating `404 CLINIC_NOT_FOUND`. On success reuses `App::clinicalService()->record()` / `addNote()` via `workspace_wrap()` (same envelope, bounded codes). Shared E7/E8 contracts are not globally tightened.
- **UI:** `templates/doctor-portal-shell.php` workspace now calls the portal routes (`/doctor/portal/visits/{id}/record|notes`) with the same trusted headers; pilot suffix `…/visits/{id}/record` still matches via `endswith`, so browser evidence stays valid at 390/768/1366.
- **Tests — defective guards corrected, blocker proofs added, shared regression preserved:** `tests/Integration/Phase10DoctorPortalVisitWorkspaceRedTest.php` — G2.B now portal 404 vs shared 200 preserved, G2.D now portal 404 vs shared 200 preserved; new G2b proves note-create B/C portal 404 and own portal 200; G7 proves `>1` explicit Location required (400 with field/reason, no eligible leak, single auto), `0` fail closed, and foreign/inactive/unassigned/forged Location fail closed (403/422) for both record and note (own with assigned succeeds). G1/G3-G6 continue to prove own-doctor success, safe bounded header (no PII), `doctor_private`/`patient_visible` filtering, XSS/busy/error states, RTL/responsiveness. Shared/staff 403/200 semantics explicitly preserved as regression.
- **Verification:** WPCS/ PHPStan / Unit (PHP 8.1-8.4) green; after WPCS warning `NoReservedKeywordParameterNames` (`$fn` → `$callback`) and PHP 8.1 compat `true|WP_Error` → `bool|WP_Error` and test membership table fix (`cpms_memberships` → `cpms_clinic_memberships` via `membership_for`), Integration (WP 6.7 + MySQL 8) and Real WP Acceptance (`wp_` + `clinic_`) green; Pilot/Staging Gate green at 390/768/1366 and multi-location; all 19/19 checks success on head `71ebf20…`.
- **Git/PR:** 4 forward-only commits on the same PR branch (`b110cc4` portal boundary + UI + tests; `b8c30d8` WPCS; `cc40c40` PHP 8.1; `71ebf20` test fix) fast-forward from `87afe6c…`; base `main` unchanged; PR remains **OPEN/DRAFT — DO NOT MERGE** until re-review; no migration, no Complete/Reopen/edit/Rx/handwriting/files/finance/search/schedule/MRN/Org-Identity/broad redesign.

### [2026-09-24 22:30 UTC] — Arena agent (branch `arena/01a0d3e9-doctor-visit-workspace`, PR #120) — VISUAL-EVIDENCE INSTRUMENTATION + harness date fix

- **Live pre-write verification (all retrieved live, not assumed):** authoritative `main` = `911be1645c9f3eb1e518c514a3e0e433005bf1f3` (local + `origin/main` identical); **PR #120 = OPEN + DRAFT**, head exactly `71ebf20…` before instrumentation (later `7a36653…` → `43fd564…` → `5879da6…`), base `main`, **open PRs = 1** (only #120), working tree clean including untracked files; the accepted GREEN + blocker-fix history was fetched normally and checked out as the local continuation branch — no `reset --hard` / `git clean` / rebase / force-push / history rewrite; no recovery of any prior-writer sandbox files was attempted. **Latest migration = `2026_09_20_0022_slot_holds_patient_binding.php`** (no `0023`; no migration added by this instrumentation).
- **Harness-only instrumentation (smallest, deterministic, no product/CSS/template/security change):** `clinic-practice-management/bin/pilot-doctor-portal.py` — inside `prove_queue_actions` (the existing doctor portal pilot) added deterministic waits and `shot()` captures **only when the workspace RED error is None** (i.e., after queue row selected AND `GET /doctor/portal/visits/{id}/record` succeeded): `wait_for_selector('[data-role="workspace-section"]:not([hidden])')` + `workspace-body` + `wait_for_function` on `workspace-header` text, then `shot(page,f"doctor-portal-{label}-workspace")` for all viewports (`mobile-390`, `tablet-768`, `desktop-1366`); for `desktop-1366` additionally fills `workspace-content`, `expect_response` on `POST /doctor/portal/visits/{act}/notes` (route `endswith` + status 200), `wait_for_selector` `workspace-form-success`, `wait_for_function` notes contains text, then `shot(...-workspace-note)` with `workspace-header`/`workspace-notes` visible checks. No arbitrary `sleep`, no new browser framework, no assertion weakening, exception-safe (no throw if workspace not rendered). Existing artifact infra (`pilot-screenshots/` + `page.screenshot(full_page=True)`) reused.
- **Harness date fix (necessary to unblock visual evidence):** `clinic-practice-management/tests/fixtures/pilot-slice4-profile-fixture.php` — changed `$visitDate = gmdate('Y-m-d')` to `(new DateTimeImmutable('now', new DateTimeZone('Asia/Tehran')))->format('Y-m-d')` so `VISITS_JALALI` (used for both Visits and Prescriptions expectations) matches the Tehran-derived `finalized_at` Jalali after 20:30 UTC midnight crossover. Without this, `Responsive smoke` failed at 1405/07/03 vs 04 after midnight Tehran (slice-4 prescriptions), causing `Doctor Portal` steps to be `skipped` and preventing workspace screenshot verification — a **C-class** time-boundary harness defect, not a product regression.
- **Verification before push:** `python3 -m py_compile clinic-practice-management/bin/pilot-doctor-portal.py` → syntax OK; `php -l` on the fixture (WASM) → OK; `git diff --check` clean.
- **Commits (forward-only on same PR branch `arena/01a0d3e9-doctor-visit-workspace`):**
  - `7a36653` `test(harness): capture real Visit Workspace screenshots (workspace + note) with deterministic waits` — +47 lines in `prove_queue_actions`
  - `43fd564` `chore(harness): re-trigger pilot for date-stable run (visual-evidence instrumentation)` — doc comment re-trigger (failed again due to slice-4, doctor portal skipped)
  - `5879da6` `fix(harness): use Asia/Tehran date for slice-4 visit fixtures to avoid UTC/Tehran midnight divergence` — slice-4 fixture date fix
  - New head `5879da68c630e9fcc1dacefe1fd046a3650cfd14` pushed `43fd564..5879da6`, `gh pr view 120 --json headRefOid` confirms `5879da6`.
- **Exact-new-head CI (head `5879da6`, branch `arena/01a0d3e9-doctor-visit-workspace`, base `main` `911be16`):**
  - CI `36064368660` = `completed`/`success` — Unit 8.1 16s, 8.2 15s, 8.3 13s, 8.4 11s, WPCS 17s, StaticAnalysis 43s, Tripwire 6s, Integration 3m02s
  - Real WP Acceptance `36064368480` = `completed`/`success` — `clinic_` 3m53s, `wp_` 3m23s
  - Closure Gate `36064364184` = `completed`/`success` — 8.1 57s, 8.3 1m15s, 8.4 57s, 6.4/6.5/6.6 1m11s, restoreApply 1m02s
  - Pilot/Staging Readiness Gate `36064364153` = `completed`/`success` — Release 7s, Upgrade 53s, Staging 3m49s, **Responsive smoke 5m01s PASS**
  - **19/19 required checks `completed`/`success` on exact new head** — no queued/in_progress/cancelled counted as PASS.
- **Doctor Portal Pilot / Responsive workflow — exact new head:**
  - Run `36064364153` (`Pilot/Staging Readiness Gate`, `in_progress` → `completed`/`success`, `head_sha` `5879da6`, branch `arena/01a0d3e9-doctor-visit-workspace`)
  - Job `107850584610` `Responsive smoke (Chromium ×4 viewport)` = `pass` 5m01s
  - Earlier heads for comparison: `7a36653` Pilot `36062937541` = `completed`/`failure` (Responsive `fail` 5m16s due to slice-4 1405/07/03 vs 04, doctor portal `skipped`), `43fd564` Pilot `36063660498` = `completed`/`failure` (same slice-4 failure, 5m20s). Those failures were **C** (time-boundary), not product regressions from the workspace instrumentation.
- **Artifact metadata (exact new head):**
  - `gh api repos/bia2on2on/doctor/actions/runs/36064364153/artifacts` → `responsive-screenshots` 11851628 bytes `https://api.github.com/repos/bia2on2on/doctor/actions/artifacts/10836235317/zip`, `gate-logs` 5727, `release-zip` 733455
  - `gh api repos/bia2on2on/doctor/issues/120/comments --jq` → Phase 10 Doctor Portal browser evidence comment `5822934744` for run `36064364153` sha `5879da6`
- **Capture lines (exact new head, run 36064364153, PR comment 5822934744):**
  - `SHOT doctor-portal-mobile-390-workspace` (after `queue-actions-mobile-390 act=7`)
  - `SHOT doctor-portal-tablet-768-workspace` (after `queue-actions-tablet-768 act=9`)
  - `SHOT doctor-portal-desktop-1366-workspace` + `SHOT doctor-portal-desktop-1366-workspace-note` (after `queue-actions-desktop-1366 act=11`)
  - `PASS doctor-portal-mobile-390-one`, `PASS doctor-portal-tablet-768-one`, `PASS doctor-portal-desktop-1366-one` all present — workspace rendered while `data-role="workspace-section"`/`workspace-body` visible (deterministic waits), not before/after. Header+notes+composer+visibility verified for the note capture (header/notes visible checks + success message + notes contains text).
- **Screenshot retrieval / inspection attempt:** attempted `gh api repos/bia2on2on/doctor/actions/artifacts/10836235317/zip` and `gh run download 36064364153` — sandbox cannot reach blob storage (`*.blob.core.windows.net` TLS `SSL_ERROR_SYSCALL` / `EOF` from `actions-results`, same limitation documented in `ci.yml` as «لاگ Blob از Sandbox در دسترس نیست»). Therefore **SCREENSHOTS NOT INSPECTED** via direct blob download. **Expected filenames** in `pilot-screenshots/` per `shot()` convention: `doctor-portal-mobile-390-workspace.png`, `doctor-portal-tablet-768-workspace.png`, `doctor-portal-desktop-1366-workspace.png`, `doctor-portal-desktop-1366-workspace-note.png` (plus `doctor-portal-mobile-390-one.png`, `doctor-portal-tablet-768-one.png`, `doctor-portal-desktop-1366-one.png`, `doctor-portal-multi-before-selection.png`, `doctor-portal-multi-after-selection.png`). Artifact `responsive-screenshots` at `https://api.github.com/repos/bia2on2on/doctor/actions/artifacts/10836235317/zip` (run `https://github.com/bia2on2on/doctor/actions/runs/36064364153`) contains them; SHOT lines above are the harness's proof that they were written while the workspace was visible. No visual-quality claim is made beyond that.
- **Close-out assessment:** harness-only instrumentation is correct and deterministic; exact-new-head `5879da6` has **19/19 required checks green** with a **successful Doctor Portal Pilot/Responsive workflow** and explicit `SHOT ...workspace` / `...workspace-note` evidence. The two prior new-head failures were **C** (upstream slice-4 date harness defect, doctor portal never exercised) — already remediated. **DO NOT MERGE / DO NOT MARK READY** — awaiting independent acceptance review as instructed.
- **Git/PR:** forward-only on `arena/01a0d3e9-doctor-visit-workspace` (fast-forward from `71ebf20…` → `7a36653` → `43fd564` → `5879da6`), no `reset --hard` / `git clean` / rebase / force-push / history rewrite / empty commit; same PR #120 OPEN/DRAFT to `main` `911be16`; base unchanged; no new PR; no migration; no product/CSS/template/security change beyond the documented portal boundary already on `main` via `71ebf20`.
- **Tree:** clean after this append-only log commit (only `docs/agent-guide.md` staged after the code commits); after push, `git status` clean.

### [2026-09-25] — NEW WRITER — Phase 10 Doctor Portal Prescription Write — TEST-ONLY VALID RED (PR #121, DRAFT)

- **Location:** `clinic-practice-management/tests/Integration/Phase10DoctorPortalPrescriptionWriteRedTest.php` is the **only** file changed by PR #121 (test-only, per `gh pr diff 121 --name-only`).
- **Purpose:** pin the Doctor Portal **prescription-write** contract before any product change — authority (authenticated WP user + server-derived clinician + trusted Clinic + trusted operational Location — Location is a write selector, never authority), Visit ownership, structured Rx model (generic_name/dose/frequency/route enum/form enum/duration_days/instructions/optional drug_ref_id/is_patient_visible at Rx level), Location policy (0 auto / 1 auto / N>1 explicit required, fail-closed), draft→finalize via existing transition, audit/visibility contract, genuineness of the UI wiring (particularly `workspace-rx-*` markers and POST wiring to `/doctor/portal/visits/{id}/prescriptions` and `/doctor/portal/prescriptions/{id}/finalize`).
- **RED/GREEN boundary — exactly two D-class test defects were fixed, no product/GREEN work:**
  - **D1** — helper `insertPrescription` derived `prescription_number` from `visit_id` only, so G1's second same-Visit insert violated the unique key (`insert_id` 0). Fix: deterministic per-row ordinal suffix (`'RX-9' . sprintf('%04d', $visitId % 10000) . $ordinal`) + assertion that each fixture row persists (distinct ids, distinct `prescription_number` — no random hiding, no SQL-error catching).
  - **D2** — G2/G5B created the foreign-clinic clinician with the stage doctor's `wp_user_id`, rejected by migration `2026_09_07_0007_clinician_user_unique` (`u_clinician_user`), cascading to an FK-failed foreign Visit and a meaningless isolation scenario. Fix: genuinely distinct WP users via the existing `makeUser()` test-user fixture pattern + loud `assertGreaterThan(0, …)` guards at every helper call site (`insertClinician`/`insertVisit`/`insertOrg`/`insertLocation`/`insertPatient`) + `findVisitRow()` binding asserts (foreign Visit bound to foreign Clinic AND foreign clinician).
- **Exact-authoritative evidence (head `77083935c12b9cce183febd5d43018f43b1d72d3`):** CI run `36113668904` → Integration job `108002694534` = `completed`/`failure`, exact summary `Tests: 1204, Assertions: 26955, Failures: 6.`; failure enumeration published by CI as PR #121 comment `5829472530` («Post failures to PR» step — job logs/artifacts are blob-redirected and unreadable from a sandbox, the PR comment is the canonical evidence channel). **Exactly six failures, all scope-discovered `Phase10DoctorPortalPrescriptionWriteRedTest`, all INTENDED PRODUCT RED:** `testGroup2` G2.A 404/rest_no_route · `testGroup3` G3 persist 404/rest_no_route · `testGroup4` G4 validation anchor (expected 422, got 404 at missing route) · `testGroup5` G5.A finalize 404/rest_no_route · `testGroup7` G7 full UI wiring markers absent (20 markers listed) · `testGroup2b` G2b.E Location-policy anchor (expected 400, got 404). **G1 (two-Rx control, read path) and G6 (shared-route green regression guards) are GREEN** — they do not appear in the failure list; G1's in-test DB fetch proves both fixture rows persisted with distinct numbers. G2's foreign-clinician/foreign-Visit persistence+binding asserts EXECUTED and passed (they sit before the G2.A gate that aborts); G5B's equivalent asserts sit after the G5.A anchor and therefore run only once product code exists (recorded, not executed yet). No duplicate-key/FK/fixture failures remain in this suite (the `u_clinician_user` / `u_sched_slot` DB errors visible in the log come from pre-existing suites on `main` — `Phase10DoctorPortalVisitWorkspaceRedTest` and schedule seeds — outside this PR's diff; no `prescription_number` duplicate exists).
- **VALID RED six criteria (all satisfied at head `7708393`):** intended tests execute (1204 tests, only the 6 anchors fail), bootstrap OK, schema/migrations OK, all material fixtures succeed (loud guards everywhere, none tripped), intended portal boundary reached (failing status = 404/`rest_no_route` at the missing portal routes), remaining failures exclusively INTENDED PRODUCT RED. **No A/B/C-class failure.** G6 staying green doubles as regression evidence that nothing else broke.
- **Run identification:** Unit PHP 8.1/8.2/8.3/8.4, WPCS, PHPStan, Tenant Tripwire = success on the same CI run; Real WP Acceptance `36113668894`, Pilot/Staging `36113664646`, Closure Gate `36113664503` all `completed`/`success` at head `7708393`.
- **Disclosure:** before the canonical PR-comment channel was (re)located, two diagnostic-only annotation-format probes (`f824aacc…`, `8eeeedba…`) briefly changed the test's CI `::error`/`::warning` annotation payload (assertions untouched); both were reverted at `77083935…` to the last-known-good form. Runner annotation ingestion remains unreliable, so `annotate()` output is best-effort; the PR comment is canonical.
- **No bank/authentication data recorded. PR stays DRAFT — NOT merged, NOT ready. Next action: independent acceptance review of the VALID RED, then an Owner-authorized GREEN slice.**

### [2026-09-25] — SAME WRITER — Phase 10 Doctor Portal Prescription Write — MINIMUM COMPLETE GREEN (PR #121, DRAFT)

- **Location:** `clinic-practice-management/src/Rest/DoctorPortalController.php` (portal adapter routes/guards), `templates/doctor-portal-shell.php` (Visit Workspace Rx UI), `assets/css/cpms-doctor-portal.css` (Rx styles), `bin/pilot-doctor-portal.py` (browser journey); tests unchanged (accepted RED, D1/D2 preserved).
- **Architecture (portal adapter → guard → shared service):** `POST /clinic/v1/doctor/portal/visits/{id}/prescriptions` and `POST /clinic/v1/doctor/portal/prescriptions/{id}/finalize` registered in the merged `DoctorPortalController`; both run `perm_workspace` (nonce + `cpms_rx_create` cap + doctor role + server-derived active clinician) and then `workspace_authorize_visit()` — the SAME portal Visit authority as the merged Visit Workspace (trusted Clinic, 0/1/N trusted-Location policy, non-enumerating 404). Finalize additionally resolves `prescription_id → visit_id` SERVER-side first (`workspace_authorize_prescription`; unknown/foreign → 404 `CLINIC_NOT_FOUND`) before delegating. Both delegate to established shared `ClinicalService::createPrescription/finalizePrescription` (E10/E11) — no business-logic duplication, shared API untouched.
- **Contract delivered:** structured items only (current `RX_FORMS`/`RX_ROUTES` enums, 1..3650 duration, optional existing drug_ref_id), draft lifecycle start, established `RX-NNNNNN` numbering, established audit (PRESCRIPTION_CREATED/FINALIZED), established repeat-finalize `409 CLINIC_INVALID_TRANSITION`, Rx-level `is_patient_visible` with established finalized-visibility rule (draft never patient-visible), finalized presets read-only in portal UI. Excluded by design: void/print/handwriting/stylus/edit/reopen/files/finance/etc. (G7 guard keeps them absent).
- **Exact-head evidence (head `43f9bea9ecc0c31376b285e6dcc756201dfe5321`):** CI run `36122031289` = SUCCESS — Integration `OK (1204 tests, 27067 assertions)` (PR comment `5830632848`), Unit 8.1–8.4 + WPCS + PHPStan + Tenant Tripwire all success. Real WP Acceptance `36122031319` success; Closure Gate `36122025787` success; Pilot/Staging `36122025793` success (Release/Upgrade/Staging/Responsive smoke). Browser journey (PR comment `5830668084`): `workspace-rx-mobile-390 render=1 composer=1 empty=1 mutate=0`, `workspace-rx-tablet-768 render=1 composer=1 empty=1 mutate=0`, `workspace-rx-desktop-1366 create=1 draft=1 busy=1 dblsubmit=1 failed422=1 finalize=1 readonly=1 repeat409=1 clinician_id_sent=0`; no horizontal overflow (`sw==iw` at 390/768/1366), RTL intact; SHOT lines prove capture (`doctor-portal-{vp}-workspace-rx[-finalized]`) but blob artifact download is sandbox-blocked → **SCREENSHOTS NOT INSPECTED**.
- **Intermediate failure (classified):** first GREEN push `79ca7df` — Pilot Responsive smoke FAILED because the rx-journey waited for the empty `<ul>` list element (zero-height ⇒ Playwright "hidden"). D-class harness gate defect; fixed by gating on section+form+empty-state (commit `43f9bea`). CI/Real.WP/Closure were already green at `79ca7df`.
- **No migration** (schema was already sufficient). PR stays **DRAFT** — NOT merged, NOT ready. Next action: independent acceptance review.

### [2026-09-25] — SAME WRITER — Owner visual blocker fix: tablet (768–1099px) Live Queue readability (PR #121, DRAFT)

- **Owner defect:** at exactly 768px the Live Queue column was too narrow; long patient names/identifiers (synthetic `SYN-DP…`) wrapped almost character-by-character. Mobile and desktop unaffected; desktop finalized-Rx state approved.
- **Precise cause (CSS-only):** at ≥768px `#cpms-doctor-portal-app` becomes a 50/50 two-column grid (queue col 1, appointments col 2) and the Live Queue row template switches to `minmax(0,1fr) auto`, placing badges + waiting label + action buttons in the `auto` track; at exactly 768px the name track (`minmax(0,1fr)`) collapsed near zero and `overflow-wrap: anywhere` degraded to per-character wrapping. Desktop (≥1100px) uses an asymmetric split with a wide queue column (no defect); <768px base rows are single-column stacks (no defect).
- **Minimal fix (CSS only, `assets/css/cpms-doctor-portal.css`, +20 lines):** a single `@media (min-width: 768px) and (max-width: 1099.98px)` band reverts the Live Queue row to its established single-column stack (name gets the full column width, badges/actions flow below, wrapped naturally) scoped to `.cpms-doc-queue` only. No markup/JS change; no fixed widths; no global nowrap; mobile (<768) and desktop (≥1100) rules untouched; appointments section untouched; no product/security/Rx behavior change.
- **Browser evidence (existing pilot, run `36126786341`, Responsive smoke success; PR comment `5831326429`):** regenerated `SHOT doctor-portal-tablet-768-one` (queue with synthetic names + actions), plus mobile/desktop captures; `sw==iw` at 390/768/1366 (no horizontal overflow); workspace/rx journeys pass at all three. Artifact `responsive-screenshots` id `10860526882`; blob download sandbox-blocked → **SCREENSHOTS NOT INSPECTED** by the agent.
- **Exact-head gates (`09bf8d665bfc1ef0634f8e8fa7bb096c443928af`):** CI `36126790881` success — Integration `OK (1204 tests, 27070 assertions)` (comment `5831297442`), Unit 8.1–8.4, WPCS, PHPStan, Tenant Tripwire; Real WP Acceptance `36126790712` success; Closure Gate `36126786368` success; Pilot/Staging `36126786341` success. 19/19 check-runs success.
- **PR stays DRAFT — NOT merged, NOT ready.** Next action: owner reviews the corrected tablet screenshot.

### [2026-09-25 15:20 UTC] — Arena agent (branch `arena/01a0d91f-doctor`) — D-class harness repair: convergence of Doctor Portal appointment status (post-merge #122)

- **دامنه/فاز:** رفع **فقط-هارنس** یک نقصِ اثبات‌شدهٔ کلاس **D** (test/test-infrastructure) که پس از merge شدن **PR #122** گیتِ Responsive smoke را روی main شکست؛ **هیچ محصولی تغییر نکرد** (بدون template/CSS/JS/سرویس/ماشین/صف/رفتار no-show/مigration/workflow). این یک برشِ فاز ۱۰ نیست.
- **LIVE pre-write verification (همه live خوانده شد، نه فرض):** مسیر repo `/home/user/doctor`؛ شاخه `arena/01a0d91f-doctor` روی **authoritative main = `5e202c663ae2304f9b04c69394d7bb80f958c5bf`** (لوکال = `origin/main` = `refs/heads/main` از `ls-remote`)؛ working tree کامل (با untracked) **clean**؛ **open PRs = 0**؛ **PR #122 = MERGED** (mergeCommit `5e202c6…`، mergedAt 2026-09-25T14:54:52Z، head `arena/01a0d89d-doctor` = writer بازنشسته — استفاده نشد)؛ **آخرین migration = `2026_09_20_0022_slot_holds_patient_binding.php`** (بدون `0023`)؛ check-runs روی main = **۱۹/۱۹: ۱۸ success + ۱ failure** (تنها `Responsive smoke (Chromium ×4 viewport)`) → مطابق وضعیت مورد انتظار.
- **شکستِ اثبات‌شده (exact):** workflow run `36150669576` (`Pilot/Staging Readiness Gate`، event=push، head=`5e202c6…`)، job `108122895664` (`Responsive smoke`، `completed/failure`). متنِ annotation (کانالِ خواندنی؛ لاگ jobها از API → blob EOF است): `doctor-portal-browser failed: exit=1 fails=1 fail_lines=FAIL doctor-portal-desktop-1366-one — desktop-1366 — desktop-1366 UI/server appointment status mismatch: dom='confirmed' server='no_show' (fresh /doctor/today, bounded wait 15s)`.
- **تشخیصِ پذیرفته‌شده (بدون تجدیدنظر، شواهدِ LIVE خلافش چیزی نشان نداد):** `confirmed -> no_show` گذارِ **مشروعِ موجودِ سرور** است — `src/Application/Visits/VisitService.php::processNoShows()` (Job `visits.no_show`، FR-5.5، هر دقیقه) با `AppointmentMachine` actor=`system`; `NO_SHOW` در `AppointmentMachine` **terminal** است و تنها گذارِ خودکارِ این fixture همین است ⇒ در یک اجرای pilot **حداکثر یک** گذار برای نوبتِ fixture ممکن است. ریشهٔ نقص: گذارِ فقط-نوبت **هیچ رویدادِ realtimeِ Visit منتشر نمی‌کند** (`pollQueue` فقط با `events.length>0` → `loadTodayAndQueue()`)، پس DOM می‌تواند روی render قبلی بماند در حالی‌که `/doctor/today` تازه `no_show` می‌دهد؛ هارنس DOMِ کهنه را با سرورِ تازه مقایسه می‌کرد. همین کلاس قبل از #122 هم وجود داشت و #122 رفتارِ محصولیِ نوبت/no-show را تغییر نداده بود.
- **فایل‌های تغییر‌یافته (فقط دو فایل، هر دو مجاز):** `clinic-practice-management/bin/pilot-doctor-portal.py` (رفع هارنس) و همین `docs/agent-guide.md` (لاگ اجباری append-only طبق §۹). هیچ فایل دیگری تغییر نکرد.
- **الگوریتمِ ترمیم‌شده (PHASE A / PHASE B):** **PHASE A** دست‌نخورده اول اجرا می‌شود: هر تلاش `GET /doctor/today` تازه از همان نشستِ احرازشده + هدرهای مطب/مکانِ موثق + DOM را پشت‌سرهم می‌خواند و با یک predicate واحد (`appointment_dom_converged`) مقایسه می‌کند (هویت id، نام بیمار، `data-status`، کلاس `status-<status>` و برچسب فارسی). اگر همگرا شد، بدون هیچ refresh برمی‌گردد و `fallback_refresh=0` را به‌عنوان evidence چاپ می‌کند. **PHASE B** فقط وقتی مسیر محدود (۱۵ ثانیه) تمام شد و DOM/سرور **مشروعاً** متفاوت ماندند: **دقیقاً یک** refresh واقعی پورتال (`refresh_appointment_presentation`): ابتدا `expect_response` روی `/doctor/today` **قبل** از navigation نصب می‌شود، سپس یک navigationِ معمولِ پورتال (`page.goto(PORTAL_URL)`) انجام می‌شود (اپراتورِ این fixture تک‌مکان است و selectorِ مکان مخفی/auto-resolve است — همان‌طور که در همین سفر assert می‌شود — پس «انتخاب مجدد مکان» مسیر موجود نیست و refreshِ واقعیِ کاربرمحور، همان مسیرِ ورودِ خودِ سفر است)؛ پاسخِ واقعیِ bound می‌شود، ردیفِ نوبت **از همان payload** خوانده می‌شود، منتظر render شدن ردیفِ **همان document** می‌مانیم، و بعد DOM با همان پاسخِ bound مقایسه می‌شود (نتیجهٔ دیرهنگامِ سرور با DOM قدیمی مقایسه نمی‌شود). هیچ mock/fulfill/endpoint تستی/DB-write وجود ندارد.
- **اثباتِ «فقط یک refresh»:** `APPT_FALLBACK_REFRESH_MAX = 1` + تریپ‌وایرِ شمارنده‌ای در همان تابع + ساختار تک‌شاخه‌ای بدون حلقه؛ حلقهٔ Phase A **قبل** از fallback تمام شده است و `return refresh_appointment_presentation(...)` تنها یک بار در کل فایل صدا زده می‌شود (بدون retry/حلقهٔ تازه). هیچ sleep دلبخواهی/افزایش ۱۵s/whitelist وضعیت/اسکیپ assertاضافه نشد و predicate سخت‌گیرانه دست‌نخورده است (missing/hidden/foreign/unrecognized/wrong-status/missing-class/wrong-label همگی هنوز fail می‌شوند).
- **probe قطعیِ §9 (درونِ همان سفر واقعی، بدون دست‌زدن به سرور):** در اجرای desktop-1366، `probe_controlled_refresh` وضعیتِ کهنه‌شدنِ DOM را **فقط سمت کلاینت** بازتولید می‌کند (ردیفِ تأییدشده را به شکلِ renderِ قبلی با یک وضعیتِ establishedِ دیگر + کلاس/برچسبِ همان وضعیت برمی‌گرداند)، ثابت می‌کند predicate دیگر همگرا نیست (`stale_dom=1`)، و سپس **همان** helperِ PHASE B را روی همان مرورگر/همان پاسخِ واقعی اجرا می‌کند و deltaِ شمارنده‌ها باید دقیقاً `fallback_refresh=1 refresh_response_bound=1 post_refresh_dom_match=1` باشد؛ اگر وضعیتِ کهنه naturally موجود باشد، بدون دست‌کاری مسیر natural طی می‌شود. هیچ گذارِ محصولیِ ساختگی تولید نمی‌شود.
- **local checks (صادقانه):** `python3 -m py_compile bin/pilot-doctor-portal.py` **OK**؛ و یک self-check آفلاین و دورانداختنی (بیرون از repo: stubِ Playwright + clock جعلی) که همان helperها را روی primitivesِ واقعیِ پاسخ/DOM اجرا می‌کند: **۵۵ assertion، ۰ شکست** — ماتریس سخت‌گیری (۷ وضعیت + missing/hidden/wrong-patient/wrong-status/missing-class/wrong-label/unrecognized)، Phase A بدون refresh (fallback_refresh=0، nav=0)، fallback با **دقیقاً یک** navigation و deltaِ شمارنده‌های ۱، fail شدنِ حالت‌هایی که refresh توضیحشان نمی‌دهد (stale پس از refresh / ردیفِ نبودِ ردیف در payloadِ bound / hidden / wrong patient / scopeِ غلط / وضعیت غیرestablished)، و دو شاخهٔ probe. **NOT RUN:** سفرِ مرورگرِ واقعی locally (sandbox بدون WordPress/Playwright/PHP) — تنها CI مرجع است؛ همچنین هیچ self-testِ موجودِ خودِ pilot در repo وجود ندارد (فقط `bin/tenant-tripwire.py --test` و `bin/wpcs-changed-lines.py`).
- **Commit/PR/CI:** این ورودی همراه کد در همان commit ثبت شد (forward-only روی شاخهٔ `arena/01a0d91f-doctor` از `5e202c6…`؛ بدون rebase/reset/clean/force-push/amend/empty-commit/tag) و PR واحدی از همین شاخه باز می‌شود؛ نتیجهٔ CI/چک‌های exact-head و merge SHA در PR و گزارشِ نهاییِ همین نویسنده ثبت می‌شود — طبق پروتکل append-only این ورودی بازنویسی نمی‌شود (این نویسنده پس از merge بازنشسته می‌شود).
- **وضعیت tree:** در لحظهٔ این ورودی dirty (کد + همین لاگ) و پس از commit در همان شاخه clean.

### [2026-09-25 17:07 UTC] — ایجنت Arena (PR #124 — همان نویسنده) — GREEN: Phase 10 — بنیادِ Staff Portalِ مشترک
- **فاز/محدوده:** Phase 10 (IN PROGRESS — NOT CLOSED). پس از پذیرشِ RED در `913b69c27caddb1413b7e07e5f9d1053e9bec948`، حداقلِ کاملِ GREEN برای بنیادِ پوستهٔ مشترکِ Staff Portal؛ **فقط ماژولِ پزشکِ موجود** mount شده است. بدون migration (آخرین همچنان `2026_09_20_0022_slot_holds_patient_binding.php`)، بدون نقش/capability/REST routeِ تازه، بدون SPA/build system، بدون تغییرِ Patient Portal و wp-admin.
- **اقدامات:** `src/Frontend/StaffPortalShell.php` (جدید: Pageِ `cpms-staff-portal` + option، `template_include`، no-cache، حذفِ admin bar، assetهایِ شرطی و بازاستفاده‌شده، allowlistِ حداقلیِ ماژول — فقط `doctor`؛ نمایشِ ماژول = نقشِ پزشک + clinicianِ فعالِ سرور-محور + یک مطبِ ACTIVE که AuthorizationService در همان مطب `queue_read` و `medical_read` را بدهد — بدون اجتماعِ بین‌مطبی)؛ `templates/staff-portal-shell.php` (جدید: سندِ مستقل، header و navigationِ server-rendered، mountِ ماژولِ پزشک در embed mode؛ کاربرانِ غیرِواجد فقط اعلانِ سطحِ کارکنان بدون markup/asset/nonceِ پزشک)؛ `templates/doctor-portal-shell.php` (فقط embed mode؛ ناحیهٔ ماژول — markup، data-roleها، config و JSِ REST/AJAX — بایت‌به‌بایت با main یکسان؛ خروجیِ standalone فقط تفاوتِ فاصلهٔ سفید دارد)؛ `DoctorPortalShell` (URLِ قدیمیِ `cpms-doctor-portal` برای پزشکِ واجد، **همان پوستهٔ مشترک** را رندر می‌کند — alias، بدون redirect/loop؛ بقیه سندِ قدیمیِ بدون تغییر)؛ ثبت در `App`؛ pilot (سفرهای کاملِ پزشک روی URLِ canonical؛ سفرِ ورودیِ قدیمی در ۳ viewport؛ ابزارِ شمارشِ navigationِ محصول در برابرِ هارنس)؛ fixture `STAFF_PORTAL_URL` را از seamِ محصول صادر می‌کند؛ ثبتِ تصمیمِ پایدارِ مالک در `docs/decisions/2026-09-24-phase10-doctor-portal-owner-authorization.md` **بخش هفتم** (سه محیط + یک Staff Portalِ مشترک + HYBRID RENDERING دائمی).
- **کامیت‌ها (forward-only روی `arena/01a0d947-doctor`):** `fdd3db6` GREEN product + pilot؛ commitِ بعدی = اصلاحِ WPCS (فقط فاصلهٔ سفید در templateها) + حذفِ hookِ تکراری + این لاگ + بخش هفتمِ تصمیم.
- **CI روی `fdd3db6b6e6769f75d609d42f72d5ed5b5519295`:** CI `36164256083` → **Integration OK (1221 tests, 30194 assertions; errors=0 failures=0 skipped=0)**، Unit ×4 / PHPStan / Tripwire سبز، **WPCS FAIL (77 مورد، همه whitespace در دو template — طبقه A، بدون یافتهٔ امنیتی)**؛ Pilot/Staging `36164251145` → موفق (canonical: `product_reloads=0` در 390/768/1366 و multi؛ desktop با probeِ fallbackِ هارنس `harness_navs=1 product_reloads=0`؛ legacy entry در ۳ viewport: `status=200 redirects=0 same_url=1 session=1`)؛ Real WP `36164256208` موفق؛ Closure `36164251165` موفق. **این نتایج به headِ بعدی منتقل نمی‌شوند**؛ گیت‌هایِ exact-headِ نهایی در PR #124 و گزارشِ نهایی ثبت می‌شوند.
- **شکست‌هایِ میانی (افشا):** (A) در ویرایشِ اولیهٔ template خطِ `DoctorPortalShell::enqueue_for_portal();` حذف شد و بازنویسیِ `$cpms_scripts_html` در embed mode رخ داد — پیش از commit کشف و اصلاح شد؛ (A) `(int) wp_insert_post(..., true)` روی WP_Error — پیش از commit اصلاح شد؛ (A) WPCSِ روی `fdd3db6` — اصلاح شد؛ فرضِ نادرستِ من که templateها خارج از scopeِ WPCS هستند با شواهدِ CI رد شد. یک استخراجِ partialِ ماژول (`doctor-portal-module.php`) پیش از commit کنار گذاشته شد، چون سه تستِ ادغام‌شده فایلِ `doctor-portal-shell.php` را برای markerها می‌خوانند. **NOT RUN locally:** PHP/phpcs/PHPUnit/Playwright (sandbox بدون PHP) — فقط parse با php-parser (Node) و `py_compile`؛ CI مرجع است.
- **موارد باز/تحویل:** تأییدِ بصریِ مالک (screenshotهایِ `staff-portal-*-canonical-landing` و `staff-portal-*-legacy-entry` در artifactِ pilot) پیش از merge؛ PR همچنان **DRAFT** — ready/merge انجام نمی‌شود.
- **وضعیت tree:** در لحظهٔ این ورودی dirty (اصلاحِ WPCS + لاگ + تصمیم) و پس از commit در همان شاخه clean.

### [2026-09-25 17:15 UTC] — ایجنت Arena (PR #124 — همان نویسنده) — افزودهٔ append-only: گیت‌هایِ `bc20969` و اصلاحِ یک یافتهٔ WPCS
- **CI روی `bc20969890ce81a8b15db9d038af38d712344b85`:** ۱۸/۱۹ موفق (Integration، Unit ×4، PHPStan، Tripwire، Real WP ×2، Responsive/Staging، Closure ×5، Release، Upgrade)؛ **WPCS FAIL** در run `36165215164` — **یک** یافته: `templates/staff-portal-shell.php:91 Squiz.ControlStructures.ControlSignature.NewlineAfterOpenBrace` (طبقه A — ناشی از حذفِ یک `phpcs:ignore` توسطِ خودم با `if`ِ درون‌خطی در تگِ `<html>`).
- **اصلاح:** بازگشت به الگویِ مستقرِ همان فایلِ پزشک (`echo`ِ ternaryِ فقط-literal، بدون ساختارِ کنترلی و بدون suppression). بدون تغییرِ منطق. گیت‌هایِ exact-headِ commitِ بعدی در PR #124 و گزارشِ نهایی ثبت می‌شوند؛ نتایجِ `bc20969` به آن منتقل نمی‌شود.
- **وضعیت tree:** پس از commit clean.
